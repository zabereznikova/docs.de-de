<Namespace Name="System.Diagnostics.PerformanceData">
  <Docs>
    <summary>Verwenden Sie die Klassen in diesem Namespace, um Zählerdaten bereitzustellen. Die Zähler werden verwendet, um Leistungsmetriken wie den Systemmonitor für Consumer verfügbar zu machen. Der Namespace enthält keine Klassen zum Verarbeiten der Zählerdaten. Eine vollständige Beschreibung der Leistung Leistungsindikatoren Architektur, finden Sie unter <see href="https://msdn.microsoft.com/en-us/library/windows/desktop/aa373083\(v=vs.85\">Leistungsindikatoren</see>.aspx).</summary>
    <remarks>
      <format type="text/markdown"><![CDATA[  
  
## Remarks  
 Die Klassen in diesem Namespace unterstützen die neue Architektur (Version 2.0) für Leistungsindikatoren, die in Windows Vista eingeführt wurde. In der neuen Architektur der Anbieter reagiert nicht mehr direkt Consumeranforderungen jedoch stattdessen einfach verwaltet die Leistungsindikatordaten. Das System injiziert einen Thread in den Prozess des Anbieters ein, wenn der Anbieter eine Instanz des Indikatorensatzes erstellt; der Thread ist verantwortlich für die Verarbeitung von Consumeranforderungen.  
  
 Die folgenden Schritte zeigen das Verfahren zum Schreiben eines Anbieters Leistungsindikatoren.  
  
1.  Die Indikatoren, die vom Anbieter wird in einem XML-basierte Manifest definiert. Die Leistungsindikatoren werden in Indikatorensätzen logisch gruppiert. Die Leistungsindikatoren in einem Indikatorensatz werden durch einen numerischen Bezeichner identifiziert, die innerhalb der Indikatorensatz eindeutig ist. Ein Anbieter kann eine oder mehrere Indikatorensätze definieren. Einem Indikatorensatz wird durch eine Guid identifiziert, die zu einem Anbieter eindeutig ist. Beachten Sie, dass, wenn Sie diese Klassen zum Schreiben von Ihrem Anbieters verwenden:  
  
    -   Das Rückrufattribut des Anbieterelements wird ignoriert.  
  
    -   Der Verweiswert für das Namensattribut des CounterAttribute-Elements wird ignoriert.  
  
     Weitere Informationen zum Schreiben des Manifests, finden Sie unter [Leistung Leistungsindikatorenschema](https://msdn.microsoft.com/en-us/library/windows/desktop/aa373092\(v=vs.85\).aspx).  
  
2.  Verwenden Sie nach dem Schreiben des Manifests, das [CTRPP](https://msdn.microsoft.com/en-us/library/windows/desktop/aa372128\(v=vs.85\).aspx) Tool, um das Manifest (Ctrpp provider.man) zu kompilieren. Das Tool generiert vier Dateien: h "," c "," RC, und * _r.h. Sie können die h- und .c-Dateien ignorieren. Die RC-Datei enthält die lokalisierten Zeichenfolgen im Manifest definiert. Sie verwenden die RC- und die Datei * _r.h-Dateien, um die kompilierte Ressourcendatei (res) zu erstellen, die Sie in Ihrem Projekt einschließen. Der folgende Aufruf zeigt, wie die Ressourcendatei zu kompilieren:  
  
    ```  
    rc /r /i "c:\Program Files\Microsoft SDKs\Windows\v6.0\Include" provider.rc  
    ```  
  
     Wenn Sie eine Fehlermeldung erhalten, die auf sal.h verweist, kopieren Sie die sal.h-Datei aus Ihrem Microsoft Visual Studio, Visual C#-Includeverzeichnis in das Verzeichnis, das Sie für die/i-Option angeben.  
  
     Fügen Sie einen Pfad zu die kompilierte Ressourcendatei (res) zur Anwendung-Eigenschaftenseite des Projekts hinzu.  
  
3.  Schreiben Sie den Anbieter. Die folgenden Schritte zeigen die Aufrufe von einem Anbieter:  
  
    1.  Rufen Sie die <xref:System.Diagnostics.PerformanceData.CounterSet.%23ctor%2A?displayProperty=nameWithType> Konstruktor, um den Indikatorensatz zu definieren. Rufen Sie diese Methode für jeden Leistungsindikator im Manifest definierten festgelegt.  
  
    2.  Für jeden Leistungsindikator, rufen Sie eine von der <xref:System.Diagnostics.PerformanceData.CounterSet.AddCounter%2A?displayProperty=nameWithType> Methoden, um die Indikatoren hinzufügen. Rufen Sie diese Methode für jeden Leistungsindikator in der Indikatorensatz definiert.  
  
    3.  Rufen Sie die <xref:System.Diagnostics.PerformanceData.CounterSet.CreateCounterSetInstance%2A?displayProperty=nameWithType> Methode, um eine Instanz des Indikatorensatzes erstellen (eine Instanz enthält die Indikatordaten). Rufen Sie für einzelne Instanz von Indikatorensätzen diese Methode einmal an. Rufen Sie diese Methode für jede Instanz für die müssen Sie Leistungsindikatordaten angeben (verwenden Sie einen eindeutigen Namen für jede Instanz), für mehrere Instanz Indikatorensätze.  
  
    4.  Verwenden der <xref:System.Diagnostics.PerformanceData.CounterSetInstance.Counters%2A?displayProperty=nameWithType> Eigenschaft aufgerufen und der Leistungsindikatordaten für den Leistungsindikator festgelegt.  
  
4.  Nachdem Sie Ihren Anbieter abgeschlossen haben, verwenden Sie das Tool LodCtr zum Registrieren der Leistungsindikatoren auf dem Computer. Beispiel:  
  
    ```  
    lodctr /m:provider.man  
    ```  
  
     Im Beispiel wird vorausgesetzt, das Manifest und ausführbare Datei im aktuellen Verzeichnis sind. Ausführliche Informationen zur Verwendung der LodCtr finden Sie unter [Microsoft Technet](Http://Go.Microsoft.Com/FWLink/?LinkId=84000).  
  
 Sie können die Klassen in diesem Namespace auf Computern, auf denen Windows Vista und höheren Betriebssystemen ausgeführt.  
  
   
  
## Examples  
 Das folgende Beispiel zeigt ein einfaches Manifest:  
  
```  
<!-- <?xml version="1.0" encoding="UTF-16"?> -->  
<instrumentationManifest xsi:schemaLocation="http://schemas.microsoft.com/win/2004/08/events eventman.xsd"   
     xmlns:win="http://manifests.microsoft.com/win/2004/08/windows/events"   
     xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"   
     xmlns:xs="http://www.w3.org/2001/XMLSchema"   
     xmlns:trace=http://schemas.microsoft.com/win/2004/08/events/trace>  
  
    <instrumentation>  
  
        <counters xmlns=http://schemas.microsoft.com/win/2005/12/counters>  
  
            <provider  
              applicationIdentity = "provider1.exe"  
              providerType = "userMode"  
              providerGuid = "{51D1685C-35ED-45be-99FE-17261A4F27F3}">  
  
               <counterSet guid = "{582803C9-AACD-45e5-8C30-571141A22092}"  
                  uri = "Microsoft.Windows.System.PerfCounters.Typing"  
                  name = "$(string.CounterSet1.Name)"   
                  description = "$(string.CounterSet1.Description)"   
                  instances = "single">  
  
                    <counter id = "1"  
                      uri = "Microsoft.Windows.System.PerfCounters.Typing.TotalWords"  
                      name = "$(string.CS1.Counter1.Name)"  
                      description = "$(string.CS1.Counter1.Description)"  
                      type = "perf_counter_rawcount"  
                      detailLevel = "standard"/>  
  
                    <counter id = "2"  
                      uri = "Microsoft.Windows.System.PerfCounters.Typing.WordsInInterval"  
                      name = "$(string.CS1.Counter2.Name)"  
                      description = "$(string.CS1.Counter2.Description)"  
                      type = "perf_counter_delta"  
                      detailLevel = "standard"/>  
  
                    <counter id = "3"  
                      uri = "Microsoft.Windows.System.PerfCounters.Typing.LetterAPressed"  
                      name = "$(string.CS1.Counter3.Name)"  
                      description = "$(string.CS1.Counter3.Description)"  
                      type = "perf_counter_rawcount"  
                      detailLevel = "standard"/>  
  
                    <counter id = "4"  
                      uri = "Microsoft.Windows.System.PerfCounters.Typing.WordsContainingLetterA"  
                      name = "$(string.CS1.Counter4.Name)"   
                      description = "$(string.CS1.Counter4.Description)"   
                      type = "perf_counter_rawcount"  
                      detailLevel = "standard"/>  
  
                    <counter id = "5"  
                      uri = "Microsoft.Windows.System.PerfCounters.Typing.PercentOfWordsContainingLetterA"  
                      name = "$(string.CS1.Counter5.Name)"   
                      description = "$(string.CS1.Counter5.Description)"   
                      type = "perf_sample_fraction"  
                      baseID = "6"  
                      detailLevel = "standard">  
                      <counterAttributes>  
                          <counterAttribute name = "displayAsReal" />  
                      </counterAttributes>  
                    </counter>  
  
                    <counter id = "6"  
                      uri = "Microsoft.Windows.System.PerfCounters.Typing.PercentBase"  
                      type = "perf_sample_base"  
                      detailLevel = "standard">  
                      <counterAttributes>  
                          <counterAttribute name = "noDisplay" />  
                      </counterAttributes>  
                    </counter>  
  
                </counterSet>  
            </provider>  
        </counters>  
    </instrumentation>  
  
    <localization>  
        <resources culture="en-US">  
            <stringTable>  
  
                <string id="CounterSet1.Name" value="Typing"/>  
                <string id="CounterSet1.Description" value="Captures simple typing metrics."/>  
                <string id="CS1.Counter1.Name" value="Total Words Typed"/>   
                <string id="CS1.Counter1.Description" value="The total number of words typed."/>  
                <string id="CS1.Counter2.Name" value="Words Typed In Interval"/>   
                <string id="CS1.Counter2.Description" value="The total number of words typed in the interval."/>  
                <string id="CS1.Counter3.Name" value="Letter A Pressed"/>   
                <string id="CS1.Counter3.Description" value="The number of times that the letter A is pressed."/>  
                <string id="CS1.Counter4.Name" value="Words Containing A"/>   
                <string id="CS1.Counter4.Description" value="The number of words that contain the letter A."/>  
                <string id="CS1.Counter5.Name" value="Percent of Words Containing A"/>   
                <string id="CS1.Counter5.Description" value="The percent of words that contain the letter A in the last interval."/>  
  
            </stringTable>  
        </resources>  
    </localization>  
</instrumentationManifest>  
```  
  
 Das folgende Beispiel zeigt einen einfachen anbieterimplementierung für das Manifest:  
  
```  
using System.Diagnostics.PerformanceData;  
  
        private static Guid providerId = new Guid("{51D1685C-35ED-45be-99FE-17261A4F27F3}");  
        private static Guid typingCounterSetId = new Guid("{582803C9-AACD-45e5-8C30-571141A22092}");  
  
        private static CounterSet typingCounterSet;         // Defines the counter set  
        private static CounterSetInstance typingCsInstance; // Instance of the counter set  
  
        private static int numberOfLetterAInWord = 0;  
  
        . . .  
  
            // Create the 'Typing' counter set.  
            typingCounterSet = new CounterSet(providerId, typingCounterSetId, CounterSetInstanceType.Single);  
  
            // Add the counters to the counter set definition.  
            typingCounterSet.AddCounter(1, CounterType.RawData32, "Total Word Count");  
            typingCounterSet.AddCounter(2, CounterType.Delta32, "Words Typed In Interval");  
            typingCounterSet.AddCounter(3, CounterType.RawData32, "A Key Pressed");  
            typingCounterSet.AddCounter(4, CounterType.RawData32, "Words Containing A");  
            typingCounterSet.AddCounter(5, CounterType.SampleFraction, "Percent of Words Containg A");  
            typingCounterSet.AddCounter(6, CounterType.SampleBase, "Percent Base");  
  
            // Create an instance of the counter set (contains the counter data).  
            typingCsInstance = typingCounterSet.CreateCounterSetInstance("Typing Instance");  
            typingCsInstance.Counters[1].Value = 0;  
            typingCsInstance.Counters[2].Value = 0;  
            typingCsInstance.Counters[3].Value = 0;  
            typingCsInstance.Counters[4].Value = 0;  
            typingCsInstance.Counters[5].Value = 0;  
            typingCsInstance.Counters[6].Value = 0;  
  
        . . .  
  
        private void Form1_FormClosing(object sender, FormClosingEventArgs e)  
        {  
            typingCounterSet.Dispose();  
        }  
  
        // Simple effort to capture letter A key press and words typed.  
        private void textInput_KeyDown(object sender, KeyEventArgs e)  
        {  
            Keys keyData = e.KeyData;  
  
            switch (e.KeyData)  
            {  
                case Keys.A :  
                    // In the .NET 3.5 Framework, you had to use the  
                    // Value property to set and increment the counter   
                    // value. Beginning with the .NET 4.0 Framework,   
                    // the Value property is safe to use in a multi-  
                    // threaded application.  
                    typingCsInstance.Counters["A Key Pressed"].Value++;  
                    numberOfLetterAInWord++;  
  
                    break;  
  
                case Keys.Enter:  
                case Keys.Space:  
                case Keys.Tab:  
  
                    if (numberOfLetterAInWord > 0)  
                    {  
                        // Beginning with the .NET 4.0 Framework, you   
                        // can use the Increment method to increment   
                        // the counter value by 1. The Increment method   
                        // is safe to use in a multi-threaded   
                        // application.  
                        typingCsInstance.Counters["Words Containing A"].Increment();  
                        typingCsInstance.Counters["Percent of Words Containg A"].Increment();  
                        numberOfLetterAInWord = 0;  
                    }  
  
                    typingCsInstance.Counters["Percent Base"].Increment();  
                    typingCsInstance.Counters["Total Word Count"].Increment();  
                    typingCsInstance.Counters["Words Typed In Interval"].Increment();  
  
                    break;  
            }  
        }  
```  
  
 ]]></format>
    </remarks>
  </Docs>
</Namespace>