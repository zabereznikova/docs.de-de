---
title: Ref-Rückgabewerte und lokale ref-Variablen (Leitfaden für C#)
description: Erfahren Sie, wie Sie ref-Rückgaben und lokale ref-Werte definieren und verwenden können.
author: rpetrusha
ms.author: ronpet
ms.date: 04/04/2018
ms.topic: article
ms.prod: .net
ms.technology: devlang-csharp
ms.devlang: csharp
ms.openlocfilehash: 98c58d083cb806a92e28c1c9d27effa1124fd153
ms.sourcegitcommit: 86adcc06e35390f13c1e372c36d2e044f1fc31ef
ms.translationtype: HT
ms.contentlocale: de-DE
ms.lasthandoff: 04/26/2018
---
# <a name="ref-returns-and-ref-locals"></a><span data-ttu-id="f0746-103">Ref-Rückgaben und lokale ref-Variablen</span><span class="sxs-lookup"><span data-stu-id="f0746-103">Ref returns and ref locals</span></span>

<span data-ttu-id="f0746-104">Ab C# 7.0 unterstützt C# Verweisrückgabewerte (ref-Rückgaben).</span><span class="sxs-lookup"><span data-stu-id="f0746-104">Starting with C# 7.0, C# supports reference return values (ref returns).</span></span> <span data-ttu-id="f0746-105">Mit einem Verweisrückgabewert kann eine Methode einen Verweis auf eine Variable statt eines Werts an eine aufrufende Funktion zurückgeben.</span><span class="sxs-lookup"><span data-stu-id="f0746-105">A reference return value allows a method to return a reference to a variable, rather than a value, back to a caller.</span></span> <span data-ttu-id="f0746-106">Die aufrufende Funktion kann dann wählen, ob sie die zurückgegebene Variable behandelt, als wäre sie als Wert oder als Verweis zurückgegeben worden.</span><span class="sxs-lookup"><span data-stu-id="f0746-106">The caller can then choose to treat the returned variable as if it were returned by value or by reference.</span></span> <span data-ttu-id="f0746-107">Die aufrufende Funktion kann eine neue Variable erstellen, die selbst einen Verweis auf den zurückgegebenen Wert (als lokaler Verweis bezeichnet) darstellt.</span><span class="sxs-lookup"><span data-stu-id="f0746-107">The caller can create a new variable that is itself a reference to the returned value, called a ref local.</span></span>

## <a name="what-is-a-reference-return-value"></a><span data-ttu-id="f0746-108">Was ist ein Verweisrückgabewert?</span><span class="sxs-lookup"><span data-stu-id="f0746-108">What is a reference return value?</span></span>

<span data-ttu-id="f0746-109">Die meisten Entwickler sind mit dem Übergeben eines Arguments *als Verweis* an eine aufgerufene Methode vertraut.</span><span class="sxs-lookup"><span data-stu-id="f0746-109">Most developers are familiar with passing an argument to a called method *by reference*.</span></span> <span data-ttu-id="f0746-110">Die Argumentliste einer aufgerufenen Methode enthält eine Variable, die als Verweis übergeben wird.</span><span class="sxs-lookup"><span data-stu-id="f0746-110">A called method's argument list includes a variable passed by reference.</span></span> <span data-ttu-id="f0746-111">Änderungen, die an diesem Wert von der aufgerufenen Methode vorgenommen werden, werden vom Aufrufer überwacht.</span><span class="sxs-lookup"><span data-stu-id="f0746-111">Any changes made to its value by the called method are observed by the caller.</span></span> <span data-ttu-id="f0746-112">Ein *Verweisrückgabewert* bedeutet, dass eine Methode einen *Verweis* (oder Alias) an einige Variablen zurückgibt.</span><span class="sxs-lookup"><span data-stu-id="f0746-112">A *reference return value* means that a method returns a *reference* (or an alias) to some variable.</span></span> <span data-ttu-id="f0746-113">Der Geltungsbereich der Variable muss die Methode enthalten.</span><span class="sxs-lookup"><span data-stu-id="f0746-113">That variable's scope must include the method.</span></span> <span data-ttu-id="f0746-114">Die Lebensdauer dieser Variable muss über die Rückgabe der Methode hinausgehen.</span><span class="sxs-lookup"><span data-stu-id="f0746-114">That variable's lifetime must extend beyond the return of the method.</span></span> <span data-ttu-id="f0746-115">Änderungen am Methodenrückgabewert durch die aufrufende Funktion werden auf die Variable angewendet, die von der Methode zurückgegeben wird.</span><span class="sxs-lookup"><span data-stu-id="f0746-115">Modifications to the method's return value by the caller are made to the variable that is returned by the method.</span></span>

<span data-ttu-id="f0746-116">Die Deklaration der Rückgabe eines *Verweisrückgabewerts* durch eine Methode weist darauf hin, dass die Methode einen Alias an eine Variable zurückgibt.</span><span class="sxs-lookup"><span data-stu-id="f0746-116">Declaring that a method returns a *reference return value* indicates that the method returns an alias to a variable.</span></span> <span data-ttu-id="f0746-117">Das Entwurfsziel dahinter besteht häufig darin, dem aufrufenden Code über den Alias Zugriff auf diese Variable sowie die Möglichkeit zu deren Änderung zu gewähren.</span><span class="sxs-lookup"><span data-stu-id="f0746-117">The design intent is often that the calling code should have access to that variable through the alias, including to modify it.</span></span> <span data-ttu-id="f0746-118">Daraus folgt, dass vom Verweis zurückgegebene Methoden nicht den Rückgabetyp `void` aufweisen dürfen.</span><span class="sxs-lookup"><span data-stu-id="f0746-118">It follows that methods returning by reference can't have the return type `void`.</span></span>

<span data-ttu-id="f0746-119">Es gibt einige Einschränkungen für den Ausdruck, den eine Methode als Verweisrückgabewert zurückgeben kann.</span><span class="sxs-lookup"><span data-stu-id="f0746-119">There are some restrictions on the expression that a method can return as a reference return value.</span></span> <span data-ttu-id="f0746-120">Es gelten folgende Beschränkungen:</span><span class="sxs-lookup"><span data-stu-id="f0746-120">Restrictions include:</span></span>

- <span data-ttu-id="f0746-121">Der Rückgabewert muss eine Lebensdauer aufweisen, die über die Ausführung der Methode hinausgeht.</span><span class="sxs-lookup"><span data-stu-id="f0746-121">The return value must have a lifetime that extends beyond the execution of the method.</span></span> <span data-ttu-id="f0746-122">Mit anderen Worten: Er darf in der Methode, die diesen zurückgibt, keine lokale Variable sein.</span><span class="sxs-lookup"><span data-stu-id="f0746-122">In other words, it cannot be a local variable in the method that returns it.</span></span> <span data-ttu-id="f0746-123">Es kann sich dabei um eine Instanz oder ein statisches Feld einer Klasse oder um ein Argument handeln, das an die Methode übergeben wurde.</span><span class="sxs-lookup"><span data-stu-id="f0746-123">It can be an instance or static field of a class, or it can be an argument passed to the method.</span></span> <span data-ttu-id="f0746-124">Beim Versuch, eine lokale Variable zurückzugeben, tritt der Compilerfehler CS8168 „Cannot return local 'obj' by reference because it is not a ref local.“ (Der lokale Wert „obj“ kann nicht als Verweis zurückgegeben werden, da er kein lokaler ref-Wert ist.) auf.</span><span class="sxs-lookup"><span data-stu-id="f0746-124">Attempting to return a local variable generates compiler error CS8168, "Cannot return local 'obj' by reference because it is not a ref local."</span></span>

- <span data-ttu-id="f0746-125">Der Rückgabewert darf nicht das `null`-Literal sein.</span><span class="sxs-lookup"><span data-stu-id="f0746-125">The return value cannot be the literal `null`.</span></span> <span data-ttu-id="f0746-126">Bei der Rückgabe von `null` tritt der Compilerfehler CS8156 „Ein Ausdruck kann in diesem Kontext nicht verwendet werden, weil er ggf. nicht als Verweis zurückgegeben wird.“ auf.</span><span class="sxs-lookup"><span data-stu-id="f0746-126">Returning `null` generates compiler error CS8156, "An expression cannot be used in this context because it may not be returned by reference."</span></span>

   <span data-ttu-id="f0746-127">Eine Methode mit einer Verweisrückgabe kann einen Alias an eine Variable zurückgeben, deren Wert derzeit null ist (nicht instanziiert) oder ein [Nullable-Typ](../nullable-types/index.md) für einen Werttyp darstellt.</span><span class="sxs-lookup"><span data-stu-id="f0746-127">A method with a ref return can return an alias to a variable whose value is currently the null (uninstantiated) value or a [nullable type](../nullable-types/index.md) for a value type.</span></span>
 
- <span data-ttu-id="f0746-128">Der Rückgabewert darf keine Konstante, kein Enumerationsmember, nicht der by-value-Rückgabewert einer Eigenschaft und keine Methode einer `class` oder `struct` sein.</span><span class="sxs-lookup"><span data-stu-id="f0746-128">The return value cannot be a constant, an enumeration member, the by-value return value from a property, or a method of a `class` or `struct`.</span></span> <span data-ttu-id="f0746-129">Wenn gegen diese Regel verstoßen wird, tritt der Compilerfehler CS8156 „Ein Ausdruck kann in diesem Kontext nicht verwendet werden, weil er ggf. nicht als Verweis zurückgegeben wird.“ auf.</span><span class="sxs-lookup"><span data-stu-id="f0746-129">Violating this rule generates compiler error CS8156, "An expression cannot be used in this context because it may not be returned by reference."</span></span>

<span data-ttu-id="f0746-130">Zusätzlich sind Verweisrückgabewerte nicht für asynchrone Methoden zulässig.</span><span class="sxs-lookup"><span data-stu-id="f0746-130">In addition, reference return values are not allowed on async methods.</span></span> <span data-ttu-id="f0746-131">Eine asynchrone Methode wird möglicherweise zurückgegeben, bevor die Ausführung abgeschlossen ist, wobei der Rückgabewert noch unbekannt ist.</span><span class="sxs-lookup"><span data-stu-id="f0746-131">An asynchronous method may return before it has finished execution, while its return value is still unknown.</span></span>
 
## <a name="defining-a-ref-return-value"></a><span data-ttu-id="f0746-132">Definieren eines ref-Rückgabewerts</span><span class="sxs-lookup"><span data-stu-id="f0746-132">Defining a ref return value</span></span>

<span data-ttu-id="f0746-133">Sie definieren einen ref-Rückgabewert, indem Sie dem Rückgabetyp der Methodensignatur das Schlüsselwort [ref](../../language-reference/keywords/ref.md) hinzufügen.</span><span class="sxs-lookup"><span data-stu-id="f0746-133">You define a ref return value by adding the [ref](../../language-reference/keywords/ref.md) keyword to the return type of the method signature.</span></span> <span data-ttu-id="f0746-134">Beispielsweise gibt die folgende Signatur an, dass die `GetContactInformation`-Eigenschaft einen Verweis auf ein `Person`-Objekt an die aufrufende Funktion zurückgibt:</span><span class="sxs-lookup"><span data-stu-id="f0746-134">For example, the following signature indicates that the `GetContactInformation` property returns a reference to a `Person` object to the caller:</span></span>

```csharp
public ref Person GetContactInformation(string fname, string lname);
```

<span data-ttu-id="f0746-135">Darüber hinaus muss vor dem Namen des Objekts, das durch jede [return](../../language-reference/keywords/return.md)-Anweisung im Methodentext zurückgegeben wird, das Schlüsselwort [ref](../../language-reference/keywords/ref.md) stehen.</span><span class="sxs-lookup"><span data-stu-id="f0746-135">In addition, the name of the object returned by each [return](../../language-reference/keywords/return.md) statement in the method body must be preceded by the [ref](../../language-reference/keywords/ref.md) keyword.</span></span> <span data-ttu-id="f0746-136">Beispielsweise gibt die folgende `return`-Anweisung einen Verweis auf ein `Person`-Objekt mit dem Namen `p` zurück:</span><span class="sxs-lookup"><span data-stu-id="f0746-136">For example, the following `return` statement returns a reference to a `Person` object named `p`:</span></span>

```csharp
return ref p;
```

## <a name="consuming-a-ref-return-value"></a><span data-ttu-id="f0746-137">Verarbeiten eines ref-Rückgabewerts</span><span class="sxs-lookup"><span data-stu-id="f0746-137">Consuming a ref return value</span></span>

<span data-ttu-id="f0746-138">Der Verweisrückgabewert ist ein Alias für eine andere Variable im Bereich der aufgerufenen Methode.</span><span class="sxs-lookup"><span data-stu-id="f0746-138">The ref return value is an alias to another variable in the called method's scope.</span></span> <span data-ttu-id="f0746-139">Sie können jede Verwendung der Verweisrückgabe als Verwendung der Variable interpretieren, für die diese als Alias fungiert:</span><span class="sxs-lookup"><span data-stu-id="f0746-139">You can interpret any use of the ref return as using the variable it aliases:</span></span>

- <span data-ttu-id="f0746-140">Wenn Sie den jeweiligen Wert zuweisen, weisen Sie der Variable, für die diese als Alias fungiert, einen Wert zu.</span><span class="sxs-lookup"><span data-stu-id="f0746-140">When you assign its value, you are assigning a value to the variable it aliases.</span></span>
- <span data-ttu-id="f0746-141">Wenn Sie dessen Wert lesen, lesen Sie den Wert der Variable, für die diese als Alias fungiert.</span><span class="sxs-lookup"><span data-stu-id="f0746-141">When you read its value, you are reading the value of the variable it aliases.</span></span>
- <span data-ttu-id="f0746-142">Wenn Sie diesen *nach Verweis* zurückgeben, geben Sie einen Alias für diese Variable zurück.</span><span class="sxs-lookup"><span data-stu-id="f0746-142">If you return it *by reference*, you are returning an alias to that same variable.</span></span>
- <span data-ttu-id="f0746-143">Wenn Sie ihn *nach Verweis* an eine andere Methode übergeben, übergeben Sie einen Verweis auf die Variable, für die diese als Alias fungiert.</span><span class="sxs-lookup"><span data-stu-id="f0746-143">If you pass it to another method *by reference*, you are passing a reference to the variable it aliases.</span></span>
- <span data-ttu-id="f0746-144">Wenn Sie einen Alias für einen [lokalen Verweis](#ref-local) erstellen, erstellen Sie einen neuen Alias für diese Variable.</span><span class="sxs-lookup"><span data-stu-id="f0746-144">When you make a [ref local](#ref-local) alias, you make a new alias to the same variable.</span></span>


## <a name="ref-locals"></a><span data-ttu-id="f0746-145">Lokale ref-Variablen</span><span class="sxs-lookup"><span data-stu-id="f0746-145">Ref locals</span></span>

<span data-ttu-id="f0746-146">Nehmen Sie an, dass die `GetContactInformation`-Methode als Verweisrückgabe deklariert ist:</span><span class="sxs-lookup"><span data-stu-id="f0746-146">Assume the `GetContactInformation` method is declared as a ref return:</span></span>

```csharp
public ref Person GetContactInformation(string fname, string lname)
```

<span data-ttu-id="f0746-147">Eine by-value-Zuweisung liest den Wert einer Variable und weist diesen einer neuen Variable zu:</span><span class="sxs-lookup"><span data-stu-id="f0746-147">A by-value assignment reads the value of a variable and assigns it to a new variable:</span></span>

```csharp
Person p = contacts.GetContactInformation("Brandie", "Best");
```

<span data-ttu-id="f0746-148">Die vorangehende Zuweisung deklariert `p` als lokale Variable.</span><span class="sxs-lookup"><span data-stu-id="f0746-148">The preceding assignment declares `p` as a local variable.</span></span> <span data-ttu-id="f0746-149">Der ursprüngliche Wert wird durch Lesen des Rückgabewerts kopiert, der von `GetContactInformation` zurückgegeben wurde.</span><span class="sxs-lookup"><span data-stu-id="f0746-149">Its initial value is copied from reading the value returned by `GetContactInformation`.</span></span> <span data-ttu-id="f0746-150">Keine der zukünftigen Zuweisungen zu `p` ändern den Wert der von `GetContactInformation` zurückgegebenen Variable.</span><span class="sxs-lookup"><span data-stu-id="f0746-150">Any future assignments to `p` will not change the value of the variable returned by `GetContactInformation`.</span></span> <span data-ttu-id="f0746-151">Die Variable `p` fungiert nicht mehr als Alias für die zurückgegebene Variable.</span><span class="sxs-lookup"><span data-stu-id="f0746-151">The variable `p` is no longer an alias to the variable returned.</span></span>

<span data-ttu-id="f0746-152">Sie deklarieren eine *lokale Verweisvariable*, um den Alias in den ursprünglichen Wert zu kopieren.</span><span class="sxs-lookup"><span data-stu-id="f0746-152">You declare a *ref local* variable to copy the alias to the original value.</span></span> <span data-ttu-id="f0746-153">In der folgenden Zuweisung ist `p` ein Alias für die von `GetContactInformation` zurückgegebene Variable.</span><span class="sxs-lookup"><span data-stu-id="f0746-153">In the following assignment, `p` is an alias to the variable returned from `GetContactInformation`.</span></span>

```csharp
ref Person p = ref contacts.GetContactInformation("Brandie", "Best");
```

<span data-ttu-id="f0746-154">Die nachfolgende Verwendung von `p` ist mit der Verwendung der von `GetContactInformation` zurückgegebenen Variable identisch, da `p` ein Alias für diese Variable darstellt.</span><span class="sxs-lookup"><span data-stu-id="f0746-154">Subsequent usage of `p` is the same as using the variable returned by `GetContactInformation` because `p` is an alias for that variable.</span></span> <span data-ttu-id="f0746-155">Durch Änderungen an `p` wird auch die von `GetContactInformation` zurückgegebene Variable geändert.</span><span class="sxs-lookup"><span data-stu-id="f0746-155">Changes to `p` also change the variable returned from `GetContactInformation`.</span></span>

<span data-ttu-id="f0746-156">Das Schlüsselwort `ref` wird sowohl vor der Deklaration lokaler Variablen *als auch* vor dem Methodenaufruf verwendet.</span><span class="sxs-lookup"><span data-stu-id="f0746-156">The `ref` keyword is used both before the local variable declaration *and* before the method call.</span></span> 

<span data-ttu-id="f0746-157">Auch auf Werte können Sie per Verweis zugreifen.</span><span class="sxs-lookup"><span data-stu-id="f0746-157">You can access a value by reference in the same way.</span></span> <span data-ttu-id="f0746-158">In einigen Fällen erhöht dies die Leistung, da ein möglicherweise aufwendiger Kopiervorgang vermieden wird.</span><span class="sxs-lookup"><span data-stu-id="f0746-158">In some cases, accessing a value by reference increases performance by avoiding a potentially expensive copy operation.</span></span> <span data-ttu-id="f0746-159">In der folgenden Anweisung wird z.B. gezeigt, wie ein lokaler Verweiswert definiert wird, mit dem auf einen Wert verwiesen wird.</span><span class="sxs-lookup"><span data-stu-id="f0746-159">For example, the following statement shows how one can define a ref local value that is used to reference a value.</span></span>

```csharp
ref VeryLargeStruct reflocal = ref veryLargeStruct;
```

<span data-ttu-id="f0746-160">Das Schlüsselwort `ref` wird sowohl vor der Deklaration lokaler Variablen *als auch* vor dem Wert im zweiten Beispiel verwendet.</span><span class="sxs-lookup"><span data-stu-id="f0746-160">The `ref` keyword is used both before the local variable declaration *and* before the value in the second example.</span></span> <span data-ttu-id="f0746-161">Wenn nicht in beiden Beispielen beide `ref`-Schlüsselwörter in den Ergebnissen der Variablendeklaration und der Zuweisung enthalten sind, tritt der Compilerfehler CS8172 "Cannot initialize a by-reference variable with a value." (Eine by-reference-Variable kann nicht mit einem Wert initialisiert werden) auf.</span><span class="sxs-lookup"><span data-stu-id="f0746-161">Failure to include both `ref` keywords in the variable declaration and assignment in both examples results in compiler error CS8172, "Cannot initialize a by-reference variable with a value."</span></span> 

<span data-ttu-id="f0746-162">Vor C# 7.3 konnten lokalen ref-Variablen nach der Initialisierung nicht neu zugewiesen werden, um auf einen anderen Speicher zu verweisen.</span><span class="sxs-lookup"><span data-stu-id="f0746-162">Prior to C# 7.3, ref local variables couldn't be reassigned to refer to different storage after being initialized.</span></span> <span data-ttu-id="f0746-163">Diese Einschränkung wurde entfernt.</span><span class="sxs-lookup"><span data-stu-id="f0746-163">That restriction has been removed.</span></span> <span data-ttu-id="f0746-164">Im folgenden Beispiel wird eine Neuzuweisung veranschaulicht:</span><span class="sxs-lookup"><span data-stu-id="f0746-164">The following example shows a reassignment:</span></span>

```csharp
ref VeryLargeStruct reflocal = ref veryLargeStruct; // initialization
refLocal = ref anotherVeryLargeStruct; // reassigned, refLocal refers to different storage.
```

 <span data-ttu-id="f0746-165">Lokale ref-Variablen müssen noch immer initialisiert werden, wenn sie deklariert werden.</span><span class="sxs-lookup"><span data-stu-id="f0746-165">Ref local variables must still be initialized when they are declared.</span></span>

## <a name="ref-returns-and-ref-locals-an-example"></a><span data-ttu-id="f0746-166">Ref-Rückgaben und lokale ref-Variablen: ein Beispiel</span><span class="sxs-lookup"><span data-stu-id="f0746-166">Ref returns and ref locals: an example</span></span>

<span data-ttu-id="f0746-167">Das folgende Beispiel definiert eine `NumberStore`-Klasse, die ein Array von Integer-Werten speichert.</span><span class="sxs-lookup"><span data-stu-id="f0746-167">The following example defines a `NumberStore` class that stores an array of integer values.</span></span> <span data-ttu-id="f0746-168">Die `FindNumber`-Methode gibt die erste Anzahl, die größer als oder gleich der Anzahl ist, die als Argument übergeben wurde, als Verweis zurück.</span><span class="sxs-lookup"><span data-stu-id="f0746-168">The `FindNumber` method returns by reference the first number that is greater than or equal to the number passed as an argument.</span></span> <span data-ttu-id="f0746-169">Wenn keine Anzahl größer als oder gleich dem Argument ist, gibt die Methode die Anzahl im Index 0 zurück.</span><span class="sxs-lookup"><span data-stu-id="f0746-169">If no number is greater than or equal to the argument, the method returns the number in index 0.</span></span> 

[!code-csharp[ref-returns](../../../../samples/snippets/csharp/programming-guide/ref-returns/NumberStore.cs#1)]

<span data-ttu-id="f0746-170">Im folgenden Beispiel wird die `NumberStore.FindNumber`-Methode aufgerufen, um den ersten Wert abzurufen, der größer als oder gleich 16 ist.</span><span class="sxs-lookup"><span data-stu-id="f0746-170">The following example calls the `NumberStore.FindNumber` method to retrieve the first value that is greater than or equal to 16.</span></span> <span data-ttu-id="f0746-171">Die aufrufende Funktion verdoppelt dann den von der Methode zurückgegebenen Wert.</span><span class="sxs-lookup"><span data-stu-id="f0746-171">The caller then doubles the value returned by the method.</span></span> <span data-ttu-id="f0746-172">Die Ausgabe aus dem Beispiel zeigt die Änderung, die im Wert der Arrayelemente der `NumberStore`-Instanz wiedergegeben wird.</span><span class="sxs-lookup"><span data-stu-id="f0746-172">The output from the example shows the change reflected in the value of the array elements of the `NumberStore` instance.</span></span>

[!code-csharp[ref-returns](../../../../samples/snippets/csharp/programming-guide/ref-returns/NumberStore.cs#2)]

<span data-ttu-id="f0746-173">Ohne Unterstützung für Verweisrückgabewerte wird ein solcher Vorgang durchgeführt, indem der Index des Arrayelements zusammen mit seinem Wert zurückgegeben wird.</span><span class="sxs-lookup"><span data-stu-id="f0746-173">Without support for reference return values, such an operation is performed by returning the index of the array element along with its value.</span></span> <span data-ttu-id="f0746-174">Die aufrufende Funktion kann diesen Index dann dazu verwenden, den Wert in einem separaten Methodenaufruf zu ändern.</span><span class="sxs-lookup"><span data-stu-id="f0746-174">The caller can then use this index to modify the value in a separate method call.</span></span> <span data-ttu-id="f0746-175">Die aufrufende Funktion kann den Index jedoch auch ändern, um auf andere Arraywerte zuzugreifen und diese zu bearbeiten.</span><span class="sxs-lookup"><span data-stu-id="f0746-175">However, the caller can also modify the index to access and possibly modify other array values.</span></span>  

<span data-ttu-id="f0746-176">Im folgenden Beispiel wird gezeigt, wie die `FindNumber`-Methode nach C# 7.3 neu geschrieben werden kann, damit sie die lokale ref-Neuzuweisung verwendet:</span><span class="sxs-lookup"><span data-stu-id="f0746-176">The following example shows how the `FindNumber` method could be rewritten after C# 7.3 to use ref local reassignment:</span></span>

[!code-csharp[ref-returns](../../../../samples/snippets/csharp/programming-guide/ref-returns/NumberStoreUpdated.cs#1)]

<span data-ttu-id="f0746-177">Diese zweite Version ist durch längere Sequenzen in Szenarios, in denen die gesuchte Zahl am Ende des Arrays liegt, effizienter.</span><span class="sxs-lookup"><span data-stu-id="f0746-177">This second version is more efficient with longer sequences in scenarios where the number sought is closer to the end of the array.</span></span>

## <a name="see-also"></a><span data-ttu-id="f0746-178">Siehe auch</span><span class="sxs-lookup"><span data-stu-id="f0746-178">See also</span></span>

[<span data-ttu-id="f0746-179">ref (C#-Referenz)</span><span class="sxs-lookup"><span data-stu-id="f0746-179">ref keyword</span></span>](../../language-reference/keywords/ref.md)  
[<span data-ttu-id="f0746-180">Verweissemantik mit Werttypen</span><span class="sxs-lookup"><span data-stu-id="f0746-180">Reference Semantics with Value Types</span></span>](../../../csharp/reference-semantics-with-value-types.md)
