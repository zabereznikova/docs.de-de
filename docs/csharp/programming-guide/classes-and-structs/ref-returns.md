---
title: Ref-Rückgabewerte und lokale ref-Variablen (Leitfaden für C#)
description: Erfahren Sie, wie Sie ref-Rückgaben und lokale ref-Werte definieren und verwenden können.
ms.date: 04/04/2018
ms.openlocfilehash: 87a9538db60d69062f0fb48ed9683a9d4f972b91
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: HT
ms.contentlocale: de-DE
ms.lasthandoff: 03/14/2020
ms.locfileid: "79170072"
---
# <a name="ref-returns-and-ref-locals"></a><span data-ttu-id="f0593-103">Ref-Rückgaben und lokale ref-Variablen</span><span class="sxs-lookup"><span data-stu-id="f0593-103">Ref returns and ref locals</span></span>

<span data-ttu-id="f0593-104">Ab C# 7.0 unterstützt C# Verweisrückgabewerte (ref-Rückgaben).</span><span class="sxs-lookup"><span data-stu-id="f0593-104">Starting with C# 7.0, C# supports reference return values (ref returns).</span></span> <span data-ttu-id="f0593-105">Mit einem Verweisrückgabewert kann eine Methode einen Verweis auf eine Variable statt eines Werts an eine aufrufende Funktion zurückgeben.</span><span class="sxs-lookup"><span data-stu-id="f0593-105">A reference return value allows a method to return a reference to a variable, rather than a value, back to a caller.</span></span> <span data-ttu-id="f0593-106">Die aufrufende Funktion kann dann wählen, ob sie die zurückgegebene Variable behandelt, als wäre sie als Wert oder als Verweis zurückgegeben worden.</span><span class="sxs-lookup"><span data-stu-id="f0593-106">The caller can then choose to treat the returned variable as if it were returned by value or by reference.</span></span> <span data-ttu-id="f0593-107">Die aufrufende Funktion kann eine neue Variable erstellen, die selbst einen Verweis auf den zurückgegebenen Wert (als lokaler Verweis bezeichnet) darstellt.</span><span class="sxs-lookup"><span data-stu-id="f0593-107">The caller can create a new variable that is itself a reference to the returned value, called a ref local.</span></span>

## <a name="what-is-a-reference-return-value"></a><span data-ttu-id="f0593-108">Was ist ein Verweisrückgabewert?</span><span class="sxs-lookup"><span data-stu-id="f0593-108">What is a reference return value?</span></span>

<span data-ttu-id="f0593-109">Die meisten Entwickler sind mit dem Übergeben eines Arguments *als Verweis* an eine aufgerufene Methode vertraut.</span><span class="sxs-lookup"><span data-stu-id="f0593-109">Most developers are familiar with passing an argument to a called method *by reference*.</span></span> <span data-ttu-id="f0593-110">Die Argumentliste einer aufgerufenen Methode enthält eine Variable, die als Verweis übergeben wird.</span><span class="sxs-lookup"><span data-stu-id="f0593-110">A called method's argument list includes a variable passed by reference.</span></span> <span data-ttu-id="f0593-111">Änderungen, die an diesem Wert von der aufgerufenen Methode vorgenommen werden, werden vom Aufrufer überwacht.</span><span class="sxs-lookup"><span data-stu-id="f0593-111">Any changes made to its value by the called method are observed by the caller.</span></span> <span data-ttu-id="f0593-112">Ein *Verweisrückgabewert* bedeutet, dass eine Methode einen *Verweis* (oder Alias) an einige Variablen zurückgibt.</span><span class="sxs-lookup"><span data-stu-id="f0593-112">A *reference return value* means that a method returns a *reference* (or an alias) to some variable.</span></span> <span data-ttu-id="f0593-113">Der Geltungsbereich der Variable muss die Methode enthalten.</span><span class="sxs-lookup"><span data-stu-id="f0593-113">That variable's scope must include the method.</span></span> <span data-ttu-id="f0593-114">Die Lebensdauer dieser Variable muss über die Rückgabe der Methode hinausgehen.</span><span class="sxs-lookup"><span data-stu-id="f0593-114">That variable's lifetime must extend beyond the return of the method.</span></span> <span data-ttu-id="f0593-115">Änderungen am Methodenrückgabewert durch die aufrufende Funktion werden auf die Variable angewendet, die von der Methode zurückgegeben wird.</span><span class="sxs-lookup"><span data-stu-id="f0593-115">Modifications to the method's return value by the caller are made to the variable that is returned by the method.</span></span>

<span data-ttu-id="f0593-116">Die Deklaration der Rückgabe eines *Verweisrückgabewerts* durch eine Methode weist darauf hin, dass die Methode einen Alias an eine Variable zurückgibt.</span><span class="sxs-lookup"><span data-stu-id="f0593-116">Declaring that a method returns a *reference return value* indicates that the method returns an alias to a variable.</span></span> <span data-ttu-id="f0593-117">Das Entwurfsziel dahinter besteht häufig darin, dem aufrufenden Code über den Alias Zugriff auf diese Variable sowie die Möglichkeit zu deren Änderung zu gewähren.</span><span class="sxs-lookup"><span data-stu-id="f0593-117">The design intent is often that the calling code should have access to that variable through the alias, including to modify it.</span></span> <span data-ttu-id="f0593-118">Daraus folgt, dass vom Verweis zurückgegebene Methoden nicht den Rückgabetyp `void` aufweisen dürfen.</span><span class="sxs-lookup"><span data-stu-id="f0593-118">It follows that methods returning by reference can't have the return type `void`.</span></span>

<span data-ttu-id="f0593-119">Es gibt einige Einschränkungen für den Ausdruck, den eine Methode als Verweisrückgabewert zurückgeben kann.</span><span class="sxs-lookup"><span data-stu-id="f0593-119">There are some restrictions on the expression that a method can return as a reference return value.</span></span> <span data-ttu-id="f0593-120">Es gelten folgende Beschränkungen:</span><span class="sxs-lookup"><span data-stu-id="f0593-120">Restrictions include:</span></span>

- <span data-ttu-id="f0593-121">Der Rückgabewert muss eine Lebensdauer aufweisen, die über die Ausführung der Methode hinausgeht.</span><span class="sxs-lookup"><span data-stu-id="f0593-121">The return value must have a lifetime that extends beyond the execution of the method.</span></span> <span data-ttu-id="f0593-122">Mit anderen Worten: Er darf in der Methode, die diesen zurückgibt, keine lokale Variable sein.</span><span class="sxs-lookup"><span data-stu-id="f0593-122">In other words, it cannot be a local variable in the method that returns it.</span></span> <span data-ttu-id="f0593-123">Es kann sich dabei um eine Instanz oder ein statisches Feld einer Klasse oder um ein Argument handeln, das an die Methode übergeben wurde.</span><span class="sxs-lookup"><span data-stu-id="f0593-123">It can be an instance or static field of a class, or it can be an argument passed to the method.</span></span> <span data-ttu-id="f0593-124">Beim Versuch, eine lokale Variable zurückzugeben, tritt der Compilerfehler CS8168 „Cannot return local 'obj' by reference because it is not a ref local.“ (Der lokale Wert „obj“ kann nicht als Verweis zurückgegeben werden, da er kein lokaler ref-Wert ist.) auf.</span><span class="sxs-lookup"><span data-stu-id="f0593-124">Attempting to return a local variable generates compiler error CS8168, "Cannot return local 'obj' by reference because it is not a ref local."</span></span>

- <span data-ttu-id="f0593-125">Der Rückgabewert darf nicht das `null`-Literal sein.</span><span class="sxs-lookup"><span data-stu-id="f0593-125">The return value cannot be the literal `null`.</span></span> <span data-ttu-id="f0593-126">Bei der Rückgabe von `null` tritt der Compilerfehler CS8156 „Ein Ausdruck kann in diesem Kontext nicht verwendet werden, weil er ggf. nicht als Verweis zurückgegeben wird.“ auf.</span><span class="sxs-lookup"><span data-stu-id="f0593-126">Returning `null` generates compiler error CS8156, "An expression cannot be used in this context because it may not be returned by reference."</span></span>

   <span data-ttu-id="f0593-127">Eine Methode mit einer Verweisrückgabe kann einen Alias an eine Variable zurückgeben, deren Wert derzeit NULL ist (nicht instanziiert) oder ein [Nullable-Werttyp](../../language-reference/builtin-types/nullable-value-types.md) für einen Werttyp darstellt.</span><span class="sxs-lookup"><span data-stu-id="f0593-127">A method with a ref return can return an alias to a variable whose value is currently the null (uninstantiated) value or a [nullable value type](../../language-reference/builtin-types/nullable-value-types.md) for a value type.</span></span>

- <span data-ttu-id="f0593-128">Der Rückgabewert darf keine Konstante, kein Enumerationsmember, nicht der by-value-Rückgabewert einer Eigenschaft und keine Methode einer `class` oder `struct` sein.</span><span class="sxs-lookup"><span data-stu-id="f0593-128">The return value cannot be a constant, an enumeration member, the by-value return value from a property, or a method of a `class` or `struct`.</span></span> <span data-ttu-id="f0593-129">Wenn gegen diese Regel verstoßen wird, tritt der Compilerfehler CS8156 „Ein Ausdruck kann in diesem Kontext nicht verwendet werden, weil er ggf. nicht als Verweis zurückgegeben wird.“ auf.</span><span class="sxs-lookup"><span data-stu-id="f0593-129">Violating this rule generates compiler error CS8156, "An expression cannot be used in this context because it may not be returned by reference."</span></span>

<span data-ttu-id="f0593-130">Zusätzlich sind Verweisrückgabewerte nicht für asynchrone Methoden zulässig.</span><span class="sxs-lookup"><span data-stu-id="f0593-130">In addition, reference return values are not allowed on async methods.</span></span> <span data-ttu-id="f0593-131">Eine asynchrone Methode wird möglicherweise zurückgegeben, bevor die Ausführung abgeschlossen ist, wobei der Rückgabewert noch unbekannt ist.</span><span class="sxs-lookup"><span data-stu-id="f0593-131">An asynchronous method may return before it has finished execution, while its return value is still unknown.</span></span>

## <a name="defining-a-ref-return-value"></a><span data-ttu-id="f0593-132">Definieren eines ref-Rückgabewerts</span><span class="sxs-lookup"><span data-stu-id="f0593-132">Defining a ref return value</span></span>

<span data-ttu-id="f0593-133">Eine Methode, die einen *Verweisrückgabewert* zurückgibt, muss die beiden folgenden Bedingungen erfüllen:</span><span class="sxs-lookup"><span data-stu-id="f0593-133">A method that returns a *reference return value* must satisfy the following two conditions:</span></span>

- <span data-ttu-id="f0593-134">Die Methodensignatur enthält das [ref](../../language-reference/keywords/ref.md)-Schlüsselwort vor den Rückgabetyp.</span><span class="sxs-lookup"><span data-stu-id="f0593-134">The method signature includes the [ref](../../language-reference/keywords/ref.md) keyword in front of the return type.</span></span>
- <span data-ttu-id="f0593-135">Jede [return](../../language-reference/keywords/return.md)-Anweisung im Hauptteil der Methode enthält das [ref](../../language-reference/keywords/ref.md)-Schlüsselwort vor den Namen der zurückgegebenen Instanz.</span><span class="sxs-lookup"><span data-stu-id="f0593-135">Each [return](../../language-reference/keywords/return.md) statement in the method body includes the [ref](../../language-reference/keywords/ref.md) keyword in front of the name of the returned instance.</span></span>

<span data-ttu-id="f0593-136">Das folgende Beispiel zeigt eine Methode, die die Bedingungen erfüllt und einen Verweis auf ein `Person`-Objekt mit dem Namen `p` zurückgibt:</span><span class="sxs-lookup"><span data-stu-id="f0593-136">The following example shows a method that satisfies those conditions and returns a reference to a `Person` object named `p`:</span></span>

```csharp
public ref Person GetContactInformation(string fname, string lname)
{
    // ...method implementation...
    return ref p;
}
```

## <a name="consuming-a-ref-return-value"></a><span data-ttu-id="f0593-137">Verarbeiten eines ref-Rückgabewerts</span><span class="sxs-lookup"><span data-stu-id="f0593-137">Consuming a ref return value</span></span>

<span data-ttu-id="f0593-138">Der Verweisrückgabewert ist ein Alias für eine andere Variable im Bereich der aufgerufenen Methode.</span><span class="sxs-lookup"><span data-stu-id="f0593-138">The ref return value is an alias to another variable in the called method's scope.</span></span> <span data-ttu-id="f0593-139">Sie können jede Verwendung der Verweisrückgabe als Verwendung der Variable interpretieren, für die diese als Alias fungiert:</span><span class="sxs-lookup"><span data-stu-id="f0593-139">You can interpret any use of the ref return as using the variable it aliases:</span></span>

- <span data-ttu-id="f0593-140">Wenn Sie den jeweiligen Wert zuweisen, weisen Sie der Variable, für die diese als Alias fungiert, einen Wert zu.</span><span class="sxs-lookup"><span data-stu-id="f0593-140">When you assign its value, you are assigning a value to the variable it aliases.</span></span>
- <span data-ttu-id="f0593-141">Wenn Sie dessen Wert lesen, lesen Sie den Wert der Variable, für die diese als Alias fungiert.</span><span class="sxs-lookup"><span data-stu-id="f0593-141">When you read its value, you are reading the value of the variable it aliases.</span></span>
- <span data-ttu-id="f0593-142">Wenn Sie diesen *nach Verweis* zurückgeben, geben Sie einen Alias für diese Variable zurück.</span><span class="sxs-lookup"><span data-stu-id="f0593-142">If you return it *by reference*, you are returning an alias to that same variable.</span></span>
- <span data-ttu-id="f0593-143">Wenn Sie ihn *nach Verweis* an eine andere Methode übergeben, übergeben Sie einen Verweis auf die Variable, für die diese als Alias fungiert.</span><span class="sxs-lookup"><span data-stu-id="f0593-143">If you pass it to another method *by reference*, you are passing a reference to the variable it aliases.</span></span>
- <span data-ttu-id="f0593-144">Wenn Sie einen Alias für einen [lokalen Verweis](#ref-locals) erstellen, erstellen Sie einen neuen Alias für diese Variable.</span><span class="sxs-lookup"><span data-stu-id="f0593-144">When you make a [ref local](#ref-locals) alias, you make a new alias to the same variable.</span></span>

## <a name="ref-locals"></a><span data-ttu-id="f0593-145">Lokale ref-Variablen</span><span class="sxs-lookup"><span data-stu-id="f0593-145">Ref locals</span></span>

<span data-ttu-id="f0593-146">Nehmen Sie an, dass die `GetContactInformation`-Methode als Verweisrückgabe deklariert ist:</span><span class="sxs-lookup"><span data-stu-id="f0593-146">Assume the `GetContactInformation` method is declared as a ref return:</span></span>

```csharp
public ref Person GetContactInformation(string fname, string lname)
```

<span data-ttu-id="f0593-147">Eine by-value-Zuweisung liest den Wert einer Variable und weist diesen einer neuen Variable zu:</span><span class="sxs-lookup"><span data-stu-id="f0593-147">A by-value assignment reads the value of a variable and assigns it to a new variable:</span></span>

```csharp
Person p = contacts.GetContactInformation("Brandie", "Best");
```

<span data-ttu-id="f0593-148">Die vorangehende Zuweisung deklariert `p` als lokale Variable.</span><span class="sxs-lookup"><span data-stu-id="f0593-148">The preceding assignment declares `p` as a local variable.</span></span> <span data-ttu-id="f0593-149">Der ursprüngliche Wert wird durch Lesen des Rückgabewerts kopiert, der von `GetContactInformation` zurückgegeben wurde.</span><span class="sxs-lookup"><span data-stu-id="f0593-149">Its initial value is copied from reading the value returned by `GetContactInformation`.</span></span> <span data-ttu-id="f0593-150">Keine der zukünftigen Zuweisungen zu `p` ändern den Wert der von `GetContactInformation` zurückgegebenen Variable.</span><span class="sxs-lookup"><span data-stu-id="f0593-150">Any future assignments to `p` will not change the value of the variable returned by `GetContactInformation`.</span></span> <span data-ttu-id="f0593-151">Die Variable `p` fungiert nicht mehr als Alias für die zurückgegebene Variable.</span><span class="sxs-lookup"><span data-stu-id="f0593-151">The variable `p` is no longer an alias to the variable returned.</span></span>

<span data-ttu-id="f0593-152">Sie deklarieren eine *lokale Verweisvariable*, um den Alias in den ursprünglichen Wert zu kopieren.</span><span class="sxs-lookup"><span data-stu-id="f0593-152">You declare a *ref local* variable to copy the alias to the original value.</span></span> <span data-ttu-id="f0593-153">In der folgenden Zuweisung ist `p` ein Alias für die von `GetContactInformation` zurückgegebene Variable.</span><span class="sxs-lookup"><span data-stu-id="f0593-153">In the following assignment, `p` is an alias to the variable returned from `GetContactInformation`.</span></span>

```csharp
ref Person p = ref contacts.GetContactInformation("Brandie", "Best");
```

<span data-ttu-id="f0593-154">Die nachfolgende Verwendung von `p` ist mit der Verwendung der von `GetContactInformation` zurückgegebenen Variable identisch, da `p` ein Alias für diese Variable darstellt.</span><span class="sxs-lookup"><span data-stu-id="f0593-154">Subsequent usage of `p` is the same as using the variable returned by `GetContactInformation` because `p` is an alias for that variable.</span></span> <span data-ttu-id="f0593-155">Durch Änderungen an `p` wird auch die von `GetContactInformation` zurückgegebene Variable geändert.</span><span class="sxs-lookup"><span data-stu-id="f0593-155">Changes to `p` also change the variable returned from `GetContactInformation`.</span></span>

<span data-ttu-id="f0593-156">Das Schlüsselwort `ref` wird sowohl vor der Deklaration lokaler Variablen *als auch* vor dem Methodenaufruf verwendet.</span><span class="sxs-lookup"><span data-stu-id="f0593-156">The `ref` keyword is used both before the local variable declaration *and* before the method call.</span></span>

<span data-ttu-id="f0593-157">Auch auf Werte können Sie per Verweis zugreifen.</span><span class="sxs-lookup"><span data-stu-id="f0593-157">You can access a value by reference in the same way.</span></span> <span data-ttu-id="f0593-158">In einigen Fällen erhöht dies die Leistung, da ein möglicherweise aufwendiger Kopiervorgang vermieden wird.</span><span class="sxs-lookup"><span data-stu-id="f0593-158">In some cases, accessing a value by reference increases performance by avoiding a potentially expensive copy operation.</span></span> <span data-ttu-id="f0593-159">In der folgenden Anweisung wird z.B. gezeigt, wie ein lokaler Verweiswert definiert wird, mit dem auf einen Wert verwiesen wird.</span><span class="sxs-lookup"><span data-stu-id="f0593-159">For example, the following statement shows how one can define a ref local value that is used to reference a value.</span></span>

```csharp
ref VeryLargeStruct reflocal = ref veryLargeStruct;
```

<span data-ttu-id="f0593-160">Das Schlüsselwort `ref` wird sowohl vor der Deklaration lokaler Variablen *als auch* vor dem Wert im zweiten Beispiel verwendet.</span><span class="sxs-lookup"><span data-stu-id="f0593-160">The `ref` keyword is used both before the local variable declaration *and* before the value in the second example.</span></span> <span data-ttu-id="f0593-161">Wenn nicht in beiden Beispielen beide `ref`-Schlüsselwörter in den Ergebnissen der Variablendeklaration und der Zuweisung enthalten sind, tritt der Compilerfehler CS8172 "Cannot initialize a by-reference variable with a value." (Eine by-reference-Variable kann nicht mit einem Wert initialisiert werden) auf.</span><span class="sxs-lookup"><span data-stu-id="f0593-161">Failure to include both `ref` keywords in the variable declaration and assignment in both examples results in compiler error CS8172, "Cannot initialize a by-reference variable with a value."</span></span>

<span data-ttu-id="f0593-162">Vor C# 7.3 konnten lokalen ref-Variablen nach der Initialisierung nicht neu zugewiesen werden, um auf einen anderen Speicher zu verweisen.</span><span class="sxs-lookup"><span data-stu-id="f0593-162">Prior to C# 7.3, ref local variables couldn't be reassigned to refer to different storage after being initialized.</span></span> <span data-ttu-id="f0593-163">Diese Einschränkung wurde entfernt.</span><span class="sxs-lookup"><span data-stu-id="f0593-163">That restriction has been removed.</span></span> <span data-ttu-id="f0593-164">Im folgenden Beispiel wird eine Neuzuweisung veranschaulicht:</span><span class="sxs-lookup"><span data-stu-id="f0593-164">The following example shows a reassignment:</span></span>

```csharp
ref VeryLargeStruct reflocal = ref veryLargeStruct; // initialization
refLocal = ref anotherVeryLargeStruct; // reassigned, refLocal refers to different storage.
```

 <span data-ttu-id="f0593-165">Lokale ref-Variablen müssen noch immer initialisiert werden, wenn sie deklariert werden.</span><span class="sxs-lookup"><span data-stu-id="f0593-165">Ref local variables must still be initialized when they are declared.</span></span>

## <a name="ref-returns-and-ref-locals-an-example"></a><span data-ttu-id="f0593-166">Ref-Rückgaben und lokale ref-Variablen: ein Beispiel</span><span class="sxs-lookup"><span data-stu-id="f0593-166">Ref returns and ref locals: an example</span></span>

<span data-ttu-id="f0593-167">Das folgende Beispiel definiert eine `NumberStore`-Klasse, die ein Array von Integer-Werten speichert.</span><span class="sxs-lookup"><span data-stu-id="f0593-167">The following example defines a `NumberStore` class that stores an array of integer values.</span></span> <span data-ttu-id="f0593-168">Die `FindNumber`-Methode gibt die erste Anzahl, die größer als oder gleich der Anzahl ist, die als Argument übergeben wurde, als Verweis zurück.</span><span class="sxs-lookup"><span data-stu-id="f0593-168">The `FindNumber` method returns by reference the first number that is greater than or equal to the number passed as an argument.</span></span> <span data-ttu-id="f0593-169">Wenn keine Anzahl größer als oder gleich dem Argument ist, gibt die Methode die Anzahl im Index 0 zurück.</span><span class="sxs-lookup"><span data-stu-id="f0593-169">If no number is greater than or equal to the argument, the method returns the number in index 0.</span></span>

[!code-csharp[ref-returns](../../../../samples/snippets/csharp/programming-guide/ref-returns/NumberStore.cs#1)]

<span data-ttu-id="f0593-170">Im folgenden Beispiel wird die `NumberStore.FindNumber`-Methode aufgerufen, um den ersten Wert abzurufen, der größer als oder gleich 16 ist.</span><span class="sxs-lookup"><span data-stu-id="f0593-170">The following example calls the `NumberStore.FindNumber` method to retrieve the first value that is greater than or equal to 16.</span></span> <span data-ttu-id="f0593-171">Die aufrufende Funktion verdoppelt dann den von der Methode zurückgegebenen Wert.</span><span class="sxs-lookup"><span data-stu-id="f0593-171">The caller then doubles the value returned by the method.</span></span> <span data-ttu-id="f0593-172">Die Ausgabe aus dem Beispiel zeigt die Änderung, die im Wert der Arrayelemente der `NumberStore`-Instanz wiedergegeben wird.</span><span class="sxs-lookup"><span data-stu-id="f0593-172">The output from the example shows the change reflected in the value of the array elements of the `NumberStore` instance.</span></span>

[!code-csharp[ref-returns](../../../../samples/snippets/csharp/programming-guide/ref-returns/NumberStore.cs#2)]

<span data-ttu-id="f0593-173">Ohne Unterstützung für Verweisrückgabewerte wird ein solcher Vorgang durchgeführt, indem der Index des Arrayelements zusammen mit seinem Wert zurückgegeben wird.</span><span class="sxs-lookup"><span data-stu-id="f0593-173">Without support for reference return values, such an operation is performed by returning the index of the array element along with its value.</span></span> <span data-ttu-id="f0593-174">Die aufrufende Funktion kann diesen Index dann dazu verwenden, den Wert in einem separaten Methodenaufruf zu ändern.</span><span class="sxs-lookup"><span data-stu-id="f0593-174">The caller can then use this index to modify the value in a separate method call.</span></span> <span data-ttu-id="f0593-175">Die aufrufende Funktion kann den Index jedoch auch ändern, um auf andere Arraywerte zuzugreifen und diese zu bearbeiten.</span><span class="sxs-lookup"><span data-stu-id="f0593-175">However, the caller can also modify the index to access and possibly modify other array values.</span></span>  

<span data-ttu-id="f0593-176">Im folgenden Beispiel wird gezeigt, wie die `FindNumber`-Methode nach C# 7.3 neu geschrieben werden kann, damit sie die lokale ref-Neuzuweisung verwendet:</span><span class="sxs-lookup"><span data-stu-id="f0593-176">The following example shows how the `FindNumber` method could be rewritten after C# 7.3 to use ref local reassignment:</span></span>

[!code-csharp[ref-returns](../../../../samples/snippets/csharp/programming-guide/ref-returns/NumberStoreUpdated.cs#1)]

<span data-ttu-id="f0593-177">Diese zweite Version ist durch längere Sequenzen in Szenarios, in denen die gesuchte Zahl am Ende des Arrays liegt, effizienter.</span><span class="sxs-lookup"><span data-stu-id="f0593-177">This second version is more efficient with longer sequences in scenarios where the number sought is closer to the end of the array.</span></span>

## <a name="see-also"></a><span data-ttu-id="f0593-178">Weitere Informationen</span><span class="sxs-lookup"><span data-stu-id="f0593-178">See also</span></span>

- [<span data-ttu-id="f0593-179">ref (C#-Referenz)</span><span class="sxs-lookup"><span data-stu-id="f0593-179">ref keyword</span></span>](../../language-reference/keywords/ref.md)
- [<span data-ttu-id="f0593-180">Schreiben von sicherem und effizientem Code</span><span class="sxs-lookup"><span data-stu-id="f0593-180">Write safe efficient code</span></span>](../../write-safe-efficient-code.md)
