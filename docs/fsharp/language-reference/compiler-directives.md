---
title: Compileranweisungen
description: Erfahren Sie F# mehr über sprach Präprozessordirektiven, bedingte Kompilierungs Direktiven, Zeilen Anweisungen und Compilerdirektiven.
ms.date: 12/10/2018
ms.openlocfilehash: 16db2efb2fee2c2c5e94aa98eb0a13183a4e0e0b
ms.sourcegitcommit: f20dd18dbcf2275513281f5d9ad7ece6a62644b4
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 07/30/2019
ms.locfileid: "68630400"
---
# <a name="compiler-directives"></a><span data-ttu-id="cb843-103">Compileranweisungen</span><span class="sxs-lookup"><span data-stu-id="cb843-103">Compiler Directives</span></span>

<span data-ttu-id="cb843-104">In diesem Thema werden Prozessor- und Compileranweisungen beschrieben.</span><span class="sxs-lookup"><span data-stu-id="cb843-104">This topic describes processor directives and compiler directives.</span></span>

## <a name="preprocessor-directives"></a><span data-ttu-id="cb843-105">Präprozessordirektiven</span><span class="sxs-lookup"><span data-stu-id="cb843-105">Preprocessor Directives</span></span>

<span data-ttu-id="cb843-106">Einer Präprozessordirektive wird das #-Symbol vorangestellt und wird für sich genommen in einer Zeile angezeigt.</span><span class="sxs-lookup"><span data-stu-id="cb843-106">A preprocessor directive is prefixed with the # symbol and appears on a line by itself.</span></span> <span data-ttu-id="cb843-107">Sie wird durch den Präprozessor interpretiert, der vom Compiler ausgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="cb843-107">It is interpreted by the preprocessor, which runs before the compiler itself.</span></span>

<span data-ttu-id="cb843-108">In der folgenden Tabelle werden die Präprozessordirektiven aufgelistet, die in F# verfügbar sind.</span><span class="sxs-lookup"><span data-stu-id="cb843-108">The following table lists the preprocessor directives that are available in F#.</span></span>

|<span data-ttu-id="cb843-109">Direktive</span><span class="sxs-lookup"><span data-stu-id="cb843-109">Directive</span></span>|<span data-ttu-id="cb843-110">Beschreibung</span><span class="sxs-lookup"><span data-stu-id="cb843-110">Description</span></span>|
|---------|-----------|
|<span data-ttu-id="cb843-111">`#if`*Symbol*</span><span class="sxs-lookup"><span data-stu-id="cb843-111">`#if` *symbol*</span></span>|<span data-ttu-id="cb843-112">Unterstützt die bedingte Kompilierung.</span><span class="sxs-lookup"><span data-stu-id="cb843-112">Supports conditional compilation.</span></span> <span data-ttu-id="cb843-113">Code im-Abschnitt, nachdem `#if` der eingefügt wurde, wenn das *Symbol* definiert ist.</span><span class="sxs-lookup"><span data-stu-id="cb843-113">Code in the section after the `#if` is included if the *symbol* is defined.</span></span> <span data-ttu-id="cb843-114">Das Symbol kann auch mit `!`negiert werden.</span><span class="sxs-lookup"><span data-stu-id="cb843-114">The symbol can also be negated with `!`.</span></span>|
|`#else`|<span data-ttu-id="cb843-115">Unterstützt die bedingte Kompilierung.</span><span class="sxs-lookup"><span data-stu-id="cb843-115">Supports conditional compilation.</span></span> <span data-ttu-id="cb843-116">Markiert einen einzubeziehenden Codeabschnitt, wenn das mit dem vorherigen verwendeten `#if` nicht definiert ist.</span><span class="sxs-lookup"><span data-stu-id="cb843-116">Marks a section of code to include if the symbol used with the previous `#if` is not defined.</span></span>|
|`#endif`|<span data-ttu-id="cb843-117">Unterstützt die bedingte Kompilierung.</span><span class="sxs-lookup"><span data-stu-id="cb843-117">Supports conditional compilation.</span></span> <span data-ttu-id="cb843-118">Markiert das Ende eines bedingten Codeabschnitts.</span><span class="sxs-lookup"><span data-stu-id="cb843-118">Marks the end of a conditional section of code.</span></span>|
|<span data-ttu-id="cb843-119">`#`Stimmen *int*,</span><span class="sxs-lookup"><span data-stu-id="cb843-119">`#`[line] *int*,</span></span><br/><span data-ttu-id="cb843-120">`#`Stimmen *int* *Zeichenfolge*,</span><span class="sxs-lookup"><span data-stu-id="cb843-120">`#`[line] *int* *string*,</span></span><br/><span data-ttu-id="cb843-121">`#`Stimmen *int* *wörtliche Zeichenfolge*</span><span class="sxs-lookup"><span data-stu-id="cb843-121">`#`[line] *int* *verbatim-string*</span></span>|<span data-ttu-id="cb843-122">Gibt die ursprüngliche Quellcodezeile und den Dateinamen für das Debuggen an.</span><span class="sxs-lookup"><span data-stu-id="cb843-122">Indicates the original source code line and file name, for debugging.</span></span> <span data-ttu-id="cb843-123">Diese Funktion wird für Tools bereitgestellt, die F#-Quellcode generieren.</span><span class="sxs-lookup"><span data-stu-id="cb843-123">This feature is provided for tools that generate F# source code.</span></span>|
|<span data-ttu-id="cb843-124">`#nowarn`*warningCode*</span><span class="sxs-lookup"><span data-stu-id="cb843-124">`#nowarn` *warningcode*</span></span>|<span data-ttu-id="cb843-125">Deaktiviert eine Compilerwarnung oder Warnungen.</span><span class="sxs-lookup"><span data-stu-id="cb843-125">Disables a compiler warning or warnings.</span></span> <span data-ttu-id="cb843-126">Suchen Sie zum Deaktivieren einer Warnung nach ihrer Nummer in der Compilerausgabe, und setzen Sie sie in Anführungszeichen.</span><span class="sxs-lookup"><span data-stu-id="cb843-126">To disable a warning, find its number from the compiler output and include it in quotation marks.</span></span> <span data-ttu-id="cb843-127">Lassen Sie das Präfix „FS“ weg.</span><span class="sxs-lookup"><span data-stu-id="cb843-127">Omit the "FS" prefix.</span></span> <span data-ttu-id="cb843-128">Zum Deaktivieren von mehreren Warnnummern in derselben Zeile müssen Sie jede Nummer in Anführungszeichen setzen und jede Zeichenfolge durch ein Leerzeichen abtrennen.</span><span class="sxs-lookup"><span data-stu-id="cb843-128">To disable multiple warning numbers on the same line, include each number in quotation marks, and separate each string by a space.</span></span> <span data-ttu-id="cb843-129">Zum Beispiel:</span><span class="sxs-lookup"><span data-stu-id="cb843-129">For example:</span></span>

`#nowarn "9" "40"`

<span data-ttu-id="cb843-130">Die Auswirkung der Deaktivierung einer Warnung gilt für die gesamte Datei, einschließlich der Teile der Datei, die der Direktive vorangestellt sind.</span><span class="sxs-lookup"><span data-stu-id="cb843-130">The effect of disabling a warning applies to the entire file, including portions of the file that precede the directive.|</span></span>

## <a name="conditional-compilation-directives"></a><span data-ttu-id="cb843-131">Anweisungen für die bedingte Kompilierung</span><span class="sxs-lookup"><span data-stu-id="cb843-131">Conditional Compilation Directives</span></span>

<span data-ttu-id="cb843-132">Code, der von einer dieser Direktiven deaktiviert wird, wird im Visual Studio Code-Editor abgeblendet angezeigt.</span><span class="sxs-lookup"><span data-stu-id="cb843-132">Code that is deactivated by one of these directives appears dimmed in the Visual Studio Code Editor.</span></span>

> [!NOTE]
> <span data-ttu-id="cb843-133">Das Verhalten der Anweisungen für die bedingte Kompilierung entspricht nicht dem in anderen Sprachen.</span><span class="sxs-lookup"><span data-stu-id="cb843-133">The behavior of the conditional compilation directives is not the same as it is in other languages.</span></span> <span data-ttu-id="cb843-134">Beispielsweise können Sie keine booleschen Ausdrücke mit Symbolen verwenden, zudem verfügen `true` und `false` über keine besondere Bedeutung.</span><span class="sxs-lookup"><span data-stu-id="cb843-134">For example, you cannot use Boolean expressions involving symbols, and `true` and `false` have no special meaning.</span></span> <span data-ttu-id="cb843-135">In der `if`-Direktive von Ihnen verwendete Symbole müssen über die Befehlszeile oder in den Projekteinstellungen definiert werden. Es steht keine `define`-Präprozessordirektive zur Verfügung.</span><span class="sxs-lookup"><span data-stu-id="cb843-135">Symbols that you use in the `if` directive must be defined by the command line or in the project settings; there is no `define` preprocessor directive.</span></span>

<span data-ttu-id="cb843-136">Im folgenden Code wird die Verwendung der Direktiven `#if`, `#else` und `#endif` veranschaulicht.</span><span class="sxs-lookup"><span data-stu-id="cb843-136">The following code illustrates the use of the `#if`, `#else`, and `#endif` directives.</span></span> <span data-ttu-id="cb843-137">In diesem Beispiel enthält der Code zwei Versionen der Definition von `function1`.</span><span class="sxs-lookup"><span data-stu-id="cb843-137">In this example, the code contains two versions of the definition of `function1`.</span></span> <span data-ttu-id="cb843-138">Wenn `VERSION1` mithilfe der [-define-Compileroption](https://msdn.microsoft.com/library/434394ae-0d4a-459c-a684-bffede519a04)definiert wird, wird der Code `#if` zwischen der- `#else` Direktive und der-Direktive aktiviert.</span><span class="sxs-lookup"><span data-stu-id="cb843-138">When `VERSION1` is defined by using the [-define compiler option](https://msdn.microsoft.com/library/434394ae-0d4a-459c-a684-bffede519a04), the code between the `#if` directive and the `#else` directive is activated.</span></span> <span data-ttu-id="cb843-139">Andernfalls wird der Code zwischen `#else` und `#endif` aktiviert.</span><span class="sxs-lookup"><span data-stu-id="cb843-139">Otherwise, the code between `#else` and `#endif` is activated.</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-2/snippet7301.fs)]

<span data-ttu-id="cb843-140">Es gibt keine `#define`-Präprozessoranweisung in F#.</span><span class="sxs-lookup"><span data-stu-id="cb843-140">There is no `#define` preprocessor directive in F#.</span></span> <span data-ttu-id="cb843-141">Sie müssen die Compileroption oder Projekteinstellungen verwenden, um die durch die `#if`-Direktive verwendeten Symbole zu definieren.</span><span class="sxs-lookup"><span data-stu-id="cb843-141">You must use the compiler option or project settings to define the symbols used by the `#if` directive.</span></span>

<span data-ttu-id="cb843-142">Direktiven für die bedingte Kompilierung können geschachtelt werden.</span><span class="sxs-lookup"><span data-stu-id="cb843-142">Conditional compilation directives can be nested.</span></span> <span data-ttu-id="cb843-143">Der Einzug ist für Präprozessordirektiven nicht entscheidend.</span><span class="sxs-lookup"><span data-stu-id="cb843-143">Indentation is not significant for preprocessor directives.</span></span>

<span data-ttu-id="cb843-144">Sie können ein Symbol auch mit `!`negieren.</span><span class="sxs-lookup"><span data-stu-id="cb843-144">You can also negate a symbol with `!`.</span></span> <span data-ttu-id="cb843-145">In diesem Beispiel ist der Wert einer Zeichenfolge _nur beim_ Debuggen etwas:</span><span class="sxs-lookup"><span data-stu-id="cb843-145">In this example, a string's value is something only when _not_ debugging:</span></span>

```fsharp
#if !DEBUG
let str = "Not debugging!"
#else
let str = "Debugging!"
#endif
```

## <a name="line-directives"></a><span data-ttu-id="cb843-146">Line-Direktiven</span><span class="sxs-lookup"><span data-stu-id="cb843-146">Line Directives</span></span>

<span data-ttu-id="cb843-147">Beim Erstellen meldet der Compiler Fehler im F#-Code durch das Referenzieren von Zeilennummern, in denen die jeweiligen Fehler auftreten.</span><span class="sxs-lookup"><span data-stu-id="cb843-147">When building, the compiler reports errors in F# code by referencing line numbers on which each error occurs.</span></span> <span data-ttu-id="cb843-148">Diese Zeilennummern beginnen bei 1 für die erste Zeile in einer Datei.</span><span class="sxs-lookup"><span data-stu-id="cb843-148">These line numbers start at 1 for the first line in a file.</span></span> <span data-ttu-id="cb843-149">Wenn Sie jedoch F#-Quellcode aus einem anderen Tool generieren, sind die Zeilennummern im generierten Code im Allgemeinen nicht relevant, da die Fehler im generierten F#-Code höchstwahrscheinlich auf eine andere Quelle zurückgehen.</span><span class="sxs-lookup"><span data-stu-id="cb843-149">However, if you are generating F# source code from another tool, the line numbers in the generated code are generally not of interest, because the errors in the generated F# code most likely arise from another source.</span></span> <span data-ttu-id="cb843-150">Mit der `#line`-Direktive können Autoren von Tools, die F#-Quellcode generieren, Informationen über die ursprünglichen Zeilennummern und Quelldateien an den generierten F#-Code weitergeben.</span><span class="sxs-lookup"><span data-stu-id="cb843-150">The `#line` directive provides a way for authors of tools that generate F# source code to pass information about the original line numbers and source files to the generated F# code.</span></span>

<span data-ttu-id="cb843-151">Beim Verwenden der `#line`-Direktive müssen Dateinamen in Anführungszeichen gesetzt werden.</span><span class="sxs-lookup"><span data-stu-id="cb843-151">When you use the `#line` directive, file names must be enclosed in quotation marks.</span></span> <span data-ttu-id="cb843-152">Sofern das verbatim-Token (`@`) nicht am Anfang der Zeichenfolge angezeigt wird, müssen Sie umgekehrte Schrägstriche durch die Verwendung von zwei (und nicht nur einem) umgekehrten Schrägstrichen escapen, um sie im Pfad zu verwenden.</span><span class="sxs-lookup"><span data-stu-id="cb843-152">Unless the verbatim token (`@`) appears in front of the string, you must escape backslash characters by using two backslash characters instead of one in order to use them in the path.</span></span> <span data-ttu-id="cb843-153">Im Folgenden finden Sie gültige Zeilentoken.</span><span class="sxs-lookup"><span data-stu-id="cb843-153">The following are valid line tokens.</span></span> <span data-ttu-id="cb843-154">In diesen Beispielen wird davon ausgegangen, dass die ursprüngliche `Script1`-Datei in einer automatisch generierten F#-Codedatei resultiert, wenn er über ein Tool ausgeführt wird, und dass der Code am Speicherort dieser Anweisungen aus einigen Token in Zeile 25 in der Datei `Script1` generiert wird.</span><span class="sxs-lookup"><span data-stu-id="cb843-154">In these examples, assume that the original file `Script1` results in an automatically generated F# code file when it is run through a tool, and that the code at the location of these directives is generated from some tokens at line 25 in file `Script1`.</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-2/snippet7303.fs)]

<span data-ttu-id="cb843-155">Diese Token geben an, dass der an diesem Speicherort generierte F#-Code aus einigen Konstrukten in oder in der Nähe der Zeile `25` in `Script1` abgeleitet ist.</span><span class="sxs-lookup"><span data-stu-id="cb843-155">These tokens indicate that the F# code generated at this location is derived from some constructs at or near line `25` in `Script1`.</span></span>

## <a name="compiler-directives"></a><span data-ttu-id="cb843-156">Compileranweisungen</span><span class="sxs-lookup"><span data-stu-id="cb843-156">Compiler Directives</span></span>

<span data-ttu-id="cb843-157">Compilerdirektiven ähneln Präprozessordirektiven, da ihnen ein #-Zeichen vorangestellt ist. Anstelle jedoch durch den Präprozessor interpretiert zu werden, werden sie durch den Compiler interpretiert und verarbeitet.</span><span class="sxs-lookup"><span data-stu-id="cb843-157">Compiler directives resemble preprocessor directives, because they are prefixed with a # sign, but instead of being interpreted by the preprocessor, they are left for the compiler to interpret and act on.</span></span>

<span data-ttu-id="cb843-158">Die folgende Tabelle enthält die Compilerdirektive, die in F# verfügbar ist.</span><span class="sxs-lookup"><span data-stu-id="cb843-158">The following table lists the compiler directive that is available in F#.</span></span>

|<span data-ttu-id="cb843-159">Direktive</span><span class="sxs-lookup"><span data-stu-id="cb843-159">Directive</span></span>|<span data-ttu-id="cb843-160">Beschreibung</span><span class="sxs-lookup"><span data-stu-id="cb843-160">Description</span></span>|
|---------|-----------|
|<span data-ttu-id="cb843-161">`#light` ["on"&#124;"off"]</span><span class="sxs-lookup"><span data-stu-id="cb843-161">`#light` ["on"&#124;"off"]</span></span>|<span data-ttu-id="cb843-162">Aktiviert oder deaktiviert die einfache Syntax für die Kompatibilität mit anderen MK-Versionen.</span><span class="sxs-lookup"><span data-stu-id="cb843-162">Enables or disables lightweight syntax, for compatibility with other versions of ML.</span></span> <span data-ttu-id="cb843-163">Standardmäßig ist die einfache Syntax aktiviert.</span><span class="sxs-lookup"><span data-stu-id="cb843-163">By default, lightweight syntax is enabled.</span></span> <span data-ttu-id="cb843-164">Die ausführliche Syntax ist immer aktiviert.</span><span class="sxs-lookup"><span data-stu-id="cb843-164">Verbose syntax is always enabled.</span></span> <span data-ttu-id="cb843-165">Daher können Sie die einfache und ausführliche Syntax verwenden.</span><span class="sxs-lookup"><span data-stu-id="cb843-165">Therefore, you can use both lightweight syntax and verbose syntax.</span></span> <span data-ttu-id="cb843-166">Die Direktive `#light` an sich entspricht `#light "on"`.</span><span class="sxs-lookup"><span data-stu-id="cb843-166">The directive `#light` by itself is equivalent to `#light "on"`.</span></span> <span data-ttu-id="cb843-167">Beim Angeben von `#light "off"` müssen Sie die ausführliche Syntax für alle Sprachkonstrukte verwenden.</span><span class="sxs-lookup"><span data-stu-id="cb843-167">If you specify `#light "off"`, you must use verbose syntax for all language constructs.</span></span> <span data-ttu-id="cb843-168">Bei der in der Dokumentation für F# gezeigten Syntax wird davon ausgegangen, dass Sie die einfache Syntax verwenden.</span><span class="sxs-lookup"><span data-stu-id="cb843-168">Syntax in the documentation for F# is presented with the assumption that you are using lightweight syntax.</span></span> <span data-ttu-id="cb843-169">Weitere Informationen finden Sie unter ausführliche [Syntax](verbose-syntax.md).</span><span class="sxs-lookup"><span data-stu-id="cb843-169">For more information, see [Verbose Syntax](verbose-syntax.md).</span></span>|

<span data-ttu-id="cb843-170">Interpreter (fsi.exe)-Anweisungen finden Sie unter [Interaktive Programmierung mit F#](../tutorials/fsharp-interactive/index.md).</span><span class="sxs-lookup"><span data-stu-id="cb843-170">For interpreter (fsi.exe) directives, see [Interactive Programming with F#](../tutorials/fsharp-interactive/index.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="cb843-171">Siehe auch</span><span class="sxs-lookup"><span data-stu-id="cb843-171">See also</span></span>

- [<span data-ttu-id="cb843-172">F#-Sprachreferenz</span><span class="sxs-lookup"><span data-stu-id="cb843-172">F# Language Reference</span></span>](index.md)
- [<span data-ttu-id="cb843-173">Compileroptionen</span><span class="sxs-lookup"><span data-stu-id="cb843-173">Compiler Options</span></span>](compiler-options.md)
