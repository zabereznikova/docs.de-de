---
title: Zitieren von Code (F#)
description: Erfahren Sie mehr über F# Codezitate, eine Sprachfunktion, die Ihnen zum Generieren von und arbeiten programmgesteuert mit F#-Codeausdrücken ermöglicht.
ms.date: 05/16/2016
ms.openlocfilehash: 27e9cf1d99e2b5955cc6359653fc87bdbe824cc7
ms.sourcegitcommit: db8b83057d052c1f9f249d128b08d4423af0f7c2
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 11/02/2018
ms.locfileid: "47397201"
---
# <a name="code-quotations"></a><span data-ttu-id="316db-103">Zitieren von Code</span><span class="sxs-lookup"><span data-stu-id="316db-103">Code Quotations</span></span>

> [!NOTE]
<span data-ttu-id="316db-104">Mit dem API-Referenz-Link gelangen Sie auf MSDN.</span><span class="sxs-lookup"><span data-stu-id="316db-104">The API reference link will take you to MSDN.</span></span>  <span data-ttu-id="316db-105">Die docs.microsoft.com-API-Referenz ist nicht abgeschlossen.</span><span class="sxs-lookup"><span data-stu-id="316db-105">The docs.microsoft.com API reference is not complete.</span></span>

<span data-ttu-id="316db-106">In diesem Thema wird beschrieben, *Codezitate*, eine Sprachfunktion, die Ihnen zum Generieren von und arbeiten programmgesteuert mit F#-Codeausdrücken ermöglicht.</span><span class="sxs-lookup"><span data-stu-id="316db-106">This topic describes *code quotations*, a language feature that enables you to generate and work with F# code expressions programmatically.</span></span> <span data-ttu-id="316db-107">Dieses Feature ermöglicht Ihnen eine abstrakte Syntaxstruktur generieren, die F#-Code darstellt.</span><span class="sxs-lookup"><span data-stu-id="316db-107">This feature lets you generate an abstract syntax tree that represents F# code.</span></span> <span data-ttu-id="316db-108">Die abstrakte Syntaxstruktur kann dann durchlaufen und gemäß den Anforderungen Ihrer Anwendung verarbeitet werden.</span><span class="sxs-lookup"><span data-stu-id="316db-108">The abstract syntax tree can then be traversed and processed according to the needs of your application.</span></span> <span data-ttu-id="316db-109">Beispielsweise können Sie die Struktur zum Generieren von F#-Code oder Generieren von Code in einer anderen Sprache.</span><span class="sxs-lookup"><span data-stu-id="316db-109">For example, you can use the tree to generate F# code or generate code in some other language.</span></span>

## <a name="quoted-expressions"></a><span data-ttu-id="316db-110">Zitierte Ausdrücke</span><span class="sxs-lookup"><span data-stu-id="316db-110">Quoted Expressions</span></span>

<span data-ttu-id="316db-111">Ein *in Anführungszeichen Ausdruck* ist ein F#-Ausdruck in Ihrem Code, der getrennt wird, so, dass es nicht als Teil des Programms kompiliert wird, sondern stattdessen in ein Objekt, das einen F#-Ausdruck darstellt.</span><span class="sxs-lookup"><span data-stu-id="316db-111">A *quoted expression* is an F# expression in your code that is delimited in such a way that it is not compiled as part of your program, but instead is compiled into an object that represents an F# expression.</span></span> <span data-ttu-id="316db-112">Sie können einen Ausdruck in Anführungszeichen in eine von zwei Arten kennzeichnen: entweder mit oder ohne Typinformationen.</span><span class="sxs-lookup"><span data-stu-id="316db-112">You can mark a quoted expression in one of two ways: either with type information or without type information.</span></span> <span data-ttu-id="316db-113">Wenn Sie Typinformationen einschließen möchten, verwenden Sie die Symbole `<@` und `@>` zitierten Ausdrucks zu trennen.</span><span class="sxs-lookup"><span data-stu-id="316db-113">If you want to include type information, you use the symbols `<@` and `@>` to delimit the quoted expression.</span></span> <span data-ttu-id="316db-114">Wenn Sie Typinformationen nicht benötigen, verwenden Sie die Symbole `<@@` und `@@>`.</span><span class="sxs-lookup"><span data-stu-id="316db-114">If you do not need type information, you use the symbols `<@@` and `@@>`.</span></span> <span data-ttu-id="316db-115">Der folgende Code zeigt die typisierte und nicht typisierte Angebote.</span><span class="sxs-lookup"><span data-stu-id="316db-115">The following code shows typed and untyped quotations.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-3/snippet501.fs)]

<span data-ttu-id="316db-116">Durchlaufen einer Ausdrucksbaumstruktur zu großen ist schneller, wenn Sie keine Typinformationen einschließen.</span><span class="sxs-lookup"><span data-stu-id="316db-116">Traversing a large expression tree is faster if you do not include type information.</span></span> <span data-ttu-id="316db-117">Der resultierende Typ eines Ausdrucks für die typisierte Symbole in Anführungszeichen ist `Expr<'T>`, in dem der Typparameter den Typ des Ausdrucks gemäß der Typrückschluss-Algorithmus, der F#-Compiler den Typ aufweist.</span><span class="sxs-lookup"><span data-stu-id="316db-117">The resulting type of an expression quoted with the typed symbols is `Expr<'T>`, where the type parameter has the type of the expression as determined by the F# compiler's type inference algorithm.</span></span> <span data-ttu-id="316db-118">Wenn Sie Zitieren von Code ohne Typinformationen verwenden, ist der Typ des zitierten Ausdrucks der nicht generischen Typ [Expr](https://msdn.microsoft.com/library/ed6a2caf-69d4-45c2-ab97-e9b3be9bce65).</span><span class="sxs-lookup"><span data-stu-id="316db-118">When you use code quotations without type information, the type of the quoted expression is the non-generic type [Expr](https://msdn.microsoft.com/library/ed6a2caf-69d4-45c2-ab97-e9b3be9bce65).</span></span> <span data-ttu-id="316db-119">Rufen Sie die [Raw](https://msdn.microsoft.com/library/47fb94f1-e77f-4c68-aabc-2b0ba40d59c2) Eigenschaft für die typisierte `Expr` Klasse, die nicht typisierte erhalten `Expr` Objekt.</span><span class="sxs-lookup"><span data-stu-id="316db-119">You can call the [Raw](https://msdn.microsoft.com/library/47fb94f1-e77f-4c68-aabc-2b0ba40d59c2) property on the typed `Expr` class to obtain the untyped `Expr` object.</span></span>

<span data-ttu-id="316db-120">Es gibt eine Vielzahl statischer Methoden, mit denen Sie zum Generieren von F#-Ausdrucksobjekten programmgesteuert in die `Expr` Klasse ohne zitierte Ausdrücke.</span><span class="sxs-lookup"><span data-stu-id="316db-120">There are a variety of static methods that allow you to generate F# expression objects programmatically in the `Expr` class without using quoted expressions.</span></span>

<span data-ttu-id="316db-121">Beachten Sie, dass ein Code-Angebot einen vollständigen Ausdruck enthalten muss.</span><span class="sxs-lookup"><span data-stu-id="316db-121">Note that a code quotation must include a complete expression.</span></span> <span data-ttu-id="316db-122">Für eine `let` Bindung, beispielsweise benötigen Sie sowohl die Definition der gebundenen Namens und einen zusätzlichen Ausdruck, der die Bindung verwendet.</span><span class="sxs-lookup"><span data-stu-id="316db-122">For a `let` binding, for example, you need both the definition of the bound name and an additional expression that uses the binding.</span></span> <span data-ttu-id="316db-123">In ausführlicher Syntax ist dies ein Ausdruck, der `in` Schlüsselwort.</span><span class="sxs-lookup"><span data-stu-id="316db-123">In verbose syntax, this is an expression that follows the `in` keyword.</span></span> <span data-ttu-id="316db-124">Auf der obersten Ebene in einem Modul Dies ist nur des nächsten Ausdrucks in das Modul, aber in einem Angebot, es unbedingt erforderlich ist.</span><span class="sxs-lookup"><span data-stu-id="316db-124">At the top-level in a module, this is just the next expression in the module, but in a quotation, it is explicitly required.</span></span>

<span data-ttu-id="316db-125">Aus diesem Grund ist der folgende Ausdruck ungültig.</span><span class="sxs-lookup"><span data-stu-id="316db-125">Therefore, the following expression is not valid.</span></span>

```fsharp
// Not valid:
// <@ let f x = x + 1 @>
```

<span data-ttu-id="316db-126">Die folgenden Ausdrücke sind jedoch gültig.</span><span class="sxs-lookup"><span data-stu-id="316db-126">But the following expressions are valid.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-3/snippet502.fs)]

<span data-ttu-id="316db-127">Zum Zitieren von Code verwenden zu können, müssen Sie eine Importdeklaration hinzufügen (mithilfe der `open` Schlüsselwort), öffnet der [Microsoft.FSharp.Quotations](https://msdn.microsoft.com/library/e9ce8a3a-e00c-4190-bad5-cce52ee089b2) Namespace.</span><span class="sxs-lookup"><span data-stu-id="316db-127">To use code quotations, you must add an import declaration (by using the `open` keyword) that opens the [Microsoft.FSharp.Quotations](https://msdn.microsoft.com/library/e9ce8a3a-e00c-4190-bad5-cce52ee089b2) namespace.</span></span>

<span data-ttu-id="316db-128">F#-PowerPack bietet Unterstützung für die Auswertung und Ausführung von F#-Ausdrucksobjekten.</span><span class="sxs-lookup"><span data-stu-id="316db-128">The F# PowerPack provides support for evaluating and executing F# expression objects.</span></span>

## <a name="expr-type"></a><span data-ttu-id="316db-129">Expr-Typ</span><span class="sxs-lookup"><span data-stu-id="316db-129">Expr Type</span></span>

<span data-ttu-id="316db-130">Eine Instanz von der `Expr` Typ stellt ein F#-Ausdrucks dar.</span><span class="sxs-lookup"><span data-stu-id="316db-130">An instance of the `Expr` type represents an F# expression.</span></span> <span data-ttu-id="316db-131">Der generische und nicht generischen `Expr` Typen sind in der F#-Bibliothek-Dokumentation dokumentiert.</span><span class="sxs-lookup"><span data-stu-id="316db-131">Both the generic and the non-generic `Expr` types are documented in the F# library documentation.</span></span> <span data-ttu-id="316db-132">Weitere Informationen finden Sie unter [Microsoft.FSharp.Quotations-Namespace](https://msdn.microsoft.com/visualfsharpdocs/conceptual/microsoft.fsharp.quotations-namespace-%5bfsharp%5d) und [Quotations.Expr-Klasse](https://msdn.microsoft.com/visualfsharpdocs/conceptual/quotations.expr-class-%5bfsharp%5d).</span><span class="sxs-lookup"><span data-stu-id="316db-132">For more information, see [Microsoft.FSharp.Quotations Namespace](https://msdn.microsoft.com/visualfsharpdocs/conceptual/microsoft.fsharp.quotations-namespace-%5bfsharp%5d) and [Quotations.Expr Class](https://msdn.microsoft.com/visualfsharpdocs/conceptual/quotations.expr-class-%5bfsharp%5d).</span></span>

## <a name="splicing-operators"></a><span data-ttu-id="316db-133">Wird beim Splicing von Operatoren</span><span class="sxs-lookup"><span data-stu-id="316db-133">Splicing Operators</span></span>

<span data-ttu-id="316db-134">Einfügen, können Sie literale Codezitate mit Ausdrücken kombinieren, die Sie programmgesteuert oder aus einem anderen Code-Angebot erstellt haben.</span><span class="sxs-lookup"><span data-stu-id="316db-134">Splicing enables you to combine literal code quotations with expressions that you have created programmatically or from another code quotation.</span></span> <span data-ttu-id="316db-135">Die `%` und `%%` Operatoren ermöglichen es Ihnen, ein F#-Expression-Objekt in ein Code-Angebot hinzuzufügen.</span><span class="sxs-lookup"><span data-stu-id="316db-135">The `%` and `%%` operators enable you to add an F# expression object into a code quotation.</span></span> <span data-ttu-id="316db-136">Sie verwenden die `%` Operator zum Einfügen von einem typisierten Datenquellenausdrucks-Objekt in ein typisiertes Zitat; Sie verwenden die `%%` Operator, um eine nicht typisierte Datenquellenausdrucks-Objekt in eine nicht typisierte Anführungszeichen einfügen.</span><span class="sxs-lookup"><span data-stu-id="316db-136">You use the `%` operator to insert a typed expression object into a typed quotation; you use the `%%` operator to insert an untyped expression object into an untyped quotation.</span></span> <span data-ttu-id="316db-137">Beide Operatoren sind Unäroperatoren-Präfix.</span><span class="sxs-lookup"><span data-stu-id="316db-137">Both operators are unary prefix operators.</span></span> <span data-ttu-id="316db-138">Also wenn `expr` ist ein nicht typisierter Ausdruck vom Typ `Expr`, der folgende Code ist ungültig.</span><span class="sxs-lookup"><span data-stu-id="316db-138">Thus if `expr` is an untyped expression of type `Expr`, the following code is valid.</span></span>

```fsharp
<@@ 1 + %%expr @@>
```

<span data-ttu-id="316db-139">Und wenn `expr` ist ein typisiertes Zitat des Typs `Expr<int>`, der folgende Code ist ungültig.</span><span class="sxs-lookup"><span data-stu-id="316db-139">And if `expr` is a typed quotation of type `Expr<int>`, the following code is valid.</span></span>

```fsharp
<@ 1 + %expr @>
```

## <a name="example"></a><span data-ttu-id="316db-140">Beispiel</span><span class="sxs-lookup"><span data-stu-id="316db-140">Example</span></span>

### <a name="description"></a><span data-ttu-id="316db-141">Beschreibung</span><span class="sxs-lookup"><span data-stu-id="316db-141">Description</span></span>

<span data-ttu-id="316db-142">Das folgende Beispiel veranschaulicht die Verwendung von Zitieren von Code zum F#-Code in ein Datenquellenausdrucks-Objekt und klicken Sie dann die F#-Code, der den Ausdruck darstellt, zu drucken.</span><span class="sxs-lookup"><span data-stu-id="316db-142">The following example illustrates the use of code quotations to put F# code into an expression object and then print the F# code that represents the expression.</span></span> <span data-ttu-id="316db-143">Eine Funktion `println` definiert ist, enthält eine rekursive Funktion `print` , anzeigt, dass ein F#-Expression-Objekt (des Typs `Expr`) in einem freundlichen Format.</span><span class="sxs-lookup"><span data-stu-id="316db-143">A function `println` is defined that contains a recursive function `print` that displays an F# expression object (of type `Expr`) in a friendly format.</span></span> <span data-ttu-id="316db-144">Es gibt mehrere aktive Muster in den [Microsoft.FSharp.Quotations.Patterns](https://msdn.microsoft.com/library/093944a9-c752-403a-8983-5fcd5dbf92a4) und [Microsoft.FSharp.Quotations.DerivedPatterns](https://msdn.microsoft.com/library/d2434a6e-ae7b-4f3d-b567-c162938bc9cd) Module, die zum Ausdrucksobjekten verwendet werden können.</span><span class="sxs-lookup"><span data-stu-id="316db-144">There are several active patterns in the [Microsoft.FSharp.Quotations.Patterns](https://msdn.microsoft.com/library/093944a9-c752-403a-8983-5fcd5dbf92a4) and [Microsoft.FSharp.Quotations.DerivedPatterns](https://msdn.microsoft.com/library/d2434a6e-ae7b-4f3d-b567-c162938bc9cd) modules that can be used to analyze expression objects.</span></span> <span data-ttu-id="316db-145">In diesem Beispiel enthält keine alle Muster, die angezeigt werden, möglicherweise in einem F#-Ausdruck.</span><span class="sxs-lookup"><span data-stu-id="316db-145">This example does not include all the possible patterns that might appear in an F# expression.</span></span> <span data-ttu-id="316db-146">Eine nicht erkannte Muster löst eine Übereinstimmung mit dem Platzhaltermuster (`_`) und gerendert wird, indem Sie die `ToString` Methode, das auf die `Expr` eingeben, können Sie das aktive Muster der Match-Ausdruck hinzu.</span><span class="sxs-lookup"><span data-stu-id="316db-146">Any unrecognized pattern triggers a match to the wildcard pattern (`_`) and is rendered by using the `ToString` method, which, on the `Expr` type, lets you know the active pattern to add to your match expression.</span></span>

### <a name="code"></a><span data-ttu-id="316db-147">Code</span><span class="sxs-lookup"><span data-stu-id="316db-147">Code</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-3/snippet601.fs)]

### <a name="output"></a><span data-ttu-id="316db-148">Output</span><span class="sxs-lookup"><span data-stu-id="316db-148">Output</span></span>

```fsharp
fun (x:System.Int32) -> x + 1
a + 1
let f = fun (x:System.Int32) -> x + 10 in f 10
```

## <a name="example"></a><span data-ttu-id="316db-149">Beispiel</span><span class="sxs-lookup"><span data-stu-id="316db-149">Example</span></span>

### <a name="description"></a><span data-ttu-id="316db-150">Beschreibung</span><span class="sxs-lookup"><span data-stu-id="316db-150">Description</span></span>

<span data-ttu-id="316db-151">Sie können auch die drei aktive Muster in den [ExprShape-Modul](https://msdn.microsoft.com/library/7685150e-2432-4d39-9338-57292eff18de) um Ausdrucksbaumstrukturen mit weniger aktive Muster zu durchlaufen.</span><span class="sxs-lookup"><span data-stu-id="316db-151">You can also use the three active patterns in the [ExprShape module](https://msdn.microsoft.com/library/7685150e-2432-4d39-9338-57292eff18de) to traverse expression trees with fewer active patterns.</span></span> <span data-ttu-id="316db-152">Diese aktive Muster können hilfreich sein, wenn Sie eine Struktur zu durchlaufen möchten, aber alle Informationen in den meisten der Knoten ist nicht erforderlich.</span><span class="sxs-lookup"><span data-stu-id="316db-152">These active patterns can be useful when you want to traverse a tree but you do not need all the information in most of the nodes.</span></span> <span data-ttu-id="316db-153">Wenn Sie diese Muster verwenden, alle F#-Ausdruck entspricht einem der folgenden drei Muster: `ShapeVar` ist der Ausdruck einer Variablen, `ShapeLambda` der Ausdruck ist ein Lambda-Ausdruck oder `ShapeCombination` nichts ist der Ausdruck.</span><span class="sxs-lookup"><span data-stu-id="316db-153">When you use these patterns, any F# expression matches one of the following three patterns: `ShapeVar` if the expression is a variable, `ShapeLambda` if the expression is a lambda expression, or `ShapeCombination` if the expression is anything else.</span></span> <span data-ttu-id="316db-154">Wenn Sie eine Ausdrucksbaumstruktur durchlaufen, indem Sie die aktive Muster wie im vorherigen Codebeispiel, müssen Sie viele weitere Muster verwenden, um alle möglichen F# Ausdruckstypen verarbeiten, und Code komplexer sein wird.</span><span class="sxs-lookup"><span data-stu-id="316db-154">If you traverse an expression tree by using the active patterns as in the previous code example, you have to use many more patterns to handle all possible F# expression types, and your code will be more complex.</span></span> <span data-ttu-id="316db-155">Weitere Informationen finden Sie unter [ExprShape.ShapeVar&#124;ShapeLambda&#124;aktives ShapeCombination-Muster](https://msdn.microsoft.com/visualfsharpdocs/conceptual/exprshape.shapevarhshapelambdahshapecombination-active-pattern-%5bfsharp%5d).</span><span class="sxs-lookup"><span data-stu-id="316db-155">For more information, see [ExprShape.ShapeVar&#124;ShapeLambda&#124;ShapeCombination Active Pattern](https://msdn.microsoft.com/visualfsharpdocs/conceptual/exprshape.shapevarhshapelambdahshapecombination-active-pattern-%5bfsharp%5d).</span></span>

<span data-ttu-id="316db-156">Im folgenden Codebeispiel wird als Grundlage für komplexere Durchläufe dienen.</span><span class="sxs-lookup"><span data-stu-id="316db-156">The following code example can be used as a basis for more complex traversals.</span></span> <span data-ttu-id="316db-157">In diesem Code wird eine Ausdrucksbaumstruktur erstellt wird, für einen Ausdruck, der einen Funktionsaufruf umfasst `add`.</span><span class="sxs-lookup"><span data-stu-id="316db-157">In this code, an expression tree is created for an expression that involves a function call, `add`.</span></span> <span data-ttu-id="316db-158">Die [SpecificCall](https://msdn.microsoft.com/library/05a77b21-20fe-4b9a-8e07-aa999538198d) aktives Muster verwendet, um jeden Aufruf von erkennen `add` in der Ausdrucksbaumstruktur.</span><span class="sxs-lookup"><span data-stu-id="316db-158">The [SpecificCall](https://msdn.microsoft.com/library/05a77b21-20fe-4b9a-8e07-aa999538198d) active pattern is used to detect any call to `add` in the expression tree.</span></span> <span data-ttu-id="316db-159">Dieses aktive Muster weist die Argumente des Aufrufs an die `exprList` Wert.</span><span class="sxs-lookup"><span data-stu-id="316db-159">This active pattern assigns the arguments of the call to the `exprList` value.</span></span> <span data-ttu-id="316db-160">In diesem Fall stehen nur zwei, damit diese abgerufen werden, und die Funktion rekursiv, auf den Argumenten aufgerufen wird.</span><span class="sxs-lookup"><span data-stu-id="316db-160">In this case, there are only two, so these are pulled out and the function is called recursively on the arguments.</span></span> <span data-ttu-id="316db-161">Die Ergebnisse werden in einem Code-Angebot, die einen Aufruf darstellt eingefügt `mul` mithilfe des Operators für die Einfügung (`%%`).</span><span class="sxs-lookup"><span data-stu-id="316db-161">The results are inserted into a code quotation that represents a call to `mul` by using the splice operator (`%%`).</span></span> <span data-ttu-id="316db-162">Die `println` Funktion aus dem vorherigen Beispiel wird verwendet, um die Ergebnisse anzuzeigen.</span><span class="sxs-lookup"><span data-stu-id="316db-162">The `println` function from the previous example is used to display the results.</span></span>

<span data-ttu-id="316db-163">Der Code in anderen Verzweigungen aktives Muster wird nur die gleichen Ausdrucksbaumstruktur generiert, ist die einzige Änderung in der sich ergebenden Ausdrucks die Änderung von `add` zu `mul`.</span><span class="sxs-lookup"><span data-stu-id="316db-163">The code in the other active pattern branches just regenerates the same expression tree, so the only change in the resulting expression is the change from `add` to `mul`.</span></span>

### <a name="code"></a><span data-ttu-id="316db-164">Code</span><span class="sxs-lookup"><span data-stu-id="316db-164">Code</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-3/snippet701.fs)]

### <a name="output"></a><span data-ttu-id="316db-165">Output</span><span class="sxs-lookup"><span data-stu-id="316db-165">Output</span></span>

```fsharp
1 + Module1.add(2,Module1.add(3,4))
1 + Module1.mul(2,Module1.mul(3,4))
```

## <a name="see-also"></a><span data-ttu-id="316db-166">Siehe auch</span><span class="sxs-lookup"><span data-stu-id="316db-166">See also</span></span>

- [<span data-ttu-id="316db-167">F#-Sprachreferenz</span><span class="sxs-lookup"><span data-stu-id="316db-167">F# Language Reference</span></span>](index.md)
