---
title: "Problembehandlung bei Interoperabilität (Visual Basic)"
ms.custom: 
ms.date: 07/20/2015
ms.prod: .net
ms.reviewer: 
ms.suite: 
ms.technology: devlang-visual-basic
ms.topic: article
helpviewer_keywords:
- interop, deploying assemblies
- assemblies [Visual Basic]
- interop, installing assemblies that share components
- COM objects, troubleshooting
- interop, sharing components
- troubleshooting interoperability [Visual Basic]
- interoperability, troubleshooting
- COM interop [Visual Basic], troubleshooting
- assemblies [Visual Basic], deploying
- troubleshooting Visual Basic, interoperability
- interop assemblies
- interoperability, sharing components
- shared components, using with assemblies
ms.assetid: b324cc1e-b03c-4f39-aea6-6a6d5bfd0e37
caps.latest.revision: "21"
author: dotnet-bot
ms.author: dotnetcontent
ms.openlocfilehash: 33e5a03f29f521a2ba7f93bdd084655e8fc7de8d
ms.sourcegitcommit: 34ec7753acf76f90a0fa845235ef06663dc9e36e
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 12/21/2017
---
# <a name="troubleshooting-interoperability-visual-basic"></a><span data-ttu-id="e9b1d-102">Problembehandlung bei Interoperabilität (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="e9b1d-102">Troubleshooting Interoperability (Visual Basic)</span></span>
<span data-ttu-id="e9b1d-103">Wenn Sie COM- und des verwalteten Codes der Zusammenwirken der [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)], können Sie eine oder mehrere der folgenden Probleme auftreten.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-103">When you interoperate between COM and the managed code of the [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)], you may encounter one or more of the following common issues.</span></span>  
  
##  <a name="vbconinteroperabilitymarshalinganchor1"></a><span data-ttu-id="e9b1d-104">Interop-Marshalling</span><span class="sxs-lookup"><span data-stu-id="e9b1d-104">Interop Marshaling</span></span>  
 <span data-ttu-id="e9b1d-105">In einigen Fällen müssen unter Umständen Datentypen verwenden, die nicht Teil der [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)].</span><span class="sxs-lookup"><span data-stu-id="e9b1d-105">At times, you may have to use data types that are not part of the [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)].</span></span> <span data-ttu-id="e9b1d-106">Interop-Assemblys behandeln die meisten Aufgaben für COM-Objekte, aber möglicherweise müssen Sie die Daten zu steuern, die verwendet werden, wenn verwaltete Objekte für COM verfügbar gemacht werden</span><span class="sxs-lookup"><span data-stu-id="e9b1d-106">Interop assemblies handle most of the work for COM objects, but you may have to control the data types that are used when managed objects are exposed to COM.</span></span> <span data-ttu-id="e9b1d-107">Strukturen in Klassenbibliotheken müssen angeben, z. B. die `BStr` nicht verwalteten Typ für Zeichenfolgen an COM-Objekten, die von Visual Basic 6.0 und früheren Versionen erstellt gesendet.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-107">For example, structures in class libraries must specify the `BStr` unmanaged type on strings sent to COM objects created by Visual Basic 6.0 and earlier versions.</span></span> <span data-ttu-id="e9b1d-108">In solchen Fällen können Sie die <xref:System.Runtime.InteropServices.MarshalAsAttribute> Attribut, das dazu führen, dass verwaltete Typen als nicht verwaltete Typen verfügbar gemacht werden.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-108">In such cases, you can use the <xref:System.Runtime.InteropServices.MarshalAsAttribute> attribute to cause managed types to be exposed as unmanaged types.</span></span>  
  
##  <a name="vbconinteroperabilitymarshalinganchor2"></a><span data-ttu-id="e9b1d-109">Exportieren von Zeichenfolgen mit fester Länge zu nicht verwaltetem Code</span><span class="sxs-lookup"><span data-stu-id="e9b1d-109">Exporting Fixed-Length Strings to Unmanaged Code</span></span>  
 <span data-ttu-id="e9b1d-110">In Visual Basic 6.0 und früheren Versionen werden Zeichenfolgen als Bytefolgen ohne abschließendes null-Zeichen in COM-Objekte exportiert.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-110">In Visual Basic 6.0 and earlier versions, strings are exported to COM objects as sequences of bytes without a null termination character.</span></span> <span data-ttu-id="e9b1d-111">Um die Kompatibilität mit anderen Sprachen fügt Visual Basic .NET ein abschließendes Zeichen beim Exportieren von Zeichenfolgen.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-111">For compatibility with other languages, Visual Basic .NET includes a termination character when exporting strings.</span></span> <span data-ttu-id="e9b1d-112">Die beste Möglichkeit, diese Inkompatibilität zu behandeln ist, Zeichenfolgen zu exportieren, die das Abschlusszeichen als Arrays von fehlender `Byte` oder `Char`.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-112">The best way to address this incompatibility is to export strings that lack the termination character as arrays of `Byte` or `Char`.</span></span>  
  
##  <a name="vbconinteroperabilitymarshalinganchor3"></a><span data-ttu-id="e9b1d-113">Ausführende Vererbungshierarchien</span><span class="sxs-lookup"><span data-stu-id="e9b1d-113">Exporting Inheritance Hierarchies</span></span>  
 <span data-ttu-id="e9b1d-114">Verwalteter Klassen Hierarchien vereinfachen, wenn als COM-Objekte verfügbar gemacht.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-114">Managed class hierarchies flatten out when exposed as COM objects.</span></span> <span data-ttu-id="e9b1d-115">Z. B. Wenn Sie eine Basisklasse mit einem Member definieren und dann erben die Basisklasse in einer abgeleiteten Klasse, die als COM-Objekt verfügbar gemacht wird, werden Clients, die in der COM-Objekt die abgeleitete Klasse verwenden nicht die geerbten Member verwenden können.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-115">For example, if you define a base class with a member, and then inherit the base class in a derived class that is exposed as a COM object, clients that use the derived class in the COM object will not be able to use the inherited members.</span></span> <span data-ttu-id="e9b1d-116">Member der Basisklasse können von COM-Objekte nur als Instanzen einer Basisklasse zugegriffen werden und dann nur, wenn die Basisklasse ebenfalls als COM-Objekt erstellt wurde.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-116">Base class members can be accessed from COM objects only as instances of a base class, and then only if the base class is also created as a COM object.</span></span>  
  
## <a name="overloaded-methods"></a><span data-ttu-id="e9b1d-117">Überladene Methoden</span><span class="sxs-lookup"><span data-stu-id="e9b1d-117">Overloaded Methods</span></span>  
 <span data-ttu-id="e9b1d-118">Sie können jedoch überladene Methoden mit [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)], sie werden nicht von COM unterstützt.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-118">Although you can create overloaded methods with [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)], they are not supported by COM.</span></span> <span data-ttu-id="e9b1d-119">Wenn eine Klasse, die überladene Methoden enthält, die als COM-Objekt verfügbar ist, werden neue Namen von Methoden für überladenen Methoden generiert.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-119">When a class that contains overloaded methods is exposed as a COM object, new method names are generated for the overloaded methods.</span></span>  
  
 <span data-ttu-id="e9b1d-120">Betrachten Sie beispielsweise eine Klasse mit zwei Überladungen der `Synch` Methode.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-120">For example, consider a class that has two overloads of the `Synch` method.</span></span> <span data-ttu-id="e9b1d-121">Wenn die Klasse als COM-Objekt verfügbar gemacht wird, ist der neu generierten Methodennamen möglicherweise `Synch` und `Synch_2`.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-121">When the class is exposed as a COM object, the new generated method names could be `Synch` and `Synch_2`.</span></span>  
  
 <span data-ttu-id="e9b1d-122">Die Umbenennung kann dazu führen, dass zwei Probleme für Consumer des COM-Objekts.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-122">The renaming can cause two problems for consumers of the COM object.</span></span>  
  
1.  <span data-ttu-id="e9b1d-123">Clients können die generierten Methodennamen nicht erwarten.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-123">Clients might not expect the generated method names.</span></span>  
  
2.  <span data-ttu-id="e9b1d-124">Die generierte Methodennamen in der Klasse als COM-Objekt verfügbar gemachte können ändern, wenn neue Überladungen der Klasse oder seine Basisklasse hinzugefügt werden.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-124">The generated method names in the class exposed as a COM object can change when new overloads are added to the class or its base class.</span></span> <span data-ttu-id="e9b1d-125">Dies kann Versionsprobleme verursachen.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-125">This can cause versioning problems.</span></span>  
  
 <span data-ttu-id="e9b1d-126">Um beide Probleme zu beheben, weisen Sie jeder Methode einen eindeutigen Namen ein, anstatt Sie mithilfe des Überladens, wenn Sie Objekte entwickeln, die als COM-Objekte verfügbar gemacht werden.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-126">To solve both problems, give each method a unique name, instead of using overloading, when you develop objects that will be exposed as COM objects.</span></span>  
  
##  <a name="vbconinteroperabilitymarshalinganchor4"></a><span data-ttu-id="e9b1d-127">Verwenden von COM-Objekten über die Interop-Assemblys</span><span class="sxs-lookup"><span data-stu-id="e9b1d-127">Use of COM Objects Through Interop Assemblies</span></span>  
 <span data-ttu-id="e9b1d-128">Sie verwenden die Interop-Assemblys, fast so, als ob sie verwalteten Code als Ersatz für die COM-Objekte sind, die sie darstellen.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-128">You use interop assemblies almost as if they are managed code replacements for the COM objects they represent.</span></span> <span data-ttu-id="e9b1d-129">Da diese Wrapper und keine eigentlichen COM-Objekte sind, bestehen jedoch einige Unterschiede zwischen der Verwendung von Interop-Assemblys und Standardassemblys.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-129">However, because they are wrappers and not actual COM objects, there are some differences between using interop assemblies and standard assemblies.</span></span> <span data-ttu-id="e9b1d-130">Dieser Unterschied umfassen des Offenlegen von Klassen und Datentypen für Parameter und Rückgabewerte.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-130">These areas of difference include the exposure of classes, and data types for parameters and return values.</span></span>  
  
##  <a name="vbconinteroperabilitymarshalinganchor5"></a><span data-ttu-id="e9b1d-131">Klassen, die verfügbar gemacht werden, als beide Schnittstellen und Klassen</span><span class="sxs-lookup"><span data-stu-id="e9b1d-131">Classes Exposed as Both Interfaces and Classes</span></span>  
 <span data-ttu-id="e9b1d-132">Im Gegensatz zu Klassen in Standardassemblys werden Klassen für COM verfügbar gemacht, in Interop-Assemblys als eine Schnittstelle und eine Klasse, die COM-Klasse darstellt.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-132">Unlike classes in standard assemblies, COM classes are exposed in interop assemblies as both an interface and a class that represents the COM class.</span></span> <span data-ttu-id="e9b1d-133">Der Name der Schnittstelle ist identisch mit der COM-Klasse.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-133">The interface's name is identical to that of the COM class.</span></span> <span data-ttu-id="e9b1d-134">Der Name der Interop-Klasse wird mit der ursprünglichen COM-Klasse identisch, aber mit dem Wort "Class" angehängt.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-134">The name of the interop class is the same as that of the original COM class, but with the word "Class" appended.</span></span> <span data-ttu-id="e9b1d-135">Nehmen Sie beispielsweise an, dass Sie ein Projekt mit einem Verweis auf eine Interop-Assembly für COM-Objekt verfügen.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-135">For example, suppose you have a project with a reference to an interop assembly for a COM object.</span></span> <span data-ttu-id="e9b1d-136">Wenn die COM-Klasse heißt `MyComClass`, IntelliSense und den Objektkatalog Anzeigen eine Schnittstelle mit dem Namen `MyComClass` und eine Klasse namens `MyComClassClass`.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-136">If the COM class is named `MyComClass`, IntelliSense and the Object Browser show an interface named `MyComClass` and a class named `MyComClassClass`.</span></span>  
  
##  <a name="vbconinteroperabilitymarshalinganchor6"></a><span data-ttu-id="e9b1d-137">Erstellen von Instanzen einer .NET Framework-Klasse</span><span class="sxs-lookup"><span data-stu-id="e9b1d-137">Creating Instances of a .NET Framework Class</span></span>  
 <span data-ttu-id="e9b1d-138">Im Allgemeinen erstellen Sie eine Instanz von einem [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)] -Klasse unter Verwendung der `New` Anweisung durch einen Klassennamen.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-138">Generally, you create an instance of a [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)] class using the `New` statement with a class name.</span></span> <span data-ttu-id="e9b1d-139">Eine COM-Klasse dargestellt, die von einer Interop-Assembly ist eine Groß-/Kleinschreibung, die in der können Sie die `New` -Anweisung mit einer Schnittstelle.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-139">Having a COM class represented by an interop assembly is the one case in which you can use the `New` statement with an interface.</span></span> <span data-ttu-id="e9b1d-140">Außer bei der COM-Klasse mit einer `Inherits` -Anweisung können Sie die Benutzeroberfläche verwenden, wie Sie eine Klasse.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-140">Unless you are using the COM class with an `Inherits` statement, you can use the interface just as you would a class.</span></span> <span data-ttu-id="e9b1d-141">Der folgende Code veranschaulicht, wie ein `Command` Objekt in ein Projekt, das einen Verweis auf das Microsoft ActiveX Data Objekte 2.8-Bibliothek COM-Objekt enthält:</span><span class="sxs-lookup"><span data-stu-id="e9b1d-141">The following code demonstrates how to create a `Command` object in a project that has a reference to the Microsoft ActiveX Data Objects 2.8 Library COM object:</span></span>  
  
 [!code-vb[VbVbalrInterop#20](../../../visual-basic/programming-guide/com-interop/codesnippet/VisualBasic/troubleshooting-interoperability_1.vb)]  
  
 <span data-ttu-id="e9b1d-142">Wenn Sie die COM-Klasse als Basis für eine abgeleitete Klasse verwenden, müssen Sie die Interop-Klasse verwenden, die COM-Klasse, wie im folgenden Code darstellt:</span><span class="sxs-lookup"><span data-stu-id="e9b1d-142">However, if you are using the COM class as the base for a derived class, you must use the interop class that represents the COM class, as in the following code:</span></span>  
  
 [!code-vb[VbVbalrInterop#21](../../../visual-basic/programming-guide/com-interop/codesnippet/VisualBasic/troubleshooting-interoperability_2.vb)]  
  
> [!NOTE]
>  <span data-ttu-id="e9b1d-143">Interop-Assemblys implementiert implizit Schnittstellen, die COM-Klassen darstellen.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-143">Interop assemblies implicitly implement interfaces that represent COM classes.</span></span> <span data-ttu-id="e9b1d-144">Sie sollten nicht versuchen, verwenden Sie die `Implements` Anweisung zum Implementieren dieser Schnittstellen oder Fehler führt.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-144">You should not try to use the `Implements` statement to implement these interfaces or an error will result.</span></span>  
  
##  <a name="vbconinteroperabilitymarshalinganchor7"></a><span data-ttu-id="e9b1d-145">Datentypen für Parameter und Rückgabewerte</span><span class="sxs-lookup"><span data-stu-id="e9b1d-145">Data Types for Parameters and Return Values</span></span>  
 <span data-ttu-id="e9b1d-146">Im Gegensatz zu Membern von Standardassemblys möglicherweise Interop-Assembly-Member-Datentypen, die unterscheiden sich von denjenigen, die in der ursprünglichen Deklaration des Objekts verwendet.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-146">Unlike members of standard assemblies, interop assembly members may have data types that differ from those used in the  original object declaration.</span></span> <span data-ttu-id="e9b1d-147">Interop-Assemblys, mit der common Language Runtime kompatible Typen implizit COM-Typen konvertieren, beachten Sie besonders auf die Datentypen, die von beiden Seiten verwendet werden, um Laufzeitfehler zu vermeiden.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-147">Although interop assemblies implicitly convert COM types to compatible common language runtime types, you should pay attention to the data types that are used by both sides to prevent runtime errors.</span></span> <span data-ttu-id="e9b1d-148">Beispielsweise ist in COM-Objekte, die in Visual Basic 6.0 und früheren Versionen, die Werte des Typs erstellt `Integer` gehen davon aus den [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)] entspricht der Typ `Short`.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-148">For example, in COM objects created in Visual Basic 6.0 and earlier versions, values of type `Integer` assume the [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)] equivalent type, `Short`.</span></span> <span data-ttu-id="e9b1d-149">Es wird empfohlen, dass Sie den Objektkatalog verwenden, um untersuchen Sie die Eigenschaften der importierten Elemente ein, bevor Sie sie verwenden.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-149">It is recommended that you use the Object Browser to examine the characteristics of imported members before you use them.</span></span>  
  
##  <a name="vbconinteroperabilitymarshalinganchor8"></a><span data-ttu-id="e9b1d-150">COM Methoden auf Modulebene</span><span class="sxs-lookup"><span data-stu-id="e9b1d-150">Module level COM methods</span></span>  
 <span data-ttu-id="e9b1d-151">Die meisten COM-Objekte werden verwendet, durch das Erstellen einer Instanz eines COM-Klasse unter Verwendung der `New` -Schlüsselwort und dem anschließenden Aufrufen der Methoden des Objekts.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-151">Most COM objects are used by creating an instance of a COM class using the `New` keyword and then calling methods of the object.</span></span> <span data-ttu-id="e9b1d-152">Eine Ausnahme dieser Regel betrifft COM-Objekte, die enthalten `AppObj` oder `GlobalMultiUse` COM-Klassen.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-152">One exception to this rule involves COM objects that contain `AppObj` or `GlobalMultiUse` COM classes.</span></span> <span data-ttu-id="e9b1d-153">Solche Klassen ähneln Methoden auf Modulebene in Visual Basic .NET Klassen.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-153">Such classes resemble module level methods in Visual Basic .NET classes.</span></span> <span data-ttu-id="e9b1d-154">Visual Basic 6.0 und früheren Versionen erstellen implizit Instanzen der betroffenen Objekte für Sie erstmalig, die eine ihrer Methoden aufgerufen werden.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-154">Visual Basic 6.0 and earlier versions implicitly create instances of such objects for you the first time that you call one of their methods.</span></span> <span data-ttu-id="e9b1d-155">Fügen Sie z. B. in Visual Basic 6.0 einen Verweis auf die Microsoft DAO 3.6-Objektbibliothek, und rufen die `DBEngine` Methode ohne zunächst eine Instanz erstellt:</span><span class="sxs-lookup"><span data-stu-id="e9b1d-155">For example, in Visual Basic 6.0 you can add a reference to the Microsoft DAO 3.6 Object Library and call the `DBEngine` method without first creating an instance:</span></span>  
  
```vb  
Dim db As DAO.Database  
' Open the database.  
Set db = DBEngine.OpenDatabase("C:\nwind.mdb")  
' Use the database object.  
```  
  
 <span data-ttu-id="e9b1d-156">Visual Basic .NET erfordert, dass Sie immer Instanzen von COM-Objekten erstellen, bevor Sie deren Methoden verwenden können.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-156">Visual Basic .NET requires that you always create instances of COM objects before you can use their methods.</span></span> <span data-ttu-id="e9b1d-157">Diese Methoden in Visual Basic verwenden, deklarieren Sie eine Variable, der die gewünschte Klasse aus, und verwenden das new-Schlüsselwort, um die Objektvariable zuweisen.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-157">To use these methods in Visual Basic, declare a variable of the desired class and use the new keyword to assign the object to the object variable.</span></span> <span data-ttu-id="e9b1d-158">Die `Shared` -Schlüsselwort kann verwendet werden, wenn Sie sicherstellen möchten, dass nur eine Instanz der Klasse erstellt wird.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-158">The `Shared` keyword can be used when you want to make sure that only one instance of the class is created.</span></span>  
  
 [!code-vb[VbVbalrInterop#23](../../../visual-basic/programming-guide/com-interop/codesnippet/VisualBasic/troubleshooting-interoperability_3.vb)]  
  
##  <a name="vbconinteroperabilitymarshalinganchor9"></a><span data-ttu-id="e9b1d-159">Nicht behandelte Fehler in den Ereignishandlern</span><span class="sxs-lookup"><span data-stu-id="e9b1d-159">Unhandled Errors in Event Handlers</span></span>  
 <span data-ttu-id="e9b1d-160">Ein häufig auftretendes Problem der Interop-umfasst Fehler im Ereignishandler, die von COM-Objekten ausgelöste Ereignisse behandeln.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-160">One common interop problem involves errors in event handlers that handle events raised by COM objects.</span></span> <span data-ttu-id="e9b1d-161">Diese Fehler werden ignoriert, es sei denn, Sie prüfen Sie insbesondere nach Fehlern, die mit `On Error` oder `Try...Catch...Finally` Anweisungen.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-161">Such errors are ignored unless you specifically check for errors using `On Error` or `Try...Catch...Finally` statements.</span></span> <span data-ttu-id="e9b1d-162">Beispielsweise wird im folgende Beispiel aus einem Visual Basic .NET-Projekt, das einen Verweis auf das Microsoft ActiveX Data Objekte 2.8-Bibliothek COM-Objekt enthält.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-162">For example, the following example is from a Visual Basic .NET project that has a reference to the Microsoft ActiveX Data Objects 2.8 Library COM object.</span></span>  
  
 [!code-vb[VbVbalrInterop#24](../../../visual-basic/programming-guide/com-interop/codesnippet/VisualBasic/troubleshooting-interoperability_4.vb)]  
  
 <span data-ttu-id="e9b1d-163">In diesem Beispiel löst einen Fehler aus, wie erwartet.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-163">This example raises an error as expected.</span></span> <span data-ttu-id="e9b1d-164">Jedoch wenn Sie, die dasselbe Beispiel ohne versuchen das `Try...Catch...Finally` Block, der Fehler wird ignoriert, als ob Sie verwendet die `OnError Resume Next` Anweisung.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-164">However, if you try the same example without the `Try...Catch...Finally` block, the error is ignored as if you used the `OnError Resume Next` statement.</span></span> <span data-ttu-id="e9b1d-165">Ohne Fehlerbehandlung schlägt fehl, die Division durch 0 (null) im Hintergrund.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-165">Without error handling, the division by zero silently fails.</span></span> <span data-ttu-id="e9b1d-166">Da solche Fehler nie Fehler nicht behandelte Ausnahme auslöst, ist es wichtig, dass Sie verwenden bestimmt eine Form der Ausnahmebehandlung im Ereignishandler, die Ereignisse aus COM-Objekte zu verarbeiten.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-166">Because such errors never raise unhandled exception errors, it is important that you use some form of exception handling in event handlers that handle events from COM objects.</span></span>  
  
### <a name="understanding-com-interop-errors"></a><span data-ttu-id="e9b1d-167">Grundlegendes zu COM-Interop-Fehler</span><span class="sxs-lookup"><span data-stu-id="e9b1d-167">Understanding COM interop errors</span></span>  
 <span data-ttu-id="e9b1d-168">Ohne Fehlerbehandlung generieren Interop-Aufrufe häufig Fehler, die wenig Informationen bereitstellen.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-168">Without error handling, interop calls often generate errors that provide little information.</span></span> <span data-ttu-id="e9b1d-169">Verwenden Sie nach Möglichkeit strukturierte Fehlerbehandlung, um weitere Informationen zu Problemen bereitstellen, wenn sie auftreten.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-169">Whenever possible, use structured error handling to provide more information about problems when they occur.</span></span> <span data-ttu-id="e9b1d-170">Dies kann besonders hilfreich sein, wenn Sie Anwendungen Debuggen.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-170">This can be especially helpful when you debug applications.</span></span> <span data-ttu-id="e9b1d-171">Zum Beispiel:</span><span class="sxs-lookup"><span data-stu-id="e9b1d-171">For example:</span></span>  
  
 [!code-vb[VbVbalrInterop#25](../../../visual-basic/programming-guide/com-interop/codesnippet/VisualBasic/troubleshooting-interoperability_5.vb)]  
  
 <span data-ttu-id="e9b1d-172">Sie können Informationen wie die fehlerbeschreibung, HRESULT und die Quelle der COM-Fehler untersuchen des Inhalts des Ausnahmeobjekts finden.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-172">You can find information such as the error description, HRESULT, and the source of COM errors by examining the contents of the exception object.</span></span>  
  
##  <a name="vbconinteroperabilitymarshalinganchor10"></a><span data-ttu-id="e9b1d-173">ActiveX-Steuerelement-Probleme</span><span class="sxs-lookup"><span data-stu-id="e9b1d-173">ActiveX Control Issues</span></span>  
 <span data-ttu-id="e9b1d-174">Die meisten ActiveX-Steuerelemente, die mit Visual Basic 6.0 arbeiten werden mit Visual Basic .NET arbeiten, ohne Probleme.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-174">Most ActiveX controls that work with Visual Basic 6.0 work with Visual Basic .NET without trouble.</span></span> <span data-ttu-id="e9b1d-175">Die wichtigsten Ausnahmen sind Container oder Steuerelemente, die visuell andere Steuerelemente enthalten.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-175">The main exceptions are container controls, or controls that visually contain other controls.</span></span> <span data-ttu-id="e9b1d-176">Einige Beispiele für ältere Steuerelemente, die mit nicht ordnungsgemäß funktionieren [!INCLUDE[vsprvs](~/includes/vsprvs-md.md)] lauten wie folgt:</span><span class="sxs-lookup"><span data-stu-id="e9b1d-176">Some examples of older controls that do not work correctly with [!INCLUDE[vsprvs](~/includes/vsprvs-md.md)] are as follows:</span></span>  
  
-   <span data-ttu-id="e9b1d-177">Microsoft Forms 2.0-Frame-Steuerelement</span><span class="sxs-lookup"><span data-stu-id="e9b1d-177">Microsoft Forms 2.0 Frame control</span></span>  
  
-   <span data-ttu-id="e9b1d-178">Auf-ab-Steuerelement, auch bekannt als das Drehfeld-Steuerelement</span><span class="sxs-lookup"><span data-stu-id="e9b1d-178">Up-Down control, also known as the spin control</span></span>  
  
-   <span data-ttu-id="e9b1d-179">Sheridan Registerkarten-Steuerelement</span><span class="sxs-lookup"><span data-stu-id="e9b1d-179">Sheridan Tab Control</span></span>  
  
 <span data-ttu-id="e9b1d-180">Es gibt nur wenige problemumgehungen für nicht unterstützte ActiveX-Steuerelement-Probleme.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-180">There are only a few workarounds for unsupported ActiveX control problems.</span></span> <span data-ttu-id="e9b1d-181">Sie können vorhandene Steuerelementen zu migrieren [!INCLUDE[vsprvs](~/includes/vsprvs-md.md)] Wenn Sie den ursprünglichen Quellcode besitzen.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-181">You can migrate existing controls to [!INCLUDE[vsprvs](~/includes/vsprvs-md.md)] if you own the original source code.</span></span> <span data-ttu-id="e9b1d-182">Überprüfen Sie andernfalls können Sie mit dem Softwareanbieter für aktualisiert. NET-kompatiblen Versionen von Steuerelementen ersetzen unterstützt ActiveX-Steuerelemente.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-182">Otherwise, you can check with software vendors for updated .NET-compatible versions of controls to replace unsupported ActiveX controls.</span></span>  
  
##  <a name="vbconinteroperabilitymarshalinganchor11"></a><span data-ttu-id="e9b1d-183">Übergeben von ReadOnly-Eigenschaften der Steuerelemente ByRef</span><span class="sxs-lookup"><span data-stu-id="e9b1d-183">Passing ReadOnly Properties of Controls ByRef</span></span>  
 <span data-ttu-id="e9b1d-184">Visual Basic .NET in einigen Fällen löst COM-Fehler, z. B. "Fehler 0x800A017F CTL_E_SETNOTSUPPORTED", wenn übergeben werden `ReadOnly` Eigenschaften von einigen älteren ActiveX-Steuerelementen als `ByRef` Parameter anderer Prozeduren.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-184">Visual Basic .NET sometimes raises COM errors such as, "Error 0x800A017F CTL_E_SETNOTSUPPORTED", when you pass `ReadOnly` properties of some older ActiveX controls as `ByRef` parameters to other procedures.</span></span> <span data-ttu-id="e9b1d-185">Ähnliche Prozeduraufrufe aus Visual Basic 6.0 lösen keine Fehler, und die Parameter werden behandelt, als ob Sie sie als Wert übergeben.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-185">Similar procedure calls from Visual Basic 6.0 do not raise an error, and the parameters are treated as if you passed them by value.</span></span> <span data-ttu-id="e9b1d-186">Die Visual Basic .NET Fehlermeldung gibt an, dass Sie versuchen, eine Eigenschaft zu ändern, die über keine Eigenschaft `Set` Prozedur.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-186">The Visual Basic .NET error message indicates that you are trying to change a property that does not have a property `Set` procedure.</span></span>  
  
 <span data-ttu-id="e9b1d-187">Wenn Sie die aufgerufene Prozedur können zugreifen, können Sie diesen Fehler verhindern, mithilfe der `ByVal` Schlüsselwort, um Parameter zu deklarieren, die akzeptieren `ReadOnly` Eigenschaften.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-187">If you have access to the procedure being called, you can prevent this error by using the `ByVal` keyword to declare parameters that accept `ReadOnly` properties.</span></span> <span data-ttu-id="e9b1d-188">Zum Beispiel:</span><span class="sxs-lookup"><span data-stu-id="e9b1d-188">For example:</span></span>  
  
 [!code-vb[VbVbalrInterop#26](../../../visual-basic/programming-guide/com-interop/codesnippet/VisualBasic/troubleshooting-interoperability_6.vb)]  
  
 <span data-ttu-id="e9b1d-189">Wenn Sie keinen Zugriff auf den Quellcode für die aufgerufene Prozedur haben, können Sie die Eigenschaft als Wert übergeben werden, indem Sie einen zusätzlichen Satz von Klammern um die aufrufende Prozedur hinzufügen erzwingen.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-189">If you do not have access to the source code for the procedure being called, you can force the property to be passed by value by adding an extra set of brackets around the calling procedure.</span></span> <span data-ttu-id="e9b1d-190">Z. B. in einem Projekt, das einen Verweis auf das Microsoft ActiveX Data Objekte 2.8-Bibliothek COM-Objekt enthält, können Sie:</span><span class="sxs-lookup"><span data-stu-id="e9b1d-190">For example, in a project that has a reference to the Microsoft ActiveX Data Objects 2.8 Library COM object, you can use:</span></span>  
  
 [!code-vb[VbVbalrInterop#27](../../../visual-basic/programming-guide/com-interop/codesnippet/VisualBasic/troubleshooting-interoperability_7.vb)]  
  
##  <a name="vbconinteroperabilitymarshalinganchor12"></a><span data-ttu-id="e9b1d-191">Bereitstellen von Assemblys, die Interop verfügbar machen.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-191">Deploying Assemblies That Expose Interop</span></span>  
 <span data-ttu-id="e9b1d-192">Bereitstellen von Assemblys, die COM-Schnittstellen verfügbar machen, stellt einige eindeutige Herausforderungen.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-192">Deploying assemblies that expose COM interfaces presents some unique challenges.</span></span> <span data-ttu-id="e9b1d-193">Beispielsweise tritt ein mögliches Problem beim separate Anwendungen die gleiche COM-Assembly verweisen.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-193">For example, a potential problem occurs when separate applications reference the same COM assembly.</span></span> <span data-ttu-id="e9b1d-194">Diese Situation ist üblich, wenn eine neue Version einer Assembly installiert wird und eine andere Anwendung wird weiterhin die alte Version der Assembly.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-194">This situation is common when a new version of an assembly is installed and another application is still using the old version of the assembly.</span></span> <span data-ttu-id="e9b1d-195">Wenn Sie eine Assembly zu deinstallieren, die gemeinsam eine DLL können Sie unbeabsichtigt nicht verfügbar für die andere Assemblys erleichtern.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-195">If you uninstall an assembly that shares a DLL, you can unintentionally make it unavailable to the other assemblies.</span></span>  
  
 <span data-ttu-id="e9b1d-196">Um dieses Problem zu vermeiden, sollten Sie freigegebene Assemblys zum globalen Assemblycache (GAC) installieren und verwenden ein Mergemodul für die Komponente.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-196">To avoid this problem, you should install shared assemblies to the Global Assembly Cache (GAC) and use a MergeModule for the component.</span></span> <span data-ttu-id="e9b1d-197">Wenn Sie die Anwendung im GAC installieren können, sollten sie in einem Unterverzeichnis versionsspezifische auf CommonFilesFolder installiert werden.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-197">If you cannot install the application in the GAC, it should be installed to CommonFilesFolder in a version-specific subdirectory.</span></span>  
  
 <span data-ttu-id="e9b1d-198">Assemblys, die nicht gemeinsam genutzt werden, sollten im Verzeichnis mit der aufrufenden Anwendung nebeneinander befinden.</span><span class="sxs-lookup"><span data-stu-id="e9b1d-198">Assemblies that are not shared should be located side by side in the directory with the calling application.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="e9b1d-199">Siehe auch</span><span class="sxs-lookup"><span data-stu-id="e9b1d-199">See Also</span></span>  
 <xref:System.Runtime.InteropServices.MarshalAsAttribute>  
 [<span data-ttu-id="e9b1d-200">COM-Interop</span><span class="sxs-lookup"><span data-stu-id="e9b1d-200">COM Interop</span></span>](../../../visual-basic/programming-guide/com-interop/index.md)  
 [<span data-ttu-id="e9b1d-201">Tlbimp.exe (Type Library Importer-Tool)</span><span class="sxs-lookup"><span data-stu-id="e9b1d-201">Tlbimp.exe (Type Library Importer)</span></span>](../../../framework/tools/tlbimp-exe-type-library-importer.md)  
 [<span data-ttu-id="e9b1d-202">Tlbexp.exe (Type Library Exporter-Tool)</span><span class="sxs-lookup"><span data-stu-id="e9b1d-202">Tlbexp.exe (Type Library Exporter)</span></span>](http://msdn.microsoft.com/library/a487d61b-d166-467b-a7ca-d8b52fbff42d)  
 [<span data-ttu-id="e9b1d-203">Exemplarische Vorgehensweise: Implementieren der Vererbung mit COM-Objekten</span><span class="sxs-lookup"><span data-stu-id="e9b1d-203">Walkthrough: Implementing Inheritance with COM Objects</span></span>](../../../visual-basic/programming-guide/com-interop/walkthrough-implementing-inheritance-with-com-objects.md)  
 [<span data-ttu-id="e9b1d-204">Inherits-Anweisung</span><span class="sxs-lookup"><span data-stu-id="e9b1d-204">Inherits Statement</span></span>](../../../visual-basic/language-reference/statements/inherits-statement.md)  
 [<span data-ttu-id="e9b1d-205">Globaler Assemblycache</span><span class="sxs-lookup"><span data-stu-id="e9b1d-205">Global Assembly Cache</span></span>](../../../framework/app-domains/gac.md)
