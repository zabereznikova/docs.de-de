---
title: 'Exemplarische Vorgehensweise: Aufrufen von Windows-APIs (Visual Basic)'
ms.date: 07/20/2015
helpviewer_keywords:
- DLLs, calling
- Windows API, walkthroughs
- platform invoke, walkthroughs
- API calls [Visual Basic], walkthroughs [Visual Basic]
- Windows API, calling
- walkthroughs [Visual Basic], API calls
- DllImport attribute, calling Windows API
- Declare statement [Visual Basic], declaring DLL functions
ms.assetid: 9280ca96-7a93-47a3-8d01-6d01be0657cb
ms.openlocfilehash: 70914d63773c6a94ad92cf6301a8e2bc1368e7a1
ms.sourcegitcommit: c7a7e1468bf0fa7f7065de951d60dfc8d5ba89f5
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 05/14/2019
ms.locfileid: "65592713"
---
# <a name="walkthrough-calling-windows-apis-visual-basic"></a><span data-ttu-id="53829-102">Exemplarische Vorgehensweise: Aufrufen von Windows-APIs (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="53829-102">Walkthrough: Calling Windows APIs (Visual Basic)</span></span>
<span data-ttu-id="53829-103">Windows-APIs sind Dynamic Link Libraries (DLLs), die Teil des Windows-Betriebssystems sind.</span><span class="sxs-lookup"><span data-stu-id="53829-103">Windows APIs are dynamic-link libraries (DLLs) that are part of the Windows operating system.</span></span> <span data-ttu-id="53829-104">Können Sie diese Aufgaben auszuführen, wenn es schwierig, Ihre eigenen Prozeduren zu schreiben ist.</span><span class="sxs-lookup"><span data-stu-id="53829-104">You use them to perform tasks when it is difficult to write equivalent procedures of your own.</span></span> <span data-ttu-id="53829-105">Windows stellt z. B. eine Funktion namens `FlashWindowEx` , mit der Sie die Titelleiste für eine Anwendung wechseln Sie zwischen hellen und dunklen Graustufen.</span><span class="sxs-lookup"><span data-stu-id="53829-105">For example, Windows provides a function named `FlashWindowEx` that lets you make the title bar for an application alternate between light and dark shades.</span></span>  
  
 <span data-ttu-id="53829-106">Der Vorteil der Verwendung von Windows-APIs in Ihrem Code ist, dass sie Entwicklungszeit einsparen können, da sie enthalten, Dutzende von nützlichen Funktionen, die bereits geschrieben wurden und darauf warten, verwendet werden.</span><span class="sxs-lookup"><span data-stu-id="53829-106">The advantage of using Windows APIs in your code is that they can save development time because they contain dozens of useful functions that are already written and waiting to be used.</span></span> <span data-ttu-id="53829-107">Der Nachteil ist, dass die Windows-APIs arbeiten mit und fehleranfällig, wenn etwas schief geht, schwierig sein kann.</span><span class="sxs-lookup"><span data-stu-id="53829-107">The disadvantage is that Windows APIs can be difficult to work with and unforgiving when things go wrong.</span></span>  
  
 <span data-ttu-id="53829-108">Windows-APIs stellen eine besondere Kategorie der Interoperabilität dar.</span><span class="sxs-lookup"><span data-stu-id="53829-108">Windows APIs represent a special category of interoperability.</span></span> <span data-ttu-id="53829-109">Windows-APIs verwenden nicht verwalteten Code, verfügen nicht über integrierte Typbibliotheken, und verwenden die Datentypen unterscheiden, die mit Visual Studio verwendet.</span><span class="sxs-lookup"><span data-stu-id="53829-109">Windows APIs do not use managed code, do not have built-in type libraries, and use data types that are different than those used with Visual Studio.</span></span> <span data-ttu-id="53829-110">Aufgrund dieser Unterschiede und daran, dass Windows-APIs sind nicht COM-Objekten, die Interoperabilität mit Windows-APIs und .NET Framework erfolgt mithilfe von Plattformaufruf, oder PInvoke.</span><span class="sxs-lookup"><span data-stu-id="53829-110">Because of these differences, and because Windows APIs are not COM objects, interoperability with Windows APIs and the .NET Framework is performed using platform invoke, or PInvoke.</span></span> <span data-ttu-id="53829-111">Der Plattformaufruf ist ein Dienst, der von verwaltetem Code zum Aufrufen von nicht verwalteter Funktionen in DLLs implementiert werden können.</span><span class="sxs-lookup"><span data-stu-id="53829-111">Platform invoke is a service that enables managed code to call unmanaged functions implemented in DLLs.</span></span> <span data-ttu-id="53829-112">Weitere Informationen finden Sie unter [Consuming Unmanaged DLL Functions](../../../framework/interop/consuming-unmanaged-dll-functions.md).</span><span class="sxs-lookup"><span data-stu-id="53829-112">For more information, see [Consuming Unmanaged DLL Functions](../../../framework/interop/consuming-unmanaged-dll-functions.md).</span></span> <span data-ttu-id="53829-113">Sie können die PInvoke in Visual Basic verwenden, indem Sie entweder die `Declare` -Anweisung oder das Anwenden der `DllImport` -Attribut auf eine leere Prozedur.</span><span class="sxs-lookup"><span data-stu-id="53829-113">You can use PInvoke in Visual Basic by using either the `Declare` statement or applying the `DllImport` attribute to an empty procedure.</span></span>  
  
 <span data-ttu-id="53829-114">Windows-API-Aufrufe ein wichtiger Teil der Visual Basic-Programmierung in der Vergangenheit wurden, sind aber selten notwendig, mit Visual Basic .NET.</span><span class="sxs-lookup"><span data-stu-id="53829-114">Windows API calls were an important part of Visual Basic programming in the past, but are seldom necessary with Visual Basic .NET.</span></span> <span data-ttu-id="53829-115">Wann immer möglich, sollten Sie verwalteten Code von .NET Framework zum Ausführen von Aufgaben anstelle von Windows-API-Aufrufe verwenden.</span><span class="sxs-lookup"><span data-stu-id="53829-115">Whenever possible, you should use managed code from the .NET Framework to perform tasks, instead of Windows API calls.</span></span> <span data-ttu-id="53829-116">Diese exemplarische Vorgehensweise enthält Informationen über die Situationen, in denen die Verwendung Windows-APIs erforderlich ist.</span><span class="sxs-lookup"><span data-stu-id="53829-116">This walkthrough provides information for those situations in which using Windows APIs is necessary.</span></span>  
  
[!INCLUDE[note_settings_general](~/includes/note-settings-general-md.md)]  
  
## <a name="api-calls-using-declare"></a><span data-ttu-id="53829-117">Deklarieren von API-Aufrufe mit</span><span class="sxs-lookup"><span data-stu-id="53829-117">API Calls Using Declare</span></span>  
 <span data-ttu-id="53829-118">Die gängigste Methode zum Aufrufen von Windows-APIs ist die Verwendung der `Declare` Anweisung.</span><span class="sxs-lookup"><span data-stu-id="53829-118">The most common way to call Windows APIs is by using the `Declare` statement.</span></span>  
  
#### <a name="to-declare-a-dll-procedure"></a><span data-ttu-id="53829-119">Deklarieren eine DLL-Prozedur</span><span class="sxs-lookup"><span data-stu-id="53829-119">To declare a DLL procedure</span></span>  
  
1. <span data-ttu-id="53829-120">Bestimmen Sie den Namen der Funktion, die Sie aufrufen möchten, sowie die Argumente, die Argumenttypen und zurück-Wert, als auch den Namen und Speicherort der DLL, die sie enthält.</span><span class="sxs-lookup"><span data-stu-id="53829-120">Determine the name of the function you want to call, plus its arguments, argument types, and return value, as well as the name and location of the DLL that contains it.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="53829-121">Ausführliche Informationen zu den Windows-APIs finden Sie unter den Win32 SDK-Dokumentation in der Platform SDK-Windows-API.</span><span class="sxs-lookup"><span data-stu-id="53829-121">For complete information about the Windows APIs, see the Win32 SDK documentation in the Platform SDK Windows API.</span></span> <span data-ttu-id="53829-122">Weitere Informationen über die Konstanten, die Windows-APIs verwenden, überprüfen Sie die Headerdateien, z. B. Windows.h in im Plattform-SDK enthalten.</span><span class="sxs-lookup"><span data-stu-id="53829-122">For more information about the constants that Windows APIs use, examine the header files such as Windows.h included with the Platform SDK.</span></span>  
  
2. <span data-ttu-id="53829-123">Öffnen Sie ein neues Windows-Anwendungsprojekt, indem Sie auf **neu** auf die **Datei** Menü, und klicken Sie dann auf **Projekt**.</span><span class="sxs-lookup"><span data-stu-id="53829-123">Open a new Windows Application project by clicking **New** on the **File** menu, and then clicking **Project**.</span></span> <span data-ttu-id="53829-124">Das Dialogfeld **Neues Projekt** wird angezeigt.</span><span class="sxs-lookup"><span data-stu-id="53829-124">The **New Project** dialog box appears.</span></span>  
  
3. <span data-ttu-id="53829-125">Wählen Sie **Windows-Anwendung** aus der Liste der Projektvorlagen für Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="53829-125">Select **Windows Application** from the list of Visual Basic project templates.</span></span> <span data-ttu-id="53829-126">Das neue Projekt wird angezeigt.</span><span class="sxs-lookup"><span data-stu-id="53829-126">The new project is displayed.</span></span>  
  
4. <span data-ttu-id="53829-127">Fügen Sie die folgenden `Declare` Funktion entweder auf die Klasse oder das Modul, in dem Sie die DLL verwenden möchten:</span><span class="sxs-lookup"><span data-stu-id="53829-127">Add the following `Declare` function either to the class or module in which you want to use the DLL:</span></span>  
  
     [!code-vb[VbVbalrInterop#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#9)]  
  
### <a name="parts-of-the-declare-statement"></a><span data-ttu-id="53829-128">Teile der Declare-Anweisung</span><span class="sxs-lookup"><span data-stu-id="53829-128">Parts of the Declare Statement</span></span>  
 <span data-ttu-id="53829-129">Die `Declare` Anweisung enthält die folgenden Elemente.</span><span class="sxs-lookup"><span data-stu-id="53829-129">The `Declare` statement includes the following elements.</span></span>  
  
#### <a name="auto-modifier"></a><span data-ttu-id="53829-130">Auto-Modifizierer</span><span class="sxs-lookup"><span data-stu-id="53829-130">Auto modifier</span></span>  
 <span data-ttu-id="53829-131">Die `Auto` Modifizierer weist die Runtime die Zeichenfolge, die basierend auf den Methodennamen entsprechend den Regeln der common Language Runtime (oder Aliasname angegeben) zu konvertieren.</span><span class="sxs-lookup"><span data-stu-id="53829-131">The `Auto` modifier instructs the runtime to convert the string based on the method name according to common language runtime rules (or alias name if specified).</span></span>  
  
#### <a name="lib-and-alias-keywords"></a><span data-ttu-id="53829-132">LIB "und"-Alias-Schlüsselwörter</span><span class="sxs-lookup"><span data-stu-id="53829-132">Lib and Alias keywords</span></span>  
 <span data-ttu-id="53829-133">Die folgenden Namen der `Function` -Schlüsselwort ist der Name Ihrer Anwendung verwendet, um den Zugriff auf die importierten Funktion.</span><span class="sxs-lookup"><span data-stu-id="53829-133">The name following the `Function` keyword is the name your program uses to access the imported function.</span></span> <span data-ttu-id="53829-134">Kann es sein, identisch mit den tatsächlichen Namen der Funktion, die Sie aufrufen, oder Sie können alle gültigen Prozedurnamen und klicken Sie dann einsetzen der `Alias` Schlüsselwort, um den tatsächlichen Namen der Funktion geben Sie Sie aufrufen.</span><span class="sxs-lookup"><span data-stu-id="53829-134">It can be the same as the real name of the function you are calling, or you can use any valid procedure name and then employ the `Alias` keyword to specify the real name of the function you are calling.</span></span>  
  
 <span data-ttu-id="53829-135">Geben Sie die `Lib` Schlüsselwort, gefolgt von den Namen und Speicherort der DLL, die die Funktion enthält Sie aufrufen.</span><span class="sxs-lookup"><span data-stu-id="53829-135">Specify the `Lib` keyword, followed by the name and location of the DLL that contains the function you are calling.</span></span> <span data-ttu-id="53829-136">Sie müssen sich nicht um den Pfad zu Dateien in der Windows-Systemverzeichnisse anzugeben.</span><span class="sxs-lookup"><span data-stu-id="53829-136">You do not need to specify the path for files located in the Windows system directories.</span></span>  
  
 <span data-ttu-id="53829-137">Verwenden der `Alias` Schlüsselwort, wenn der Name der Funktion, die Sie aufrufen, ist kein Name ein gültiger Visual Basic-Prozedur oder steht in Konflikt mit dem Namen anderer Elemente in Ihrer Anwendung.</span><span class="sxs-lookup"><span data-stu-id="53829-137">Use the `Alias` keyword if the name of the function you are calling is not a valid Visual Basic procedure name, or conflicts with the name of other items in your application.</span></span> <span data-ttu-id="53829-138">`Alias` Gibt den tatsächlichen Namen der aufgerufenen Funktion an.</span><span class="sxs-lookup"><span data-stu-id="53829-138">`Alias` indicates the true name of the function being called.</span></span>  
  
#### <a name="argument-and-data-type-declarations"></a><span data-ttu-id="53829-139">Argument "und" Data-Type-Deklarationen</span><span class="sxs-lookup"><span data-stu-id="53829-139">Argument and Data Type Declarations</span></span>  
 <span data-ttu-id="53829-140">Deklarieren Sie die Argumente und deren Datentypen.</span><span class="sxs-lookup"><span data-stu-id="53829-140">Declare the arguments and their data types.</span></span> <span data-ttu-id="53829-141">Dieser Teil kann schwierig sein, da die Datentypen, die Windows verwendet Visual Studio-Datentypen nicht übereinstimmen.</span><span class="sxs-lookup"><span data-stu-id="53829-141">This part can be challenging because the data types that Windows uses do not correspond to Visual Studio data types.</span></span> <span data-ttu-id="53829-142">Visual Basic ist eine Menge Arbeit für Sie durch die Konvertierung von Argumenten zu kompatiblen Datentypen einen Prozess namens *Marshalling*.</span><span class="sxs-lookup"><span data-stu-id="53829-142">Visual Basic does a lot of the work for you by converting arguments to compatible data types, a process called *marshaling*.</span></span> <span data-ttu-id="53829-143">Sie können explizit steuern, wie Argumente gemarshallt werden, mithilfe der <xref:System.Runtime.InteropServices.MarshalAsAttribute> im definierten Attribut der <xref:System.Runtime.InteropServices> Namespace.</span><span class="sxs-lookup"><span data-stu-id="53829-143">You can explicitly control how arguments are marshaled by using the <xref:System.Runtime.InteropServices.MarshalAsAttribute> attribute defined in the <xref:System.Runtime.InteropServices> namespace.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="53829-144">Frühere Versionen von Visual Basic konnten Sie Parameter zu deklarieren `As Any`, d. h. diese Daten für alle Daten verwendet werden konnten.</span><span class="sxs-lookup"><span data-stu-id="53829-144">Previous versions of Visual Basic allowed you to declare parameters `As Any`, meaning that data of any data type could be used.</span></span> <span data-ttu-id="53829-145">Visual Basic erfordert, dass Sie einen bestimmten Datentyp für alle verwenden `Declare` Anweisungen.</span><span class="sxs-lookup"><span data-stu-id="53829-145">Visual Basic requires that you use a specific data type for all `Declare` statements.</span></span>  
  
#### <a name="windows-api-constants"></a><span data-ttu-id="53829-146">Windows-API-Konstanten</span><span class="sxs-lookup"><span data-stu-id="53829-146">Windows API Constants</span></span>  
 <span data-ttu-id="53829-147">Einige Argumente sind Kombinationen von Konstanten.</span><span class="sxs-lookup"><span data-stu-id="53829-147">Some arguments are combinations of constants.</span></span> <span data-ttu-id="53829-148">Z. B. die `MessageBox` in dieser exemplarischen Vorgehensweise gezeigten API akzeptiert ein ganzzahliges Argument aufgerufen `Typ` , die steuert, wie das Meldungsfeld angezeigt wird.</span><span class="sxs-lookup"><span data-stu-id="53829-148">For example, the `MessageBox` API shown in this walkthrough accepts an integer argument called `Typ` that controls how the message box is displayed.</span></span> <span data-ttu-id="53829-149">Sie können den numerischen Wert dieser Konstanten bestimmen, indem Sie die Untersuchung der `#define` Anweisungen in der WinUser.h-Datei.</span><span class="sxs-lookup"><span data-stu-id="53829-149">You can determine the numeric value of these constants by examining the `#define` statements in the file WinUser.h.</span></span> <span data-ttu-id="53829-150">Die numerischen Werte werden im Hexadezimalformat, in der Regel angezeigt, daher Sie verwenden einen Rechner sollten, um Sie hinzuzufügen, und Konvertieren in eine Dezimalzahl.</span><span class="sxs-lookup"><span data-stu-id="53829-150">The numeric values are generally shown in hexadecimal, so you may want to use a calculator to add them and convert to decimal.</span></span> <span data-ttu-id="53829-151">Wenn Sie die Konstanten für das Ausrufezeichen Format kombinieren möchten z. B. `MB_ICONEXCLAMATION` 0x00000030 und die Ja/keine Formatvorlage `MB_YESNO` 0 x 00000004, Sie können die Zahlen addieren und erhalten ein Ergebnis, 0 x 00000034 bzw. 52 Dezimalformat.</span><span class="sxs-lookup"><span data-stu-id="53829-151">For example, if you want to combine the constants for the exclamation style `MB_ICONEXCLAMATION` 0x00000030 and the Yes/No style `MB_YESNO` 0x00000004, you can add the numbers and get a result of 0x00000034, or 52 decimal.</span></span> <span data-ttu-id="53829-152">Sie können das decimal Ergebnis direkt verwenden, es ist besser, deklarieren diese Werte als Konstanten in der Anwendung und kombinieren mithilfe der `Or` Operator.</span><span class="sxs-lookup"><span data-stu-id="53829-152">Although you can use the decimal result directly, it is better to declare these values as constants in your application and combine them using the `Or` operator.</span></span>  
  
###### <a name="to-declare-constants-for-windows-api-calls"></a><span data-ttu-id="53829-153">Zum Deklarieren von Konstanten für die Windows-API-Aufrufe</span><span class="sxs-lookup"><span data-stu-id="53829-153">To declare constants for Windows API calls</span></span>  
  
1. <span data-ttu-id="53829-154">Lesen Sie die Dokumentation für die Windows-Funktion, die Sie aufrufen.</span><span class="sxs-lookup"><span data-stu-id="53829-154">Consult the documentation for the Windows function you are calling.</span></span> <span data-ttu-id="53829-155">Bestimmen Sie den Namen der verwendeten Konstanten und den Namen der h-Datei, die die numerischen Werte für diese Konstanten enthält.</span><span class="sxs-lookup"><span data-stu-id="53829-155">Determine the name of the constants it uses and the name of the .h file that contains the numeric values for these constants.</span></span>  
  
2. <span data-ttu-id="53829-156">Verwenden Sie einen Text-Editor wie Editor zum Anzeigen des Inhalts der Headerdatei (. h), und suchen Sie die Werte der Konstanten, die Sie verwenden.</span><span class="sxs-lookup"><span data-stu-id="53829-156">Use a text editor, such as Notepad, to view the contents of the header (.h) file, and find the values associated with the constants you are using.</span></span> <span data-ttu-id="53829-157">Z. B. die `MessageBox` -API verwendet die Konstante `MB_ICONQUESTION` zu einem Fragezeichen in der MessageBox anzeigen.</span><span class="sxs-lookup"><span data-stu-id="53829-157">For example, the `MessageBox` API uses the constant `MB_ICONQUESTION` to show a question mark in the message box.</span></span> <span data-ttu-id="53829-158">Die Definition für `MB_ICONQUESTION` steht in WinUser.h und sieht wie folgt aus:</span><span class="sxs-lookup"><span data-stu-id="53829-158">The definition for `MB_ICONQUESTION` is in WinUser.h and appears as follows:</span></span>  
  
     `#define MB_ICONQUESTION             0x00000020L`  
  
3. <span data-ttu-id="53829-159">Hinzufügen der entsprechenden `Const` Anweisungen, die der Klasse oder das Modul, um diese Konstanten für Ihre Anwendung verfügbar machen.</span><span class="sxs-lookup"><span data-stu-id="53829-159">Add equivalent `Const` statements to your class or module to make these constants available to your application.</span></span> <span data-ttu-id="53829-160">Zum Beispiel:</span><span class="sxs-lookup"><span data-stu-id="53829-160">For example:</span></span>  
  
     [!code-vb[VbVbalrInterop#11](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#11)]  
  
###### <a name="to-call-the-dll-procedure"></a><span data-ttu-id="53829-161">Die DLL-Prozedur aufrufen</span><span class="sxs-lookup"><span data-stu-id="53829-161">To call the DLL procedure</span></span>  
  
1. <span data-ttu-id="53829-162">Fügen Sie eine Schaltfläche mit dem Namen `Button1` auf den Start des Projekts, und doppelklicken Sie darauf, um ihren Code anzuzeigen.</span><span class="sxs-lookup"><span data-stu-id="53829-162">Add a button named `Button1` to the startup form for your project, and then double-click it to view its code.</span></span> <span data-ttu-id="53829-163">Der Ereignishandler für die Schaltfläche wird angezeigt.</span><span class="sxs-lookup"><span data-stu-id="53829-163">The event handler for the button is displayed.</span></span>  
  
2. <span data-ttu-id="53829-164">Fügen Sie Code in die `Click` -Ereignishandler für die Schaltfläche, die Sie hinzugefügt haben, rufen Sie die Prozedur, und geben Sie die entsprechenden Argumenten:</span><span class="sxs-lookup"><span data-stu-id="53829-164">Add code to the `Click` event handler for the button you added, to call the procedure and provide the appropriate arguments:</span></span>  
  
     [!code-vb[VbVbalrInterop#12](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#12)]  
  
3. <span data-ttu-id="53829-165">Führen Sie das Projekt durch Drücken von F5.</span><span class="sxs-lookup"><span data-stu-id="53829-165">Run the project by pressing F5.</span></span> <span data-ttu-id="53829-166">Das Meldungsfeld wird angezeigt, mit den beiden **Ja** und **keine** Antwort Schaltflächen.</span><span class="sxs-lookup"><span data-stu-id="53829-166">The message box is displayed with both **Yes** and **No** response buttons.</span></span> <span data-ttu-id="53829-167">Klicken Sie auf eines.</span><span class="sxs-lookup"><span data-stu-id="53829-167">Click either one.</span></span>  
  
#### <a name="data-marshaling"></a><span data-ttu-id="53829-168">Daten-Marshalling</span><span class="sxs-lookup"><span data-stu-id="53829-168">Data Marshaling</span></span>  
 <span data-ttu-id="53829-169">Visual Basic konvertiert automatisch die Datentypen der Parameter und Rückgabewerte für die Windows-API-Aufrufe, aber Sie können die `MarshalAs` Attribut explizit nicht verwaltete Datentypen angeben, die eine API erwartet.</span><span class="sxs-lookup"><span data-stu-id="53829-169">Visual Basic automatically converts the data types of parameters and return values for Windows API calls, but you can use the `MarshalAs` attribute to explicitly specify unmanaged data types that an API expects.</span></span> <span data-ttu-id="53829-170">Weitere Informationen zum Interop-Marshalling, finden Sie unter [Interop-Marshalling](../../../framework/interop/interop-marshaling.md).</span><span class="sxs-lookup"><span data-stu-id="53829-170">For more information about interop marshaling, see [Interop Marshaling](../../../framework/interop/interop-marshaling.md).</span></span>  
  
###### <a name="to-use-declare-and-marshalas-in-an-api-call"></a><span data-ttu-id="53829-171">Verwenden von Declare und MarshalAs in einer API-Aufruf</span><span class="sxs-lookup"><span data-stu-id="53829-171">To use Declare and MarshalAs in an API call</span></span>  
  
1. <span data-ttu-id="53829-172">Bestimmen Sie den Namen der Funktion aufgerufen werden, sowie deren Argumente, Datentypen werden soll, und Wert zurück.</span><span class="sxs-lookup"><span data-stu-id="53829-172">Determine the name of the function you want to call, plus its arguments, data types, and return value.</span></span>  
  
2. <span data-ttu-id="53829-173">Zur Vereinfachung der Zugriff auf die `MarshalAs` -Attribut enthält, fügen eine `Imports` Anweisung am Anfang des Codes für die Klasse oder das Modul, wie im folgenden Beispiel gezeigt:</span><span class="sxs-lookup"><span data-stu-id="53829-173">To simplify access to the `MarshalAs` attribute, add an `Imports` statement to the top of the code for the class or module, as in the following example:</span></span>  
  
     [!code-vb[VbVbalrInterop#13](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#13)]  
  
3. <span data-ttu-id="53829-174">Fügen Sie einen Funktionsprototyp, für die importierte Funktion auf die Klasse oder das Modul, die Sie verwenden und Anwenden der `MarshalAs` -Attribut auf die Parameter oder Rückgabewert.</span><span class="sxs-lookup"><span data-stu-id="53829-174">Add a function prototype for the imported function to the class or module you are using, and apply the `MarshalAs` attribute to the parameters or return value.</span></span> <span data-ttu-id="53829-175">Im folgenden Beispiel ist ein API-Aufruf, der den Typ erwartet `void*` wird gemarshallt als `AsAny`:</span><span class="sxs-lookup"><span data-stu-id="53829-175">In the following example, an API call that expects the type `void*` is marshaled as `AsAny`:</span></span>  
  
     [!code-vb[VbVbalrInterop#14](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#14)]  
  
## <a name="api-calls-using-dllimport"></a><span data-ttu-id="53829-176">API-Aufrufe mit DllImport</span><span class="sxs-lookup"><span data-stu-id="53829-176">API Calls Using DllImport</span></span>  
 <span data-ttu-id="53829-177">Die `DllImport` Attribut bietet eine zweite Möglichkeit zum Aufrufen von Funktionen in DLLs ohne Typbibliotheken.</span><span class="sxs-lookup"><span data-stu-id="53829-177">The `DllImport` attribute provides a second way to call functions in DLLs without type libraries.</span></span> <span data-ttu-id="53829-178">`DllImport` ist ungefähr äquivalent zur Verwendung einer `Declare` Anweisung bietet jedoch mehr Kontrolle über die wie Funktionen aufgerufen werden.</span><span class="sxs-lookup"><span data-stu-id="53829-178">`DllImport` is roughly equivalent to using a `Declare` statement but provides more control over how functions are called.</span></span>  
  
 <span data-ttu-id="53829-179">Sie können `DllImport` mit den meisten Windows-API aufruft, solange der Aufruf an einen freigegebenen verweist (bezeichnet *statische*) Methode.</span><span class="sxs-lookup"><span data-stu-id="53829-179">You can use `DllImport` with most Windows API calls as long as the call refers to a shared (sometimes called *static*) method.</span></span> <span data-ttu-id="53829-180">Sie können keine Methoden, die eine Instanz einer Klasse benötigen.</span><span class="sxs-lookup"><span data-stu-id="53829-180">You cannot use methods that require an instance of a class.</span></span> <span data-ttu-id="53829-181">Im Gegensatz zu `Declare` Anweisungen `DllImport` können keine Aufrufe der `MarshalAs` Attribut.</span><span class="sxs-lookup"><span data-stu-id="53829-181">Unlike `Declare` statements, `DllImport` calls cannot use the `MarshalAs` attribute.</span></span>  
  
#### <a name="to-call-a-windows-api-using-the-dllimport-attribute"></a><span data-ttu-id="53829-182">Zum Aufrufen einer Windows-API, die mit dem DllImport-Attribut</span><span class="sxs-lookup"><span data-stu-id="53829-182">To call a Windows API using the DllImport attribute</span></span>  
  
1. <span data-ttu-id="53829-183">Öffnen Sie ein neues Windows-Anwendungsprojekt, indem Sie auf **neu** auf die **Datei** Menü, und klicken Sie dann auf **Projekt**.</span><span class="sxs-lookup"><span data-stu-id="53829-183">Open a new Windows Application project by clicking **New** on the **File** menu, and then clicking **Project**.</span></span> <span data-ttu-id="53829-184">Das Dialogfeld **Neues Projekt** wird angezeigt.</span><span class="sxs-lookup"><span data-stu-id="53829-184">The **New Project** dialog box appears.</span></span>  
  
2. <span data-ttu-id="53829-185">Wählen Sie **Windows-Anwendung** aus der Liste der Projektvorlagen für Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="53829-185">Select **Windows Application** from the list of Visual Basic project templates.</span></span> <span data-ttu-id="53829-186">Das neue Projekt wird angezeigt.</span><span class="sxs-lookup"><span data-stu-id="53829-186">The new project is displayed.</span></span>  
  
3. <span data-ttu-id="53829-187">Fügen Sie eine Schaltfläche mit dem Namen `Button2` auf das Startformular.</span><span class="sxs-lookup"><span data-stu-id="53829-187">Add a button named `Button2` to the startup form.</span></span>  
  
4. <span data-ttu-id="53829-188">Doppelklicken Sie auf `Button2` die Codeansicht für das Formular zu öffnen.</span><span class="sxs-lookup"><span data-stu-id="53829-188">Double-click `Button2` to open the code view for the form.</span></span>  
  
5. <span data-ttu-id="53829-189">Zur Vereinfachung der Zugriff auf `DllImport`, Hinzufügen einer `Imports` Anweisung am Anfang des Codes für die Startup-Form-Klasse:</span><span class="sxs-lookup"><span data-stu-id="53829-189">To simplify access to `DllImport`, add an `Imports` statement to the top of the code for the startup form class:</span></span>  
  
     [!code-vb[VbVbalrInterop#13](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#13)]  
  
6. <span data-ttu-id="53829-190">Deklarieren Sie eine leere Funktion vorherigen der `End Class` -Anweisung für das Formular, und klicken Sie auf die Namen der Funktion `MoveFile`.</span><span class="sxs-lookup"><span data-stu-id="53829-190">Declare an empty function preceding the `End Class` statement for the form, and name the function `MoveFile`.</span></span>  
  
7. <span data-ttu-id="53829-191">Anwenden der `Public` und `Shared` Modifizierer der Deklaration der Funktion und Festlegen von Parametern für `MoveFile` basierend auf den Argumenten, die die Windows-API-Funktion verwendet:</span><span class="sxs-lookup"><span data-stu-id="53829-191">Apply the `Public` and `Shared` modifiers to the function declaration and set parameters for `MoveFile` based on the arguments the Windows API function uses:</span></span>  
  
     [!code-vb[VbVbalrInterop#16](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#16)]  
  
     <span data-ttu-id="53829-192">Die Funktion kann ein beliebiger gültiger Prozedurname haben; die `DllImport` -Attribut gibt den Namen in der DLL.</span><span class="sxs-lookup"><span data-stu-id="53829-192">Your function can have any valid procedure name; the `DllImport` attribute specifies the name in the DLL.</span></span> <span data-ttu-id="53829-193">Er führt auch die Interop-Marshalling für die Parameter und Rückgabewerte, sodass Sie Visual Studio-Datentypen auswählen können, die auf die Daten ähneln der API verwendeten Datentypen.</span><span class="sxs-lookup"><span data-stu-id="53829-193">It also handles interoperability marshaling for the parameters and return values, so you can choose Visual Studio data types that are similar to the data types the API uses.</span></span>  
  
8. <span data-ttu-id="53829-194">Anwenden der `DllImport` Attribut der empty-Funktion.</span><span class="sxs-lookup"><span data-stu-id="53829-194">Apply the `DllImport` attribute to the empty function.</span></span> <span data-ttu-id="53829-195">Der erste Parameter ist der Name und Speicherort der DLL mit der Funktion, die Sie aufrufen.</span><span class="sxs-lookup"><span data-stu-id="53829-195">The first parameter is the name and location of the DLL containing the function you are calling.</span></span> <span data-ttu-id="53829-196">Sie müssen sich nicht um den Pfad zu Dateien in der Windows-Systemverzeichnisse anzugeben.</span><span class="sxs-lookup"><span data-stu-id="53829-196">You do not need to specify the path for files located in the Windows system directories.</span></span> <span data-ttu-id="53829-197">Der zweite Parameter ist ein benanntes Argument, das den Namen der Funktion in der Windows-API angibt.</span><span class="sxs-lookup"><span data-stu-id="53829-197">The second parameter is a named argument that specifies the name of the function in the Windows API.</span></span> <span data-ttu-id="53829-198">In diesem Beispiel die `DllImport` Attribut erzwingt, dass Aufrufe von `MoveFile` an `MoveFileW` in KERNEL32. DLL.</span><span class="sxs-lookup"><span data-stu-id="53829-198">In this example, the `DllImport` attribute forces calls to `MoveFile` to be forwarded to `MoveFileW` in KERNEL32.DLL.</span></span> <span data-ttu-id="53829-199">Die `MoveFileW` -Methode kopiert eine Datei aus dem Pfad `src` auf den Pfad `dst`.</span><span class="sxs-lookup"><span data-stu-id="53829-199">The `MoveFileW` method copies a file from the path `src` to the path `dst`.</span></span>  
  
     [!code-vb[VbVbalrInterop#17](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#17)]  
  
9. <span data-ttu-id="53829-200">Fügen Sie Code in die `Button2_Click` -Ereignishandler, um die Funktion aufrufen:</span><span class="sxs-lookup"><span data-stu-id="53829-200">Add code to the `Button2_Click` event handler to call the function:</span></span>  
  
     [!code-vb[VbVbalrInterop#18](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#18)]  
  
10. <span data-ttu-id="53829-201">Erstellen Sie eine Datei mit dem Namen "Test.txt", und fügen Sie ihn in das Verzeichnis "C:\Tmp" auf Ihrer Festplatte.</span><span class="sxs-lookup"><span data-stu-id="53829-201">Create a file named Test.txt and place it in the C:\Tmp directory on your hard drive.</span></span> <span data-ttu-id="53829-202">Erstellen Sie ggf. die Tmp-Verzeichnis.</span><span class="sxs-lookup"><span data-stu-id="53829-202">Create the Tmp directory if necessary.</span></span>  
  
11. <span data-ttu-id="53829-203">Drücken Sie F5, um die Anwendung zu starten.</span><span class="sxs-lookup"><span data-stu-id="53829-203">Press F5 to start the application.</span></span> <span data-ttu-id="53829-204">Das Hauptformular angezeigt wird.</span><span class="sxs-lookup"><span data-stu-id="53829-204">The main form appears.</span></span>  
  
12. <span data-ttu-id="53829-205">Klicken Sie auf **Button2**.</span><span class="sxs-lookup"><span data-stu-id="53829-205">Click **Button2**.</span></span> <span data-ttu-id="53829-206">Die Meldung "die Datei erfolgreich verschoben wurde" wird angezeigt, wenn die Datei verschoben werden kann.</span><span class="sxs-lookup"><span data-stu-id="53829-206">The message "The file was moved successfully" is displayed if the file can be moved.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="53829-207">Siehe auch</span><span class="sxs-lookup"><span data-stu-id="53829-207">See also</span></span>

- <xref:System.Runtime.InteropServices.DllImportAttribute>
- <xref:System.Runtime.InteropServices.MarshalAsAttribute>
- [<span data-ttu-id="53829-208">Declare-Anweisung</span><span class="sxs-lookup"><span data-stu-id="53829-208">Declare Statement</span></span>](../../../visual-basic/language-reference/statements/declare-statement.md)
- [<span data-ttu-id="53829-209">Auto</span><span class="sxs-lookup"><span data-stu-id="53829-209">Auto</span></span>](../../../visual-basic/language-reference/modifiers/auto.md)
- [<span data-ttu-id="53829-210">Alias</span><span class="sxs-lookup"><span data-stu-id="53829-210">Alias</span></span>](../../../visual-basic/language-reference/statements/alias-clause.md)
- [<span data-ttu-id="53829-211">COM-Interop</span><span class="sxs-lookup"><span data-stu-id="53829-211">COM Interop</span></span>](../../../visual-basic/programming-guide/com-interop/index.md)
- [<span data-ttu-id="53829-212">Creating Prototypes in Managed Code (Erstellen von Prototypen in verwaltetem Code)</span><span class="sxs-lookup"><span data-stu-id="53829-212">Creating Prototypes in Managed Code</span></span>](../../../framework/interop/creating-prototypes-in-managed-code.md)
- [<span data-ttu-id="53829-213">Marshallen von Delegaten als Rückrufmethode</span><span class="sxs-lookup"><span data-stu-id="53829-213">Marshaling a Delegate as a Callback Method</span></span>](../../../framework/interop/marshaling-a-delegate-as-a-callback-method.md)
