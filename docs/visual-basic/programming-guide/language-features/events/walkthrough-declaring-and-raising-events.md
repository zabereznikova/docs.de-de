---
title: Deklarieren und Auslösen von Ereignissen
ms.date: 07/20/2015
helpviewer_keywords:
- declarations [Visual Basic], events
- events [Visual Basic], walkthroughs
- declaring events [Visual Basic], walkthroughs
- events [Visual Basic], declaring
- events [Visual Basic], raising
- raising events [Visual Basic], walkthroughs
ms.assetid: 8ffb3be8-097d-4d3c-b71e-04555ebda2a2
ms.openlocfilehash: 6f4c303604f9cf55b4ecd500636e0d2772b6234c
ms.sourcegitcommit: 17ee6605e01ef32506f8fdc686954244ba6911de
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 11/22/2019
ms.locfileid: "74345089"
---
# <a name="walkthrough-declaring-and-raising-events-visual-basic"></a><span data-ttu-id="f34c4-102">Exemplarische Vorgehensweise: Deklarieren und Auslösen von Ereignissen (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="f34c4-102">Walkthrough: Declaring and Raising Events (Visual Basic)</span></span>
<span data-ttu-id="f34c4-103">Diese exemplarische Vorgehensweise veranschaulicht, wie Sie Ereignisse für eine Klasse mit dem Namen `Widget`deklarieren und Auswerfen.</span><span class="sxs-lookup"><span data-stu-id="f34c4-103">This walkthrough demonstrates how to declare and raise events for a class named `Widget`.</span></span> <span data-ttu-id="f34c4-104">Nachdem Sie die Schritte ausgeführt haben, können Sie das Begleit Thema Exemplarische Vorgehensweise [: Behandeln von Ereignissen](../../../../visual-basic/programming-guide/language-features/events/walkthrough-handling-events.md)lesen, in dem gezeigt wird, wie Ereignisse aus `Widget` Objekten verwendet werden, um Statusinformationen in einer Anwendung bereitzustellen.</span><span class="sxs-lookup"><span data-stu-id="f34c4-104">After you complete the steps, you might want to read the companion topic, [Walkthrough: Handling Events](../../../../visual-basic/programming-guide/language-features/events/walkthrough-handling-events.md), which shows how to use events from `Widget` objects to provide status information in an application.</span></span>  
  
## <a name="the-widget-class"></a><span data-ttu-id="f34c4-105">Die widgeklasse</span><span class="sxs-lookup"><span data-stu-id="f34c4-105">The Widget Class</span></span>  
 <span data-ttu-id="f34c4-106">Angenommen, Sie verfügen über eine `Widget`-Klasse.</span><span class="sxs-lookup"><span data-stu-id="f34c4-106">Assume for the moment that you have a `Widget` class.</span></span> <span data-ttu-id="f34c4-107">Die `Widget`-Klasse verfügt über eine-Methode, deren Ausführung viel Zeit in Anspruch nehmen kann, und Sie möchten, dass Ihre Anwendung in der Lage ist, einen beliebigen Abschluss Indikator zu verwenden.</span><span class="sxs-lookup"><span data-stu-id="f34c4-107">Your `Widget` class has a method that can take a long time to execute, and you want your application to be able to put up some kind of completion indicator.</span></span>  
  
 <span data-ttu-id="f34c4-108">Natürlich können Sie das `Widget`-Objekt das Dialogfeld "Prozentsatz vervollständigen" anzeigen lassen, aber dann würden Sie in jedem Projekt, in dem Sie die `Widget`-Klasse verwendet haben, an diesem Dialogfeld hängen.</span><span class="sxs-lookup"><span data-stu-id="f34c4-108">Of course, you could make the `Widget` object show a percent-complete dialog box, but then you would be stuck with that dialog box in every project in which you used the `Widget` class.</span></span> <span data-ttu-id="f34c4-109">Ein gutes Prinzip des Objekt Entwurfs besteht darin, dass die Anwendung, die ein Objekt verwendet, die Benutzeroberfläche verarbeitet – es sei denn, der gesamte Zweck des Objekts besteht darin, ein Formular oder Dialogfeld zu verwalten.</span><span class="sxs-lookup"><span data-stu-id="f34c4-109">A good principle of object design is to let the application that uses an object handle the user interface—unless the whole purpose of the object is to manage a form or dialog box.</span></span>  
  
 <span data-ttu-id="f34c4-110">Der Zweck `Widget` besteht darin, andere Aufgaben auszuführen. es ist daher besser, ein `PercentDone`-Ereignis hinzuzufügen und die Prozedur, die `Widget`Methoden aufruft, dieses Ereignis zu behandeln und Statusaktualisierungen anzuzeigen.</span><span class="sxs-lookup"><span data-stu-id="f34c4-110">The purpose of `Widget` is to perform other tasks, so it is better to add a `PercentDone` event and let the procedure that calls `Widget`'s methods handle that event and display status updates.</span></span> <span data-ttu-id="f34c4-111">Das `PercentDone` Ereignis kann auch einen Mechanismus zum Abbrechen der Aufgabe bereitstellen.</span><span class="sxs-lookup"><span data-stu-id="f34c4-111">The `PercentDone` event can also provide a mechanism for canceling the task.</span></span>  
  
#### <a name="to-build-the-code-example-for-this-topic"></a><span data-ttu-id="f34c4-112">So erstellen Sie das Codebeispiel für dieses Thema</span><span class="sxs-lookup"><span data-stu-id="f34c4-112">To build the code example for this topic</span></span>  
  
1. <span data-ttu-id="f34c4-113">Öffnen Sie ein neues Visual Basic Windows-Anwendungsprojekt, und erstellen Sie ein Formular mit dem Namen `Form1`.</span><span class="sxs-lookup"><span data-stu-id="f34c4-113">Open a new Visual Basic Windows Application project and create a form named `Form1`.</span></span>  
  
2. <span data-ttu-id="f34c4-114">Fügen Sie `Form1`zwei Schaltflächen und eine Bezeichnung hinzu.</span><span class="sxs-lookup"><span data-stu-id="f34c4-114">Add two buttons and a label to `Form1`.</span></span>  
  
3. <span data-ttu-id="f34c4-115">Benennen Sie die Objekte wie in der folgenden Tabelle gezeigt.</span><span class="sxs-lookup"><span data-stu-id="f34c4-115">Name the objects as shown in the following table.</span></span>  
  
    |<span data-ttu-id="f34c4-116">Object</span><span class="sxs-lookup"><span data-stu-id="f34c4-116">Object</span></span>|<span data-ttu-id="f34c4-117">Eigenschaft</span><span class="sxs-lookup"><span data-stu-id="f34c4-117">Property</span></span>|<span data-ttu-id="f34c4-118">Einstellung</span><span class="sxs-lookup"><span data-stu-id="f34c4-118">Setting</span></span>|  
    |------------|--------------|-------------|  
    |`Button1`|`Text`|<span data-ttu-id="f34c4-119">Startaufgabe</span><span class="sxs-lookup"><span data-stu-id="f34c4-119">Start Task</span></span>|  
    |`Button2`|`Text`|<span data-ttu-id="f34c4-120">Abbrechen</span><span class="sxs-lookup"><span data-stu-id="f34c4-120">Cancel</span></span>|  
    |`Label`|<span data-ttu-id="f34c4-121">`(Name)`, `Text`</span><span class="sxs-lookup"><span data-stu-id="f34c4-121">`(Name)`, `Text`</span></span>|<span data-ttu-id="f34c4-122">lblprozentudone, 0</span><span class="sxs-lookup"><span data-stu-id="f34c4-122">lblPercentDone, 0</span></span>|  
  
4. <span data-ttu-id="f34c4-123">Wählen Sie im Menü **Projekt** die Option **Klasse hinzufügen** aus, um dem Projekt eine Klasse mit dem Namen `Widget.vb` hinzuzufügen.</span><span class="sxs-lookup"><span data-stu-id="f34c4-123">On the **Project** menu, choose **Add Class** to add a class named `Widget.vb` to the project.</span></span>  
  
#### <a name="to-declare-an-event-for-the-widget-class"></a><span data-ttu-id="f34c4-124">So deklarieren Sie ein Ereignis für die Widget-Klasse</span><span class="sxs-lookup"><span data-stu-id="f34c4-124">To declare an event for the Widget class</span></span>  
  
- <span data-ttu-id="f34c4-125">Verwenden Sie das `Event`-Schlüsselwort, um ein Ereignis in der `Widget` Klasse zu deklarieren.</span><span class="sxs-lookup"><span data-stu-id="f34c4-125">Use the `Event` keyword to declare an event in the `Widget` class.</span></span> <span data-ttu-id="f34c4-126">Beachten Sie, dass ein Ereignis `ByVal` und `ByRef` Argumente aufweisen kann, wie `Widget`das `PercentDone` Ereignis veranschaulicht:</span><span class="sxs-lookup"><span data-stu-id="f34c4-126">Note that an event can have `ByVal` and `ByRef` arguments, as `Widget`'s `PercentDone` event demonstrates:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#1](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Widget.vb#1)]  
  
 <span data-ttu-id="f34c4-127">Wenn das aufrufenden Objekt ein `PercentDone` Ereignis empfängt, enthält das `Percent`-Argument den Prozentsatz der abgeschlossenen Aufgabe.</span><span class="sxs-lookup"><span data-stu-id="f34c4-127">When the calling object receives a `PercentDone` event, the `Percent` argument contains the percentage of the task that is complete.</span></span> <span data-ttu-id="f34c4-128">Das `Cancel`-Argument kann `True` festgelegt werden, um die Methode abzubrechen, die das Ereignis ausgelöst hat.</span><span class="sxs-lookup"><span data-stu-id="f34c4-128">The `Cancel` argument can be set to `True` to cancel the method that raised the event.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="f34c4-129">Sie können Ereignis Argumente genau wie Argumente von Prozeduren deklarieren, mit den folgenden Ausnahmen: Ereignisse können keine `Optional`-oder `ParamArray` Argumente aufweisen, und Ereignisse haben keine Rückgabewerte.</span><span class="sxs-lookup"><span data-stu-id="f34c4-129">You can declare event arguments just as you do arguments of procedures, with the following exceptions: Events cannot have `Optional` or `ParamArray` arguments, and events do not have return values.</span></span>  
  
 <span data-ttu-id="f34c4-130">Das `PercentDone`-Ereignis wird von der `LongTask`-Methode der `Widget`-Klasse ausgelöst.</span><span class="sxs-lookup"><span data-stu-id="f34c4-130">The `PercentDone` event is raised by the `LongTask` method of the `Widget` class.</span></span> <span data-ttu-id="f34c4-131">`LongTask` verwendet zwei Argumente: die Zeitdauer, die die Methode für die Ausführung der Methode voraussetzt, und das minimale Zeitintervall, bevor `LongTask` angehalten wird, um das `PercentDone` Ereignis zu erhöhen.</span><span class="sxs-lookup"><span data-stu-id="f34c4-131">`LongTask` takes two arguments: the length of time the method pretends to be doing work, and the minimum time interval before `LongTask` pauses to raise the `PercentDone` event.</span></span>  
  
#### <a name="to-raise-the-percentdone-event"></a><span data-ttu-id="f34c4-132">So heben Sie das Ereignis "" vom</span><span class="sxs-lookup"><span data-stu-id="f34c4-132">To raise the PercentDone event</span></span>  
  
1. <span data-ttu-id="f34c4-133">Um den Zugriff auf die von dieser Klasse verwendete `Timer`-Eigenschaft zu vereinfachen, fügen Sie am Anfang des Deklarations Abschnitts Ihres Class-Moduls oberhalb der `Class Widget`-Anweisung eine `Imports`-Anweisung hinzu.</span><span class="sxs-lookup"><span data-stu-id="f34c4-133">To simplify access to the `Timer` property used by this class, add an `Imports` statement to the top of the declarations section of your class module, above the `Class Widget` statement.</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Widget.vb#2)]  
  
2. <span data-ttu-id="f34c4-134">Fügen Sie der `Widget` -Klasse folgenden Code hinzu:</span><span class="sxs-lookup"><span data-stu-id="f34c4-134">Add the following code to the `Widget` class:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#3](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Widget.vb#3)]  
  
 <span data-ttu-id="f34c4-135">Wenn die Anwendung die `LongTask`-Methode aufruft, löst die `Widget`-Klasse alle `MinimumInterval` Sekunden das `PercentDone`-Ereignis aus.</span><span class="sxs-lookup"><span data-stu-id="f34c4-135">When your application calls the `LongTask` method, the `Widget` class raises the `PercentDone` event every `MinimumInterval` seconds.</span></span> <span data-ttu-id="f34c4-136">Wenn das Ereignis zurückgibt, prüft `LongTask`, ob das `Cancel`-Argument auf `True`festgelegt wurde.</span><span class="sxs-lookup"><span data-stu-id="f34c4-136">When the event returns, `LongTask` checks to see if the `Cancel` argument was set to `True`.</span></span>  
  
 <span data-ttu-id="f34c4-137">Hier sind einige Haftungsausschlüsse erforderlich.</span><span class="sxs-lookup"><span data-stu-id="f34c4-137">A few disclaimers are necessary here.</span></span> <span data-ttu-id="f34c4-138">Der Einfachheit halber wird bei der `LongTask` Prozedur davon ausgegangen, dass Sie im Voraus wissen, wie lange die Aufgabe dauert.</span><span class="sxs-lookup"><span data-stu-id="f34c4-138">For simplicity, the `LongTask` procedure assumes you know in advance how long the task will take.</span></span> <span data-ttu-id="f34c4-139">Dies ist fast nie der Fall.</span><span class="sxs-lookup"><span data-stu-id="f34c4-139">This is almost never the case.</span></span> <span data-ttu-id="f34c4-140">Das Aufteilen von Aufgaben in Blöcke mit gleichmäßiger Größe kann schwierig sein, und häufig ist das, was für Benutzer am wichtigsten ist, einfach die Zeitspanne, die verstrichen ist, bevor Sie einen Hinweis darauf erhalten, dass etwas passiert.</span><span class="sxs-lookup"><span data-stu-id="f34c4-140">Dividing tasks into chunks of even size can be difficult, and often what matters most to users is simply the amount of time that passes before they get an indication that something is happening.</span></span>  
  
 <span data-ttu-id="f34c4-141">Möglicherweise haben Sie in diesem Beispiel einen weiteren Fehler erkannt.</span><span class="sxs-lookup"><span data-stu-id="f34c4-141">You may have spotted another flaw in this sample.</span></span> <span data-ttu-id="f34c4-142">Die `Timer`-Eigenschaft gibt die Anzahl der Sekunden zurück, die seit Mitternacht vergangen sind. Daher bleibt die Anwendung hängen, wenn Sie unmittelbar vor Mitternacht gestartet wird.</span><span class="sxs-lookup"><span data-stu-id="f34c4-142">The `Timer` property returns the number of seconds that have passed since midnight; therefore, the application gets stuck if it is started just before midnight.</span></span> <span data-ttu-id="f34c4-143">Ein sorgfältiger Ansatz zum Messen der Zeit würde Grenzbedingungen wie diese berücksichtigen oder die Verwendung von Eigenschaften wie `Now`vermeiden.</span><span class="sxs-lookup"><span data-stu-id="f34c4-143">A more careful approach to measuring time would take boundary conditions such as this into consideration, or avoid them altogether, using properties such as `Now`.</span></span>  
  
 <span data-ttu-id="f34c4-144">Nun, da die `Widget` Klasse Ereignisse hervorrufen kann, können Sie zur nächsten exemplarischen Vorgehensweise wechseln.</span><span class="sxs-lookup"><span data-stu-id="f34c4-144">Now that the `Widget` class can raise events, you can move to the next walkthrough.</span></span> <span data-ttu-id="f34c4-145">Exemplarische Vorgehensweise [: Behandeln von Ereignissen](../../../../visual-basic/programming-guide/language-features/events/walkthrough-handling-events.md) veranschaulicht, wie `WithEvents` verwendet wird, um dem `PercentDone` Ereignis einen Ereignishandler zuzuordnen.</span><span class="sxs-lookup"><span data-stu-id="f34c4-145">[Walkthrough: Handling Events](../../../../visual-basic/programming-guide/language-features/events/walkthrough-handling-events.md) demonstrates how to use `WithEvents` to associate an event handler with the `PercentDone` event.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="f34c4-146">Siehe auch</span><span class="sxs-lookup"><span data-stu-id="f34c4-146">See also</span></span>

- <xref:Microsoft.VisualBasic.DateAndTime.Timer%2A>
- <xref:Microsoft.VisualBasic.DateAndTime.Now%2A>
- [<span data-ttu-id="f34c4-147">Exemplarische Vorgehensweise: Behandeln von Ereignissen</span><span class="sxs-lookup"><span data-stu-id="f34c4-147">Walkthrough: Handling Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/walkthrough-handling-events.md)
- [<span data-ttu-id="f34c4-148">Ereignisse</span><span class="sxs-lookup"><span data-stu-id="f34c4-148">Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/index.md)
