---
title: Behandeln von Ereignissen
ms.date: 07/20/2015
helpviewer_keywords:
- event handling [Visual Basic], walkthroughs
- walkthroughs [Visual Basic], event handling
- variables [Visual Basic], WithEvents
- events [Visual Basic], walkthroughs
- WithEvents keyword [Visual Basic], walkthroughs
- event handlers [Visual Basic], walkthroughs
ms.assetid: f145b3fc-5ae0-4509-a2aa-1ff6934706bd
ms.openlocfilehash: cb42f2e41e366cf8765cb9395d1a5641434bab40
ms.sourcegitcommit: 17ee6605e01ef32506f8fdc686954244ba6911de
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 11/22/2019
ms.locfileid: "74345080"
---
# <a name="walkthrough-handling-events-visual-basic"></a><span data-ttu-id="19322-102">Exemplarische Vorgehensweise: Behandeln von Ereignissen (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="19322-102">Walkthrough: Handling Events (Visual Basic)</span></span>
<span data-ttu-id="19322-103">Dies ist der zweite von zwei Themen, die veranschaulichen, wie mit Ereignissen gearbeitet wird.</span><span class="sxs-lookup"><span data-stu-id="19322-103">This is the second of two topics that demonstrate how to work with events.</span></span> <span data-ttu-id="19322-104">Das erste Thema, Exemplarische Vorgehensweise [: Deklarieren und Auswerfen von Ereignissen](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), zeigt, wie Sie Ereignisse deklarieren und Auswerfen.</span><span class="sxs-lookup"><span data-stu-id="19322-104">The first topic, [Walkthrough: Declaring and Raising Events](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), shows how to declare and raise events.</span></span> <span data-ttu-id="19322-105">In diesem Abschnitt wird das Formular und die Klasse aus dieser exemplarischen Vorgehensweise verwendet, um zu veranschaulichen, wie Ereignisse behandelt werden.</span><span class="sxs-lookup"><span data-stu-id="19322-105">This section uses the form and class from that walkthrough to show how to handle events when they take place.</span></span>  
  
 <span data-ttu-id="19322-106">Im Beispiel für die `Widget`-Klasse werden herkömmliche Ereignis behandlungsanweisungen verwendet.</span><span class="sxs-lookup"><span data-stu-id="19322-106">The `Widget` class example uses traditional event-handling statements.</span></span> <span data-ttu-id="19322-107">Visual Basic bietet weitere Techniken zum Arbeiten mit Ereignissen.</span><span class="sxs-lookup"><span data-stu-id="19322-107">Visual Basic provides other techniques for working with events.</span></span> <span data-ttu-id="19322-108">Als Übung können Sie dieses Beispiel so ändern, dass die Anweisungen `AddHandler` und `Handles` verwendet werden.</span><span class="sxs-lookup"><span data-stu-id="19322-108">As an exercise, you can modify this example to use the `AddHandler` and `Handles` statements.</span></span>  
  
### <a name="to-handle-the-percentdone-event-of-the-widget-class"></a><span data-ttu-id="19322-109">So behandeln Sie das Ereignis "-Ereignis" der Widget-Klasse</span><span class="sxs-lookup"><span data-stu-id="19322-109">To handle the PercentDone event of the Widget class</span></span>  
  
1. <span data-ttu-id="19322-110">Fügen Sie den folgenden Code in `Form1`ein:</span><span class="sxs-lookup"><span data-stu-id="19322-110">Place the following code in `Form1`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#4)]  
  
     <span data-ttu-id="19322-111">Das `WithEvents`-Schlüsselwort gibt an, dass die Variable `mWidget` verwendet wird, um die Ereignisse eines Objekts zu verarbeiten.</span><span class="sxs-lookup"><span data-stu-id="19322-111">The `WithEvents` keyword specifies that the variable `mWidget` is used to handle an object's events.</span></span> <span data-ttu-id="19322-112">Sie geben die Art des Objekts an, indem Sie den Namen der Klasse angeben, aus der das Objekt erstellt wird.</span><span class="sxs-lookup"><span data-stu-id="19322-112">You specify the kind of object by supplying the name of the class from which the object will be created.</span></span>  
  
     <span data-ttu-id="19322-113">Die Variable `mWidget` wird in `Form1` deklariert, da `WithEvents` Variablen auf Klassenebene basieren müssen.</span><span class="sxs-lookup"><span data-stu-id="19322-113">The variable `mWidget` is declared in `Form1` because `WithEvents` variables must be class-level.</span></span> <span data-ttu-id="19322-114">Dies gilt unabhängig vom Typ der Klasse, in der Sie Sie platzieren.</span><span class="sxs-lookup"><span data-stu-id="19322-114">This is true regardless of the type of class you place them in.</span></span>  
  
     <span data-ttu-id="19322-115">Die Variable `mblnCancel` wird verwendet, um die `LongTask` Methode abzubrechen.</span><span class="sxs-lookup"><span data-stu-id="19322-115">The variable `mblnCancel` is used to cancel the `LongTask` method.</span></span>  
  
## <a name="writing-code-to-handle-an-event"></a><span data-ttu-id="19322-116">Schreiben von Code zum Behandeln eines Ereignisses</span><span class="sxs-lookup"><span data-stu-id="19322-116">Writing Code to Handle an Event</span></span>  
 <span data-ttu-id="19322-117">Sobald Sie eine Variable mit `WithEvents`deklarieren, wird der Variablenname in der linken Dropdown Liste des **Code-Editors**der Klasse angezeigt.</span><span class="sxs-lookup"><span data-stu-id="19322-117">As soon as you declare a variable using `WithEvents`, the variable name appears in the left drop-down list of the class's **Code Editor**.</span></span> <span data-ttu-id="19322-118">Wenn Sie `mWidget`auswählen, werden die Ereignisse der `Widget`-Klasse in der rechten Dropdown Liste angezeigt.</span><span class="sxs-lookup"><span data-stu-id="19322-118">When you select `mWidget`, the `Widget` class's events appear in the right drop-down list.</span></span> <span data-ttu-id="19322-119">Wenn Sie ein Ereignis auswählen, wird die entsprechende Ereignis Prozedur mit dem Präfix `mWidget` und einem Unterstrich angezeigt.</span><span class="sxs-lookup"><span data-stu-id="19322-119">Selecting an event displays the corresponding event procedure, with the prefix `mWidget` and an underscore.</span></span> <span data-ttu-id="19322-120">Alle Ereignis Prozeduren, die einer `WithEvents` Variablen zugeordnet sind, erhalten den Variablennamen als Präfix.</span><span class="sxs-lookup"><span data-stu-id="19322-120">All the event procedures associated with a `WithEvents` variable are given the variable name as a prefix.</span></span>  
  
#### <a name="to-handle-an-event"></a><span data-ttu-id="19322-121">So behandeln Sie ein Ereignis</span><span class="sxs-lookup"><span data-stu-id="19322-121">To handle an event</span></span>  
  
1. <span data-ttu-id="19322-122">Wählen Sie im **Code-Editor**in der Dropdown Liste links die Option `mWidget` aus.</span><span class="sxs-lookup"><span data-stu-id="19322-122">Select `mWidget` from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="19322-123">Wählen Sie das `PercentDone`-Ereignis in der rechten Dropdown Liste aus.</span><span class="sxs-lookup"><span data-stu-id="19322-123">Select the `PercentDone` event from the right drop-down list.</span></span> <span data-ttu-id="19322-124">Der **Code-Editor** öffnet die `mWidget_PercentDone` Ereignis Prozedur.</span><span class="sxs-lookup"><span data-stu-id="19322-124">The **Code Editor** opens the `mWidget_PercentDone` event procedure.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="19322-125">Der **Code-Editor** ist für das Einfügen neuer Ereignishandler nützlich, jedoch nicht erforderlich.</span><span class="sxs-lookup"><span data-stu-id="19322-125">The **Code Editor** is useful, but not required, for inserting new event handlers.</span></span> <span data-ttu-id="19322-126">In dieser exemplarischen Vorgehensweise ist es direkter, einfach die Ereignishandler direkt in Ihren Code zu kopieren.</span><span class="sxs-lookup"><span data-stu-id="19322-126">In this walkthrough, it is more direct to just copy the event handlers directly into your code.</span></span>  
  
3. <span data-ttu-id="19322-127">Fügen Sie dem `mWidget_PercentDone` -Ereignishandler folgenden Code hinzu:</span><span class="sxs-lookup"><span data-stu-id="19322-127">Add the following code to the `mWidget_PercentDone` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#5)]  
  
     <span data-ttu-id="19322-128">Wenn das `PercentDone`-Ereignis ausgelöst wird, zeigt die Ereignis Prozedur den in einem `Label`-Steuerelement abgeschlossenen Prozentsatz an.</span><span class="sxs-lookup"><span data-stu-id="19322-128">Whenever the `PercentDone` event is raised, the event procedure displays the percent complete in a `Label` control.</span></span> <span data-ttu-id="19322-129">Mit der `DoEvents`-Methode kann die Bezeichnung neu gezeichnet werden, und der Benutzer erhält die Möglichkeit, auf die Schaltfläche " **Abbrechen** " zu klicken.</span><span class="sxs-lookup"><span data-stu-id="19322-129">The `DoEvents` method allows the label to repaint, and also gives the user the opportunity to click the **Cancel** button.</span></span>  
  
4. <span data-ttu-id="19322-130">Fügen Sie den folgenden Code für den `Button2_Click`-Ereignishandler hinzu:</span><span class="sxs-lookup"><span data-stu-id="19322-130">Add the following code for the `Button2_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#6)]  
  
 <span data-ttu-id="19322-131">Wenn der Benutzer auf die Schaltfläche **Abbrechen** klickt, während `LongTask` ausgeführt wird, wird das `Button2_Click`-Ereignis ausgeführt, sobald die `DoEvents`-Anweisung das Eintreten der Ereignisverarbeitung zulässt.</span><span class="sxs-lookup"><span data-stu-id="19322-131">If the user clicks the **Cancel** button while `LongTask` is running, the `Button2_Click` event is executed as soon as the `DoEvents` statement allows event processing to occur.</span></span> <span data-ttu-id="19322-132">Die Variable auf Klassenebene `mblnCancel` ist auf `True`festgelegt, und das `mWidget_PercentDone` Ereignis testet Sie, und legt das `ByRef Cancel`-Argument auf `True`fest.</span><span class="sxs-lookup"><span data-stu-id="19322-132">The class-level variable `mblnCancel` is set to `True`, and the `mWidget_PercentDone` event then tests it and sets the `ByRef Cancel` argument to `True`.</span></span>  
  
## <a name="connecting-a-withevents-variable-to-an-object"></a><span data-ttu-id="19322-133">Verbinden einer widervents-Variablen mit einem Objekt</span><span class="sxs-lookup"><span data-stu-id="19322-133">Connecting a WithEvents Variable to an Object</span></span>  
 <span data-ttu-id="19322-134">`Form1` ist nun so eingerichtet, dass die Ereignisse eines `Widget` Objekts behandelt werden.</span><span class="sxs-lookup"><span data-stu-id="19322-134">`Form1` is now set up to handle a `Widget` object's events.</span></span> <span data-ttu-id="19322-135">Sie müssen nur noch eine `Widget` finden.</span><span class="sxs-lookup"><span data-stu-id="19322-135">All that remains is to find a `Widget` somewhere.</span></span>  
  
 <span data-ttu-id="19322-136">Wenn Sie zur Entwurfszeit eine Variable `WithEvents` deklarieren, ist ihr kein Objekt zugeordnet.</span><span class="sxs-lookup"><span data-stu-id="19322-136">When you declare a variable `WithEvents` at design time, no object is associated with it.</span></span> <span data-ttu-id="19322-137">Eine `WithEvents` Variable ist genau wie jede andere Objekt Variable.</span><span class="sxs-lookup"><span data-stu-id="19322-137">A `WithEvents` variable is just like any other object variable.</span></span> <span data-ttu-id="19322-138">Sie müssen ein-Objekt erstellen und einen Verweis darauf mit der `WithEvents`-Variablen zuweisen.</span><span class="sxs-lookup"><span data-stu-id="19322-138">You have to create an object and assign a reference to it with the `WithEvents` variable.</span></span>  
  
#### <a name="to-create-an-object-and-assign-a-reference-to-it"></a><span data-ttu-id="19322-139">So erstellen Sie ein Objekt und weisen ihm einen Verweis zu</span><span class="sxs-lookup"><span data-stu-id="19322-139">To create an object and assign a reference to it</span></span>  
  
1. <span data-ttu-id="19322-140">Wählen Sie **(Form1-Ereignisse)** in der linken Dropdown Liste im **Code-Editor**aus.</span><span class="sxs-lookup"><span data-stu-id="19322-140">Select **(Form1 Events)** from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="19322-141">Wählen Sie das `Load`-Ereignis in der rechten Dropdown Liste aus.</span><span class="sxs-lookup"><span data-stu-id="19322-141">Select the `Load` event from the right drop-down list.</span></span> <span data-ttu-id="19322-142">Der **Code-Editor** öffnet die `Form1_Load` Ereignis Prozedur.</span><span class="sxs-lookup"><span data-stu-id="19322-142">The **Code Editor** opens the `Form1_Load` event procedure.</span></span>  
  
3. <span data-ttu-id="19322-143">Fügen Sie den folgenden Code für die `Form1_Load` Ereignis Prozedur hinzu, um die `Widget`zu erstellen:</span><span class="sxs-lookup"><span data-stu-id="19322-143">Add the following code for the `Form1_Load` event procedure to create the `Widget`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#7)]  
  
 <span data-ttu-id="19322-144">Wenn dieser Code ausgeführt wird, erstellt Visual Basic ein `Widget` Objekt und verbindet seine Ereignisse mit den Ereignis Prozeduren, die `mWidget`zugeordnet sind.</span><span class="sxs-lookup"><span data-stu-id="19322-144">When this code executes, Visual Basic creates a `Widget` object and connects its events to the event procedures associated with `mWidget`.</span></span> <span data-ttu-id="19322-145">Ab diesem Zeitpunkt wird die `mWidget_PercentDone` Ereignis Prozedur ausgeführt, wenn die `Widget` das `PercentDone`-Ereignis auslöst.</span><span class="sxs-lookup"><span data-stu-id="19322-145">From that point on, whenever the `Widget` raises its `PercentDone` event, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
#### <a name="to-call-the-longtask-method"></a><span data-ttu-id="19322-146">So greifen Sie auf die LongTask-Methode zu</span><span class="sxs-lookup"><span data-stu-id="19322-146">To call the LongTask method</span></span>  
  
- <span data-ttu-id="19322-147">Fügen Sie dem `Button1_Click` -Ereignishandler folgenden Code hinzu:</span><span class="sxs-lookup"><span data-stu-id="19322-147">Add the following code to the `Button1_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#8)]  
  
 <span data-ttu-id="19322-148">Bevor die `LongTask`-Methode aufgerufen wird, muss die Bezeichnung, die den Prozentsatz anzeigt, initialisiert werden, und das `Boolean` Flag auf Klassenebene für das Abbrechen der Methode muss auf `False`festgelegt werden.</span><span class="sxs-lookup"><span data-stu-id="19322-148">Before the `LongTask` method is called, the label that displays the percent complete must be initialized, and the class-level `Boolean` flag for canceling the method must be set to `False`.</span></span>  
  
 <span data-ttu-id="19322-149">`LongTask` wird mit einer Task Dauer von 12,2 Sekunden aufgerufen.</span><span class="sxs-lookup"><span data-stu-id="19322-149">`LongTask` is called with a task duration of 12.2 seconds.</span></span> <span data-ttu-id="19322-150">Das `PercentDone`-Ereignis wird einmal alle ein Drittel einer Sekunde ausgelöst.</span><span class="sxs-lookup"><span data-stu-id="19322-150">The `PercentDone` event is raised once every one-third of a second.</span></span> <span data-ttu-id="19322-151">Jedes Mal, wenn das Ereignis ausgelöst wird, wird die `mWidget_PercentDone` Ereignis Prozedur ausgeführt.</span><span class="sxs-lookup"><span data-stu-id="19322-151">Each time the event is raised, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
 <span data-ttu-id="19322-152">Wenn `LongTask` abgeschlossen ist, wird `mblnCancel` getestet, um zu prüfen, ob `LongTask` normal beendet wurde, oder ob er beendet wurde, weil `mblnCancel` auf `True`festgelegt wurde.</span><span class="sxs-lookup"><span data-stu-id="19322-152">When `LongTask` is done, `mblnCancel` is tested to see if `LongTask` ended normally, or if it stopped because `mblnCancel` was set to `True`.</span></span> <span data-ttu-id="19322-153">Der Prozentsatz der Fertigstellung wird nur im ersten Fall aktualisiert.</span><span class="sxs-lookup"><span data-stu-id="19322-153">The percent complete is updated only in the former case.</span></span>  
  
#### <a name="to-run-the-program"></a><span data-ttu-id="19322-154">So führen Sie das Programm aus</span><span class="sxs-lookup"><span data-stu-id="19322-154">To run the program</span></span>  
  
1. <span data-ttu-id="19322-155">Drücken Sie F5, um das Projekt in den Testlauf zu versetzen.</span><span class="sxs-lookup"><span data-stu-id="19322-155">Press F5 to put the project in run mode.</span></span>  
  
2. <span data-ttu-id="19322-156">Klicken Sie auf die Schaltfläche **Task starten** .</span><span class="sxs-lookup"><span data-stu-id="19322-156">Click the **Start Task** button.</span></span> <span data-ttu-id="19322-157">Jedes Mal, wenn das `PercentDone` Ereignis ausgelöst wird, wird die Bezeichnung mit dem Prozentsatz der abgeschlossenen Aufgabe aktualisiert.</span><span class="sxs-lookup"><span data-stu-id="19322-157">Each time the `PercentDone` event is raised, the label is updated with the percentage of the task that is complete.</span></span>  
  
3. <span data-ttu-id="19322-158">Klicken Sie auf die Schaltfläche **Abbrechen** , um den Task zu beenden.</span><span class="sxs-lookup"><span data-stu-id="19322-158">Click the **Cancel** button to stop the task.</span></span> <span data-ttu-id="19322-159">Beachten Sie, dass sich die Darstellung der Schaltfläche **Abbrechen** nicht sofort ändert, wenn Sie darauf klicken.</span><span class="sxs-lookup"><span data-stu-id="19322-159">Notice that the appearance of the **Cancel** button does not change immediately when you click it.</span></span> <span data-ttu-id="19322-160">Das `Click`-Ereignis kann erst auftreten, wenn die `My.Application.DoEvents`-Anweisung die Ereignisverarbeitung zulässt.</span><span class="sxs-lookup"><span data-stu-id="19322-160">The `Click` event cannot happen until the `My.Application.DoEvents` statement allows event processing.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="19322-161">Die `My.Application.DoEvents`-Methode verarbeitet Ereignisse nicht exakt auf die gleiche Weise wie das Formular.</span><span class="sxs-lookup"><span data-stu-id="19322-161">The `My.Application.DoEvents` method does not process events in exactly the same way as the form does.</span></span> <span data-ttu-id="19322-162">Beispielsweise müssen Sie in dieser exemplarischen Vorgehensweise zweimal auf die Schaltfläche **Abbrechen** klicken.</span><span class="sxs-lookup"><span data-stu-id="19322-162">For example, in this walkthrough, you must click the **Cancel** button twice.</span></span> <span data-ttu-id="19322-163">Um dem Formular das direkte behandeln der Ereignisse zu ermöglichen, können Sie Multithreading verwenden.</span><span class="sxs-lookup"><span data-stu-id="19322-163">To allow the form to handle the events directly, you can use multithreading.</span></span> <span data-ttu-id="19322-164">Weitere Informationen finden Sie unter [verwaltetes Threading](../../../../standard/threading/index.md).</span><span class="sxs-lookup"><span data-stu-id="19322-164">For more information, see [Managed Threading](../../../../standard/threading/index.md).</span></span>
  
 <span data-ttu-id="19322-165">Sie finden es möglicherweise Lehrweise, das Programm mit F11 auszuführen und den Codezeilen Weise schrittweise durchlaufen.</span><span class="sxs-lookup"><span data-stu-id="19322-165">You may find it instructive to run the program with F11 and step through the code a line at a time.</span></span> <span data-ttu-id="19322-166">Sie können deutlich erkennen, wie die Ausführung `LongTask`eintritt, und die `Form1` dann bei jedem ausgelöst des `PercentDone` Ereignisses kurz wieder eingibt.</span><span class="sxs-lookup"><span data-stu-id="19322-166">You can clearly see how execution enters `LongTask`, and then briefly re-enters `Form1` each time the `PercentDone` event is raised.</span></span>  
  
 <span data-ttu-id="19322-167">Was geschieht, wenn die `LongTask`-Methode erneut aufgerufen wurde, während die Ausführung wieder im Code von `Form1`war?</span><span class="sxs-lookup"><span data-stu-id="19322-167">What would happen if, while execution was back in the code of `Form1`, the `LongTask` method were called again?</span></span> <span data-ttu-id="19322-168">Im schlimmsten Fall kann ein Stapelüberlauf auftreten, wenn `LongTask` jedes Mal aufgerufen werden, wenn das Ereignis ausgelöst wurde.</span><span class="sxs-lookup"><span data-stu-id="19322-168">At worst, a stack overflow might occur if `LongTask` were called every time the event was raised.</span></span>  
  
 <span data-ttu-id="19322-169">Sie können bewirken, dass die Variable `mWidget` Ereignisse für ein anderes `Widget` Objekt behandelt, indem Sie einen Verweis auf den neuen `Widget` `mWidget`zuweisen.</span><span class="sxs-lookup"><span data-stu-id="19322-169">You can cause the variable `mWidget` to handle events for a different `Widget` object by assigning a reference to the new `Widget` to `mWidget`.</span></span> <span data-ttu-id="19322-170">Tatsächlich können Sie den Code in erstellen, `Button1_Click` diesen Schritt jedes Mal ausführen, wenn Sie auf die Schaltfläche klicken.</span><span class="sxs-lookup"><span data-stu-id="19322-170">In fact, you can make the code in `Button1_Click` do this every time you click the button.</span></span>  
  
#### <a name="to-handle-events-for-a-different-widget"></a><span data-ttu-id="19322-171">So behandeln Sie Ereignisse für ein anderes widget</span><span class="sxs-lookup"><span data-stu-id="19322-171">To handle events for a different widget</span></span>  
  
- <span data-ttu-id="19322-172">Fügen Sie die folgende Codezeile zur `Button1_Click` Prozedur hinzu, die unmittelbar vor der Zeile steht, die `mWidget.LongTask(12.2, 0.33)`liest:</span><span class="sxs-lookup"><span data-stu-id="19322-172">Add the following line of code to the `Button1_Click` procedure, immediately preceding the line that reads `mWidget.LongTask(12.2, 0.33)`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#9)]  
  
 <span data-ttu-id="19322-173">Der obige Code erstellt einen neuen `Widget` jedes Mal, wenn auf die Schaltfläche geklickt wird.</span><span class="sxs-lookup"><span data-stu-id="19322-173">The code above creates a new `Widget` each time the button is clicked.</span></span> <span data-ttu-id="19322-174">Sobald die `LongTask`-Methode abgeschlossen ist, wird der Verweis auf die `Widget` freigegeben, und die `Widget` wird zerstört.</span><span class="sxs-lookup"><span data-stu-id="19322-174">As soon as the `LongTask` method completes, the reference to the `Widget` is released, and the `Widget` is destroyed.</span></span>  
  
 <span data-ttu-id="19322-175">Eine `WithEvents` Variable kann jeweils nur einen Objekt Verweis enthalten. Wenn Sie also `mWidget`ein anderes `Widget` Objekt zuweisen, werden die Ereignisse des vorherigen `Widget` Objekts nicht mehr behandelt.</span><span class="sxs-lookup"><span data-stu-id="19322-175">A `WithEvents` variable can contain only one object reference at a time, so if you assign a different `Widget` object to `mWidget`, the previous `Widget` object's events will no longer be handled.</span></span> <span data-ttu-id="19322-176">Wenn `mWidget` die einzige Objekt Variable ist, die einen Verweis auf die alte `Widget`enthält, wird das Objekt zerstört.</span><span class="sxs-lookup"><span data-stu-id="19322-176">If `mWidget` is the only object variable containing a reference to the old `Widget`, the object is destroyed.</span></span> <span data-ttu-id="19322-177">Wenn Sie Ereignisse aus mehreren `Widget` Objekten verarbeiten möchten, können Sie die `AddHandler`-Anweisung verwenden, um Ereignisse aus den einzelnen Objekten separat zu verarbeiten.</span><span class="sxs-lookup"><span data-stu-id="19322-177">If you want to handle events from several `Widget` objects, use the `AddHandler` statement to process events from each object separately.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="19322-178">Sie können beliebig viele `WithEvents` Variablen deklarieren, aber Arrays von `WithEvents` Variablen werden nicht unterstützt.</span><span class="sxs-lookup"><span data-stu-id="19322-178">You can declare as many `WithEvents` variables as you need, but arrays of `WithEvents` variables are not supported.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="19322-179">Siehe auch</span><span class="sxs-lookup"><span data-stu-id="19322-179">See also</span></span>

- [<span data-ttu-id="19322-180">Exemplarische Vorgehensweise: Deklarieren und Auslösen von Ereignissen</span><span class="sxs-lookup"><span data-stu-id="19322-180">Walkthrough: Declaring and Raising Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md)
- [<span data-ttu-id="19322-181">Ereignisse</span><span class="sxs-lookup"><span data-stu-id="19322-181">Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/index.md)
