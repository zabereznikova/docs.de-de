---
title: Behandeln von Ereignissen (Visual Basic)
ms.date: 07/20/2015
helpviewer_keywords:
- event handling [Visual Basic], walkthroughs
- walkthroughs [Visual Basic], event handling
- variables [Visual Basic], WithEvents
- events [Visual Basic], walkthroughs
- WithEvents keyword [Visual Basic], walkthroughs
- event handlers [Visual Basic], walkthroughs
ms.assetid: f145b3fc-5ae0-4509-a2aa-1ff6934706bd
ms.openlocfilehash: 12267e0a70419298bc581421c4f3a220088d205f
ms.sourcegitcommit: 68653db98c5ea7744fd438710248935f70020dfb
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 08/22/2019
ms.locfileid: "69956313"
---
# <a name="walkthrough-handling-events-visual-basic"></a><span data-ttu-id="f0ac6-102">Exemplarische Vorgehensweise: Behandeln von Ereignissen (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="f0ac6-102">Walkthrough: Handling Events (Visual Basic)</span></span>
<span data-ttu-id="f0ac6-103">Dies ist der zweite von zwei Themen, die veranschaulichen, wie mit Ereignissen gearbeitet wird.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-103">This is the second of two topics that demonstrate how to work with events.</span></span> <span data-ttu-id="f0ac6-104">Das erste Thema, [Exemplarische Vorgehensweise: Das Deklarieren und](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md)Auswerfen von Ereignissen zeigt, wie Sie Ereignisse deklarieren und Auswerfen.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-104">The first topic, [Walkthrough: Declaring and Raising Events](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), shows how to declare and raise events.</span></span> <span data-ttu-id="f0ac6-105">In diesem Abschnitt wird das Formular und die Klasse aus dieser exemplarischen Vorgehensweise verwendet, um zu veranschaulichen, wie Ereignisse behandelt werden.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-105">This section uses the form and class from that walkthrough to show how to handle events when they take place.</span></span>  
  
 <span data-ttu-id="f0ac6-106">Im `Widget` -Klassen Beispiel werden herkömmliche-Ereignis behandlungsanweisungen verwendet.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-106">The `Widget` class example uses traditional event-handling statements.</span></span> <span data-ttu-id="f0ac6-107">Visual Basic bietet weitere Techniken zum Arbeiten mit Ereignissen.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-107">Visual Basic provides other techniques for working with events.</span></span> <span data-ttu-id="f0ac6-108">Als Übung können Sie dieses Beispiel so ändern, dass die `AddHandler` -und- `Handles` Anweisungen verwendet werden.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-108">As an exercise, you can modify this example to use the `AddHandler` and `Handles` statements.</span></span>  
  
### <a name="to-handle-the-percentdone-event-of-the-widget-class"></a><span data-ttu-id="f0ac6-109">So behandeln Sie das Ereignis "-Ereignis" der Widget-Klasse</span><span class="sxs-lookup"><span data-stu-id="f0ac6-109">To handle the PercentDone event of the Widget class</span></span>  
  
1. <span data-ttu-id="f0ac6-110">Platzieren Sie den folgenden Code `Form1`in:</span><span class="sxs-lookup"><span data-stu-id="f0ac6-110">Place the following code in `Form1`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#4)]  
  
     <span data-ttu-id="f0ac6-111">Das `WithEvents` -Schlüsselwort gibt an `mWidget` , dass die-Variable verwendet wird, um die Ereignisse eines Objekts zu verarbeiten.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-111">The `WithEvents` keyword specifies that the variable `mWidget` is used to handle an object's events.</span></span> <span data-ttu-id="f0ac6-112">Sie geben die Art des Objekts an, indem Sie den Namen der Klasse angeben, aus der das Objekt erstellt wird.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-112">You specify the kind of object by supplying the name of the class from which the object will be created.</span></span>  
  
     <span data-ttu-id="f0ac6-113">Die- `mWidget` Variable wird in `Form1` deklariert `WithEvents` , da Variablen auf Klassenebene basieren müssen.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-113">The variable `mWidget` is declared in `Form1` because `WithEvents` variables must be class-level.</span></span> <span data-ttu-id="f0ac6-114">Dies gilt unabhängig vom Typ der Klasse, in der Sie Sie platzieren.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-114">This is true regardless of the type of class you place them in.</span></span>  
  
     <span data-ttu-id="f0ac6-115">Die- `mblnCancel` Variable wird verwendet, um `LongTask` die-Methode abzubrechen.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-115">The variable `mblnCancel` is used to cancel the `LongTask` method.</span></span>  
  
## <a name="writing-code-to-handle-an-event"></a><span data-ttu-id="f0ac6-116">Schreiben von Code zum Behandeln eines Ereignisses</span><span class="sxs-lookup"><span data-stu-id="f0ac6-116">Writing Code to Handle an Event</span></span>  
 <span data-ttu-id="f0ac6-117">Sobald Sie mithilfe `WithEvents`von eine Variable deklarieren, wird der Variablenname in der linken Dropdown Liste des **Code-Editors**der Klasse angezeigt.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-117">As soon as you declare a variable using `WithEvents`, the variable name appears in the left drop-down list of the class's **Code Editor**.</span></span> <span data-ttu-id="f0ac6-118">Wenn Sie auswählen `mWidget`, werden `Widget` die Ereignisse der-Klasse in der rechten Dropdown Liste angezeigt.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-118">When you select `mWidget`, the `Widget` class's events appear in the right drop-down list.</span></span> <span data-ttu-id="f0ac6-119">Wenn Sie ein Ereignis auswählen, wird die entsprechende Ereignis Prozedur mit `mWidget` dem Präfix und einem Unterstrich angezeigt.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-119">Selecting an event displays the corresponding event procedure, with the prefix `mWidget` and an underscore.</span></span> <span data-ttu-id="f0ac6-120">Allen Ereignis Prozeduren, die `WithEvents` einer Variablen zugeordnet sind, wird der Variablenname als Präfix zugewiesen.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-120">All the event procedures associated with a `WithEvents` variable are given the variable name as a prefix.</span></span>  
  
#### <a name="to-handle-an-event"></a><span data-ttu-id="f0ac6-121">So behandeln Sie ein Ereignis</span><span class="sxs-lookup"><span data-stu-id="f0ac6-121">To handle an event</span></span>  
  
1. <span data-ttu-id="f0ac6-122">Wählen `mWidget` Sie aus der Dropdown Liste links im **Code-Editor**aus.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-122">Select `mWidget` from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="f0ac6-123">Wählen Sie `PercentDone` das Ereignis in der rechten Dropdown Liste aus.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-123">Select the `PercentDone` event from the right drop-down list.</span></span> <span data-ttu-id="f0ac6-124">Der **Code-Editor** öffnet `mWidget_PercentDone` die Ereignis Prozedur.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-124">The **Code Editor** opens the `mWidget_PercentDone` event procedure.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="f0ac6-125">Der **Code-Editor** ist für das Einfügen neuer Ereignishandler nützlich, jedoch nicht erforderlich.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-125">The **Code Editor** is useful, but not required, for inserting new event handlers.</span></span> <span data-ttu-id="f0ac6-126">In dieser exemplarischen Vorgehensweise ist es direkter, einfach die Ereignishandler direkt in Ihren Code zu kopieren.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-126">In this walkthrough, it is more direct to just copy the event handlers directly into your code.</span></span>  
  
3. <span data-ttu-id="f0ac6-127">Fügen Sie dem `mWidget_PercentDone` -Ereignishandler folgenden Code hinzu:</span><span class="sxs-lookup"><span data-stu-id="f0ac6-127">Add the following code to the `mWidget_PercentDone` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#5)]  
  
     <span data-ttu-id="f0ac6-128">Wenn das `PercentDone` -Ereignis ausgelöst wird, zeigt die Ereignis Prozedur den in einem `Label` -Steuerelement abgeschlossenen Prozentsatz an.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-128">Whenever the `PercentDone` event is raised, the event procedure displays the percent complete in a `Label` control.</span></span> <span data-ttu-id="f0ac6-129">Mit `DoEvents` der-Methode kann die Bezeichnung neu gezeichnet werden, und der Benutzer erhält die Möglichkeit, auf die Schaltfläche " **Abbrechen** " zu klicken.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-129">The `DoEvents` method allows the label to repaint, and also gives the user the opportunity to click the **Cancel** button.</span></span>  
  
4. <span data-ttu-id="f0ac6-130">Fügen Sie den folgenden Code für `Button2_Click` den-Ereignishandler hinzu:</span><span class="sxs-lookup"><span data-stu-id="f0ac6-130">Add the following code for the `Button2_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#6)]  
  
 <span data-ttu-id="f0ac6-131">Wenn der Benutzer während `LongTask` der Ausführung von auf die Schaltfläche **Abbrechen** klickt, wird das `Button2_Click` -Ereignis ausgeführt `DoEvents` , sobald die-Anweisung das Eintreten der Ereignisverarbeitung zulässt.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-131">If the user clicks the **Cancel** button while `LongTask` is running, the `Button2_Click` event is executed as soon as the `DoEvents` statement allows event processing to occur.</span></span> <span data-ttu-id="f0ac6-132">Die `mblnCancel` Variable auf Klassenebene ist auf `True`festgelegt, und `mWidget_PercentDone` das-Ereignis testet Sie, und `ByRef Cancel` legt das `True`-Argument auf fest.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-132">The class-level variable `mblnCancel` is set to `True`, and the `mWidget_PercentDone` event then tests it and sets the `ByRef Cancel` argument to `True`.</span></span>  
  
## <a name="connecting-a-withevents-variable-to-an-object"></a><span data-ttu-id="f0ac6-133">Verbinden einer widervents-Variablen mit einem Objekt</span><span class="sxs-lookup"><span data-stu-id="f0ac6-133">Connecting a WithEvents Variable to an Object</span></span>  
 <span data-ttu-id="f0ac6-134">`Form1`ist nun so eingerichtet, dass die `Widget` -Ereignisse eines-Objekts behandelt werden.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-134">`Form1` is now set up to handle a `Widget` object's events.</span></span> <span data-ttu-id="f0ac6-135">Alles, was bleibt, ist das `Widget` Auffinden eines irgendwo.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-135">All that remains is to find a `Widget` somewhere.</span></span>  
  
 <span data-ttu-id="f0ac6-136">Wenn Sie zur Entwurfszeit `WithEvents` eine Variable deklarieren, ist ihr kein Objekt zugeordnet.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-136">When you declare a variable `WithEvents` at design time, no object is associated with it.</span></span> <span data-ttu-id="f0ac6-137">Eine `WithEvents` Variable ist genauso wie jede andere Objekt Variable.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-137">A `WithEvents` variable is just like any other object variable.</span></span> <span data-ttu-id="f0ac6-138">Sie müssen ein-Objekt erstellen und ihm einen Verweis auf die `WithEvents` -Variable zuweisen.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-138">You have to create an object and assign a reference to it with the `WithEvents` variable.</span></span>  
  
#### <a name="to-create-an-object-and-assign-a-reference-to-it"></a><span data-ttu-id="f0ac6-139">So erstellen Sie ein Objekt und weisen ihm einen Verweis zu</span><span class="sxs-lookup"><span data-stu-id="f0ac6-139">To create an object and assign a reference to it</span></span>  
  
1. <span data-ttu-id="f0ac6-140">Wählen Sie **(Form1-Ereignisse)** in der linken Dropdown Liste im **Code-Editor**aus.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-140">Select **(Form1 Events)** from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="f0ac6-141">Wählen Sie `Load` das Ereignis in der rechten Dropdown Liste aus.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-141">Select the `Load` event from the right drop-down list.</span></span> <span data-ttu-id="f0ac6-142">Der **Code-Editor** öffnet `Form1_Load` die Ereignis Prozedur.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-142">The **Code Editor** opens the `Form1_Load` event procedure.</span></span>  
  
3. <span data-ttu-id="f0ac6-143">Fügen Sie den folgenden Code für `Form1_Load` die Ereignis Prozedur hinzu, `Widget`um die zu erstellen:</span><span class="sxs-lookup"><span data-stu-id="f0ac6-143">Add the following code for the `Form1_Load` event procedure to create the `Widget`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#7)]  
  
 <span data-ttu-id="f0ac6-144">Wenn dieser Code ausgeführt wird, erstellt Visual Basic `Widget` ein-Objekt und verbindet seine Ereignisse mit `mWidget`den Ereignis Prozeduren, die zugeordnet sind.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-144">When this code executes, Visual Basic creates a `Widget` object and connects its events to the event procedures associated with `mWidget`.</span></span> <span data-ttu-id="f0ac6-145">Ab diesem Zeitpunkt wird die `Widget` `mWidget_PercentDone` Ereignis Prozedur ausgeführt, `PercentDone` wenn der das-Ereignis auslöst.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-145">From that point on, whenever the `Widget` raises its `PercentDone` event, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
#### <a name="to-call-the-longtask-method"></a><span data-ttu-id="f0ac6-146">So greifen Sie auf die LongTask-Methode zu</span><span class="sxs-lookup"><span data-stu-id="f0ac6-146">To call the LongTask method</span></span>  
  
- <span data-ttu-id="f0ac6-147">Fügen Sie dem `Button1_Click` -Ereignishandler folgenden Code hinzu:</span><span class="sxs-lookup"><span data-stu-id="f0ac6-147">Add the following code to the `Button1_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#8)]  
  
 <span data-ttu-id="f0ac6-148">Bevor die `LongTask` -Methode aufgerufen wird, muss die Bezeichnung, die den Prozentsatz anzeigt, initialisiert werden, und das `Boolean` Flag auf Klassenebene zum Abbrechen der Methode muss auf `False`festgelegt werden.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-148">Before the `LongTask` method is called, the label that displays the percent complete must be initialized, and the class-level `Boolean` flag for canceling the method must be set to `False`.</span></span>  
  
 <span data-ttu-id="f0ac6-149">`LongTask`wird mit einer Task Dauer von 12,2 Sekunden aufgerufen.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-149">`LongTask` is called with a task duration of 12.2 seconds.</span></span> <span data-ttu-id="f0ac6-150">Das `PercentDone` -Ereignis wird alle ein Drittel einer Sekunde ausgelöst.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-150">The `PercentDone` event is raised once every one-third of a second.</span></span> <span data-ttu-id="f0ac6-151">Jedes Mal, wenn das-Ereignis ausgelöst `mWidget_PercentDone` wird, wird die-Ereignis Prozedur ausgeführt.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-151">Each time the event is raised, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
 <span data-ttu-id="f0ac6-152">Wenn `LongTask` abgeschlossen ist, `mblnCancel` wird getestet, ob `LongTask` normal beendet wurde, oder ob er beendet wurde `mblnCancel` , weil auf `True`festgelegt wurde.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-152">When `LongTask` is done, `mblnCancel` is tested to see if `LongTask` ended normally, or if it stopped because `mblnCancel` was set to `True`.</span></span> <span data-ttu-id="f0ac6-153">Der Prozentsatz der Fertigstellung wird nur im ersten Fall aktualisiert.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-153">The percent complete is updated only in the former case.</span></span>  
  
#### <a name="to-run-the-program"></a><span data-ttu-id="f0ac6-154">So führen Sie das Programm aus</span><span class="sxs-lookup"><span data-stu-id="f0ac6-154">To run the program</span></span>  
  
1. <span data-ttu-id="f0ac6-155">Drücken Sie F5, um das Projekt in den Testlauf zu versetzen.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-155">Press F5 to put the project in run mode.</span></span>  
  
2. <span data-ttu-id="f0ac6-156">Klicken Sie auf die Schaltfläche **Task starten** .</span><span class="sxs-lookup"><span data-stu-id="f0ac6-156">Click the **Start Task** button.</span></span> <span data-ttu-id="f0ac6-157">Jedes Mal, `PercentDone` wenn das Ereignis ausgelöst wird, wird die Bezeichnung mit dem Prozentsatz der abgeschlossenen Aufgabe aktualisiert.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-157">Each time the `PercentDone` event is raised, the label is updated with the percentage of the task that is complete.</span></span>  
  
3. <span data-ttu-id="f0ac6-158">Klicken Sie auf die Schaltfläche **Abbrechen** , um den Task zu beenden.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-158">Click the **Cancel** button to stop the task.</span></span> <span data-ttu-id="f0ac6-159">Beachten Sie, dass sich die Darstellung der Schaltfläche **Abbrechen** nicht sofort ändert, wenn Sie darauf klicken.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-159">Notice that the appearance of the **Cancel** button does not change immediately when you click it.</span></span> <span data-ttu-id="f0ac6-160">Das `Click` Ereignis kann erst auftreten, `My.Application.DoEvents` wenn die Anweisung die Ereignisverarbeitung zulässt.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-160">The `Click` event cannot happen until the `My.Application.DoEvents` statement allows event processing.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="f0ac6-161">Die `My.Application.DoEvents` -Methode verarbeitet Ereignisse nicht exakt auf die gleiche Weise wie das Formular.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-161">The `My.Application.DoEvents` method does not process events in exactly the same way as the form does.</span></span> <span data-ttu-id="f0ac6-162">Beispielsweise müssen Sie in dieser exemplarischen Vorgehensweise zweimal auf die Schaltfläche **Abbrechen** klicken.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-162">For example, in this walkthrough, you must click the **Cancel** button twice.</span></span> <span data-ttu-id="f0ac6-163">Um dem Formular das direkte behandeln der Ereignisse zu ermöglichen, können Sie Multithreading verwenden.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-163">To allow the form to handle the events directly, you can use multithreading.</span></span> <span data-ttu-id="f0ac6-164">Weitere Informationen finden Sie unter [verwaltetes Threading](../../../../standard/threading/index.md).</span><span class="sxs-lookup"><span data-stu-id="f0ac6-164">For more information, see [Managed Threading](../../../../standard/threading/index.md).</span></span>
  
 <span data-ttu-id="f0ac6-165">Sie finden es möglicherweise Lehrweise, das Programm mit F11 auszuführen und den Codezeilen Weise schrittweise durchlaufen.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-165">You may find it instructive to run the program with F11 and step through the code a line at a time.</span></span> <span data-ttu-id="f0ac6-166">Sie können deutlich erkennen, wie die `LongTask`Ausführung Eintritt und dann bei jedem Aufruf `Form1` des `PercentDone` Ereignisses kurz neu eingibt.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-166">You can clearly see how execution enters `LongTask`, and then briefly re-enters `Form1` each time the `PercentDone` event is raised.</span></span>  
  
 <span data-ttu-id="f0ac6-167">Was geschieht, wenn die `Form1` `LongTask` -Methode während der Ausführung im Code von wieder aufgerufen wurde?</span><span class="sxs-lookup"><span data-stu-id="f0ac6-167">What would happen if, while execution was back in the code of `Form1`, the `LongTask` method were called again?</span></span> <span data-ttu-id="f0ac6-168">Im schlimmsten Fall kann ein Stapelüberlauf auftreten, `LongTask` wenn jedes Mal aufgerufen wurde, wenn das Ereignis ausgelöst wurde.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-168">At worst, a stack overflow might occur if `LongTask` were called every time the event was raised.</span></span>  
  
 <span data-ttu-id="f0ac6-169">Sie können bewirken, dass `mWidget` die Variable Ereignisse für ein anderes `Widget` Objekt behandelt, indem Sie einen Verweis auf `Widget` den `mWidget`neuen auf zuweisen.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-169">You can cause the variable `mWidget` to handle events for a different `Widget` object by assigning a reference to the new `Widget` to `mWidget`.</span></span> <span data-ttu-id="f0ac6-170">Tatsächlich können Sie den Code in `Button1_Click` diesem Schritt jedes Mal erstellen, wenn Sie auf die Schaltfläche klicken.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-170">In fact, you can make the code in `Button1_Click` do this every time you click the button.</span></span>  
  
#### <a name="to-handle-events-for-a-different-widget"></a><span data-ttu-id="f0ac6-171">So behandeln Sie Ereignisse für ein anderes widget</span><span class="sxs-lookup"><span data-stu-id="f0ac6-171">To handle events for a different widget</span></span>  
  
- <span data-ttu-id="f0ac6-172">Fügen Sie die folgende Codezeile `Button1_Click` der Prozedur hinzu, die unmittelbar vor der Zeile steht, die liest: `mWidget.LongTask(12.2, 0.33)`</span><span class="sxs-lookup"><span data-stu-id="f0ac6-172">Add the following line of code to the `Button1_Click` procedure, immediately preceding the line that reads `mWidget.LongTask(12.2, 0.33)`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#9)]  
  
 <span data-ttu-id="f0ac6-173">Mit dem obigen Code wird jedes `Widget` mal ein neues erstellt, wenn auf die Schaltfläche geklickt wird.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-173">The code above creates a new `Widget` each time the button is clicked.</span></span> <span data-ttu-id="f0ac6-174">Sobald die `LongTask` Methode abgeschlossen ist, wird der Verweis auf die `Widget` freigegeben, und der `Widget` wird zerstört.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-174">As soon as the `LongTask` method completes, the reference to the `Widget` is released, and the `Widget` is destroyed.</span></span>  
  
 <span data-ttu-id="f0ac6-175">Eine `WithEvents` Variable kann jeweils nur einen Objekt Verweis enthalten. Wenn Sie also ein anderes `Widget` Objekt `mWidget`zuweisen, werden die Ereignisse des vorherigen `Widget` Objekts nicht mehr behandelt.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-175">A `WithEvents` variable can contain only one object reference at a time, so if you assign a different `Widget` object to `mWidget`, the previous `Widget` object's events will no longer be handled.</span></span> <span data-ttu-id="f0ac6-176">Wenn `mWidget` die einzige Objekt Variable ist, die einen Verweis auf den `Widget`alten enthält, wird das Objekt zerstört.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-176">If `mWidget` is the only object variable containing a reference to the old `Widget`, the object is destroyed.</span></span> <span data-ttu-id="f0ac6-177">Wenn Sie Ereignisse von mehreren `Widget` Objekten verarbeiten möchten, verwenden Sie die-Anweisung, um Ereignisse aus den `AddHandler` einzelnen Objekten separat zu verarbeiten.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-177">If you want to handle events from several `Widget` objects, use the `AddHandler` statement to process events from each object separately.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="f0ac6-178">Sie können beliebig viele `WithEvents` Variablen deklarieren, aber Arrays von `WithEvents` Variablen werden nicht unterstützt.</span><span class="sxs-lookup"><span data-stu-id="f0ac6-178">You can declare as many `WithEvents` variables as you need, but arrays of `WithEvents` variables are not supported.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="f0ac6-179">Siehe auch</span><span class="sxs-lookup"><span data-stu-id="f0ac6-179">See also</span></span>

- [<span data-ttu-id="f0ac6-180">Exemplarische Vorgehensweise: Deklarieren und wecken von Ereignissen</span><span class="sxs-lookup"><span data-stu-id="f0ac6-180">Walkthrough: Declaring and Raising Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md)
- [<span data-ttu-id="f0ac6-181">Ereignisse</span><span class="sxs-lookup"><span data-stu-id="f0ac6-181">Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/index.md)
