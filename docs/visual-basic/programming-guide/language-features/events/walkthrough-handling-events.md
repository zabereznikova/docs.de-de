---
title: Behandlung von Ereignissen (Visual Basic)
ms.date: 07/20/2015
helpviewer_keywords:
- event handling [Visual Basic], walkthroughs
- walkthroughs [Visual Basic], event handling
- variables [Visual Basic], WithEvents
- events [Visual Basic], walkthroughs
- WithEvents keyword [Visual Basic], walkthroughs
- event handlers [Visual Basic], walkthroughs
ms.assetid: f145b3fc-5ae0-4509-a2aa-1ff6934706bd
ms.openlocfilehash: 0ac3514505ca42870d77317feec30a27e4384e6c
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 05/04/2018
---
# <a name="walkthrough-handling-events-visual-basic"></a><span data-ttu-id="424d3-102">Exemplarische Vorgehensweise: Behandeln von Ereignissen (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="424d3-102">Walkthrough: Handling Events (Visual Basic)</span></span>
<span data-ttu-id="424d3-103">Dies ist die zweite von zwei Themen, die zum Arbeiten mit Ereignissen zu veranschaulichen.</span><span class="sxs-lookup"><span data-stu-id="424d3-103">This is the second of two topics that demonstrate how to work with events.</span></span> <span data-ttu-id="424d3-104">Das erste Thema [Exemplarische Vorgehensweise: Deklarieren und Auslösen von Ereignissen](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), deklarieren und Auslösen von Ereignissen veranschaulicht.</span><span class="sxs-lookup"><span data-stu-id="424d3-104">The first topic, [Walkthrough: Declaring and Raising Events](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), shows how to declare and raise events.</span></span> <span data-ttu-id="424d3-105">In diesem Abschnitt werden das Formular und die Klasse aus der dieser exemplarischen Vorgehensweise verwendet, um die zeigen, wie Sie das Behandeln von Ereignissen, die sie vorgenommen werden.</span><span class="sxs-lookup"><span data-stu-id="424d3-105">This section uses the form and class from that walkthrough to show how to handle events when they take place.</span></span>  
  
 <span data-ttu-id="424d3-106">Die `Widget` Klasse-Beispiel verwendet die herkömmliche Ereignisbehandlung Anweisungen.</span><span class="sxs-lookup"><span data-stu-id="424d3-106">The `Widget` class example uses traditional event-handling statements.</span></span> <span data-ttu-id="424d3-107">Visual Basic bietet andere Techniken zum Arbeiten mit Ereignissen.</span><span class="sxs-lookup"><span data-stu-id="424d3-107">Visual Basic provides other techniques for working with events.</span></span> <span data-ttu-id="424d3-108">Als Übung, können Sie ändern, in diesem Beispiel verwendet die `AddHandler` und `Handles` Anweisungen.</span><span class="sxs-lookup"><span data-stu-id="424d3-108">As an exercise, you can modify this example to use the `AddHandler` and `Handles` statements.</span></span>  
  
### <a name="to-handle-the-percentdone-event-of-the-widget-class"></a><span data-ttu-id="424d3-109">Behandlung des Ereignisses PercentDone der Widget-Klasse</span><span class="sxs-lookup"><span data-stu-id="424d3-109">To handle the PercentDone event of the Widget class</span></span>  
  
1.  <span data-ttu-id="424d3-110">Fügen Sie den folgenden Code in `Form1`:</span><span class="sxs-lookup"><span data-stu-id="424d3-110">Place the following code in `Form1`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#4](../../../../visual-basic/programming-guide/language-features/events/codesnippet/VisualBasic/walkthrough-handling-events_1.vb)]  
  
     <span data-ttu-id="424d3-111">Die `WithEvents` Schlüsselwort Gibt an, dass die Variable `mWidget` wird verwendet, um ein Objekt Ereignisse zu behandeln.</span><span class="sxs-lookup"><span data-stu-id="424d3-111">The `WithEvents` keyword specifies that the variable `mWidget` is used to handle an object's events.</span></span> <span data-ttu-id="424d3-112">Sie geben die Art des Objekts, indem der Name der Klasse, von der das Objekt erstellt wird.</span><span class="sxs-lookup"><span data-stu-id="424d3-112">You specify the kind of object by supplying the name of the class from which the object will be created.</span></span>  
  
     <span data-ttu-id="424d3-113">Die Variable `mWidget` im deklarierten `Form1` da `WithEvents` Variablen auf Klassenebene sein müssen.</span><span class="sxs-lookup"><span data-stu-id="424d3-113">The variable `mWidget` is declared in `Form1` because `WithEvents` variables must be class-level.</span></span> <span data-ttu-id="424d3-114">Dies gilt unabhängig vom Typ der Klasse, die in den sie eingefügt.</span><span class="sxs-lookup"><span data-stu-id="424d3-114">This is true regardless of the type of class you place them in.</span></span>  
  
     <span data-ttu-id="424d3-115">Die Variable `mblnCancel` wird verwendet, um das Abbrechen der `LongTask` Methode.</span><span class="sxs-lookup"><span data-stu-id="424d3-115">The variable `mblnCancel` is used to cancel the `LongTask` method.</span></span>  
  
## <a name="writing-code-to-handle-an-event"></a><span data-ttu-id="424d3-116">Schreiben von Code zum Behandeln eines Ereignisses</span><span class="sxs-lookup"><span data-stu-id="424d3-116">Writing Code to Handle an Event</span></span>  
 <span data-ttu-id="424d3-117">Sobald Sie deklarieren eine Variable mit `WithEvents`, der Variablennamen angezeigt, in der linken Dropdownliste der Klasse **Code-Editor**.</span><span class="sxs-lookup"><span data-stu-id="424d3-117">As soon as you declare a variable using `WithEvents`, the variable name appears in the left drop-down list of the class's **Code Editor**.</span></span> <span data-ttu-id="424d3-118">Bei Auswahl `mWidget`die `Widget` Klasse Ereignisse in der rechten Dropdownliste angezeigt werden.</span><span class="sxs-lookup"><span data-stu-id="424d3-118">When you select `mWidget`, the `Widget` class's events appear in the right drop-down list.</span></span> <span data-ttu-id="424d3-119">Auswahl eines Ereignisses wird der entsprechende Ereignisprozedur mit dem Präfix `mWidget` und einen Unterstrich.</span><span class="sxs-lookup"><span data-stu-id="424d3-119">Selecting an event displays the corresponding event procedure, with the prefix `mWidget` and an underscore.</span></span> <span data-ttu-id="424d3-120">Alle zugeordneten Ereignisprozeduren eine `WithEvents` Variable den Variablennamen als Präfix erhalten.</span><span class="sxs-lookup"><span data-stu-id="424d3-120">All the event procedures associated with a `WithEvents` variable are given the variable name as a prefix.</span></span>  
  
#### <a name="to-handle-an-event"></a><span data-ttu-id="424d3-121">So behandeln Sie ein Ereignis</span><span class="sxs-lookup"><span data-stu-id="424d3-121">To handle an event</span></span>  
  
1.  <span data-ttu-id="424d3-122">Wählen Sie `mWidget` aus der linken Dropdownliste in der **Code-Editor**.</span><span class="sxs-lookup"><span data-stu-id="424d3-122">Select `mWidget` from the left drop-down list in the **Code Editor**.</span></span>  
  
2.  <span data-ttu-id="424d3-123">Wählen Sie die `PercentDone` Ereignis aus der rechten Dropdownliste.</span><span class="sxs-lookup"><span data-stu-id="424d3-123">Select the `PercentDone` event from the right drop-down list.</span></span> <span data-ttu-id="424d3-124">Die **Code-Editor** öffnet die `mWidget_PercentDone` Ereignisprozedur.</span><span class="sxs-lookup"><span data-stu-id="424d3-124">The **Code Editor** opens the `mWidget_PercentDone` event procedure.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="424d3-125">Die **Code-Editor** ist nützlich, aber nicht erforderlich, für das Einfügen von neuen Ereignishandler.</span><span class="sxs-lookup"><span data-stu-id="424d3-125">The **Code Editor** is useful, but not required, for inserting new event handlers.</span></span> <span data-ttu-id="424d3-126">In dieser exemplarischen Vorgehensweise ist es eine direktere an die Ereignishandler einfach direkt in den Code zu kopieren.</span><span class="sxs-lookup"><span data-stu-id="424d3-126">In this walkthrough, it is more direct to just copy the event handlers directly into your code.</span></span>  
  
3.  <span data-ttu-id="424d3-127">Fügen Sie dem `mWidget_PercentDone`-Ereignishandler folgenden Code hinzu:</span><span class="sxs-lookup"><span data-stu-id="424d3-127">Add the following code to the `mWidget_PercentDone` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#5](../../../../visual-basic/programming-guide/language-features/events/codesnippet/VisualBasic/walkthrough-handling-events_2.vb)]  
  
     <span data-ttu-id="424d3-128">Wenn die `PercentDone` Ereignis wird ausgelöst, der die Ereignisprozedur zeigt die abgeschlossenen Prozentsatz in einer `Label` Steuerelement.</span><span class="sxs-lookup"><span data-stu-id="424d3-128">Whenever the `PercentDone` event is raised, the event procedure displays the percent complete in a `Label` control.</span></span> <span data-ttu-id="424d3-129">Die `DoEvents` Methode ermöglicht die Bezeichnung zu zeichnen, und gibt dem Benutzer die Möglichkeit, klicken Sie auf die **"Abbrechen"** Schaltfläche.</span><span class="sxs-lookup"><span data-stu-id="424d3-129">The `DoEvents` method allows the label to repaint, and also gives the user the opportunity to click the **Cancel** button.</span></span>  
  
4.  <span data-ttu-id="424d3-130">Fügen Sie den folgenden Code für die `Button2_Click` Ereignishandler:</span><span class="sxs-lookup"><span data-stu-id="424d3-130">Add the following code for the `Button2_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#6](../../../../visual-basic/programming-guide/language-features/events/codesnippet/VisualBasic/walkthrough-handling-events_3.vb)]  
  
 <span data-ttu-id="424d3-131">Wenn der Benutzer klickt der **"Abbrechen"** Schaltfläche beim `LongTask` ausgeführt wird, die `Button2_Click` Ereignis ausgeführt wird, sobald der `DoEvents` -Anweisung erlaubt die Verarbeitung von Ereignissen auftreten.</span><span class="sxs-lookup"><span data-stu-id="424d3-131">If the user clicks the **Cancel** button while `LongTask` is running, the `Button2_Click` event is executed as soon as the `DoEvents` statement allows event processing to occur.</span></span> <span data-ttu-id="424d3-132">Der Variablen auf Klassenebene `mblnCancel` auf festgelegt ist `True`, und die `mWidget_PercentDone` Ereignis überprüft, und legt die `ByRef Cancel` Argument für `True`.</span><span class="sxs-lookup"><span data-stu-id="424d3-132">The class-level variable `mblnCancel` is set to `True`, and the `mWidget_PercentDone` event then tests it and sets the `ByRef Cancel` argument to `True`.</span></span>  
  
## <a name="connecting-a-withevents-variable-to-an-object"></a><span data-ttu-id="424d3-133">Herstellen einer Verbindung eine WithEvents-Variable auf ein Objekt</span><span class="sxs-lookup"><span data-stu-id="424d3-133">Connecting a WithEvents Variable to an Object</span></span>  
 <span data-ttu-id="424d3-134">`Form1` ist nun eingerichtet, behandeln eine `Widget` Ereignisse des Objekts.</span><span class="sxs-lookup"><span data-stu-id="424d3-134">`Form1` is now set up to handle a `Widget` object's events.</span></span> <span data-ttu-id="424d3-135">Übrig bleibt zum Suchen einer `Widget` an einer beliebigen Stelle.</span><span class="sxs-lookup"><span data-stu-id="424d3-135">All that remains is to find a `Widget` somewhere.</span></span>  
  
 <span data-ttu-id="424d3-136">Wenn Sie eine Variable deklarieren `WithEvents` zur Entwurfszeit kein Objekt zugeordnet ist.</span><span class="sxs-lookup"><span data-stu-id="424d3-136">When you declare a variable `WithEvents` at design time, no object is associated with it.</span></span> <span data-ttu-id="424d3-137">Ein `WithEvents` Variable ist, genau wie jede andere Objektvariable.</span><span class="sxs-lookup"><span data-stu-id="424d3-137">A `WithEvents` variable is just like any other object variable.</span></span> <span data-ttu-id="424d3-138">Sie haben ein Objekt erstellt, und weisen Sie einen Verweis darauf mit der `WithEvents` Variable.</span><span class="sxs-lookup"><span data-stu-id="424d3-138">You have to create an object and assign a reference to it with the `WithEvents` variable.</span></span>  
  
#### <a name="to-create-an-object-and-assign-a-reference-to-it"></a><span data-ttu-id="424d3-139">Ein Objekt erstellt, und weisen Sie einen Verweis darauf</span><span class="sxs-lookup"><span data-stu-id="424d3-139">To create an object and assign a reference to it</span></span>  
  
1.  <span data-ttu-id="424d3-140">Wählen Sie **(Form1-Ereignisse)** aus der linken Dropdownliste in der **Code-Editor**.</span><span class="sxs-lookup"><span data-stu-id="424d3-140">Select **(Form1 Events)** from the left drop-down list in the **Code Editor**.</span></span>  
  
2.  <span data-ttu-id="424d3-141">Wählen Sie die `Load` Ereignis aus der rechten Dropdownliste.</span><span class="sxs-lookup"><span data-stu-id="424d3-141">Select the `Load` event from the right drop-down list.</span></span> <span data-ttu-id="424d3-142">Die **Code-Editor** öffnet die `Form1_Load` Ereignisprozedur.</span><span class="sxs-lookup"><span data-stu-id="424d3-142">The **Code Editor** opens the `Form1_Load` event procedure.</span></span>  
  
3.  <span data-ttu-id="424d3-143">Fügen Sie den folgenden Code für die `Form1_Load` Ereignisprozedur zum Erstellen der `Widget`:</span><span class="sxs-lookup"><span data-stu-id="424d3-143">Add the following code for the `Form1_Load` event procedure to create the `Widget`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#7](../../../../visual-basic/programming-guide/language-features/events/codesnippet/VisualBasic/walkthrough-handling-events_4.vb)]  
  
 <span data-ttu-id="424d3-144">Wenn dieser Code ausgeführt wird, erstellt Visual Basic eine `Widget` -Objekt und verbindet die zugehörigen Ereignisse auf die zugeordnete Ereignisprozeduren `mWidget`.</span><span class="sxs-lookup"><span data-stu-id="424d3-144">When this code executes, Visual Basic creates a `Widget` object and connects its events to the event procedures associated with `mWidget`.</span></span> <span data-ttu-id="424d3-145">Von diesem Zeitpunkt an, wenn die `Widget` löst seine `PercentDone` -Ereignis der `mWidget_PercentDone` Ereignisprozedur ausgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="424d3-145">From that point on, whenever the `Widget` raises its `PercentDone` event, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
#### <a name="to-call-the-longtask-method"></a><span data-ttu-id="424d3-146">Zum Aufrufen der LongTask-Methode</span><span class="sxs-lookup"><span data-stu-id="424d3-146">To call the LongTask method</span></span>  
  
-   <span data-ttu-id="424d3-147">Fügen Sie dem `Button1_Click`-Ereignishandler folgenden Code hinzu:</span><span class="sxs-lookup"><span data-stu-id="424d3-147">Add the following code to the `Button1_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#8](../../../../visual-basic/programming-guide/language-features/events/codesnippet/VisualBasic/walkthrough-handling-events_5.vb)]  
  
 <span data-ttu-id="424d3-148">Bevor Sie die `LongTask` -Methode aufgerufen wird, wird die Bezeichnung zeigt den Prozentsatz der Fertigstellung initialisiert werden müssen, und der Klassenebene `Boolean` flag für das Abbrechen der Methodennamens festgelegt werden muss, `False`.</span><span class="sxs-lookup"><span data-stu-id="424d3-148">Before the `LongTask` method is called, the label that displays the percent complete must be initialized, and the class-level `Boolean` flag for canceling the method must be set to `False`.</span></span>  
  
 <span data-ttu-id="424d3-149">`LongTask` wird mit einer Aufgabendauer von 12,2 Sekunden aufgerufen.</span><span class="sxs-lookup"><span data-stu-id="424d3-149">`LongTask` is called with a task duration of 12.2 seconds.</span></span> <span data-ttu-id="424d3-150">Die `PercentDone` Ereignis wird ausgelöst, einmal alle ein Drittel von einer Sekunde.</span><span class="sxs-lookup"><span data-stu-id="424d3-150">The `PercentDone` event is raised once every one-third of a second.</span></span> <span data-ttu-id="424d3-151">Jedes Mal, die das Ereignis ausgelöst wird, die `mWidget_PercentDone` Ereignisprozedur ausgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="424d3-151">Each time the event is raised, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
 <span data-ttu-id="424d3-152">Wenn `LongTask` erfolgt, `mblnCancel` wird getestet, um konsultieren `LongTask` normal beendet oder angehalten wurde, weil `mblnCancel` wurde festgelegt, um `True`.</span><span class="sxs-lookup"><span data-stu-id="424d3-152">When `LongTask` is done, `mblnCancel` is tested to see if `LongTask` ended normally, or if it stopped because `mblnCancel` was set to `True`.</span></span> <span data-ttu-id="424d3-153">Der Prozentsatz der Fertigstellung wird nur im ersten Fall aktualisiert.</span><span class="sxs-lookup"><span data-stu-id="424d3-153">The percent complete is updated only in the former case.</span></span>  
  
#### <a name="to-run-the-program"></a><span data-ttu-id="424d3-154">So führen Sie das Programm aus</span><span class="sxs-lookup"><span data-stu-id="424d3-154">To run the program</span></span>  
  
1.  <span data-ttu-id="424d3-155">Drücken Sie F5, um das Projekt im Ausführungsmodus.</span><span class="sxs-lookup"><span data-stu-id="424d3-155">Press F5 to put the project in run mode.</span></span>  
  
2.  <span data-ttu-id="424d3-156">Klicken Sie auf die **Aufgabe starten** Schaltfläche.</span><span class="sxs-lookup"><span data-stu-id="424d3-156">Click the **Start Task** button.</span></span> <span data-ttu-id="424d3-157">Jedes Mal die `PercentDone` Ereignis ausgelöst wird, wird die Bezeichnung mit dem Prozentsatz der Aufgabe, die abgeschlossen wird aktualisiert.</span><span class="sxs-lookup"><span data-stu-id="424d3-157">Each time the `PercentDone` event is raised, the label is updated with the percentage of the task that is complete.</span></span>  
  
3.  <span data-ttu-id="424d3-158">Klicken Sie auf die **"Abbrechen"** Schaltfläche, um den Vorgang zu beenden.</span><span class="sxs-lookup"><span data-stu-id="424d3-158">Click the **Cancel** button to stop the task.</span></span> <span data-ttu-id="424d3-159">Beachten Sie, dass die Darstellung der **"Abbrechen"** Schaltfläche nicht direkt ändert, wenn Sie darauf klicken.</span><span class="sxs-lookup"><span data-stu-id="424d3-159">Notice that the appearance of the **Cancel** button does not change immediately when you click it.</span></span> <span data-ttu-id="424d3-160">Die `Click` Ereignis nicht auftreten, bis die `My.Application.DoEvents` -Anweisung erlaubt die Verarbeitung von Ereignissen.</span><span class="sxs-lookup"><span data-stu-id="424d3-160">The `Click` event cannot happen until the `My.Application.DoEvents` statement allows event processing.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="424d3-161">Die `My.Application.DoEvents` -Methode verarbeitet Ereignisse nicht auf genau die gleiche Weise wie das Formular.</span><span class="sxs-lookup"><span data-stu-id="424d3-161">The `My.Application.DoEvents` method does not process events in exactly the same way as the form does.</span></span> <span data-ttu-id="424d3-162">Klicken Sie beispielsweise in dieser exemplarischen Vorgehensweise, Sie müssen auf die **"Abbrechen"** zweimal Schaltfläche.</span><span class="sxs-lookup"><span data-stu-id="424d3-162">For example, in this walkthrough, you must click the **Cancel** button twice.</span></span> <span data-ttu-id="424d3-163">Damit wird das Formular, um die Ereignisse direkt verarbeiten, können Sie multithreading.</span><span class="sxs-lookup"><span data-stu-id="424d3-163">To allow the form to handle the events directly, you can use multithreading.</span></span> <span data-ttu-id="424d3-164">Weitere Informationen finden Sie unter [Threading](http://msdn.microsoft.com/library/552f6c68-dbdb-4327-ae36-32cf9063d88c).</span><span class="sxs-lookup"><span data-stu-id="424d3-164">For more information, see [Threading](http://msdn.microsoft.com/library/552f6c68-dbdb-4327-ae36-32cf9063d88c).</span></span>  
  
 <span data-ttu-id="424d3-165">Sie finden es vielleicht hilfreich, das Programm mit F11 auszuführen und den Code zeilenweise durchlaufen.</span><span class="sxs-lookup"><span data-stu-id="424d3-165">You may find it instructive to run the program with F11 and step through the code a line at a time.</span></span> <span data-ttu-id="424d3-166">Klar erkennbar, wie die Ausführung eingibt `LongTask`, und klicken Sie dann eine kurze wieder hinzugefügt `Form1` jedes Mal die `PercentDone` Ereignis wird ausgelöst.</span><span class="sxs-lookup"><span data-stu-id="424d3-166">You can clearly see how execution enters `LongTask`, and then briefly re-enters `Form1` each time the `PercentDone` event is raised.</span></span>  
  
 <span data-ttu-id="424d3-167">Was geschieht, If, während der Ausführung wieder in den Code der war `Form1`, die `LongTask` Methode erneut aufrufen würden?</span><span class="sxs-lookup"><span data-stu-id="424d3-167">What would happen if, while execution was back in the code of `Form1`, the `LongTask` method were called again?</span></span> <span data-ttu-id="424d3-168">Im schlimmsten Fall kann ein Stapelüberlauf auftreten, wenn `LongTask` aufgerufen wurden, jedes Mal, wenn das Ereignis ausgelöst wurde.</span><span class="sxs-lookup"><span data-stu-id="424d3-168">At worst, a stack overflow might occur if `LongTask` were called every time the event was raised.</span></span>  
  
 <span data-ttu-id="424d3-169">Sie können dazu führen, dass die Variable `mWidget` zum Behandeln von Ereignissen für einen anderen `Widget` Objekt, indem Sie einen Verweis auf das neue `Widget` auf `mWidget`.</span><span class="sxs-lookup"><span data-stu-id="424d3-169">You can cause the variable `mWidget` to handle events for a different `Widget` object by assigning a reference to the new `Widget` to `mWidget`.</span></span> <span data-ttu-id="424d3-170">Tatsächlich können Sie den Code in vornehmen `Button1_Click` dazu jedes Mal, wenn Sie auf die Schaltfläche klicken.</span><span class="sxs-lookup"><span data-stu-id="424d3-170">In fact, you can make the code in `Button1_Click` do this every time you click the button.</span></span>  
  
#### <a name="to-handle-events-for-a-different-widget"></a><span data-ttu-id="424d3-171">So behandeln Sie Ereignisse für einen anderen Widgets</span><span class="sxs-lookup"><span data-stu-id="424d3-171">To handle events for a different widget</span></span>  
  
-   <span data-ttu-id="424d3-172">Fügen Sie die folgende Codezeile, die `Button1_Click` Prozedur, die direkt vor der Zeile `mWidget.LongTask(12.2, 0.33)`:</span><span class="sxs-lookup"><span data-stu-id="424d3-172">Add the following line of code to the `Button1_Click` procedure, immediately preceding the line that reads `mWidget.LongTask(12.2, 0.33)`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#9](../../../../visual-basic/programming-guide/language-features/events/codesnippet/VisualBasic/walkthrough-handling-events_6.vb)]  
  
 <span data-ttu-id="424d3-173">Der obige Code erstellt ein neues `Widget` jedes Mal die Schaltfläche geklickt wird.</span><span class="sxs-lookup"><span data-stu-id="424d3-173">The code above creates a new `Widget` each time the button is clicked.</span></span> <span data-ttu-id="424d3-174">Sobald die `LongTask` Methode ausgeführt wird, den Verweis auf die `Widget` freigegeben wird, und die `Widget` zerstört wird.</span><span class="sxs-lookup"><span data-stu-id="424d3-174">As soon as the `LongTask` method completes, the reference to the `Widget` is released, and the `Widget` is destroyed.</span></span>  
  
 <span data-ttu-id="424d3-175">Ein `WithEvents` Variable darf nur einen Objektverweis zu einem Zeitpunkt, wenn Sie ein anderes zuweisen `Widget` -Objekt `mWidget`, der vorherigen `Widget` objektspezifischen Ereignisse werden nicht mehr bearbeitet werden.</span><span class="sxs-lookup"><span data-stu-id="424d3-175">A `WithEvents` variable can contain only one object reference at a time, so if you assign a different `Widget` object to `mWidget`, the previous `Widget` object's events will no longer be handled.</span></span> <span data-ttu-id="424d3-176">Wenn `mWidget` ist die einzige Object-Variablen, die mit einem Verweis auf das alte `Widget`, das Objekt zerstört wird.</span><span class="sxs-lookup"><span data-stu-id="424d3-176">If `mWidget` is the only object variable containing a reference to the old `Widget`, the object is destroyed.</span></span> <span data-ttu-id="424d3-177">Wenn Sie mehrere Ereignisse behandeln möchten `Widget` Objekte, verwenden die `AddHandler` Anweisung, um Ereignisse aus jedem Objekt separat zu verarbeiten.</span><span class="sxs-lookup"><span data-stu-id="424d3-177">If you want to handle events from several `Widget` objects, use the `AddHandler` statement to process events from each object separately.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="424d3-178">Sie können beliebig viele deklarieren `WithEvents` Variablen als Sie benötigen, aber Arrays von `WithEvents` Variablen werden nicht unterstützt.</span><span class="sxs-lookup"><span data-stu-id="424d3-178">You can declare as many `WithEvents` variables as you need, but arrays of `WithEvents` variables are not supported.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="424d3-179">Siehe auch</span><span class="sxs-lookup"><span data-stu-id="424d3-179">See Also</span></span>  
 [<span data-ttu-id="424d3-180">Exemplarische Vorgehensweise: Deklarieren und Auslösen von Ereignissen</span><span class="sxs-lookup"><span data-stu-id="424d3-180">Walkthrough: Declaring and Raising Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md)  
 [<span data-ttu-id="424d3-181">Ereignisse</span><span class="sxs-lookup"><span data-stu-id="424d3-181">Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/index.md)
