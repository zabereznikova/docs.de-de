---
title: Logische und bitweise Operatoren in Visual Basic
ms.date: 07/20/2015
helpviewer_keywords:
- short-circuiting
- Boolean expressions
- logical operators [Visual Basic], Boolean expressions
- operators [Visual Basic], logical
- AndAlso operator [Visual Basic]
- Not operator [Visual Basic], Boolean expressions
- Xor operator [Visual Basic], Boolean expressions
- And operator [Visual Basic], logical operators
- logical operators [Visual Basic]
- expressions [Visual Basic], Boolean
- Or operator [Visual Basic], logical operators
- Visual Basic code, operators
- short-circuiting [Visual Basic], logical operators
- logical operators [Visual Basic], short-circuiting
- Visual Basic code, expressions
- logical operators [Visual Basic], binary
- OrElse operator [Visual Basic]
- logical operators [Visual Basic], unary
ms.assetid: ca474e13-567d-4b1d-a18b-301433705e57
ms.openlocfilehash: 40076b2ad6606b4c565bcd39dbeea9e55da47211
ms.sourcegitcommit: 68653db98c5ea7744fd438710248935f70020dfb
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 08/22/2019
ms.locfileid: "69963315"
---
# <a name="logical-and-bitwise-operators-in-visual-basic"></a><span data-ttu-id="07381-102">Logische und bitweise Operatoren in Visual Basic</span><span class="sxs-lookup"><span data-stu-id="07381-102">Logical and Bitwise Operators in Visual Basic</span></span>
<span data-ttu-id="07381-103">Logische Operatoren `Boolean` vergleichen Ausdrücke und geben `Boolean` ein Ergebnis zurück.</span><span class="sxs-lookup"><span data-stu-id="07381-103">Logical operators compare `Boolean` expressions and return a `Boolean` result.</span></span> <span data-ttu-id="07381-104">Die `And`Operatoren `AndAlso`, `Or` `Not` ,, `OrElse` und`Xor` sind *Binär* , da Sie zwei Operanden annehmen, während der Operator *unär* ist, da er einen einzelnen Operanden annimmt.</span><span class="sxs-lookup"><span data-stu-id="07381-104">The `And`, `Or`, `AndAlso`, `OrElse`, and `Xor` operators are *binary* because they take two operands, while the `Not` operator is *unary* because it takes a single operand.</span></span> <span data-ttu-id="07381-105">Einige dieser Operatoren können auch bitweise logische Operationen für ganzzahlige Werte ausführen.</span><span class="sxs-lookup"><span data-stu-id="07381-105">Some of these operators can also perform bitwise logical operations on integral values.</span></span>  
  
## <a name="unary-logical-operator"></a><span data-ttu-id="07381-106">Unärer logischer Operator</span><span class="sxs-lookup"><span data-stu-id="07381-106">Unary Logical Operator</span></span>  
 <span data-ttu-id="07381-107">Der [Not-Operator](../../../../visual-basic/language-reference/operators/not-operator.md) führt eine logische Negation `Boolean` für einen-Ausdruck aus.</span><span class="sxs-lookup"><span data-stu-id="07381-107">The [Not Operator](../../../../visual-basic/language-reference/operators/not-operator.md) performs logical *negation* on a `Boolean` expression.</span></span> <span data-ttu-id="07381-108">Es ergibt das logische Gegenteil des Operanden.</span><span class="sxs-lookup"><span data-stu-id="07381-108">It yields the logical opposite of its operand.</span></span> <span data-ttu-id="07381-109">`True`Wenn der Ausdruck ergibt `Not` , wird zurück `False`gegeben. wenn der Ausdruck `False`ergibt, wird `Not` zurück `True`gegeben.</span><span class="sxs-lookup"><span data-stu-id="07381-109">If the expression evaluates to `True`, then `Not` returns `False`; if the expression evaluates to `False`, then `Not` returns `True`.</span></span> <span data-ttu-id="07381-110">Dies wird anhand des folgenden Beispiels veranschaulicht.</span><span class="sxs-lookup"><span data-stu-id="07381-110">The following example illustrates this.</span></span>  
  
 [!code-vb[VbVbalrOperators#77](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#77)]  
  
## <a name="binary-logical-operators"></a><span data-ttu-id="07381-111">Binäre logische Operatoren</span><span class="sxs-lookup"><span data-stu-id="07381-111">Binary Logical Operators</span></span>  
 <span data-ttu-id="07381-112">Der [and-Operator](../../../../visual-basic/language-reference/operators/and-operator.md) führt eine logische *Konjunktion* zweier `Boolean` Ausdrücke aus.</span><span class="sxs-lookup"><span data-stu-id="07381-112">The [And Operator](../../../../visual-basic/language-reference/operators/and-operator.md) performs logical *conjunction* on two `Boolean` expressions.</span></span> <span data-ttu-id="07381-113">Wenn beide Ausdrücke als `True`ausgewertet `And` werden, gibt `True`zurück.</span><span class="sxs-lookup"><span data-stu-id="07381-113">If both expressions evaluate to `True`, then `And` returns `True`.</span></span> <span data-ttu-id="07381-114">Wenn mindestens einer der Ausdrücke als `False`ausgewertet `And` wird, wird zurück `False`gegeben.</span><span class="sxs-lookup"><span data-stu-id="07381-114">If at least one of the expressions evaluates to `False`, then `And` returns `False`.</span></span>  
  
 <span data-ttu-id="07381-115">Der [OR-Operator](../../../../visual-basic/language-reference/operators/or-operator.md) führt eine logische Disjunktion oder `Boolean` die *Einbindung* zweier Ausdrücke aus.</span><span class="sxs-lookup"><span data-stu-id="07381-115">The [Or Operator](../../../../visual-basic/language-reference/operators/or-operator.md) performs logical *disjunction* or *inclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="07381-116">Wenn einer der `True`Ausdrücke ergibt oder beide als `True`ausgewertet werden `Or` , gibt zurück `True`.</span><span class="sxs-lookup"><span data-stu-id="07381-116">If either expression evaluates to `True`, or both evaluate to `True`, then `Or` returns `True`.</span></span> <span data-ttu-id="07381-117">Wenn keiner der Ausdrücke `True`ergibt, wird `False` `Or` zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="07381-117">If neither expression evaluates to `True`, `Or` returns `False`.</span></span>  
  
 <span data-ttu-id="07381-118">Der [Xor-Operator](../../../../visual-basic/language-reference/operators/xor-operator.md) führt einen logischen Ausschluss `Boolean` für zwei Ausdrücke aus.</span><span class="sxs-lookup"><span data-stu-id="07381-118">The [Xor Operator](../../../../visual-basic/language-reference/operators/xor-operator.md) performs logical *exclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="07381-119">Wenn genau ein Ausdruck `True`ergibt, aber nicht `Xor` beides, wird zurück `True`gegeben.</span><span class="sxs-lookup"><span data-stu-id="07381-119">If exactly one expression evaluates to `True`, but not both, `Xor` returns `True`.</span></span> <span data-ttu-id="07381-120">Wenn beide Ausdrücke als ausgewertet `True` `False`werden oder beide als ausgewertet `Xor` werden `False`, gibt zurück.</span><span class="sxs-lookup"><span data-stu-id="07381-120">If both expressions evaluate to `True` or both evaluate to `False`, `Xor` returns `False`.</span></span>  
  
 <span data-ttu-id="07381-121">Im folgenden Beispiel werden die `And`Operatoren, `Xor` `Or`und veranschaulicht.</span><span class="sxs-lookup"><span data-stu-id="07381-121">The following example illustrates the `And`, `Or`, and `Xor` operators.</span></span>  
  
 [!code-vb[VbVbalrOperators#78](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#78)]  
  
## <a name="short-circuiting-logical-operations"></a><span data-ttu-id="07381-122">Kurzschluss logische Vorgänge</span><span class="sxs-lookup"><span data-stu-id="07381-122">Short-Circuiting Logical Operations</span></span>  
 <span data-ttu-id="07381-123">Der [andwas-Operator](../../../../visual-basic/language-reference/operators/andalso-operator.md) ist dem `And` -Operator sehr ähnlich, da er auch eine logische Konjunktion für zwei `Boolean` Ausdrücke ausführt.</span><span class="sxs-lookup"><span data-stu-id="07381-123">The [AndAlso Operator](../../../../visual-basic/language-reference/operators/andalso-operator.md) is very similar to the `And` operator, in that it also performs logical conjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="07381-124">Der Hauptunterschied zwischen den beiden besteht darin `AndAlso` , dass das *Kurzschluss* Verhalten aufweist.</span><span class="sxs-lookup"><span data-stu-id="07381-124">The key difference between the two is that `AndAlso` exhibits *short-circuiting* behavior.</span></span> <span data-ttu-id="07381-125">Wenn der erste Ausdruck in einem `AndAlso` Ausdruck als `False`ausgewertet wird, wird der zweite Ausdruck nicht ausgewertet, da er das Endergebnis nicht ändern kann, `AndAlso` und `False`gibt zurück.</span><span class="sxs-lookup"><span data-stu-id="07381-125">If the first expression in an `AndAlso` expression evaluates to `False`, then the second expression is not evaluated because it cannot alter the final result, and `AndAlso` returns `False`.</span></span>  
  
 <span data-ttu-id="07381-126">Entsprechend führt der [OrElse-Operator](../../../../visual-basic/language-reference/operators/orelse-operator.md) eine Kurzschluss logische Disjunktion für zwei `Boolean` Ausdrücke aus.</span><span class="sxs-lookup"><span data-stu-id="07381-126">Similarly, the [OrElse Operator](../../../../visual-basic/language-reference/operators/orelse-operator.md) performs short-circuiting logical disjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="07381-127">Wenn der erste Ausdruck in einem `OrElse` Ausdruck als `True`ausgewertet wird, wird der zweite Ausdruck nicht ausgewertet, da er das Endergebnis nicht ändern kann, `OrElse` und `True`gibt zurück.</span><span class="sxs-lookup"><span data-stu-id="07381-127">If the first expression in an `OrElse` expression evaluates to `True`, then the second expression is not evaluated because it cannot alter the final result, and `OrElse` returns `True`.</span></span>  
  
### <a name="short-circuiting-trade-offs"></a><span data-ttu-id="07381-128">Kurze Schluss Kompromisse</span><span class="sxs-lookup"><span data-stu-id="07381-128">Short-Circuiting Trade-Offs</span></span>  
 <span data-ttu-id="07381-129">Kurzschluss Vorgänge können die Leistung verbessern, indem Sie keinen Ausdruck auswerten, der das Ergebnis der logischen Operation nicht ändern kann.</span><span class="sxs-lookup"><span data-stu-id="07381-129">Short-circuiting can improve performance by not evaluating an expression that cannot alter the result of the logical operation.</span></span> <span data-ttu-id="07381-130">Wenn dieser Ausdruck jedoch weitere Aktionen ausführt, überspringt der Kurzschluss diese Aktionen.</span><span class="sxs-lookup"><span data-stu-id="07381-130">However, if that expression performs additional actions, short-circuiting skips those actions.</span></span> <span data-ttu-id="07381-131">Wenn der Ausdruck z. b. einen Aufruf `Function` einer Prozedur enthält, wird diese Prozedur nicht aufgerufen, wenn der Ausdruck kurzgeschlossen wird, und jeglicher zusätzlicher Code, der in der `Function` enthalten ist, wird nicht ausgeführt.</span><span class="sxs-lookup"><span data-stu-id="07381-131">For example, if the expression includes a call to a `Function` procedure, that procedure is not called if the expression is short-circuited, and any additional code contained in the `Function` does not run.</span></span> <span data-ttu-id="07381-132">Daher kann die Funktion nur gelegentlich ausgeführt werden und wird möglicherweise nicht ordnungsgemäß getestet.</span><span class="sxs-lookup"><span data-stu-id="07381-132">Therefore, the function might run only occasionally, and might not be tested correctly.</span></span> <span data-ttu-id="07381-133">Oder die Programmlogik hängt möglicherweise vom Code in der `Function`ab.</span><span class="sxs-lookup"><span data-stu-id="07381-133">Or the program logic might depend on the code in the `Function`.</span></span>  
  
 <span data-ttu-id="07381-134">Das folgende Beispiel veranschaulicht den Unterschied `And`zwischen `Or`, und ihren Kurzschluss Entsprechungen.</span><span class="sxs-lookup"><span data-stu-id="07381-134">The following example illustrates the difference between `And`, `Or`, and their short-circuiting counterparts.</span></span>  
  
 [!code-vb[VbVbalrOperators#81](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#81)]  
  
 [!code-vb[VbVbalrOperators#80](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#80)]  
  
 [!code-vb[VbVbalrOperators#79](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#79)]  
  
 <span data-ttu-id="07381-135">Beachten Sie im vorhergehenden Beispiel, dass einige wichtige Codes `checkIfValid()` in nicht ausgeführt werden, wenn der-Befehl kurzgeschlossen wird.</span><span class="sxs-lookup"><span data-stu-id="07381-135">In the preceding example, note that some important code inside `checkIfValid()` does not run when the call is short-circuited.</span></span> <span data-ttu-id="07381-136">Die erste `If` Anweisung ruft `checkIfValid()` auf, `12 > 45` Obwohl `False`zurückgibt `And` , da keine Kurzschluss ist.</span><span class="sxs-lookup"><span data-stu-id="07381-136">The first `If` statement calls `checkIfValid()` even though `12 > 45` returns `False`, because `And` does not short-circuit.</span></span> <span data-ttu-id="07381-137">Mit der `If` zweiten Anweisung wird nicht `checkIfValid()`aufgerufen, da `12 > 45` bei `False`Rückgabe `AndAlso` von der zweite Ausdruck kurz ist.</span><span class="sxs-lookup"><span data-stu-id="07381-137">The second `If` statement does not call `checkIfValid()`, because when `12 > 45` returns `False`, `AndAlso` short-circuits the second expression.</span></span> <span data-ttu-id="07381-138">Die dritte `If` -Anweisung `checkIfValid()` wird aufgerufen `12 < 45` , `True`obwohl zurück `Or` gibt, da keine Kurzschluss ist.</span><span class="sxs-lookup"><span data-stu-id="07381-138">The third `If` statement calls `checkIfValid()` even though `12 < 45` returns `True`, because `Or` does not short-circuit.</span></span> <span data-ttu-id="07381-139">In der `If` vierten Anweisung wird nicht `checkIfValid()`aufgerufen, da `12 < 45` bei `True`Rückgabe `OrElse` von der zweite Ausdruck kurz ist.</span><span class="sxs-lookup"><span data-stu-id="07381-139">The fourth `If` statement does not call `checkIfValid()`, because when `12 < 45` returns `True`, `OrElse` short-circuits the second expression.</span></span>  
  
## <a name="bitwise-operations"></a><span data-ttu-id="07381-140">Bitweise Vorgänge</span><span class="sxs-lookup"><span data-stu-id="07381-140">Bitwise Operations</span></span>  
 <span data-ttu-id="07381-141">Bitweise Operationen Werten zwei ganzzahlige Werte im Binär Formular (Basis 2) aus.</span><span class="sxs-lookup"><span data-stu-id="07381-141">Bitwise operations evaluate two integral values in binary (base 2) form.</span></span> <span data-ttu-id="07381-142">Sie vergleichen die Bits an den entsprechenden Positionen und weisen dann basierend auf dem Vergleich Werte zu.</span><span class="sxs-lookup"><span data-stu-id="07381-142">They compare the bits at corresponding positions and then assign values based on the comparison.</span></span> <span data-ttu-id="07381-143">Im folgenden Beispiel wird der `And` -Operator veranschaulicht.</span><span class="sxs-lookup"><span data-stu-id="07381-143">The following example illustrates the `And` operator.</span></span>  
  
 [!code-vb[VbVbalrConcepts#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrConcepts/VB/Class1.vb#2)]  
  
 <span data-ttu-id="07381-144">Im vorangehenden Beispiel wird der Wert `x` von auf 1 festgelegt.</span><span class="sxs-lookup"><span data-stu-id="07381-144">The preceding example sets the value of `x` to 1.</span></span> <span data-ttu-id="07381-145">Dies geschieht aus folgenden Gründen:</span><span class="sxs-lookup"><span data-stu-id="07381-145">This happens for the following reasons:</span></span>  
  
- <span data-ttu-id="07381-146">Die Werte werden als Binär Werte behandelt:</span><span class="sxs-lookup"><span data-stu-id="07381-146">The values are treated as binary:</span></span>  
  
     <span data-ttu-id="07381-147">3 in binärer Form = 011</span><span class="sxs-lookup"><span data-stu-id="07381-147">3 in binary form = 011</span></span>  
  
     <span data-ttu-id="07381-148">5 in binärer Form = 101</span><span class="sxs-lookup"><span data-stu-id="07381-148">5 in binary form = 101</span></span>  
  
- <span data-ttu-id="07381-149">Der `And` Operator vergleicht die binären Darstellungen, eine binäre Position (Bit) gleichzeitig.</span><span class="sxs-lookup"><span data-stu-id="07381-149">The `And` operator compares the binary representations, one binary position (bit) at a time.</span></span> <span data-ttu-id="07381-150">Wenn beide Bits an einer bestimmten Position 1 sind, wird ein 1 an dieser Position im Ergebnis platziert.</span><span class="sxs-lookup"><span data-stu-id="07381-150">If both bits at a given position are 1, then a 1 is placed in that position in the result.</span></span> <span data-ttu-id="07381-151">Wenn ein Bit 0 (null) ist, wird ein Wert von 0 an dieser Position im Ergebnis eingefügt.</span><span class="sxs-lookup"><span data-stu-id="07381-151">If either bit is 0, then a 0 is placed in that position in the result.</span></span> <span data-ttu-id="07381-152">Im vorangehenden Beispiel funktioniert dies wie folgt:</span><span class="sxs-lookup"><span data-stu-id="07381-152">In the preceding example this works out as follows:</span></span>  
  
     <span data-ttu-id="07381-153">011 (3 in binärer Form)</span><span class="sxs-lookup"><span data-stu-id="07381-153">011 (3 in binary form)</span></span>  
  
     <span data-ttu-id="07381-154">101 (5 in binärer Form)</span><span class="sxs-lookup"><span data-stu-id="07381-154">101 (5 in binary form)</span></span>  
  
     <span data-ttu-id="07381-155">001 (das Ergebnis, in binärer Form)</span><span class="sxs-lookup"><span data-stu-id="07381-155">001 (The result, in binary form)</span></span>  
  
- <span data-ttu-id="07381-156">Das Ergebnis wird als Dezimalwert behandelt.</span><span class="sxs-lookup"><span data-stu-id="07381-156">The result is treated as decimal.</span></span> <span data-ttu-id="07381-157">Der Wert 001 ist die binäre Darstellung von 1, sodass `x` = 1 ist.</span><span class="sxs-lookup"><span data-stu-id="07381-157">The value 001 is the binary representation of 1, so `x` = 1.</span></span>  
  
 <span data-ttu-id="07381-158">Der `Or` bitweise Vorgang ist ähnlich, mit der Ausnahme, dass dem Ergebnisbit 1 zugewiesen wird, wenn eine oder beide der verglichenen Bits 1 sind.</span><span class="sxs-lookup"><span data-stu-id="07381-158">The bitwise `Or` operation is similar, except that a 1 is assigned to the result bit if either or both of the compared bits is 1.</span></span> <span data-ttu-id="07381-159">`Xor`weist dem Ergebnisbit 1 zu, wenn genau eines der verglichenen Bits (nicht beides) 1 ist.</span><span class="sxs-lookup"><span data-stu-id="07381-159">`Xor` assigns a 1 to the result bit if exactly one of the compared bits (not both) is 1.</span></span> <span data-ttu-id="07381-160">`Not`nimmt einen einzelnen Operanden an und kehrt alle Bits einschließlich des Signier Bits um und weist diesen Wert dem Ergebnis zu.</span><span class="sxs-lookup"><span data-stu-id="07381-160">`Not` takes a single operand and inverts all the bits, including the sign bit, and assigns that value to the result.</span></span> <span data-ttu-id="07381-161">Dies bedeutet, dass für positive Zahlen `Not` mit Vorzeichen immer einen negativen Wert zurückgibt, und bei negativen Zahlen wird `Not` immer ein positiver oder NULL-Wert zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="07381-161">This means that for signed positive numbers, `Not` always returns a negative value, and for negative numbers, `Not` always returns a positive or zero value.</span></span>  
  
 <span data-ttu-id="07381-162">Bitweise `OrElse` Vorgänge werden von den `AndAlso` Operatoren und nicht unterstützt.</span><span class="sxs-lookup"><span data-stu-id="07381-162">The `AndAlso` and `OrElse` operators do not support bitwise operations.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="07381-163">Bitweise Vorgänge können nur für ganzzahlige Typen ausgeführt werden.</span><span class="sxs-lookup"><span data-stu-id="07381-163">Bitwise operations can be performed on integral types only.</span></span> <span data-ttu-id="07381-164">Gleit Komma Werte müssen in ganzzahlige Typen konvertiert werden, bevor die bitweise Operation fortgesetzt werden kann.</span><span class="sxs-lookup"><span data-stu-id="07381-164">Floating-point values must be converted to integral types before bitwise operation can proceed.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="07381-165">Siehe auch</span><span class="sxs-lookup"><span data-stu-id="07381-165">See also</span></span>

- [<span data-ttu-id="07381-166">Logische/bitweise Operatoren (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="07381-166">Logical/Bitwise Operators (Visual Basic)</span></span>](../../../../visual-basic/language-reference/operators/logical-bitwise-operators.md)
- [<span data-ttu-id="07381-167">Boolesche Ausdrücke</span><span class="sxs-lookup"><span data-stu-id="07381-167">Boolean Expressions</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/boolean-expressions.md)
- [<span data-ttu-id="07381-168">Arithmetische Operatoren in Visual Basic</span><span class="sxs-lookup"><span data-stu-id="07381-168">Arithmetic Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/arithmetic-operators.md)
- [<span data-ttu-id="07381-169">Vergleichs Operatoren in Visual Basic</span><span class="sxs-lookup"><span data-stu-id="07381-169">Comparison Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/comparison-operators.md)
- [<span data-ttu-id="07381-170">Verkettungs Operatoren in Visual Basic</span><span class="sxs-lookup"><span data-stu-id="07381-170">Concatenation Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/concatenation-operators.md)
- [<span data-ttu-id="07381-171">Effiziente Kombination von Operatoren</span><span class="sxs-lookup"><span data-stu-id="07381-171">Efficient Combination of Operators</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/efficient-combination-of-operators.md)
