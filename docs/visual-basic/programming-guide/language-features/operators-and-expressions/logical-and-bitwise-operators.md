---
title: Logische und bitweise Operatoren
ms.date: 07/20/2015
helpviewer_keywords:
- short-circuiting
- Boolean expressions
- logical operators [Visual Basic], Boolean expressions
- operators [Visual Basic], logical
- AndAlso operator [Visual Basic]
- Not operator [Visual Basic], Boolean expressions
- Xor operator [Visual Basic], Boolean expressions
- And operator [Visual Basic], logical operators
- logical operators [Visual Basic]
- expressions [Visual Basic], Boolean
- Or operator [Visual Basic], logical operators
- Visual Basic code, operators
- short-circuiting [Visual Basic], logical operators
- logical operators [Visual Basic], short-circuiting
- Visual Basic code, expressions
- logical operators [Visual Basic], binary
- OrElse operator [Visual Basic]
- logical operators [Visual Basic], unary
ms.assetid: ca474e13-567d-4b1d-a18b-301433705e57
ms.openlocfilehash: 19d3511363f19319c2bd8ce872b62c1462b1370f
ms.sourcegitcommit: f8c270376ed905f6a8896ce0fe25b4f4b38ff498
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 06/04/2020
ms.locfileid: "84403407"
---
# <a name="logical-and-bitwise-operators-in-visual-basic"></a><span data-ttu-id="ca4b9-102">Logische und bitweise Operatoren in Visual Basic</span><span class="sxs-lookup"><span data-stu-id="ca4b9-102">Logical and Bitwise Operators in Visual Basic</span></span>
<span data-ttu-id="ca4b9-103">Logische Operatoren vergleichen `Boolean` Ausdrücke und geben ein `Boolean` Ergebnis zurück.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-103">Logical operators compare `Boolean` expressions and return a `Boolean` result.</span></span> <span data-ttu-id="ca4b9-104">Die `And` `Or` `AndAlso` Operatoren,,, `OrElse` und `Xor` sind *Binär* , da Sie zwei Operanden annehmen, während der `Not` Operator *unär* ist, da er einen einzelnen Operanden annimmt.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-104">The `And`, `Or`, `AndAlso`, `OrElse`, and `Xor` operators are *binary* because they take two operands, while the `Not` operator is *unary* because it takes a single operand.</span></span> <span data-ttu-id="ca4b9-105">Einige dieser Operatoren können auch bitweise logische Operationen für ganzzahlige Werte ausführen.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-105">Some of these operators can also perform bitwise logical operations on integral values.</span></span>  
  
## <a name="unary-logical-operator"></a><span data-ttu-id="ca4b9-106">Unärer logischer Operator</span><span class="sxs-lookup"><span data-stu-id="ca4b9-106">Unary Logical Operator</span></span>  
 <span data-ttu-id="ca4b9-107">Der [Not-Operator](../../../language-reference/operators/not-operator.md) führt eine logische *Negation* für einen- `Boolean` Ausdruck aus.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-107">The [Not Operator](../../../language-reference/operators/not-operator.md) performs logical *negation* on a `Boolean` expression.</span></span> <span data-ttu-id="ca4b9-108">Es ergibt das logische Gegenteil des Operanden.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-108">It yields the logical opposite of its operand.</span></span> <span data-ttu-id="ca4b9-109">Wenn der Ausdruck ergibt `True` , wird `Not` zurückgegeben `False` . wenn der Ausdruck ergibt `False` , wird zurückgegeben `Not` `True` .</span><span class="sxs-lookup"><span data-stu-id="ca4b9-109">If the expression evaluates to `True`, then `Not` returns `False`; if the expression evaluates to `False`, then `Not` returns `True`.</span></span> <span data-ttu-id="ca4b9-110">Dies wird anhand des folgenden Beispiels veranschaulicht.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-110">The following example illustrates this.</span></span>  
  
 [!code-vb[VbVbalrOperators#77](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#77)]  
  
## <a name="binary-logical-operators"></a><span data-ttu-id="ca4b9-111">Binäre logische Operatoren</span><span class="sxs-lookup"><span data-stu-id="ca4b9-111">Binary Logical Operators</span></span>  
 <span data-ttu-id="ca4b9-112">Der [and-Operator](../../../language-reference/operators/and-operator.md) führt eine logische *Konjunktion* zweier `Boolean` Ausdrücke aus.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-112">The [And Operator](../../../language-reference/operators/and-operator.md) performs logical *conjunction* on two `Boolean` expressions.</span></span> <span data-ttu-id="ca4b9-113">Wenn beide Ausdrücke als ausgewertet `True` werden, `And` gibt zurück `True` .</span><span class="sxs-lookup"><span data-stu-id="ca4b9-113">If both expressions evaluate to `True`, then `And` returns `True`.</span></span> <span data-ttu-id="ca4b9-114">Wenn mindestens einer der Ausdrücke als ausgewertet `False` wird, wird `And` zurückgegeben `False` .</span><span class="sxs-lookup"><span data-stu-id="ca4b9-114">If at least one of the expressions evaluates to `False`, then `And` returns `False`.</span></span>  
  
 <span data-ttu-id="ca4b9-115">Der [OR-Operator](../../../language-reference/operators/or-operator.md) führt eine logische *Disjunktion* oder die *Einbindung* zweier `Boolean` Ausdrücke aus.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-115">The [Or Operator](../../../language-reference/operators/or-operator.md) performs logical *disjunction* or *inclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="ca4b9-116">Wenn einer der Ausdrücke ergibt `True` oder beide als ausgewertet `True` werden, `Or` gibt zurück `True` .</span><span class="sxs-lookup"><span data-stu-id="ca4b9-116">If either expression evaluates to `True`, or both evaluate to `True`, then `Or` returns `True`.</span></span> <span data-ttu-id="ca4b9-117">Wenn keiner der Ausdrücke ergibt `True` , wird `Or` zurückgegeben `False` .</span><span class="sxs-lookup"><span data-stu-id="ca4b9-117">If neither expression evaluates to `True`, `Or` returns `False`.</span></span>  
  
 <span data-ttu-id="ca4b9-118">Der [Xor-Operator](../../../language-reference/operators/xor-operator.md) führt einen logischen *Ausschluss* für zwei `Boolean` Ausdrücke aus.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-118">The [Xor Operator](../../../language-reference/operators/xor-operator.md) performs logical *exclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="ca4b9-119">Wenn genau ein Ausdruck ergibt `True` , aber nicht beides, wird `Xor` zurückgegeben `True` .</span><span class="sxs-lookup"><span data-stu-id="ca4b9-119">If exactly one expression evaluates to `True`, but not both, `Xor` returns `True`.</span></span> <span data-ttu-id="ca4b9-120">Wenn beide Ausdrücke als ausgewertet `True` werden oder beide als ausgewertet `False` werden, `Xor` gibt zurück `False` .</span><span class="sxs-lookup"><span data-stu-id="ca4b9-120">If both expressions evaluate to `True` or both evaluate to `False`, `Xor` returns `False`.</span></span>  
  
 <span data-ttu-id="ca4b9-121">Im folgenden Beispiel werden die `And` `Or` Operatoren, und veranschaulicht `Xor` .</span><span class="sxs-lookup"><span data-stu-id="ca4b9-121">The following example illustrates the `And`, `Or`, and `Xor` operators.</span></span>  
  
 [!code-vb[VbVbalrOperators#78](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#78)]  
  
## <a name="short-circuiting-logical-operations"></a><span data-ttu-id="ca4b9-122">Kurzschluss logische Vorgänge</span><span class="sxs-lookup"><span data-stu-id="ca4b9-122">Short-Circuiting Logical Operations</span></span>  
 <span data-ttu-id="ca4b9-123">Der [andwas-Operator](../../../language-reference/operators/andalso-operator.md) ist dem-Operator sehr ähnlich `And` , da er auch eine logische Konjunktion für zwei `Boolean` Ausdrücke ausführt.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-123">The [AndAlso Operator](../../../language-reference/operators/andalso-operator.md) is very similar to the `And` operator, in that it also performs logical conjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="ca4b9-124">Der Hauptunterschied zwischen den beiden besteht darin, dass das `AndAlso` *Kurzschluss* Verhalten aufweist.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-124">The key difference between the two is that `AndAlso` exhibits *short-circuiting* behavior.</span></span> <span data-ttu-id="ca4b9-125">Wenn der erste Ausdruck in einem `AndAlso` Ausdruck als `False` ausgewertet wird, wird der zweite Ausdruck nicht ausgewertet, da er das Endergebnis nicht ändern kann, und `AndAlso` gibt zurück `False` .</span><span class="sxs-lookup"><span data-stu-id="ca4b9-125">If the first expression in an `AndAlso` expression evaluates to `False`, then the second expression is not evaluated because it cannot alter the final result, and `AndAlso` returns `False`.</span></span>  
  
 <span data-ttu-id="ca4b9-126">Entsprechend führt der [OrElse-Operator](../../../language-reference/operators/orelse-operator.md) eine Kurzschluss logische Disjunktion für zwei `Boolean` Ausdrücke aus.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-126">Similarly, the [OrElse Operator](../../../language-reference/operators/orelse-operator.md) performs short-circuiting logical disjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="ca4b9-127">Wenn der erste Ausdruck in einem `OrElse` Ausdruck als `True` ausgewertet wird, wird der zweite Ausdruck nicht ausgewertet, da er das Endergebnis nicht ändern kann, und `OrElse` gibt zurück `True` .</span><span class="sxs-lookup"><span data-stu-id="ca4b9-127">If the first expression in an `OrElse` expression evaluates to `True`, then the second expression is not evaluated because it cannot alter the final result, and `OrElse` returns `True`.</span></span>  
  
### <a name="short-circuiting-trade-offs"></a><span data-ttu-id="ca4b9-128">Kurze Schluss Kompromisse</span><span class="sxs-lookup"><span data-stu-id="ca4b9-128">Short-Circuiting Trade-Offs</span></span>  
 <span data-ttu-id="ca4b9-129">Kurzschluss Vorgänge können die Leistung verbessern, indem Sie keinen Ausdruck auswerten, der das Ergebnis der logischen Operation nicht ändern kann.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-129">Short-circuiting can improve performance by not evaluating an expression that cannot alter the result of the logical operation.</span></span> <span data-ttu-id="ca4b9-130">Wenn dieser Ausdruck jedoch weitere Aktionen ausführt, überspringt der Kurzschluss diese Aktionen.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-130">However, if that expression performs additional actions, short-circuiting skips those actions.</span></span> <span data-ttu-id="ca4b9-131">Wenn der Ausdruck z. b. einen Aufruf einer `Function` Prozedur enthält, wird diese Prozedur nicht aufgerufen, wenn der Ausdruck kurzgeschlossen wird, und jeglicher zusätzlicher Code, der in der enthalten ist, wird `Function` nicht ausgeführt.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-131">For example, if the expression includes a call to a `Function` procedure, that procedure is not called if the expression is short-circuited, and any additional code contained in the `Function` does not run.</span></span> <span data-ttu-id="ca4b9-132">Daher kann die Funktion nur gelegentlich ausgeführt werden und wird möglicherweise nicht ordnungsgemäß getestet.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-132">Therefore, the function might run only occasionally, and might not be tested correctly.</span></span> <span data-ttu-id="ca4b9-133">Oder die Programmlogik hängt möglicherweise vom Code in der ab `Function` .</span><span class="sxs-lookup"><span data-stu-id="ca4b9-133">Or the program logic might depend on the code in the `Function`.</span></span>  
  
 <span data-ttu-id="ca4b9-134">Das folgende Beispiel veranschaulicht den Unterschied zwischen `And` , `Or` und ihren Kurzschluss Entsprechungen.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-134">The following example illustrates the difference between `And`, `Or`, and their short-circuiting counterparts.</span></span>  
  
 [!code-vb[VbVbalrOperators#81](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#81)]  
  
 [!code-vb[VbVbalrOperators#80](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#80)]  
  
 [!code-vb[VbVbalrOperators#79](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#79)]  
  
 <span data-ttu-id="ca4b9-135">Beachten Sie im vorhergehenden Beispiel, dass einige wichtige Codes in `checkIfValid()` nicht ausgeführt werden, wenn der-Befehl kurzgeschlossen wird.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-135">In the preceding example, note that some important code inside `checkIfValid()` does not run when the call is short-circuited.</span></span> <span data-ttu-id="ca4b9-136">Die erste `If` Anweisung ruft auf `checkIfValid()` `12 > 45` , obwohl zurückgibt `False` , da `And` keine Kurzschluss ist.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-136">The first `If` statement calls `checkIfValid()` even though `12 > 45` returns `False`, because `And` does not short-circuit.</span></span> <span data-ttu-id="ca4b9-137">Mit der zweiten `If` Anweisung wird nicht aufgerufen `checkIfValid()` , da bei `12 > 45` Rückgabe von `False` `AndAlso` der zweite Ausdruck kurz ist.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-137">The second `If` statement does not call `checkIfValid()`, because when `12 > 45` returns `False`, `AndAlso` short-circuits the second expression.</span></span> <span data-ttu-id="ca4b9-138">Die dritte- `If` Anweisung `checkIfValid()` wird aufgerufen `12 < 45` , obwohl zurückgibt `True` , da `Or` keine Kurzschluss ist.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-138">The third `If` statement calls `checkIfValid()` even though `12 < 45` returns `True`, because `Or` does not short-circuit.</span></span> <span data-ttu-id="ca4b9-139">In der vierten `If` Anweisung wird nicht aufgerufen `checkIfValid()` , da bei `12 < 45` Rückgabe von `True` `OrElse` der zweite Ausdruck kurz ist.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-139">The fourth `If` statement does not call `checkIfValid()`, because when `12 < 45` returns `True`, `OrElse` short-circuits the second expression.</span></span>  
  
## <a name="bitwise-operations"></a><span data-ttu-id="ca4b9-140">Bitweise Vorgänge</span><span class="sxs-lookup"><span data-stu-id="ca4b9-140">Bitwise Operations</span></span>  
 <span data-ttu-id="ca4b9-141">Bitweise Operationen Werten zwei ganzzahlige Werte im Binär Formular (Basis 2) aus.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-141">Bitwise operations evaluate two integral values in binary (base 2) form.</span></span> <span data-ttu-id="ca4b9-142">Sie vergleichen die Bits an den entsprechenden Positionen und weisen dann basierend auf dem Vergleich Werte zu.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-142">They compare the bits at corresponding positions and then assign values based on the comparison.</span></span> <span data-ttu-id="ca4b9-143">Im folgenden Beispiel wird der- `And` Operator veranschaulicht.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-143">The following example illustrates the `And` operator.</span></span>  
  
 [!code-vb[VbVbalrConcepts#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrConcepts/VB/Class1.vb#2)]  
  
 <span data-ttu-id="ca4b9-144">Im vorangehenden Beispiel wird der Wert von `x` auf 1 festgelegt.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-144">The preceding example sets the value of `x` to 1.</span></span> <span data-ttu-id="ca4b9-145">Dies geschieht aus folgenden Gründen:</span><span class="sxs-lookup"><span data-stu-id="ca4b9-145">This happens for the following reasons:</span></span>  
  
- <span data-ttu-id="ca4b9-146">Die Werte werden als Binär Werte behandelt:</span><span class="sxs-lookup"><span data-stu-id="ca4b9-146">The values are treated as binary:</span></span>  
  
     <span data-ttu-id="ca4b9-147">3 in binärer Form = 011</span><span class="sxs-lookup"><span data-stu-id="ca4b9-147">3 in binary form = 011</span></span>  
  
     <span data-ttu-id="ca4b9-148">5 in binärer Form = 101</span><span class="sxs-lookup"><span data-stu-id="ca4b9-148">5 in binary form = 101</span></span>  
  
- <span data-ttu-id="ca4b9-149">Der `And` Operator vergleicht die binären Darstellungen, eine binäre Position (Bit) gleichzeitig.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-149">The `And` operator compares the binary representations, one binary position (bit) at a time.</span></span> <span data-ttu-id="ca4b9-150">Wenn beide Bits an einer bestimmten Position 1 sind, wird ein 1 an dieser Position im Ergebnis platziert.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-150">If both bits at a given position are 1, then a 1 is placed in that position in the result.</span></span> <span data-ttu-id="ca4b9-151">Wenn ein Bit 0 (null) ist, wird ein Wert von 0 an dieser Position im Ergebnis eingefügt.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-151">If either bit is 0, then a 0 is placed in that position in the result.</span></span> <span data-ttu-id="ca4b9-152">Im vorangehenden Beispiel funktioniert dies wie folgt:</span><span class="sxs-lookup"><span data-stu-id="ca4b9-152">In the preceding example this works out as follows:</span></span>  
  
     <span data-ttu-id="ca4b9-153">011 (3 in binärer Form)</span><span class="sxs-lookup"><span data-stu-id="ca4b9-153">011 (3 in binary form)</span></span>  
  
     <span data-ttu-id="ca4b9-154">101 (5 in binärer Form)</span><span class="sxs-lookup"><span data-stu-id="ca4b9-154">101 (5 in binary form)</span></span>  
  
     <span data-ttu-id="ca4b9-155">001 (das Ergebnis, in binärer Form)</span><span class="sxs-lookup"><span data-stu-id="ca4b9-155">001 (The result, in binary form)</span></span>  
  
- <span data-ttu-id="ca4b9-156">Das Ergebnis wird als Dezimalwert behandelt.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-156">The result is treated as decimal.</span></span> <span data-ttu-id="ca4b9-157">Der Wert 001 ist die binäre Darstellung von 1, sodass `x` = 1 ist.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-157">The value 001 is the binary representation of 1, so `x` = 1.</span></span>  
  
 <span data-ttu-id="ca4b9-158">Der `Or` bitweise Vorgang ist ähnlich, mit der Ausnahme, dass dem Ergebnisbit 1 zugewiesen wird, wenn eine oder beide der verglichenen Bits 1 sind.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-158">The bitwise `Or` operation is similar, except that a 1 is assigned to the result bit if either or both of the compared bits is 1.</span></span> <span data-ttu-id="ca4b9-159">`Xor`weist dem Ergebnisbit 1 zu, wenn genau eines der verglichenen Bits (nicht beides) 1 ist.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-159">`Xor` assigns a 1 to the result bit if exactly one of the compared bits (not both) is 1.</span></span> <span data-ttu-id="ca4b9-160">`Not`nimmt einen einzelnen Operanden an und kehrt alle Bits einschließlich des Signier Bits um und weist diesen Wert dem Ergebnis zu.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-160">`Not` takes a single operand and inverts all the bits, including the sign bit, and assigns that value to the result.</span></span> <span data-ttu-id="ca4b9-161">Dies bedeutet, dass für positive Zahlen mit Vorzeichen `Not` immer einen negativen Wert zurückgibt, und bei negativen Zahlen wird `Not` immer ein positiver oder NULL-Wert zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-161">This means that for signed positive numbers, `Not` always returns a negative value, and for negative numbers, `Not` always returns a positive or zero value.</span></span>  
  
 <span data-ttu-id="ca4b9-162">`AndAlso` `OrElse` Bitweise Vorgänge werden von den Operatoren und nicht unterstützt.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-162">The `AndAlso` and `OrElse` operators do not support bitwise operations.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="ca4b9-163">Bitweise Vorgänge können nur für ganzzahlige Typen ausgeführt werden.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-163">Bitwise operations can be performed on integral types only.</span></span> <span data-ttu-id="ca4b9-164">Gleit Komma Werte müssen in ganzzahlige Typen konvertiert werden, bevor die bitweise Operation fortgesetzt werden kann.</span><span class="sxs-lookup"><span data-stu-id="ca4b9-164">Floating-point values must be converted to integral types before bitwise operation can proceed.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="ca4b9-165">Weitere Informationen</span><span class="sxs-lookup"><span data-stu-id="ca4b9-165">See also</span></span>

- [<span data-ttu-id="ca4b9-166">Logische/bitweise Operatoren (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="ca4b9-166">Logical/Bitwise Operators (Visual Basic)</span></span>](../../../language-reference/operators/logical-bitwise-operators.md)
- [<span data-ttu-id="ca4b9-167">Boolesche Ausdrücke</span><span class="sxs-lookup"><span data-stu-id="ca4b9-167">Boolean Expressions</span></span>](boolean-expressions.md)
- [<span data-ttu-id="ca4b9-168">Arithmetische Operatoren in Visual Basic</span><span class="sxs-lookup"><span data-stu-id="ca4b9-168">Arithmetic Operators in Visual Basic</span></span>](arithmetic-operators.md)
- [<span data-ttu-id="ca4b9-169">Comparison Operators in Visual Basic</span><span class="sxs-lookup"><span data-stu-id="ca4b9-169">Comparison Operators in Visual Basic</span></span>](comparison-operators.md)
- [<span data-ttu-id="ca4b9-170">Verkettungsoperatoren in Visual Basic</span><span class="sxs-lookup"><span data-stu-id="ca4b9-170">Concatenation Operators in Visual Basic</span></span>](concatenation-operators.md)
- [<span data-ttu-id="ca4b9-171">Effiziente Kombination von Operatoren</span><span class="sxs-lookup"><span data-stu-id="ca4b9-171">Efficient Combination of Operators</span></span>](efficient-combination-of-operators.md)
