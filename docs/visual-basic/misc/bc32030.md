---
title: '&#39;#ElseIf&#39; kann nicht auf folgen &#39;#Else&#39; als Teil einer &#39;#If&#39; Block'
ms.date: 07/20/2015
f1_keywords:
- bc32030
- vbc32030
helpviewer_keywords:
- BC32030
ms.assetid: 248d6464-3019-4753-8a33-7070bbe5d2a6
ms.openlocfilehash: 9f850162d9d82edbd60baef8c010e97ed17f9d8d
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 05/04/2018
ms.locfileid: "33626648"
---
# <a name="39elseif39-cannot-follow-39else39-as-part-of-an-39if39-block"></a><span data-ttu-id="812f4-102">&#39;#ElseIf&#39; kann nicht auf folgen &#39;#Else&#39; als Teil einer &#39;#If&#39; Block</span><span class="sxs-lookup"><span data-stu-id="812f4-102">&#39;#ElseIf&#39; cannot follow &#39;#Else&#39; as part of an &#39;#If&#39; block</span></span>
<span data-ttu-id="812f4-103">Eine bedingte `#ElseIf` -Kompilierungsdirektive folgt auf eine `#Else` -Direktive.</span><span class="sxs-lookup"><span data-stu-id="812f4-103">An `#ElseIf` conditional compilation directive follows an `#Else` directive.</span></span> <span data-ttu-id="812f4-104">`#Else` muss die letzte Direktive im bedingten Block vor der `#End If` -Direktive sein.</span><span class="sxs-lookup"><span data-stu-id="812f4-104">`#Else` must be the last directive in the conditional block before the `#End If` directive.</span></span>  
  
 <span data-ttu-id="812f4-105">**Fehler-ID:** BC32030</span><span class="sxs-lookup"><span data-stu-id="812f4-105">**Error ID:** BC32030</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="812f4-106">So beheben Sie diesen Fehler</span><span class="sxs-lookup"><span data-stu-id="812f4-106">To correct this error</span></span>  
  
1.  <span data-ttu-id="812f4-107">Überprüfen Sie, ob das vorausgehende `#Else` ein `#ElseIf`sein soll.</span><span class="sxs-lookup"><span data-stu-id="812f4-107">Check if the preceding `#Else` should be an `#ElseIf`.</span></span>  
  
2.  <span data-ttu-id="812f4-108">Überprüfen Sie, ob ein vorausgehender `#If` -Block ordnungsgemäß abgeschlossen wurde und ein neuer `#If` -Block initiiert wird.</span><span class="sxs-lookup"><span data-stu-id="812f4-108">Check that a preceding `#If` block is properly terminated and that a new `#If` block is initiated.</span></span>  
  
3.  <span data-ttu-id="812f4-109">Wenn alles andere richtig ist, verschieben Sie diese `#ElseIf` -Direktive und ihren entsprechenden Anweisungsblock, damit diese dem `#Else` -Block vorangestellt sind.</span><span class="sxs-lookup"><span data-stu-id="812f4-109">If everything else is correct, move this `#ElseIf` directive and its corresponding statement block to precede the `#Else` block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="812f4-110">Siehe auch</span><span class="sxs-lookup"><span data-stu-id="812f4-110">See Also</span></span>  
 [<span data-ttu-id="812f4-111">#If...Then...#Else-Anweisungen</span><span class="sxs-lookup"><span data-stu-id="812f4-111">#If...Then...#Else Directives</span></span>](../../visual-basic/language-reference/directives/if-then-else-directives.md)
