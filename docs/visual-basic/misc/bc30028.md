---
title: '&#39; #else &#39; muss ein entsprechender &#39; #If &#39;vorangestellt werden; oder &#39; #ElseIf &#39;'
ms.date: 07/20/2015
ms.prod: .net
ms.technology: devlang-visual-basic
ms.topic: article
f1_keywords:
- vbc30028
- bc30028
helpviewer_keywords: BC30028
ms.assetid: c6ed34de-d6ed-4227-9880-538055aff20a
caps.latest.revision: "12"
author: dotnet-bot
ms.author: dotnetcontent
ms.openlocfilehash: a64a06505996df3c7f244c617c5f4744504ae6af
ms.sourcegitcommit: bd1ef61f4bb794b25383d3d72e71041a5ced172e
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 10/18/2017
---
# <a name="39else39-must-be-preceded-by-a-matching-39if39-or-39elseif39"></a><span data-ttu-id="6aab3-102">&#39; #else &#39; muss ein entsprechender &#39; #If &#39;vorangestellt werden; oder &#39; #ElseIf &#39;</span><span class="sxs-lookup"><span data-stu-id="6aab3-102">&#39;#Else&#39; must be preceded by a matching &#39;#If&#39; or &#39;#ElseIf&#39;</span></span>
<span data-ttu-id="6aab3-103">`#Else` ist eine Direktive für die bedingte Kompilierung.</span><span class="sxs-lookup"><span data-stu-id="6aab3-103">`#Else` is a conditional compilation directive.</span></span> <span data-ttu-id="6aab3-104">Einer `#Else` -Direktive ist keine entsprechende `#If` - oder `#ElseIf` -Direktive vorangestellt.</span><span class="sxs-lookup"><span data-stu-id="6aab3-104">An `#Else` directive is not preceded by a corresponding `#If` or `#ElseIf` directive.</span></span>  
  
 <span data-ttu-id="6aab3-105">**Fehler-ID:** BC30028</span><span class="sxs-lookup"><span data-stu-id="6aab3-105">**Error ID:** BC30028</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="6aab3-106">So beheben Sie diesen Fehler</span><span class="sxs-lookup"><span data-stu-id="6aab3-106">To correct this error</span></span>  
  
1.  <span data-ttu-id="6aab3-107">Stellen Sie sicher, dass eine vorausgehende `#If` - oder `#ElseIf` -Direktive nicht durch einen dazwischen liegenden Block für die bedingte Kompilierung oder eine falsch platzierte `#Else` -Direktive von dieser `#End If`-Direktive getrennt ist.</span><span class="sxs-lookup"><span data-stu-id="6aab3-107">Check that a preceding `#If` or `#ElseIf` is not separated from this `#Else` by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
2.  <span data-ttu-id="6aab3-108">Überprüfen Sie, ob `#Else` eine andere `#Else` -Direktive vorangestellt ist.</span><span class="sxs-lookup"><span data-stu-id="6aab3-108">Check that `#Else` is preceded by another `#Else` directive.</span></span> <span data-ttu-id="6aab3-109">Wenn dies der Fall ist, entfernen Sie `#Else` , oder ändern Sie die Direktive in `#ElseIf`.</span><span class="sxs-lookup"><span data-stu-id="6aab3-109">If it is, either remove `#Else` or change it to an `#ElseIf`.</span></span>  
  
3.  <span data-ttu-id="6aab3-110">Falls der Code ansonsten in Ordnung ist, fügen Sie am Anfang des Blocks für die bedingte Kompilierung eine `#If` -Direktive hinzu.</span><span class="sxs-lookup"><span data-stu-id="6aab3-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="6aab3-111">Siehe auch</span><span class="sxs-lookup"><span data-stu-id="6aab3-111">See Also</span></span>  
 [<span data-ttu-id="6aab3-112">#If...Then...#Else-Anweisungen</span><span class="sxs-lookup"><span data-stu-id="6aab3-112">#If...Then...#Else Directives</span></span>](../../visual-basic/language-reference/directives/if-then-else-directives.md)
