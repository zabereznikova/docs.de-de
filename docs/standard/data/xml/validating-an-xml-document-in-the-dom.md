---
title: Validieren eines XML-Dokuments im Dokumentobjektmodell
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
- cpp
ms.assetid: 2c61c920-d0f8-4c72-bfcc-6524570f3060
ms.openlocfilehash: 949dc52c332b17784b0e1851d178465fe4881b6f
ms.sourcegitcommit: 33deec3e814238fb18a49b2a7e89278e27888291
ms.translationtype: HT
ms.contentlocale: de-DE
ms.lasthandoff: 06/02/2020
ms.locfileid: "84287635"
---
# <a name="validating-an-xml-document-in-the-dom"></a><span data-ttu-id="ce019-102">Validieren eines XML-Dokuments im Dokumentobjektmodell</span><span class="sxs-lookup"><span data-stu-id="ce019-102">Validating an XML Document in the DOM</span></span>

<span data-ttu-id="ce019-103">Die Klasse <xref:System.Xml.XmlDocument> validiert das XML-Dokument im DOM (Dokumentobjektmodell) standardmäßig nicht anhand eines XSD-Schemas (XML Schema Definition Language) oder einer DTD (Document Type Definition). Das XML-Dokument wird nur validiert, damit es wohlgeformt ist.</span><span class="sxs-lookup"><span data-stu-id="ce019-103">The<xref:System.Xml.XmlDocument> class does not validate the XML in the Document Object Model (DOM) against an XML Schema definition language (XSD) schema or document type definition (DTD) by default; the XML is only verified to be well-formed.</span></span>

<span data-ttu-id="ce019-104">Wenn Sie das XML-Dokument im DOM validieren möchten, können Sie es beim Laden in das DOM validieren, indem Sie einen schemavalidierenden <xref:System.Xml.XmlReader> an die <xref:System.Xml.XmlDocument.Load%2A>-Methode der <xref:System.Xml.XmlDocument>-Klasse übergeben, oder Sie validieren ein zuvor nicht validiertes XML-Dokument im DOM mithilfe der <xref:System.Xml.XmlDocument.Validate%2A>-Methode der <xref:System.Xml.XmlDocument>-Klasse.</span><span class="sxs-lookup"><span data-stu-id="ce019-104">To validate the XML in the DOM, you can validate the XML as it is loaded into the DOM by passing a schema-validating <xref:System.Xml.XmlReader> to the <xref:System.Xml.XmlDocument.Load%2A> method of the <xref:System.Xml.XmlDocument> class, or validate a previously unvalidated XML document in the DOM using the <xref:System.Xml.XmlDocument.Validate%2A> method of the <xref:System.Xml.XmlDocument> class.</span></span>

## <a name="validating-an-xml-document-as-it-is-loaded-into-the-dom"></a><span data-ttu-id="ce019-105">Validieren eines XML-Dokuments beim Laden in das Dokumentobjektmodell</span><span class="sxs-lookup"><span data-stu-id="ce019-105">Validating an XML Document As It Is Loaded into the DOM</span></span>

<span data-ttu-id="ce019-106">Die <xref:System.Xml.XmlDocument>-Klasse validiert die XML-Daten beim Laden in das DOM, wenn ein validierender <xref:System.Xml.XmlReader> an die <xref:System.Xml.XmlDocument.Load%2A>-Methode der <xref:System.Xml.XmlDocument>-Klasse übergeben wird.</span><span class="sxs-lookup"><span data-stu-id="ce019-106">The <xref:System.Xml.XmlDocument> class validates the XML data as it is loaded into the DOM when a validating <xref:System.Xml.XmlReader> is passed to the <xref:System.Xml.XmlDocument.Load%2A> method of the <xref:System.Xml.XmlDocument> class.</span></span>

<span data-ttu-id="ce019-107">Nach erfolgreicher Validierung werden Schemastandards angewendet, Textwerte bei Bedarf in atomare Werte konvertiert und Typinformationen validierten Informationselementen zugeordnet.</span><span class="sxs-lookup"><span data-stu-id="ce019-107">After successful validation, schema defaults are applied, text values are converted to atomic values as necessary, and type information is associated with validated information items.</span></span> <span data-ttu-id="ce019-108">Daher werden zuvor nicht typisierte XML-Daten durch typisierte XML-Daten ersetzt.</span><span class="sxs-lookup"><span data-stu-id="ce019-108">As a result, typed XML data replaces previously untyped XML data.</span></span>

### <a name="creating-an-xml-schema-validating-xmlreader"></a><span data-ttu-id="ce019-109">Erstellen eines "XmlReader" zum Validieren von XML-Schemata</span><span class="sxs-lookup"><span data-stu-id="ce019-109">Creating an XML Schema-Validating XmlReader</span></span>

<span data-ttu-id="ce019-110">Wenn Sie einen <xref:System.Xml.XmlReader> zum Validieren von XML-Schemata erstellen möchten, führen Sie folgende Schritte aus:</span><span class="sxs-lookup"><span data-stu-id="ce019-110">To create an XML schema-validating <xref:System.Xml.XmlReader>, follow these steps.</span></span>

1. <span data-ttu-id="ce019-111">Erstellen Sie eine neue <xref:System.Xml.XmlReaderSettings>-Instanz.</span><span class="sxs-lookup"><span data-stu-id="ce019-111">Construct a new <xref:System.Xml.XmlReaderSettings> instance.</span></span>

2. <span data-ttu-id="ce019-112">Fügen Sie der <xref:System.Xml.XmlReaderSettings.Schemas%2A>-Eigenschaft der <xref:System.Xml.XmlReaderSettings>-Instanz ein XML-Schema hinzu.</span><span class="sxs-lookup"><span data-stu-id="ce019-112">Add an XML schema to the <xref:System.Xml.XmlReaderSettings.Schemas%2A> property of the <xref:System.Xml.XmlReaderSettings> instance.</span></span>

3. <span data-ttu-id="ce019-113">Geben Sie `Schema` als den <xref:System.Xml.XmlReaderSettings.ValidationType%2A> an.</span><span class="sxs-lookup"><span data-stu-id="ce019-113">Specify `Schema` as the <xref:System.Xml.XmlReaderSettings.ValidationType%2A>.</span></span>

4. <span data-ttu-id="ce019-114">Geben Sie optional <xref:System.Xml.XmlReaderSettings.ValidationFlags%2A> und einen <xref:System.Xml.XmlReaderSettings.ValidationEventHandler> an, um während der Schemavalidierung auftretende Fehler und Warnungen zu behandeln.</span><span class="sxs-lookup"><span data-stu-id="ce019-114">Optionally specify <xref:System.Xml.XmlReaderSettings.ValidationFlags%2A> and a <xref:System.Xml.XmlReaderSettings.ValidationEventHandler> to handle schema validation errors and warnings encountered during validation.</span></span>

5. <span data-ttu-id="ce019-115">Übergeben Sie abschließend das <xref:System.Xml.XmlReaderSettings>-Objekt zusammen mit dem XML-Dokument an die <xref:System.Xml.XmlReader.Create%2A>-Methode der <xref:System.Xml.XmlReader>-Klasse. Dabei wird ein schemavalidierender <xref:System.Xml.XmlReader> erstellt.</span><span class="sxs-lookup"><span data-stu-id="ce019-115">Finally, pass the <xref:System.Xml.XmlReaderSettings> object to the <xref:System.Xml.XmlReader.Create%2A> method of the <xref:System.Xml.XmlReader> class along with the XML document, creating a schema-validating <xref:System.Xml.XmlReader>.</span></span>

### <a name="example"></a><span data-ttu-id="ce019-116">Beispiel</span><span class="sxs-lookup"><span data-stu-id="ce019-116">Example</span></span>

<span data-ttu-id="ce019-117">Im folgenden Codebeispiel validiert ein schemavalidierender <xref:System.Xml.XmlReader> die in das DOM geladenen XML-Daten.</span><span class="sxs-lookup"><span data-stu-id="ce019-117">In the code example that follows, a schema-validating <xref:System.Xml.XmlReader> validates the XML data loaded into the DOM.</span></span> <span data-ttu-id="ce019-118">Es werden ungültige Änderungen am XML-Dokument vorgenommen, und das Dokument wird anschließend erneut validiert, was zu Schemavalidierungsfehlern führt.</span><span class="sxs-lookup"><span data-stu-id="ce019-118">Invalid modifications are made to the XML document and the document is then revalidated, causing schema validation errors.</span></span> <span data-ttu-id="ce019-119">Abschließend wird einer der Fehler korrigiert, und anschließend werden Teile des XML-Dokuments teilweise validiert.</span><span class="sxs-lookup"><span data-stu-id="ce019-119">Finally, one of the errors is corrected, and then part of the XML document is partially validated.</span></span>

[!code-cpp[XmlDocumentValidation.Load#1](../../../../samples/snippets/cpp/VS_Snippets_Data/XmlDocumentValidation.Load/CPP/XmlDocumentValidationExample.cpp#1)]
[!code-csharp[XmlDocumentValidation.Load#1](../../../../samples/snippets/csharp/VS_Snippets_Data/XmlDocumentValidation.Load/CS/XmlDocumentValidationExample.cs#1)]
[!code-vb[XmlDocumentValidation.Load#1](../../../../samples/snippets/visualbasic/VS_Snippets_Data/XmlDocumentValidation.Load/VB/XmlDocumentValidationExample.vb#1)]

<span data-ttu-id="ce019-120">In diesem Beispiel wird die Datei `contosoBooks.xml` als Eingabe verwendet.</span><span class="sxs-lookup"><span data-stu-id="ce019-120">The example takes the `contosoBooks.xml` file as input.</span></span>

[!code-xml[XPathXMLExamples#2](../../../../samples/snippets/xml/VS_Snippets_Data/XPathXMLExamples/XML/contosoBooks.xml#2)]

<span data-ttu-id="ce019-121">In diesem Beispiel wird auch die Datei `contosoBooks.xsd` als Eingabe verwendet.</span><span class="sxs-lookup"><span data-stu-id="ce019-121">The example also takes the `contosoBooks.xsd` file as input.</span></span>

[!code-xml[XPathXMLExamples#3](../../../../samples/snippets/xml/VS_Snippets_Data/XPathXMLExamples/XML/contosoBooks.xsd#3)]

<span data-ttu-id="ce019-122">Beachten Sie Folgendes, wenn Sie XML-Daten beim Laden in das DOM validieren.</span><span class="sxs-lookup"><span data-stu-id="ce019-122">Consider the following when validating XML data as it is loaded into the DOM.</span></span>

- <span data-ttu-id="ce019-123">Im vorangehenden Beispiel wird der <xref:System.Xml.XmlReaderSettings.ValidationEventHandler> immer aufgerufen, sobald ein ungültiger Typ erkannt wird.</span><span class="sxs-lookup"><span data-stu-id="ce019-123">In the above example, the <xref:System.Xml.XmlReaderSettings.ValidationEventHandler> is called whenever an invalid type is encountered.</span></span> <span data-ttu-id="ce019-124">Wenn im validierenden <xref:System.Xml.XmlReaderSettings.ValidationEventHandler> kein <xref:System.Xml.XmlReader> festgelegt ist, wird beim Aufrufen von <xref:System.Xml.Schema.XmlSchemaValidationException> eine <xref:System.Xml.XmlDocument.Load%2A> ausgelöst, wenn ein Attribut- oder Elementtyp nicht mit dem entsprechenden Typ übereinstimmt, der im validierenden Schema angegeben ist.</span><span class="sxs-lookup"><span data-stu-id="ce019-124">If a <xref:System.Xml.XmlReaderSettings.ValidationEventHandler> is not set on the validating <xref:System.Xml.XmlReader>,an <xref:System.Xml.Schema.XmlSchemaValidationException> is thrown when <xref:System.Xml.XmlDocument.Load%2A> is called if any attribute or element type does not match the corresponding type specified in the validating schema.</span></span>

- <span data-ttu-id="ce019-125">Wenn ein XML-Dokument in ein <xref:System.Xml.XmlDocument>-Objekt mit einem zugeordneten Schema geladen wird, das Standardwerte definiert, behandelt das <xref:System.Xml.XmlDocument>-Objekt diese Standardwerte, als wären diese im XML-Dokument enthalten.</span><span class="sxs-lookup"><span data-stu-id="ce019-125">When an XML document is loaded into an <xref:System.Xml.XmlDocument> object with an associated schema that defines default values, the <xref:System.Xml.XmlDocument> treats these defaults as if they appeared in the XML document.</span></span> <span data-ttu-id="ce019-126">Das bedeutet, dass die <xref:System.Xml.XmlReader.IsEmptyElement%2A>-Eigenschaft immer den Wert `false` für ein Element zurückgibt, das als Standardwert aus dem Schema entnommen wurde.</span><span class="sxs-lookup"><span data-stu-id="ce019-126">This means that the <xref:System.Xml.XmlReader.IsEmptyElement%2A> property always returns `false` for an element that was defaulted from the schema.</span></span> <span data-ttu-id="ce019-127">Es wird auch im XML-Dokument als leeres Element geschrieben.</span><span class="sxs-lookup"><span data-stu-id="ce019-127">Even if in the XML document, it was written as an empty element.</span></span>

## <a name="validating-an-xml-document-in-the-dom"></a><span data-ttu-id="ce019-128">Validieren eines XML-Dokuments im Dokumentobjektmodell</span><span class="sxs-lookup"><span data-stu-id="ce019-128">Validating an XML Document in the DOM</span></span>

<span data-ttu-id="ce019-129">Die <xref:System.Xml.XmlDocument.Validate%2A>-Methode der <xref:System.Xml.XmlDocument>-Klasse validiert die in das DOM geladenen XML-Daten anhand der Schemata in der <xref:System.Xml.XmlDocument>-Eigenschaft des <xref:System.Xml.XmlDocument.Schemas%2A>-Objekts.</span><span class="sxs-lookup"><span data-stu-id="ce019-129">The <xref:System.Xml.XmlDocument.Validate%2A> method of the <xref:System.Xml.XmlDocument> class validates the XML data loaded in the DOM against the schemas in the <xref:System.Xml.XmlDocument> object's <xref:System.Xml.XmlDocument.Schemas%2A> property.</span></span> <span data-ttu-id="ce019-130">Nach erfolgreicher Validierung werden Schemastandards angewendet, Textwerte bei Bedarf in atomare Werte konvertiert und Typinformationen validierten Informationselementen zugeordnet.</span><span class="sxs-lookup"><span data-stu-id="ce019-130">After successful validation, schema defaults are applied, text values are converted to atomic values as necessary, and type information is associated with validated information items.</span></span> <span data-ttu-id="ce019-131">Daher werden zuvor nicht typisierte XML-Daten durch typisierte XML-Daten ersetzt.</span><span class="sxs-lookup"><span data-stu-id="ce019-131">As a result, typed XML data replaces previously untyped XML data.</span></span>

<span data-ttu-id="ce019-132">Das folgende Beispiel ähnelt dem Beispiel unter "Validieren eines XML-Dokuments beim Laden in das Dokumentobjektmodell".</span><span class="sxs-lookup"><span data-stu-id="ce019-132">The example below is similar to the example in "Validating an XML Document As It Is Loaded into the DOM" above.</span></span> <span data-ttu-id="ce019-133">In diesem Beispiel wird das XML-Dokument nicht beim Laden in das DOM validiert, sondern nachdem es mithilfe der <xref:System.Xml.XmlDocument.Validate%2A>-Methode der <xref:System.Xml.XmlDocument>-Klasse in das DOM geladen wurde.</span><span class="sxs-lookup"><span data-stu-id="ce019-133">In this example, the XML document is not validated as it is loaded into the DOM, but rather is validated after it has been loaded into the DOM using the <xref:System.Xml.XmlDocument.Validate%2A> method of the <xref:System.Xml.XmlDocument> class.</span></span> <span data-ttu-id="ce019-134">Die <xref:System.Xml.XmlDocument.Validate%2A>-Methode validiert das XML-Dokument anhand der in der <xref:System.Xml.XmlDocument.Schemas%2A>-Eigenschaft des <xref:System.Xml.XmlDocument> enthaltenen XML-Schemata.</span><span class="sxs-lookup"><span data-stu-id="ce019-134">The <xref:System.Xml.XmlDocument.Validate%2A> method validates the XML document against the XML schemas contained in the <xref:System.Xml.XmlDocument.Schemas%2A> property of the <xref:System.Xml.XmlDocument>.</span></span> <span data-ttu-id="ce019-135">Anschließend werden am XML-Dokument ungültige Änderungen vorgenommen, und das Dokument wird dann erneut validiert, was zu Schemavalidierungsfehlern führt.</span><span class="sxs-lookup"><span data-stu-id="ce019-135">Invalid modifications are then made to the XML document, and the document is then revalidated, causing schema validation errors.</span></span> <span data-ttu-id="ce019-136">Abschließend wird einer der Fehler korrigiert, und anschließend werden Teile des XML-Dokuments teilweise validiert.</span><span class="sxs-lookup"><span data-stu-id="ce019-136">Finally, one of the errors is corrected, and then part of the XML document is partially validated.</span></span>

[!code-csharp[XmlDocumentValidation.Validate#1](../../../../samples/snippets/csharp/VS_Snippets_Data/XmlDocumentValidation.Validate/CS/XmlDocumentValidationExample.cs#1)]
[!code-vb[XmlDocumentValidation.Validate#1](../../../../samples/snippets/visualbasic/VS_Snippets_Data/XmlDocumentValidation.Validate/VB/XmlDocumentValidationExample.vb#1)]

<span data-ttu-id="ce019-137">In dem Beispiel werden die Dateien `contosoBooks.xml` und `contosoBooks.xsd`, auf die unter "Validieren eines XML-Dokuments beim Laden in das Dokumentobjektmodell" verwiesen wird, als Eingabe verwendet.</span><span class="sxs-lookup"><span data-stu-id="ce019-137">The example takes as input the `contosoBooks.xml` and `contosoBooks.xsd` files referred to in "Validating an XML Document as it is Loaded into the DOM" above.</span></span>

## <a name="handling-validation-errors-and-warnings"></a><span data-ttu-id="ce019-138">Behandeln von Validierungsfehlern und Validierungswarnungen</span><span class="sxs-lookup"><span data-stu-id="ce019-138">Handling Validation Errors and Warnings</span></span>

<span data-ttu-id="ce019-139">Fehler bei der XML-Schema-Validierung werden gemeldet, wenn XML-Daten beim Laden in das DOM validiert werden.</span><span class="sxs-lookup"><span data-stu-id="ce019-139">XML schema validation errors are reported when validating XML data loaded in the DOM.</span></span> <span data-ttu-id="ce019-140">Ihnen werden alle Schemavalidierungsfehler mitgeteilt, die während der Validierung beim Laden oder während der Validierung eines zuvor nicht validierten XML-Dokuments auftreten.</span><span class="sxs-lookup"><span data-stu-id="ce019-140">You are notified about all schema validation errors found while validating the XML data as it is being loaded, or when validating a previously unvalidated XML document.</span></span>

<span data-ttu-id="ce019-141">Validierungsfehler werden vom <xref:System.Xml.Schema.ValidationEventHandler> behandelt.</span><span class="sxs-lookup"><span data-stu-id="ce019-141">Validation errors are handled by the <xref:System.Xml.Schema.ValidationEventHandler>.</span></span> <span data-ttu-id="ce019-142">Wenn ein <xref:System.Xml.Schema.ValidationEventHandler> der <xref:System.Xml.XmlReaderSettings>-Instanz zugeordnet oder an die <xref:System.Xml.XmlDocument.Validate%2A>-Methode der <xref:System.Xml.XmlDocument>-Klasse übergeben wurde, behandelt der <xref:System.Xml.Schema.ValidationEventHandler> Schemavalidierungsfehler. Andernfalls wird beim Auftreten eines Schemavalidierungsfehlers eine <xref:System.Xml.Schema.XmlSchemaValidationException> ausgelöst.</span><span class="sxs-lookup"><span data-stu-id="ce019-142">If a <xref:System.Xml.Schema.ValidationEventHandler> was assigned to the <xref:System.Xml.XmlReaderSettings> instance, or passed to the <xref:System.Xml.XmlDocument.Validate%2A> method of the <xref:System.Xml.XmlDocument> class, the <xref:System.Xml.Schema.ValidationEventHandler> will handle schema validation errors; otherwise an <xref:System.Xml.Schema.XmlSchemaValidationException> is raised when a schema validation error is encountered.</span></span>

> [!NOTE]
> <span data-ttu-id="ce019-143">Die XML-Daten werden trotz der Schemavalidierungsfehler in das DOM geladen, sofern der <xref:System.Xml.Schema.ValidationEventHandler> keine Ausnahme auslöst, um den Vorgang zu beenden.</span><span class="sxs-lookup"><span data-stu-id="ce019-143">The XML data is loaded into the DOM despite schema validation errors unless your <xref:System.Xml.Schema.ValidationEventHandler> raises an exception to stop the process.</span></span>
>
> <span data-ttu-id="ce019-144">Schemavalidierungswarnungen werden nicht gemeldet, sofern kein <xref:System.Xml.Schema.XmlSchemaValidationFlags.ReportValidationWarnings>-Flag für das <xref:System.Xml.XmlReaderSettings>-Objekt angegeben wurde.</span><span class="sxs-lookup"><span data-stu-id="ce019-144">Schema validation warnings are not reported unless the <xref:System.Xml.Schema.XmlSchemaValidationFlags.ReportValidationWarnings> flag is specified to the <xref:System.Xml.XmlReaderSettings> object.</span></span>

 <span data-ttu-id="ce019-145">Beispiele zur Erläuterung des <xref:System.Xml.Schema.ValidationEventHandler> finden Sie unter "Validieren eines XML-Dokuments beim Laden in das Dokumentobjektmodell" and "Validieren eines XML-Dokuments im Dokumentobjektmodell".</span><span class="sxs-lookup"><span data-stu-id="ce019-145">For examples illustrating the <xref:System.Xml.Schema.ValidationEventHandler>, see "Validating an XML Document As It Is Loaded into the DOM" and "Validating an XML Document in the DOM" above.</span></span>

## <a name="see-also"></a><span data-ttu-id="ce019-146">Siehe auch</span><span class="sxs-lookup"><span data-stu-id="ce019-146">See also</span></span>

- <xref:System.Xml.XmlDocument>
- <xref:System.Xml.XmlReader>
- <xref:System.Xml.Schema.ValidationEventHandler>
- <xref:System.Xml.XmlReaderSettings>
- [<span data-ttu-id="ce019-147">Verarbeiten von XML-Daten mithilfe des DOM</span><span class="sxs-lookup"><span data-stu-id="ce019-147">Process XML Data Using the DOM Model</span></span>](process-xml-data-using-the-dom-model.md)
- [<span data-ttu-id="ce019-148">Arbeiten mit XML-Schemata</span><span class="sxs-lookup"><span data-stu-id="ce019-148">Working with XML Schemas</span></span>](working-with-xml-schemas.md)
