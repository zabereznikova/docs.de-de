---
title: Auflösen von externen Ressourcen
ms.date: 03/30/2017
ms.technology: dotnet-standard
ms.assetid: ad3fa320-4b8f-4e5c-b549-01157591007a
ms.openlocfilehash: 05cc41cef7da07581d4f0ec8e584858b913d1a80
ms.sourcegitcommit: 5f236cd78cf09593c8945a7d753e0850e96a0b80
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 01/07/2020
ms.locfileid: "75710270"
---
# <a name="resolving-external-resources"></a><span data-ttu-id="7708b-102">Auflösen von externen Ressourcen</span><span class="sxs-lookup"><span data-stu-id="7708b-102">Resolving External Resources</span></span>
<span data-ttu-id="7708b-103">Mit der **XmlResolver**-Eigenschaft von **XmlDocument** sucht die **XmlDocument**-Klasse nach Ressourcen, die sich nicht inline in den XML-Daten befinden, z.B. externe Document Type Definitions (DTDs – Dokumenttypdefinitionen), Entitäten und Schemata.</span><span class="sxs-lookup"><span data-stu-id="7708b-103">The **XmlResolver** property of the **XmlDocument** is used by the **XmlDocument** class to locate resources that are not inline in the XML data, such as external document type definitions (DTDs), entities, and schemas.</span></span> <span data-ttu-id="7708b-104">Diese Elemente können in einem Netzwerk oder auf einem lokalen Laufwerk gesucht werden und sind durch einen URI (Uniform Resource Identifier) gekennzeichnet.</span><span class="sxs-lookup"><span data-stu-id="7708b-104">These items can be located on a network or on a local drive, and are identifiable by a Uniform Resource Identifier (URI).</span></span> <span data-ttu-id="7708b-105">Somit kann **XmlDocument** im Dokument vorhandene **EntityReference**-Knoten auflösen und das Dokument entsprechend der externen DTD oder dem externen Schema validieren.</span><span class="sxs-lookup"><span data-stu-id="7708b-105">This allows the **XmlDocument** to resolve **EntityReference** nodes that are present in the document and validate the document according to the external DTD or schema.</span></span>  
  
## <a name="fully-trusted-xmldocument"></a><span data-ttu-id="7708b-106">Vollständig vertrauenswürdiges XmlDocument</span><span class="sxs-lookup"><span data-stu-id="7708b-106">Fully-Trusted XmlDocument</span></span>  
 <span data-ttu-id="7708b-107">Die **XmlResolver**-Eigenschaft beeinflusst die Funktionalität der **XmlDocument.Load**-Methode.</span><span class="sxs-lookup"><span data-stu-id="7708b-107">The **XmlResolver** property affects the functionality of the **XmlDocument.Load** method.</span></span> <span data-ttu-id="7708b-108">In der unten stehenden Tabelle ist die Funktionsweise der **XmlDocument.XmlResolver**-Eigenschaft bei einem vollständig vertrauenswürdigen **XmlDocument**-Objekt veranschaulicht.</span><span class="sxs-lookup"><span data-stu-id="7708b-108">The table below shows how the **XmlDocument.XmlResolver** property works when the **XmlDocument** object is fully trusted.</span></span> <span data-ttu-id="7708b-109">In der folgenden Tabelle werden die **XmlDocument.Load**-Methoden veranschaulicht, wenn die Eingabe für die Load-Methode aus einem **TextReader**, einem **String**, einem **Stream** oder einem **URI** besteht.</span><span class="sxs-lookup"><span data-stu-id="7708b-109">The following table shows the **XmlDocument.Load** methods when the input to the Load is a **TextReader**, **String**, **Stream**, or **URI**.</span></span> <span data-ttu-id="7708b-110">Diese Tabelle gilt nicht für die **Load**-Methode, wenn das **XmlDocument** aus einem **XmlReader** geladen wird.</span><span class="sxs-lookup"><span data-stu-id="7708b-110">This table does not apply to the **Load** method if the **XmlDocument** is loaded from an **XmlReader**.</span></span>  
  
|<span data-ttu-id="7708b-111">XmlResolver-Eigenschaft</span><span class="sxs-lookup"><span data-stu-id="7708b-111">XmlResolver property</span></span>|<span data-ttu-id="7708b-112">Funktion</span><span class="sxs-lookup"><span data-stu-id="7708b-112">Function</span></span>|<span data-ttu-id="7708b-113">Hinweise</span><span class="sxs-lookup"><span data-stu-id="7708b-113">Notes</span></span>|  
|--------------------------|--------------|-----------|  
|<span data-ttu-id="7708b-114">Die Eigenschaft ist auf eine **XmlResolver**-Klasse festgelegt, die zuvor erstellt wurde und deren Eigenschaften bereits vom Benutzer festgelegt wurden.</span><span class="sxs-lookup"><span data-stu-id="7708b-114">The property is set to an **XmlResolver** class that has been previously created and has properties already set on it by the user.</span></span>|<span data-ttu-id="7708b-115">Das **XmlDocument** verwendet den **XmlResolver**, der zum Auflösen von Dateinamen sowie von Verweisen auf externe Ressourcen wie DTDs, Entitäten und Schemata vorgesehen ist.</span><span class="sxs-lookup"><span data-stu-id="7708b-115">The **XmlDocument** uses the **XmlResolver** that is given to resolve file names, to resolve references to external resources such as DTDs, entities, and schemas.</span></span><br /><br /> <span data-ttu-id="7708b-116">Der **XmlResolver** wird auch beim Auflösen externer Ressourcen verwendet, die beim Hinzufügen oder Bearbeiten von Knoten in **XmlDocument** benötigt werden.</span><span class="sxs-lookup"><span data-stu-id="7708b-116">The **XmlResolver** is also used when resolving external resources that are needed when adding or editing nodes in the **XmlDocument**.</span></span>|<span data-ttu-id="7708b-117">Dem **XmlDocument** wird der **XmlResolver** zugewiesen, der zum Suchen und Auflösen externer Ressourcen verwendet wird.</span><span class="sxs-lookup"><span data-stu-id="7708b-117">The **XmlResolver** given to the **XmlDocument** is the resolver that is used whenever external resources need to be located and resolved.</span></span>|  
|<span data-ttu-id="7708b-118">Die Eigenschaft ist auf **NULL** festgelegt (**Nothing** in Microsoft Visual Basic .NET).</span><span class="sxs-lookup"><span data-stu-id="7708b-118">The property is set to **null** (**Nothing** in Microsoft Visual Basic .NET).</span></span>|<span data-ttu-id="7708b-119">Funktionen, für die eine externe Ressource erforderlich ist, werden nicht unterstützt, z. B. die Suche eines externen Schemas oder einer externen DTD.</span><span class="sxs-lookup"><span data-stu-id="7708b-119">Features that require an external resource are not supported, such as locating an external schema, or DTD.</span></span> <span data-ttu-id="7708b-120">Externe Entitäten werden ebenfalls nicht aufgelöst. Bearbeitungsfunktionen wie das Einfügen von Entitätsknoten, die aufgelöst werden müssen, werden nicht unterstützt.</span><span class="sxs-lookup"><span data-stu-id="7708b-120">Nor will external entities be resolved, and performing editing functions, such as inserting entity nodes that require resolution, are not supported.</span></span>|<span data-ttu-id="7708b-121">Das **XmlDocument** lädt Dateien anonym und versucht nicht, andere Ressourcen aufzulösen.</span><span class="sxs-lookup"><span data-stu-id="7708b-121">The **XmlDocument** loads files as anonymous and does not attempt to resolve any other resources.</span></span>|  
|<span data-ttu-id="7708b-122">Die Eigenschaft ist nicht festgelegt, sondern im Standardzustand belassen.</span><span class="sxs-lookup"><span data-stu-id="7708b-122">The property is not set, but left in its default state.</span></span>|<span data-ttu-id="7708b-123">Ein **XmlUrlResolver** mit NULL-Anmeldeinformationen wird instanziiert und vom **XmlDocument** beim Auflösen von Dateinamen sowie beim Suchen externer DTDs, Entitäten und Schemata verwendet. **NULL**-Anmeldeinformationen werden beim Bearbeiten von Knoten verwendet.</span><span class="sxs-lookup"><span data-stu-id="7708b-123">An **XmlUrlResolver** with NULL credentials will be instantiated and used by the **XmlDocument** when resolving file names, locating external DTDs, entities, and schemas, and **null** credentials are used when editing nodes.</span></span>||  
  
 <span data-ttu-id="7708b-124">In der folgende Tabelle wird die **XmlDocument.Load**-Methode veranschaulicht, wenn die Eingabe für die **Load**-Methode aus einem **XmlReader** besteht und das **XmlDocument** vollständig vertrauenswürdig ist.</span><span class="sxs-lookup"><span data-stu-id="7708b-124">The following table shows the **XmlDocument.Load** method when the input to the **Load** is an **XmlReader** and the **XmlDocument** is fully trusted.</span></span>  
  
|<span data-ttu-id="7708b-125">XmlResolver-Eigenschaft</span><span class="sxs-lookup"><span data-stu-id="7708b-125">XmlResolver property</span></span>|<span data-ttu-id="7708b-126">Funktion</span><span class="sxs-lookup"><span data-stu-id="7708b-126">Function</span></span>|<span data-ttu-id="7708b-127">Hinweise</span><span class="sxs-lookup"><span data-stu-id="7708b-127">Notes</span></span>|  
|--------------------------|--------------|-----------|  
|<span data-ttu-id="7708b-128">Die **XmlResolver**-Klasse, die vom **XmlDocument** verwendet wird, ist die gleiche Klasse, die vom **XmlReader** verwendet wird.</span><span class="sxs-lookup"><span data-stu-id="7708b-128">The **XmlResolver** class used by the **XmlDocument** is the same class being used by the **XmlReader**.</span></span>|<span data-ttu-id="7708b-129">Das **XmlDocument** verwendet den **XmlResolver**, der dem **XmlReader** zugeordnet wurde.</span><span class="sxs-lookup"><span data-stu-id="7708b-129">The **XmlDocument** uses the **XmlResolver** that was assigned to the **XmlReader**.</span></span><br /><br /> <span data-ttu-id="7708b-130">Die **XmlDocument.Resolver**-Eigenschaft kann nicht unabhängig von der Vertrauensebene des **XmlDocument** festgelegt werden, da sie einen **XmlResolver** aus dem **XmlReader** abruft.</span><span class="sxs-lookup"><span data-stu-id="7708b-130">The **XmlDocument.Resolver** property cannot be set, regardless of the **XmlDocument** trust level, because it is getting an **XmlResolver** from the **XmlReader**.</span></span> <span data-ttu-id="7708b-131">Die Einstellungen des **XmlResolver** des **XmlReader** können nicht durch Festlegen der **XmlResolver**-Eigenschaft des **XmlDocument** überschrieben werden.</span><span class="sxs-lookup"><span data-stu-id="7708b-131">You cannot attempt to override the settings of the **XmlReaders**' **XmlResolver** by setting the **XmlResolver** property of the **XmlDocument**.</span></span>|<span data-ttu-id="7708b-132">Der **XmlReader** kann der **XmlTextReader**, der **XmlValidatingReader** oder ein benutzerdefinierter Reader sein.</span><span class="sxs-lookup"><span data-stu-id="7708b-132">The **XmlReader** can be the **XmlTextReader**, **XmlValidatingReader**, or a custom-written reader.</span></span> <span data-ttu-id="7708b-133">Wenn der verwendete Reader die Entitätsauflösung unterstützt, werden externe Entitäten aufgelöst.</span><span class="sxs-lookup"><span data-stu-id="7708b-133">If the reader used supports entity resolution, external entities are resolved.</span></span> <span data-ttu-id="7708b-134">Wenn der übergebene Reader keine Entitätsverweise unterstützt, werden Entitätsverweise nicht aufgelöst.</span><span class="sxs-lookup"><span data-stu-id="7708b-134">If the reader passed does not support entity references, then entity references are not resolved.</span></span>|  
  
## <a name="semi-trusted-xmldocument"></a><span data-ttu-id="7708b-135">Teilweise vertrauenswürdiges XmlDocument</span><span class="sxs-lookup"><span data-stu-id="7708b-135">Semi-Trusted XmlDocument</span></span>  
 <span data-ttu-id="7708b-136">In der folgenden Tabelle ist die Funktionsweise der **XmlDocument.XmlResolver**-Eigenschaft bei einem teilweise vertrauenswürdigen Objekt veranschaulicht.</span><span class="sxs-lookup"><span data-stu-id="7708b-136">The following table shows how the **XmlDocument.XmlResolver** property works when the object is semi-trusted.</span></span> <span data-ttu-id="7708b-137">Diese Tabelle betrifft die **XmlDocument.Load**-Methoden, wenn die Eingabe für die Load-Methode aus einem **TextReader**, einem **String**, einem **Stream** oder einem **URI** besteht.</span><span class="sxs-lookup"><span data-stu-id="7708b-137">This table applies to the **XmlDocument.Load** methods when the input to the Load is a **TextReader**, **String**, **Stream**, or **URI**.</span></span> <span data-ttu-id="7708b-138">Diese Tabelle gilt nicht für die **Load**-Methode, wenn das **XmlDocument** aus einem **XmlReader** geladen wird.</span><span class="sxs-lookup"><span data-stu-id="7708b-138">This table does not apply to the **Load** method if the **XmlDocument** is loaded from an **XmlReader**.</span></span>  
  
|<span data-ttu-id="7708b-139">XmlResolver-Eigenschaft</span><span class="sxs-lookup"><span data-stu-id="7708b-139">XmlResolver property</span></span>|<span data-ttu-id="7708b-140">Funktion</span><span class="sxs-lookup"><span data-stu-id="7708b-140">Function</span></span>|<span data-ttu-id="7708b-141">Hinweise</span><span class="sxs-lookup"><span data-stu-id="7708b-141">Notes</span></span>|  
|--------------------------|--------------|-----------|  
|<span data-ttu-id="7708b-142">In teilweise vertrauenswürdigen Szenarios kann für die **XmlResolver**-Eigenschaft kein anderer Wert als NULL festgelegt werden.</span><span class="sxs-lookup"><span data-stu-id="7708b-142">In the semi-trusted scenario, the **XmlResolver** property cannot be set to anything other than null.</span></span>|<span data-ttu-id="7708b-143">Ein **XmlUrlResolver** mit **NULL**-Anmeldeinformationen wird instanziiert und vom **XmlDocument** beim Auflösen von Dateinamen sowie beim Suchen externer DTDs, Entitäten und Schemata verwendet. **NULL**-Anmeldeinformationen werden beim Bearbeiten von Knoten verwendet.</span><span class="sxs-lookup"><span data-stu-id="7708b-143">An **XmlUrlResolver** with **null** credentials will be instantiated and used by the **XmlDocument** when resolving file names, locating external DTDs, entities, and schemas, and **null** credentials are used when editing nodes.</span></span>|<span data-ttu-id="7708b-144">Dieses Verhalten entspricht dem Verhalten, wenn die **XmlResolver**-Eigenschaft nicht festgelegt ist, sondern im Standardzustand belassen wird.</span><span class="sxs-lookup"><span data-stu-id="7708b-144">This behavior is identical to the behavior when the **XmlResolver** property is not set, but left in its default state.</span></span><br /><br /> <span data-ttu-id="7708b-145">Das **XmlDocument** verwendet für alle Aktionen anonyme Berechtigungen.</span><span class="sxs-lookup"><span data-stu-id="7708b-145">The **XmlDocument** uses anonymous permissions for all actions.</span></span>|  
|<span data-ttu-id="7708b-146">Die Eigenschaft ist auf **NULL** festgelegt (**Nothing** in Microsoft Visual Basic .NET).</span><span class="sxs-lookup"><span data-stu-id="7708b-146">The property is set to **null** (**Nothing** in Microsoft Visual Basic .NET).</span></span>|<span data-ttu-id="7708b-147">Funktionen, für die eine externe Ressource erforderlich ist, werden nicht unterstützt, z. B. die Suche eines externen Schemas oder einer externen DTD.</span><span class="sxs-lookup"><span data-stu-id="7708b-147">No features that require an external resource are supported, such as locating an external schema or DTD.</span></span> <span data-ttu-id="7708b-148">Externe Entitäten werden ebenfalls nicht aufgelöst. Bearbeitungsfunktionen wie das Einfügen von Entitätsknoten, die aufgelöst werden müssen, werden nicht unterstützt.</span><span class="sxs-lookup"><span data-stu-id="7708b-148">Nor will external entities be resolved, and performing editing functions such as inserting entity nodes that require resolution, are not supported.</span></span>|<span data-ttu-id="7708b-149">Wenn die Eigenschaft **NULL** ist, bleibt das Verhalten unabhängig von der vollständigen oder teilweisen Vertrauenswürdigkeit des **XmlDocument** gleich.</span><span class="sxs-lookup"><span data-stu-id="7708b-149">When the property is **null**, the behavior is the same regardless if the **XmlDocument** is fully trusted or semi-trusted.</span></span>|  
|<span data-ttu-id="7708b-150">Die Eigenschaft ist nicht festgelegt, sondern im Standardzustand belassen.</span><span class="sxs-lookup"><span data-stu-id="7708b-150">The property is not set, but left in its default state.</span></span>|<span data-ttu-id="7708b-151">Ein **XmlUrlResolver** mit **NULL**-Anmeldeinformationen wird instanziiert und vom **XmlDocument** beim Auflösen von Dateinamen sowie beim Suchen externer DTDs, Entitäten und Schemata verwendet. **NULL**-Anmeldeinformationen werden beim Bearbeiten von Knoten verwendet.</span><span class="sxs-lookup"><span data-stu-id="7708b-151">An **XmlUrlResolver** with **null** credentials will be instantiated and used by the **XmlDocument** when resolving file names, locating external DTDs, entities, and schemas, and **null** credentials are used when editing nodes.</span></span>|<span data-ttu-id="7708b-152">Das **XmlDocument** verwendet für alle Aktionen anonyme Berechtigungen.</span><span class="sxs-lookup"><span data-stu-id="7708b-152">The **XmlDocument** uses anonymous permissions for all actions.</span></span>|  
  
 <span data-ttu-id="7708b-153">Diese Tabelle betrifft die **XmlDocument.Load**-Methode, wenn die Eingabe für die **Load**-Methode aus einem **XmlReader** besteht und das **XmlDocument** teilweise vertrauenswürdig ist.</span><span class="sxs-lookup"><span data-stu-id="7708b-153">This table applies to the **XmlDocument.Load** method when the input to the **Load** is an **XmlReader**, and the **XmlDocument** is semi-trusted.</span></span>  
  
|<span data-ttu-id="7708b-154">XmlResolver-Eigenschaft</span><span class="sxs-lookup"><span data-stu-id="7708b-154">XmlResolver property</span></span>|<span data-ttu-id="7708b-155">Funktion</span><span class="sxs-lookup"><span data-stu-id="7708b-155">Function</span></span>|<span data-ttu-id="7708b-156">Hinweise</span><span class="sxs-lookup"><span data-stu-id="7708b-156">Notes</span></span>|  
|--------------------------|--------------|-----------|  
|<span data-ttu-id="7708b-157">Die **XmlResolver**-Klasse, die vom **XmlDocument** verwendet wird, ist die gleiche Klasse, die vom **XmlReader** verwendet wird.</span><span class="sxs-lookup"><span data-stu-id="7708b-157">The **XmlResolver** class used by the **XmlDocument** is the same one being used by the **XmlReader**.</span></span>|<span data-ttu-id="7708b-158">Das **XmlDocument** verwendet den **XmlResolver**, der dem **XmlReader** zugeordnet wurde.</span><span class="sxs-lookup"><span data-stu-id="7708b-158">The **XmlDocument** uses the **XmlResolver** that was assigned to the **XmlReader**.</span></span><br /><br /> <span data-ttu-id="7708b-159">Die **XmlDocument.Resolver**-Eigenschaft kann nicht unabhängig von der Vertrauensebene des **XmlDocument** festgelegt werden, da sie einen **XmlResolver** aus dem **XmlReader** abruft.</span><span class="sxs-lookup"><span data-stu-id="7708b-159">The **XmlDocument.Resolver** property cannot be set, regardless of the **XmlDocument** trust level, because it is getting an **XmlResolver** from the **XmlReader**.</span></span> <span data-ttu-id="7708b-160">Sie können nicht versuchen, die Einstellungen des **XmlResolver** von **XmlReaders** zu überschreiben, indem Sie die **XmlResolver** -Eigenschaft des **XmlDocument**festlegen.</span><span class="sxs-lookup"><span data-stu-id="7708b-160">You cannot attempt to override the settings of the **XmlReaders** **XmlResolver** by setting the **XmlResolver** property of the **XmlDocument**.</span></span>|<span data-ttu-id="7708b-161">Der **XmlReader** kann der **XmlTextReader**, ein validierender <xref:System.Xml.XmlReader> oder ein benutzerdefinierter Reader sein.</span><span class="sxs-lookup"><span data-stu-id="7708b-161">The **XmlReader** can be the **XmlTextReader**, validating <xref:System.Xml.XmlReader>, or a custom-written reader.</span></span> <span data-ttu-id="7708b-162">Wenn der verwendete Reader die Entitätsauflösung unterstützt, werden externe Entitäten aufgelöst.</span><span class="sxs-lookup"><span data-stu-id="7708b-162">If the reader used supports entity resolution, external entities are resolved.</span></span> <span data-ttu-id="7708b-163">Wenn der übergebene Reader keine Entitätsverweise unterstützt, werden Entitätsverweise nicht aufgelöst.</span><span class="sxs-lookup"><span data-stu-id="7708b-163">If the reader passed in does not support entity references, then entity references are not resolved.</span></span>|  
  
 <span data-ttu-id="7708b-164">Ds Festlegen des XmlResolver auf die richtigen Anmeldeinformationen ermöglicht den Zugriff auf externe Ressourcen.</span><span class="sxs-lookup"><span data-stu-id="7708b-164">Setting the XmlResolver to contain the correct credentials allows access to external resources.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="7708b-165">Es gibt keine Möglichkeit, die **XmlResolver**-Eigenschaft abzurufen.</span><span class="sxs-lookup"><span data-stu-id="7708b-165">There is no way to retrieve the **XmlResolver** property.</span></span> <span data-ttu-id="7708b-166">Dadurch wird verhindert, dass ein Benutzer einen **XmlResolver** wiederverwenden kann, für den Anmeldeinformationen festgelegt wurden.</span><span class="sxs-lookup"><span data-stu-id="7708b-166">This helps to prevent a user from reusing an **XmlResolver** on which credentials have been set.</span></span> <span data-ttu-id="7708b-167">Außerdem gilt Folgendes: Wenn ein **XmlTextReader** oder ein validierender <xref:System.Xml.XmlReader> zum Laden des **XmlDocument** verwendet wird und für **XmlDocument** ein Resolver festgelegt wurde, werden die Resolver dieser Reader nicht nach der **Load**-Phase durch das **XmlDocument** zwischengespeichert, da dies ebenfalls ein Sicherheitsrisiko darstellt.</span><span class="sxs-lookup"><span data-stu-id="7708b-167">Also, if an **XmlTextReader** or validating <xref:System.Xml.XmlReader> is used to load the **XmlDocument** and the **XmlDocument** has a resolver that has been set, the resolvers from these readers are not cached by the **XmlDocument** after the **Load** phase, since this also presents a security risk.</span></span>  
  
 <span data-ttu-id="7708b-168">Weitere Informationen finden Sie im Abschnitt Hinweise unter der <xref:System.Xml.XmlResolver> Referenzseite.</span><span class="sxs-lookup"><span data-stu-id="7708b-168">For more information, see the Remarks section of the <xref:System.Xml.XmlResolver> reference page.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="7708b-169">Siehe auch</span><span class="sxs-lookup"><span data-stu-id="7708b-169">See also</span></span>

- [<span data-ttu-id="7708b-170">XML-Dokumentobjektmodell (DOM)</span><span class="sxs-lookup"><span data-stu-id="7708b-170">XML Document Object Model (DOM)</span></span>](../../../../docs/standard/data/xml/xml-document-object-model-dom.md)
