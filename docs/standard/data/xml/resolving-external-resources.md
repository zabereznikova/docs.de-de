---
title: "Auflösen von externen Ressourcen"
ms.custom: 
ms.date: 03/30/2017
ms.prod: .net
ms.reviewer: 
ms.suite: 
ms.technology: dotnet-standard
ms.tgt_pltfrm: 
ms.topic: article
ms.assetid: ad3fa320-4b8f-4e5c-b549-01157591007a
caps.latest.revision: "4"
author: mairaw
ms.author: mairaw
manager: wpickett
ms.openlocfilehash: 824a35ee5d4ecafc45167ff3f4bc89802af4ed96
ms.sourcegitcommit: bd1ef61f4bb794b25383d3d72e71041a5ced172e
ms.translationtype: HT
ms.contentlocale: de-DE
ms.lasthandoff: 10/18/2017
---
# <a name="resolving-external-resources"></a><span data-ttu-id="a9cdd-102">Auflösen von externen Ressourcen</span><span class="sxs-lookup"><span data-stu-id="a9cdd-102">Resolving External Resources</span></span>
<span data-ttu-id="a9cdd-103">Die **XmlResolver** Eigenschaft der **XmlDocument** dient der **XmlDocument** Klasse, um Ressourcen zu suchen, die nicht Inline in den XML-Daten, z. B. externe Document Type Definitionen (DTDs), Entitäten und Schemata.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-103">The **XmlResolver** property of the **XmlDocument** is used by the **XmlDocument** class to locate resources that are not inline in the XML data, such as external document type definitions (DTDs), entities, and schemas.</span></span> <span data-ttu-id="a9cdd-104">Diese Elemente können in einem Netzwerk oder auf einem lokalen Laufwerk gesucht werden und sind durch einen URI (Uniform Resource Identifier) gekennzeichnet.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-104">These items can be located on a network or on a local drive, and are identifiable by a Uniform Resource Identifier (URI).</span></span> <span data-ttu-id="a9cdd-105">Dies ermöglicht die **XmlDocument** auflösen **EntityReference** Knoten, die im Dokument vorhanden sind, und überprüfen das Dokument entsprechend der externen DTD oder dem Schema.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-105">This allows the **XmlDocument** to resolve **EntityReference** nodes that are present in the document and validate the document according to the external DTD or schema.</span></span>  
  
## <a name="fully-trusted-xmldocument"></a><span data-ttu-id="a9cdd-106">Vollständig vertrauenswürdiges XmlDocument</span><span class="sxs-lookup"><span data-stu-id="a9cdd-106">Fully-Trusted XmlDocument</span></span>  
 <span data-ttu-id="a9cdd-107">Die **XmlResolver** Eigenschaft wirkt sich auf die Funktionalität der **XmlDocument.Load** Methode.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-107">The **XmlResolver** property affects the functionality of the **XmlDocument.Load** method.</span></span> <span data-ttu-id="a9cdd-108">Die folgende Tabelle zeigt wie die **XmlDocument.XmlResolver** -Eigenschaft funktioniert, wenn die **XmlDocument** Objekt ist vollständig vertrauenswürdig.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-108">The table below shows how the **XmlDocument.XmlResolver** property works when the **XmlDocument** object is fully trusted.</span></span> <span data-ttu-id="a9cdd-109">Die folgende Tabelle zeigt die **XmlDocument.Load** Methoden, wenn die Eingabe für das Laden einer **"TextReader"**, **Zeichenfolge**, **Stream**, oder  **URI**.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-109">The following table shows the **XmlDocument.Load** methods when the input to the Load is a **TextReader**, **String**, **Stream**, or **URI**.</span></span> <span data-ttu-id="a9cdd-110">Diese Tabelle gilt nicht für die **laden** Methode Wenn die **XmlDocument** aus geladen wird ein **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-110">This table does not apply to the **Load** method if the **XmlDocument** is loaded from an **XmlReader**.</span></span>  
  
|<span data-ttu-id="a9cdd-111">XmlResolver-Eigenschaft</span><span class="sxs-lookup"><span data-stu-id="a9cdd-111">XmlResolver property</span></span>|<span data-ttu-id="a9cdd-112">Funktion</span><span class="sxs-lookup"><span data-stu-id="a9cdd-112">Function</span></span>|<span data-ttu-id="a9cdd-113">Hinweise</span><span class="sxs-lookup"><span data-stu-id="a9cdd-113">Notes</span></span>|  
|--------------------------|--------------|-----------|  
|<span data-ttu-id="a9cdd-114">Die Eigenschaft wird festgelegt, um eine **XmlResolver** -Klasse, die zuvor erstellt wurde, und verfügt über Eigenschaften, die bereits vom Benutzer festgelegt.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-114">The property is set to an **XmlResolver** class that has been previously created and has properties already set on it by the user.</span></span>|<span data-ttu-id="a9cdd-115">Die **XmlDocument** verwendet die **XmlResolver** erhält, die zum Auflösen von Dateinamen sowie zum Auflösen von Verweisen auf externe Ressourcen wie DTDs, Entitäten und Schemata.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-115">The **XmlDocument** uses the **XmlResolver** that is given to resolve file names, to resolve references to external resources such as DTDs, entities, and schemas.</span></span><br /><br /> <span data-ttu-id="a9cdd-116">Die **XmlResolver** wird auch verwendet werden, beim Auflösen externer Ressourcen, die erforderlich sind, beim Hinzufügen oder Bearbeiten von Knoten in der **XmlDocument**.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-116">The **XmlResolver** is also used when resolving external resources that are needed when adding or editing nodes in the **XmlDocument**.</span></span>|<span data-ttu-id="a9cdd-117">Die **XmlResolver** übergeben, um die **XmlDocument** ist der Konfliktlöser, der verwendet wird, wenn externe Ressourcen gefunden und behoben werden müssen.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-117">The **XmlResolver** given to the **XmlDocument** is the resolver that is used whenever external resources need to be located and resolved.</span></span>|  
|<span data-ttu-id="a9cdd-118">Die Eigenschaft wird festgelegt, um **null** (**nichts** in Microsoft Visual Basic .NET).).</span><span class="sxs-lookup"><span data-stu-id="a9cdd-118">The property is set to **null** (**Nothing** in Microsoft Visual Basic .NET).</span></span>|<span data-ttu-id="a9cdd-119">Funktionen, für die eine externe Ressource erforderlich ist, werden nicht unterstützt, z. B. die Suche eines externen Schemas oder einer externen DTD.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-119">Features that require an external resource are not supported, such as locating an external schema, or DTD.</span></span> <span data-ttu-id="a9cdd-120">Externe Entitäten werden ebenfalls nicht aufgelöst. Bearbeitungsfunktionen wie das Einfügen von Entitätsknoten, die aufgelöst werden müssen, werden nicht unterstützt.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-120">Nor will external entities be resolved, and performing editing functions, such as inserting entity nodes that require resolution, are not supported.</span></span>|<span data-ttu-id="a9cdd-121">Die **XmlDocument** lädt Dateien anonym und versucht nicht, andere Ressourcen aufzulösen.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-121">The **XmlDocument** loads files as anonymous and does not attempt to resolve any other resources.</span></span>|  
|<span data-ttu-id="a9cdd-122">Die Eigenschaft ist nicht festgelegt, sondern im Standardzustand belassen.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-122">The property is not set, but left in its default state.</span></span>|<span data-ttu-id="a9cdd-123">Ein **XmlUrlResolver** mit NULL-Anmeldeinformationen werden instanziiert und verwendet werden, indem Sie die **XmlDocument** beim Auflösen von Dateinamen sowie Suchen externer DTDs, Entitäten und Schemata und **Null** -Anmeldeinformationen werden beim Bearbeiten von Knoten verwendet.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-123">An **XmlUrlResolver** with NULL credentials will be instantiated and used by the **XmlDocument** when resolving file names, locating external DTDs, entities, and schemas, and **null** credentials are used when editing nodes.</span></span>||  
  
 <span data-ttu-id="a9cdd-124">Die folgende Tabelle zeigt die **XmlDocument.Load** Methode Wenn die Eingabe für die **laden** ist ein **XmlReader** und die **XmlDocument** ist voll vertrauenswürdig ist.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-124">The following table shows the **XmlDocument.Load** method when the input to the **Load** is an **XmlReader** and the **XmlDocument** is fully trusted.</span></span>  
  
|<span data-ttu-id="a9cdd-125">XmlResolver-Eigenschaft</span><span class="sxs-lookup"><span data-stu-id="a9cdd-125">XmlResolver property</span></span>|<span data-ttu-id="a9cdd-126">Funktion</span><span class="sxs-lookup"><span data-stu-id="a9cdd-126">Function</span></span>|<span data-ttu-id="a9cdd-127">Hinweise</span><span class="sxs-lookup"><span data-stu-id="a9cdd-127">Notes</span></span>|  
|--------------------------|--------------|-----------|  
|<span data-ttu-id="a9cdd-128">Die **XmlResolver** Klasse, verwendet der **XmlDocument** ist die gleiche Klasse, die vom verwendeten die **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-128">The **XmlResolver** class used by the **XmlDocument** is the same class being used by the **XmlReader**.</span></span>|<span data-ttu-id="a9cdd-129">Die **XmlDocument** verwendet die **XmlResolver** , zugewiesen wurde die **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-129">The **XmlDocument** uses the **XmlResolver** that was assigned to the **XmlReader**.</span></span><br /><br /> <span data-ttu-id="a9cdd-130">Die **XmlDocument.Resolver** Eigenschaft kann nicht festgelegt werden, unabhängig von der **XmlDocument** Vertrauensebene, da er erhält eine **XmlResolver** aus der  **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-130">The **XmlDocument.Resolver** property cannot be set, regardless of the **XmlDocument** trust level, because it is getting an **XmlResolver** from the **XmlReader**.</span></span> <span data-ttu-id="a9cdd-131">Sie können nicht versuchen, die Einstellungen außer Kraft setzen die **XmlResolver**" **XmlResolver** durch Festlegen der **XmlResolver** Eigenschaft von der **XmlDocument**.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-131">You cannot attempt to override the settings of the **XmlReaders**' **XmlResolver** by setting the **XmlResolver** property of the **XmlDocument**.</span></span>|<span data-ttu-id="a9cdd-132">Die **XmlReader** kann die **XmlTextReader**, **XmlValidatingReader**, oder ein benutzerdefinierter Reader.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-132">The **XmlReader** can be the **XmlTextReader**, **XmlValidatingReader**, or a custom-written reader.</span></span> <span data-ttu-id="a9cdd-133">Wenn der verwendete Reader die Entitätsauflösung unterstützt, werden externe Entitäten aufgelöst.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-133">If the reader used supports entity resolution, external entities are resolved.</span></span> <span data-ttu-id="a9cdd-134">Wenn der übergebene Reader keine Entitätsverweise unterstützt, werden Entitätsverweise nicht aufgelöst.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-134">If the reader passed does not support entity references, then entity references are not resolved.</span></span>|  
  
## <a name="semi-trusted-xmldocument"></a><span data-ttu-id="a9cdd-135">Teilweise vertrauenswürdiges XmlDocument</span><span class="sxs-lookup"><span data-stu-id="a9cdd-135">Semi-Trusted XmlDocument</span></span>  
 <span data-ttu-id="a9cdd-136">Die folgende Tabelle zeigt, wie die **XmlDocument.XmlResolver** -Eigenschaft funktioniert, wenn das Objekt nur teilweise vertrauenswürdig ist.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-136">The following table shows how the **XmlDocument.XmlResolver** property works when the object is semi-trusted.</span></span> <span data-ttu-id="a9cdd-137">Diese Tabelle gilt für die **XmlDocument.Load** Methoden, wenn die Eingabe für das Laden einer **"TextReader"**, **Zeichenfolge**, **Stream**, oder  **URI**.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-137">This table applies to the **XmlDocument.Load** methods when the input to the Load is a **TextReader**, **String**, **Stream**, or **URI**.</span></span> <span data-ttu-id="a9cdd-138">Diese Tabelle gilt nicht für die **laden** Methode Wenn die **XmlDocument** aus geladen wird ein **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-138">This table does not apply to the **Load** method if the **XmlDocument** is loaded from an **XmlReader**.</span></span>  
  
|<span data-ttu-id="a9cdd-139">XmlResolver-Eigenschaft</span><span class="sxs-lookup"><span data-stu-id="a9cdd-139">XmlResolver property</span></span>|<span data-ttu-id="a9cdd-140">Funktion</span><span class="sxs-lookup"><span data-stu-id="a9cdd-140">Function</span></span>|<span data-ttu-id="a9cdd-141">Hinweise</span><span class="sxs-lookup"><span data-stu-id="a9cdd-141">Notes</span></span>|  
|--------------------------|--------------|-----------|  
|<span data-ttu-id="a9cdd-142">In der teilweise vertrauenswürdigen Szenarios kann die **XmlResolver** -Eigenschaft kein anderer Wert als Null festgelegt werden.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-142">In the semi-trusted scenario, the **XmlResolver** property cannot be set to anything other than null.</span></span>|<span data-ttu-id="a9cdd-143">Ein **XmlUrlResolver** mit **null** -Anmeldeinformationen wird instanziiert und verwendet werden, indem Sie die **XmlDocument** beim Auflösen von Dateinamen sowie Suchen externer DTDs, Entitäten und Schemas, und **null** -Anmeldeinformationen werden beim Bearbeiten von Knoten verwendet.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-143">An **XmlUrlResolver** with **null** credentials will be instantiated and used by the **XmlDocument** when resolving file names, locating external DTDs, entities, and schemas, and **null** credentials are used when editing nodes.</span></span>|<span data-ttu-id="a9cdd-144">Dieses Verhalten entspricht dem Verhalten bei der **XmlResolver** Eigenschaft ist nicht festgelegt, sondern bleibt im Standardzustand.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-144">This behavior is identical to the behavior when the **XmlResolver** property is not set, but left in its default state.</span></span><br /><br /> <span data-ttu-id="a9cdd-145">Die **XmlDocument** verwendet für alle Aktionen anonyme Berechtigungen.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-145">The **XmlDocument** uses anonymous permissions for all actions.</span></span>|  
|<span data-ttu-id="a9cdd-146">Die Eigenschaft wird festgelegt, um **null** (**nichts** in Microsoft Visual Basic .NET).).</span><span class="sxs-lookup"><span data-stu-id="a9cdd-146">The property is set to **null** (**Nothing** in Microsoft Visual Basic .NET).</span></span>|<span data-ttu-id="a9cdd-147">Funktionen, für die eine externe Ressource erforderlich ist, werden nicht unterstützt, z. B. die Suche eines externen Schemas oder einer externen DTD.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-147">No features that require an external resource are supported, such as locating an external schema or DTD.</span></span> <span data-ttu-id="a9cdd-148">Externe Entitäten werden ebenfalls nicht aufgelöst. Bearbeitungsfunktionen wie das Einfügen von Entitätsknoten, die aufgelöst werden müssen, werden nicht unterstützt.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-148">Nor will external entities be resolved, and performing editing functions such as inserting entity nodes that require resolution, are not supported.</span></span>|<span data-ttu-id="a9cdd-149">Wenn die Eigenschaft den Wert **null**, das Verhalten ist das gleiche unabhängig davon, ob die **XmlDocument** ist voll oder teilweisen Vertrauenswürdigkeit.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-149">When the property is **null**, the behavior is the same regardless if the **XmlDocument** is fully trusted or semi-trusted.</span></span>|  
|<span data-ttu-id="a9cdd-150">Die Eigenschaft ist nicht festgelegt, sondern im Standardzustand belassen.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-150">The property is not set, but left in its default state.</span></span>|<span data-ttu-id="a9cdd-151">Ein **XmlUrlResolver** mit **null** -Anmeldeinformationen wird instanziiert und verwendet werden, indem Sie die **XmlDocument** beim Auflösen von Dateinamen sowie Suchen externer DTDs, Entitäten und Schemas, und **null** -Anmeldeinformationen werden beim Bearbeiten von Knoten verwendet.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-151">An **XmlUrlResolver** with **null** credentials will be instantiated and used by the **XmlDocument** when resolving file names, locating external DTDs, entities, and schemas, and **null** credentials are used when editing nodes.</span></span>|<span data-ttu-id="a9cdd-152">Die **XmlDocument** verwendet für alle Aktionen anonyme Berechtigungen.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-152">The **XmlDocument** uses anonymous permissions for all actions.</span></span>|  
  
 <span data-ttu-id="a9cdd-153">Diese Tabelle gilt für die **XmlDocument.Load** Methode Wenn die Eingabe für die **laden** ist ein **XmlReader**, und die **XmlDocument** ist teilweise vertrauenswürdig ist.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-153">This table applies to the **XmlDocument.Load** method when the input to the **Load** is an **XmlReader**, and the **XmlDocument** is semi-trusted.</span></span>  
  
|<span data-ttu-id="a9cdd-154">XmlResolver-Eigenschaft</span><span class="sxs-lookup"><span data-stu-id="a9cdd-154">XmlResolver property</span></span>|<span data-ttu-id="a9cdd-155">Funktion</span><span class="sxs-lookup"><span data-stu-id="a9cdd-155">Function</span></span>|<span data-ttu-id="a9cdd-156">Hinweise</span><span class="sxs-lookup"><span data-stu-id="a9cdd-156">Notes</span></span>|  
|--------------------------|--------------|-----------|  
|<span data-ttu-id="a9cdd-157">Die **XmlResolver** Klasse, die vom der **XmlDocument** ist der gleiche verwendet wird, indem Sie die **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-157">The **XmlResolver** class used by the **XmlDocument** is the same one being used by the **XmlReader**.</span></span>|<span data-ttu-id="a9cdd-158">Die **XmlDocument** verwendet die **XmlResolver** , zugewiesen wurde die **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-158">The **XmlDocument** uses the **XmlResolver** that was assigned to the **XmlReader**.</span></span><br /><br /> <span data-ttu-id="a9cdd-159">Die **XmlDocument.Resolver** Eigenschaft kann nicht festgelegt werden, unabhängig von der **XmlDocument** Vertrauensebene, da er erhält eine **XmlResolver** aus der  **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-159">The **XmlDocument.Resolver** property cannot be set, regardless of the **XmlDocument** trust level, because it is getting an **XmlResolver** from the **XmlReader**.</span></span> <span data-ttu-id="a9cdd-160">Sie können nicht versuchen, die Einstellungen außer Kraft setzen die **XmlResolver** **XmlResolver** durch Festlegen der **XmlResolver** Eigenschaft von der **XmlDocument**.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-160">You cannot attempt to override the settings of the **XmlReaders** **XmlResolver** by setting the **XmlResolver** property of the **XmlDocument**.</span></span>|<span data-ttu-id="a9cdd-161">Die **XmlReader** kann die **XmlTextReader**, ein validierender <xref:System.Xml.XmlReader>, oder ein benutzerdefinierter Reader.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-161">The **XmlReader** can be the **XmlTextReader**, validating <xref:System.Xml.XmlReader>, or a custom-written reader.</span></span> <span data-ttu-id="a9cdd-162">Wenn der verwendete Reader die Entitätsauflösung unterstützt, werden externe Entitäten aufgelöst.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-162">If the reader used supports entity resolution, external entities are resolved.</span></span> <span data-ttu-id="a9cdd-163">Wenn der übergebene Reader keine Entitätsverweise unterstützt, werden Entitätsverweise nicht aufgelöst.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-163">If the reader passed in does not support entity references, then entity references are not resolved.</span></span>|  
  
 <span data-ttu-id="a9cdd-164">Ds Festlegen des XmlResolver auf die richtigen Anmeldeinformationen ermöglicht den Zugriff auf externe Ressourcen.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-164">Setting the XmlResolver to contain the correct credentials allows access to external resources.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="a9cdd-165">Es gibt keine Möglichkeit zum Abrufen der **XmlResolver** Eigenschaft.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-165">There is no way to retrieve the **XmlResolver** property.</span></span> <span data-ttu-id="a9cdd-166">Dadurch wird verhindert, dass einen Benutzer erneut eine **XmlResolver** für Anmeldeinformationen festgelegt wurden.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-166">This helps to prevent a user from reusing an **XmlResolver** on which credentials have been set.</span></span> <span data-ttu-id="a9cdd-167">Auch wenn ein **XmlTextReader** oder validieren <xref:System.Xml.XmlReader> zum Laden der **XmlDocument** und die **XmlDocument** verfügt über einen Konfliktlöser, der, die Resolver festgelegt wurde, Diese Reader werden nicht zwischengespeichert, durch die **XmlDocument** nach der **laden** phase, da dies auch ein Sicherheitsrisiko dar.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-167">Also, if an **XmlTextReader** or validating <xref:System.Xml.XmlReader> is used to load the **XmlDocument** and the **XmlDocument** has a resolver that has been set, the resolvers from these readers are not cached by the **XmlDocument** after the **Load** phase, since this also presents a security risk.</span></span>  
  
 <span data-ttu-id="a9cdd-168">Weitere Informationen finden Sie im Abschnitt Hinweise unter der <xref:System.Xml.XmlResolver> Referenzseite.</span><span class="sxs-lookup"><span data-stu-id="a9cdd-168">For more information, see the Remarks section of the <xref:System.Xml.XmlResolver> reference page.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="a9cdd-169">Siehe auch</span><span class="sxs-lookup"><span data-stu-id="a9cdd-169">See Also</span></span>  
 [<span data-ttu-id="a9cdd-170">XML-Dokumentobjektmodell (DOM)</span><span class="sxs-lookup"><span data-stu-id="a9cdd-170">XML Document Object Model (DOM)</span></span>](../../../../docs/standard/data/xml/xml-document-object-model-dom.md)
