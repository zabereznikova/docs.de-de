---
title: 'CA1063: iverwerfordnungs gemäß implementieren (Code Analyse)'
description: 'Weitere Informationen zur Code Analyse Regel CA1063: Implementieren von iverwerfkorrekt'
ms.date: 03/11/2019
ms.topic: reference
f1_keywords:
- ImplementIDisposableCorrectly
- CA1063
helpviewer_keywords:
- CA1063
- ImplementIDisposableCorrectly
author: gewarren
ms.author: gewarren
dev_langs:
- CSharp
ms.openlocfilehash: da8561d0d9517b52c8d630f3edf51c3e3f7bf7fd
ms.sourcegitcommit: e301979e3049ce412d19b094c60ed95b316a8f8c
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 12/16/2020
ms.locfileid: "98190974"
---
# <a name="ca1063-implement-idisposable-correctly"></a><span data-ttu-id="bd52e-103">CA1063: IDisposable korrekt implementieren.</span><span class="sxs-lookup"><span data-stu-id="bd52e-103">CA1063: Implement IDisposable correctly</span></span>

| | <span data-ttu-id="bd52e-104">Wert</span><span class="sxs-lookup"><span data-stu-id="bd52e-104">Value</span></span> |
|-|-|
| <span data-ttu-id="bd52e-105">**Regel-ID**</span><span class="sxs-lookup"><span data-stu-id="bd52e-105">**Rule ID**</span></span> |<span data-ttu-id="bd52e-106">CA1063</span><span class="sxs-lookup"><span data-stu-id="bd52e-106">CA1063</span></span>|
| <span data-ttu-id="bd52e-107">**Kategorie**</span><span class="sxs-lookup"><span data-stu-id="bd52e-107">**Category**</span></span> |<span data-ttu-id="bd52e-108">Microsoft. Design</span><span class="sxs-lookup"><span data-stu-id="bd52e-108">Microsoft.Design</span></span>|
| <span data-ttu-id="bd52e-109">**Behebung ist unterbrechend oder nicht unterbrechend**</span><span class="sxs-lookup"><span data-stu-id="bd52e-109">**Fix is breaking or non-breaking**</span></span> |<span data-ttu-id="bd52e-110">Nicht unterbrechend</span><span class="sxs-lookup"><span data-stu-id="bd52e-110">Non-breaking</span></span>|

## <a name="cause"></a><span data-ttu-id="bd52e-111">Ursache</span><span class="sxs-lookup"><span data-stu-id="bd52e-111">Cause</span></span>

<span data-ttu-id="bd52e-112">Die <xref:System.IDisposable?displayProperty=nameWithType> Schnittstelle ist nicht ordnungsgemäß implementiert.</span><span class="sxs-lookup"><span data-stu-id="bd52e-112">The <xref:System.IDisposable?displayProperty=nameWithType> interface is not implemented correctly.</span></span> <span data-ttu-id="bd52e-113">Mögliche Gründe hierfür sind:</span><span class="sxs-lookup"><span data-stu-id="bd52e-113">Possible reasons for this include:</span></span>

- <span data-ttu-id="bd52e-114"><xref:System.IDisposable> wird in der-Klasse neu implementiert.</span><span class="sxs-lookup"><span data-stu-id="bd52e-114"><xref:System.IDisposable> is reimplemented in the class.</span></span>

- <span data-ttu-id="bd52e-115">`Finalize` wird erneut überschrieben.</span><span class="sxs-lookup"><span data-stu-id="bd52e-115">`Finalize` is overridden again.</span></span>

- <span data-ttu-id="bd52e-116">`Dispose()` wird überschrieben.</span><span class="sxs-lookup"><span data-stu-id="bd52e-116">`Dispose()` is overridden.</span></span>

- <span data-ttu-id="bd52e-117">Die `Dispose()` Methode ist nicht öffentlich, [versiegelt](../../../csharp/language-reference/keywords/sealed.md)oder benannt. </span><span class="sxs-lookup"><span data-stu-id="bd52e-117">The `Dispose()` method is not public, [sealed](../../../csharp/language-reference/keywords/sealed.md), or named **Dispose**.</span></span>

- <span data-ttu-id="bd52e-118">`Dispose(bool)` ist nicht geschützt, virtuell oder nicht versiegelt.</span><span class="sxs-lookup"><span data-stu-id="bd52e-118">`Dispose(bool)` is not protected, virtual, or unsealed.</span></span>

- <span data-ttu-id="bd52e-119">In nicht versiegelten Typen `Dispose()` muss aufruft `Dispose(true)` .</span><span class="sxs-lookup"><span data-stu-id="bd52e-119">In unsealed types, `Dispose()` must call `Dispose(true)`.</span></span>

- <span data-ttu-id="bd52e-120">Bei nicht versiegelten Typen Ruft die- `Finalize` Implementierung weder noch oder `Dispose(bool)` den Basisklassen-Finalizer auf.</span><span class="sxs-lookup"><span data-stu-id="bd52e-120">For unsealed types, the `Finalize` implementation does not call either or both `Dispose(bool)` or the base class finalizer.</span></span>

<span data-ttu-id="bd52e-121">Ein Verstoß gegen eines dieser Muster löst eine Warnung aus CA1063.</span><span class="sxs-lookup"><span data-stu-id="bd52e-121">Violation of any one of these patterns triggers warning CA1063.</span></span>

<span data-ttu-id="bd52e-122">Jeder nicht versiegelte Typ, der die-Schnittstelle deklariert und implementiert, <xref:System.IDisposable> muss eine eigene Methode bereitstellen `protected virtual void Dispose(bool)` .</span><span class="sxs-lookup"><span data-stu-id="bd52e-122">Every unsealed type that declares and implements the <xref:System.IDisposable> interface must provide its own `protected virtual void Dispose(bool)` method.</span></span> <span data-ttu-id="bd52e-123">`Dispose()` sollte aufgerufen `Dispose(true)` werden, und der Finalizer sollte aufgerufen werden `Dispose(false)` .</span><span class="sxs-lookup"><span data-stu-id="bd52e-123">`Dispose()` should call `Dispose(true)`, and the finalizer should call `Dispose(false)`.</span></span> <span data-ttu-id="bd52e-124">Wenn Sie einen nicht versiegelten Typ erstellen, der die-Schnittstelle deklariert und implementiert <xref:System.IDisposable> , müssen Sie `Dispose(bool)` Diese definieren und aufzurufen.</span><span class="sxs-lookup"><span data-stu-id="bd52e-124">If you create an unsealed type that declares and implements the <xref:System.IDisposable> interface, you must define `Dispose(bool)` and call it.</span></span> <span data-ttu-id="bd52e-125">Weitere Informationen finden Sie unter [Bereinigen von nicht verwalteten Ressourcen (.net-Handbuch)](../../../standard/garbage-collection/unmanaged.md) und Verwerfen von [Mustern](../../../standard/garbage-collection/implementing-dispose.md).</span><span class="sxs-lookup"><span data-stu-id="bd52e-125">For more information, see [Clean up unmanaged resources (.NET guide)](../../../standard/garbage-collection/unmanaged.md) and [Dispose pattern](../../../standard/garbage-collection/implementing-dispose.md).</span></span>

<span data-ttu-id="bd52e-126">Standardmäßig betrachtet diese Regel nur extern sichtbare Typen, aber dies ist [konfigurierbar](#configure-code-to-analyze).</span><span class="sxs-lookup"><span data-stu-id="bd52e-126">By default, this rule only looks at externally visible types, but this is [configurable](#configure-code-to-analyze).</span></span>

## <a name="rule-description"></a><span data-ttu-id="bd52e-127">Regelbeschreibung</span><span class="sxs-lookup"><span data-stu-id="bd52e-127">Rule description</span></span>

<span data-ttu-id="bd52e-128">Alle <xref:System.IDisposable> Typen sollten das Lösch [Muster](../../../standard/garbage-collection/implementing-dispose.md) ordnungsgemäß implementieren.</span><span class="sxs-lookup"><span data-stu-id="bd52e-128">All <xref:System.IDisposable> types should implement the [Dispose pattern](../../../standard/garbage-collection/implementing-dispose.md) correctly.</span></span>

## <a name="how-to-fix-violations"></a><span data-ttu-id="bd52e-129">Behandeln von Verstößen</span><span class="sxs-lookup"><span data-stu-id="bd52e-129">How to fix violations</span></span>

<span data-ttu-id="bd52e-130">Überprüfen Sie Ihren Code, und legen Sie fest, welche der folgenden Auflösungen diese Verletzung beheben soll:</span><span class="sxs-lookup"><span data-stu-id="bd52e-130">Examine your code and determine which of the following resolutions will fix this violation:</span></span>

- <span data-ttu-id="bd52e-131">Entfernen <xref:System.IDisposable> Sie aus der Liste der Schnittstellen, die von Ihrem Typ implementiert werden, und überschreiben Sie stattdessen die Implementierung der Basisklasse verwerfen.</span><span class="sxs-lookup"><span data-stu-id="bd52e-131">Remove <xref:System.IDisposable> from the list of interfaces that are implemented by your type, and override the base class Dispose implementation instead.</span></span>

- <span data-ttu-id="bd52e-132">Entfernen Sie den Finalizer aus dem Typ, überschreiben Sie verwerfen (bool disposing), und platzieren Sie die finalisierungslogik in den Codepfad, in dem "disposing" "false" ist.</span><span class="sxs-lookup"><span data-stu-id="bd52e-132">Remove the finalizer from your type, override Dispose(bool disposing), and put the finalization logic in the code path where 'disposing' is false.</span></span>

- <span data-ttu-id="bd52e-133">Überschreiben Sie verwerfen (bool disposing), und platzieren Sie die Lösch Logik in den Codepfad, in dem "disposing" "true" ist.</span><span class="sxs-lookup"><span data-stu-id="bd52e-133">Override Dispose(bool disposing), and put the dispose logic in the code path where 'disposing' is true.</span></span>

- <span data-ttu-id="bd52e-134">Stellen Sie sicher, dass verwerfen () als öffentlich und [versiegelt](../../../csharp/language-reference/keywords/sealed.md)deklariert ist.</span><span class="sxs-lookup"><span data-stu-id="bd52e-134">Make sure that Dispose() is declared as public and [sealed](../../../csharp/language-reference/keywords/sealed.md).</span></span>

- <span data-ttu-id="bd52e-135">Benennen Sie Ihre **verwerfen-Methode um,** und stellen Sie sicher, dass Sie als öffentlich und [versiegelt](../../../csharp/language-reference/keywords/sealed.md)deklariert ist.</span><span class="sxs-lookup"><span data-stu-id="bd52e-135">Rename your dispose method to **Dispose** and make sure that it's declared as public and [sealed](../../../csharp/language-reference/keywords/sealed.md).</span></span>

- <span data-ttu-id="bd52e-136">Stellen Sie sicher, dass verwerfen (bool) als "Protected", "Virtual" und "unsealed" deklariert ist.</span><span class="sxs-lookup"><span data-stu-id="bd52e-136">Make sure that Dispose(bool) is declared as protected, virtual, and unsealed.</span></span>

- <span data-ttu-id="bd52e-137">Ändern Sie "verwerfen ()" so, dass "verwerfen (true)" aufgerufen wird, und rufen Sie dann <xref:System.GC.SuppressFinalize%2A> auf der aktuellen Objektinstanz ( `this` oder `Me` in Visual Basic) auf, und dann wird zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="bd52e-137">Modify Dispose() so that it calls Dispose(true), then calls <xref:System.GC.SuppressFinalize%2A> on the current object instance (`this`, or `Me` in Visual Basic), and then returns.</span></span>

- <span data-ttu-id="bd52e-138">Ändern Sie den Finalizer so, dass er verwerfen (false) aufruft und dann zurückgibt.</span><span class="sxs-lookup"><span data-stu-id="bd52e-138">Modify your finalizer so that it calls Dispose(false) and then returns.</span></span>

- <span data-ttu-id="bd52e-139">Wenn Sie einen nicht versiegelten Typ erstellen, der die-Schnittstelle deklariert und implementiert <xref:System.IDisposable> , stellen Sie sicher, dass die Implementierung von <xref:System.IDisposable> dem zuvor in diesem Abschnitt beschriebenen Muster folgt.</span><span class="sxs-lookup"><span data-stu-id="bd52e-139">If you create an unsealed type that declares and implements the <xref:System.IDisposable> interface, make sure that the implementation of <xref:System.IDisposable> follows the pattern that is described earlier in this section.</span></span>

## <a name="when-to-suppress-warnings"></a><span data-ttu-id="bd52e-140">Wann sollten Warnungen unterdrückt werden?</span><span class="sxs-lookup"><span data-stu-id="bd52e-140">When to suppress warnings</span></span>

<span data-ttu-id="bd52e-141">Unterdrücken Sie keine Warnung dieser Regel.</span><span class="sxs-lookup"><span data-stu-id="bd52e-141">Do not suppress a warning from this rule.</span></span>

## <a name="configure-code-to-analyze"></a><span data-ttu-id="bd52e-142">Konfigurieren des zu analysierenden Codes</span><span class="sxs-lookup"><span data-stu-id="bd52e-142">Configure code to analyze</span></span>

<span data-ttu-id="bd52e-143">Verwenden Sie die folgende Option, um zu konfigurieren, für welche Teile Ihrer Codebasis diese Regel ausgeführt werden soll.</span><span class="sxs-lookup"><span data-stu-id="bd52e-143">Use the following option to configure which parts of your codebase to run this rule on.</span></span>

- [<span data-ttu-id="bd52e-144">Bestimmte API-Oberflächen einschließen</span><span class="sxs-lookup"><span data-stu-id="bd52e-144">Include specific API surfaces</span></span>](#include-specific-api-surfaces)

<span data-ttu-id="bd52e-145">Sie können diese Option nur für diese Regel, für alle Regeln oder für alle Regeln in dieser Kategorie (Entwurf) konfigurieren.</span><span class="sxs-lookup"><span data-stu-id="bd52e-145">You can configure this option for just this rule, for all rules, or for all rules in this category (Design).</span></span> <span data-ttu-id="bd52e-146">Weitere Informationen finden Sie unter [Konfigurationsoptionen für die Code Qualitäts Regel](../code-quality-rule-options.md).</span><span class="sxs-lookup"><span data-stu-id="bd52e-146">For more information, see [Code quality rule configuration options](../code-quality-rule-options.md).</span></span>

[!INCLUDE[api-surface](~/includes/code-analysis/api-surface.md)]

## <a name="pseudo-code-example"></a><span data-ttu-id="bd52e-147">Pseudo Codebeispiel</span><span class="sxs-lookup"><span data-stu-id="bd52e-147">Pseudo-code example</span></span>

<span data-ttu-id="bd52e-148">Der folgende Pseudo Code bietet ein allgemeines Beispiel dafür, wie "verwerfen (bool)" in einer Klasse implementiert werden sollte, die verwaltete und Native Ressourcen verwendet.</span><span class="sxs-lookup"><span data-stu-id="bd52e-148">The following pseudo-code provides a general example of how Dispose(bool) should be implemented in a class that uses managed and native resources.</span></span>

```csharp
public class Resource : IDisposable
{
    private bool isDisposed;
    private IntPtr nativeResource = Marshal.AllocHGlobal(100);
    private AnotherResource managedResource = new AnotherResource();

    // Dispose() calls Dispose(true)
    public void Dispose()
    {
        Dispose(true);
        GC.SuppressFinalize(this);
    }

    // The bulk of the clean-up code is implemented in Dispose(bool)
    protected virtual void Dispose(bool disposing)
    {
        if (isDisposed) return;

        if (disposing)
        {
            // free managed resources
            managedResource.Dispose();
        }

        // free native resources if there are any.
        if (nativeResource != IntPtr.Zero)
        {
            Marshal.FreeHGlobal(nativeResource);
            nativeResource = IntPtr.Zero;
        }

        isDisposed = true;
    }

    // NOTE: Leave out the finalizer altogether if this class doesn't
    // own unmanaged resources, but leave the other methods
    // exactly as they are.
    ~Resource()
    {
        // Finalizer calls Dispose(false)
        Dispose(false);
    }
}
```

## <a name="see-also"></a><span data-ttu-id="bd52e-149">Weitere Informationen</span><span class="sxs-lookup"><span data-stu-id="bd52e-149">See also</span></span>

- [<span data-ttu-id="bd52e-150">Verwerfen eines Musters (Framework-Entwurfs Richtlinien)</span><span class="sxs-lookup"><span data-stu-id="bd52e-150">Dispose pattern (framework design guidelines)</span></span>](../../../standard/garbage-collection/implementing-dispose.md)
- [<span data-ttu-id="bd52e-151">Bereinigen von nicht verwalteten Ressourcen (.net-Handbuch)</span><span class="sxs-lookup"><span data-stu-id="bd52e-151">Clean up unmanaged resources (.NET guide)</span></span>](../../../standard/garbage-collection/unmanaged.md)
