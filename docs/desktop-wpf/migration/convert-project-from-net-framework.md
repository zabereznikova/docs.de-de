---
title: Migrieren von WPF-Apps zu .NET Core 3.0
description: Erfahren Sie, wie Sie eine Windows Presentation Foundation (WPF)-App zu .NET Core 3.0 migrieren.
author: mjrousos
ms.date: 09/12/2019
ms.author: mikerou
ms.openlocfilehash: f52005e7c8a6312b8c4e09a950f1f635af1894e4
ms.sourcegitcommit: cdf5084648bf5e77970cbfeaa23f1cab3e6e234e
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 02/01/2020
ms.locfileid: "81432593"
---
# <a name="migrating-wpf-apps-to-net-core"></a><span data-ttu-id="998b1-103">Migrieren von WPF-Apps zu .NET Core</span><span class="sxs-lookup"><span data-stu-id="998b1-103">Migrating WPF apps to .NET Core</span></span>

<span data-ttu-id="998b1-104">In diesem Artikel werden die Schritte behandelt, die zum Migrieren einer Windows Presentation Foundation (WPF)-App von .NET Framework zu .NET Core 3.0 erforderlich sind.</span><span class="sxs-lookup"><span data-stu-id="998b1-104">This article covers the steps necessary to migrate a Windows Presentation Foundation (WPF) app from .NET Framework to .NET Core 3.0.</span></span> <span data-ttu-id="998b1-105">Wenn Sie keine WPF-App zur Verfügung haben, um portieren zu können, aber den Prozess ausprobieren möchten, können Sie die **Bean** Trader-Beispiel-App verwenden, die auf [GitHub](https://github.com/dotnet/windows-desktop/tree/master/Samples/BeanTrader)verfügbar ist.</span><span class="sxs-lookup"><span data-stu-id="998b1-105">If you don't have a WPF app on hand to port, but would like to try out the process, you can use the **Bean Trader** sample app available on [GitHub](https://github.com/dotnet/windows-desktop/tree/master/Samples/BeanTrader).</span></span> <span data-ttu-id="998b1-106">Die ursprüngliche App (für .NET Framework 4.7.2) ist im Ordner NetFx-BeanTraderClient verfügbar.</span><span class="sxs-lookup"><span data-stu-id="998b1-106">The original app (targeting .NET Framework 4.7.2) is available in the NetFx\BeanTraderClient folder.</span></span> <span data-ttu-id="998b1-107">Zuerst erklären wir die Schritte, die notwendig sind, um Apps im Allgemeinen zu portieren, und dann gehen wir durch die spezifischen Änderungen, die für das **Bean Trader-Beispiel** gelten.</span><span class="sxs-lookup"><span data-stu-id="998b1-107">First we'll explain the steps necessary to port apps in general, and then we'll walk through the specific changes that apply to the **Bean Trader** sample.</span></span>

[!INCLUDE [desktop guide under construction](../../../includes/desktop-guide-preview-note.md)]

<span data-ttu-id="998b1-108">Um zu .NET Core zu migrieren, müssen Sie zunächst Folgendes tun:</span><span class="sxs-lookup"><span data-stu-id="998b1-108">To migrate to .NET Core, you must first:</span></span>

01. <span data-ttu-id="998b1-109">Verstehen und Aktualisieren von NuGet-Abhängigkeiten:</span><span class="sxs-lookup"><span data-stu-id="998b1-109">Understand and update NuGet dependencies:</span></span>

    01. <span data-ttu-id="998b1-110">Aktualisieren Sie NuGet-Abhängigkeiten, um das `<PackageReference>` Format zu verwenden.</span><span class="sxs-lookup"><span data-stu-id="998b1-110">Upgrade NuGet dependencies to use the `<PackageReference>` format.</span></span>
    01. <span data-ttu-id="998b1-111">Überprüfen Sie NuGet-Abhängigkeiten der obersten Ebene für .NET Core- oder .NET Standard-Kompatibilität.</span><span class="sxs-lookup"><span data-stu-id="998b1-111">Review top-level NuGet dependencies for .NET Core or .NET Standard compatibility.</span></span>
    01. <span data-ttu-id="998b1-112">Aktualisieren Sie NuGet-Pakete auf neuere Versionen.</span><span class="sxs-lookup"><span data-stu-id="998b1-112">Upgrade NuGet packages to newer versions.</span></span>
    01. <span data-ttu-id="998b1-113">Verwenden Sie [.NET Portability Analyzer,](../../standard/analyzers/portability-analyzer.md) um .NET-Abhängigkeiten zu verstehen.</span><span class="sxs-lookup"><span data-stu-id="998b1-113">Use the [.NET Portability Analyzer](../../standard/analyzers/portability-analyzer.md) to understand .NET dependencies.</span></span>

01. <span data-ttu-id="998b1-114">Migrieren Sie die Projektdatei in das neue SDK-Format:</span><span class="sxs-lookup"><span data-stu-id="998b1-114">Migrate the project file to the new SDK-style format:</span></span>

    01. <span data-ttu-id="998b1-115">Wählen Sie aus, ob sowohl .NET Core als auch .NET Framework als Auch .NET Core als Ziel gesetzt werden sollen.</span><span class="sxs-lookup"><span data-stu-id="998b1-115">Choose whether to target both .NET Core and .NET Framework, or only .NET Core.</span></span>
    01. <span data-ttu-id="998b1-116">Kopieren Sie relevante Projektdateieigenschaften und Elemente in die neue Projektdatei.</span><span class="sxs-lookup"><span data-stu-id="998b1-116">Copy relevant project file properties and items to the new project file.</span></span>

01. <span data-ttu-id="998b1-117">Beheben von Buildproblemen:</span><span class="sxs-lookup"><span data-stu-id="998b1-117">Fix build issues:</span></span>

    01. <span data-ttu-id="998b1-118">Fügen Sie einen Verweis auf das [Microsoft.Windows.Compatibility-Paket hinzu.](https://www.nuget.org/packages/Microsoft.Windows.Compatibility/)</span><span class="sxs-lookup"><span data-stu-id="998b1-118">Add a reference to the [Microsoft.Windows.Compatibility](https://www.nuget.org/packages/Microsoft.Windows.Compatibility/) package.</span></span>
    01. <span data-ttu-id="998b1-119">Suchen und beheben Sie Unterschiede auf API-Ebene.</span><span class="sxs-lookup"><span data-stu-id="998b1-119">Find and fix API-level differences.</span></span>
    01. <span data-ttu-id="998b1-120">Entfernen Sie *app.config-Abschnitte,* die nicht `appSettings` oder sind. `connectionStrings`</span><span class="sxs-lookup"><span data-stu-id="998b1-120">Remove *app.config* sections other than `appSettings` or `connectionStrings`.</span></span>
    01. <span data-ttu-id="998b1-121">Generierten Code bei Bedarf neu generieren.</span><span class="sxs-lookup"><span data-stu-id="998b1-121">Regenerate generated code, if necessary.</span></span>

01. <span data-ttu-id="998b1-122">Laufzeittests:</span><span class="sxs-lookup"><span data-stu-id="998b1-122">Runtime testing:</span></span>

    01. <span data-ttu-id="998b1-123">Bestätigen Sie, dass die portierte App wie erwartet funktioniert.</span><span class="sxs-lookup"><span data-stu-id="998b1-123">Confirm the ported app works as expected.</span></span>
    01. <span data-ttu-id="998b1-124">Hüten <xref:System.NotSupportedException> Sie sich vor Ausnahmen.</span><span class="sxs-lookup"><span data-stu-id="998b1-124">Beware of <xref:System.NotSupportedException> exceptions.</span></span>

## <a name="about-the-sample"></a><span data-ttu-id="998b1-125">Über das Beispiel</span><span class="sxs-lookup"><span data-stu-id="998b1-125">About the sample</span></span>

<span data-ttu-id="998b1-126">Dieser Artikel verweist auf die [Bean Trader-Beispiel-App,](https://github.com/dotnet/windows-desktop/tree/master/Samples/BeanTrader) da sie eine Vielzahl von Abhängigkeiten verwendet, die denen ähnlich sind, die reale WPF-Apps haben könnten.</span><span class="sxs-lookup"><span data-stu-id="998b1-126">This article references the [Bean Trader sample app](https://github.com/dotnet/windows-desktop/tree/master/Samples/BeanTrader) because it uses a variety of dependencies similar to those that real-world WPF apps might have.</span></span> <span data-ttu-id="998b1-127">Die App ist nicht groß, sondern soll in Bezug auf die Komplexität ein Schritt nach oben von "Hello World" sein.</span><span class="sxs-lookup"><span data-stu-id="998b1-127">The app isn't large, but is meant to be a step up from 'Hello World' in terms of complexity.</span></span> <span data-ttu-id="998b1-128">Die App zeigt einige Probleme, die Benutzer beim Portieren echter Apps auftreten können.</span><span class="sxs-lookup"><span data-stu-id="998b1-128">The app demonstrates some issues users may encounter while porting real apps.</span></span> <span data-ttu-id="998b1-129">Die App kommuniziert mit einem WCF-Dienst, damit er ordnungsgemäß ausgeführt werden kann, müssen Sie auch das BeanTraderServer-Projekt (verfügbar im selben GitHub-Repository) ausführen und sicherstellen, dass die BeanTraderClient-Konfiguration auf den richtigen Endpunkt verweist.</span><span class="sxs-lookup"><span data-stu-id="998b1-129">The app communicates with a WCF service, so for it to run properly, you'll also need to run the BeanTraderServer project (available in the same GitHub repository) and make sure the BeanTraderClient configuration points to the correct endpoint.</span></span> <span data-ttu-id="998b1-130">(Standardmäßig geht das Beispiel davon aus, dass *http://localhost:8090*der Server auf demselben Computer unter ausgeführt wird, was wahr ist, wenn Sie BeanTraderServer lokal starten.)</span><span class="sxs-lookup"><span data-stu-id="998b1-130">(By default, the sample assumes the server is running on the same machine at *http://localhost:8090*, which will be true if you launch BeanTraderServer locally.)</span></span>

<span data-ttu-id="998b1-131">Beachten Sie, dass diese Beispiel-App dazu dient, Herausforderungen und Lösungen für die Portierung von .NET Core zu demonstrieren.</span><span class="sxs-lookup"><span data-stu-id="998b1-131">Keep in mind that this sample app is meant to demonstrate .NET Core porting challenges and solutions.</span></span> <span data-ttu-id="998b1-132">Es ist nicht dazu gedacht, Die Best Practices von WPF zu demonstrieren.</span><span class="sxs-lookup"><span data-stu-id="998b1-132">It's not meant to demonstrate WPF best practices.</span></span> <span data-ttu-id="998b1-133">In der Tat, es enthält absichtlich einige Anti-Muster, um sicherzustellen, dass Sie auf mindestens ein paar interessante Herausforderungen während der Portierung stoßen.</span><span class="sxs-lookup"><span data-stu-id="998b1-133">In fact, it deliberately includes some anti-patterns to make sure you come across at least a couple of interesting challenges while porting.</span></span>

## <a name="getting-ready"></a><span data-ttu-id="998b1-134">Vorbereitung</span><span class="sxs-lookup"><span data-stu-id="998b1-134">Getting ready</span></span>

<span data-ttu-id="998b1-135">Die hauptherausforderung beim Migrieren einer .NET Framework-App zu .NET Core besteht darin, dass ihre Abhängigkeiten möglicherweise anders oder gar nicht funktionieren.</span><span class="sxs-lookup"><span data-stu-id="998b1-135">The primary challenge of migrating a .NET Framework app to .NET Core is that its dependencies may work differently or not at all.</span></span> <span data-ttu-id="998b1-136">Die Migration ist viel einfacher als früher; viele NuGet-Pakete zielen jetzt auf .NET Standard ab.</span><span class="sxs-lookup"><span data-stu-id="998b1-136">Migration is much easier than it used to be; many NuGet packages now target .NET Standard.</span></span> <span data-ttu-id="998b1-137">Ab .NET Core 2.0 sind die Flächen .NET Framework und .NET Core ähnlich geworden.</span><span class="sxs-lookup"><span data-stu-id="998b1-137">Starting with .NET Core 2.0, the .NET Framework and .NET Core surface areas have become similar.</span></span> <span data-ttu-id="998b1-138">Dennoch bleiben einige Unterschiede (sowohl in der Unterstützung von NuGet-Paketen als auch in verfügbaren .NET-APIs) bestehen.</span><span class="sxs-lookup"><span data-stu-id="998b1-138">Even so, some differences (both in support from NuGet packages and in available .NET APIs) remain.</span></span> <span data-ttu-id="998b1-139">Der erste Schritt bei der Migration besteht darin, die Abhängigkeiten der App zu überprüfen und sicherzustellen, dass Verweise in einem Format vorliegen, das leicht zu .NET Core migriert werden kann.</span><span class="sxs-lookup"><span data-stu-id="998b1-139">The first step in migrating is to review the app's dependencies and make sure references are in a format that's easily migrated to .NET Core.</span></span>

### <a name="upgrade-to-packagereference-nuget-references"></a><span data-ttu-id="998b1-140">Upgrade `<PackageReference>` auf NuGet-Referenzen</span><span class="sxs-lookup"><span data-stu-id="998b1-140">Upgrade to `<PackageReference>` NuGet references</span></span>

<span data-ttu-id="998b1-141">Ältere .NET Framework-Projekte listen in der Regel ihre NuGet-Abhängigkeiten in einer *packages.config-Datei* auf.</span><span class="sxs-lookup"><span data-stu-id="998b1-141">Older .NET Framework projects typically list their NuGet dependencies in a *packages.config* file.</span></span> <span data-ttu-id="998b1-142">Das neue Projektdateiformat im SDK-Stil [`<PackageReference>`](/nuget/consume-packages/package-references-in-project-files) verweist auf NuGet-Pakete als Elemente in der csproj-Datei selbst und nicht in einer separaten Konfigurationsdatei.</span><span class="sxs-lookup"><span data-stu-id="998b1-142">The new SDK-style project file format references NuGet packages as [`<PackageReference>`](/nuget/consume-packages/package-references-in-project-files) elements in the csproj file itself rather than in a separate config file.</span></span>

<span data-ttu-id="998b1-143">Bei der Migration bietet die `<PackageReference>`Verwendung von -style-Referenzen zwei Vorteile:</span><span class="sxs-lookup"><span data-stu-id="998b1-143">When migrating, there are two advantages to using `<PackageReference>`-style references:</span></span>

- <span data-ttu-id="998b1-144">Dies ist der Stil des NuGet-Verweises, der für die neue .NET Core-Projektdatei erforderlich ist.</span><span class="sxs-lookup"><span data-stu-id="998b1-144">This is the style of NuGet reference that is required for the new .NET Core project file.</span></span> <span data-ttu-id="998b1-145">Wenn Sie bereits `<PackageReference>`verwenden, können diese Projektdateielemente direkt in das neue Projekt kopiert und eingefügt werden.</span><span class="sxs-lookup"><span data-stu-id="998b1-145">If you're already using `<PackageReference>`, those project file elements can be copied and pasted directly into the new project.</span></span>
- <span data-ttu-id="998b1-146">Im Gegensatz zu einer `<PackageReference>` packages.config-Datei beziehen sich Elemente nur auf die Abhängigkeiten der obersten Ebene, von denen Ihr Projekt direkt abhängt.</span><span class="sxs-lookup"><span data-stu-id="998b1-146">Unlike a packages.config file, `<PackageReference>` elements only refer to the top-level dependencies that your project depends on directly.</span></span> <span data-ttu-id="998b1-147">Alle anderen transitiven NuGet-Pakete werden zur Wiederherstellungszeit ermittelt und in der automatisch generierten Datei obj-project.assets.json aufgezeichnet.</span><span class="sxs-lookup"><span data-stu-id="998b1-147">All other transitive NuGet packages will be determined at restore time and recorded in the autogenerated obj\project.assets.json file.</span></span> <span data-ttu-id="998b1-148">Dies erleichtert die Bestimmung der Abhängigkeiten Ihres Projekts erheblich, was nützlich ist, um zu bestimmen, ob die erforderlichen Abhängigkeiten auf .NET Core funktionieren oder nicht.</span><span class="sxs-lookup"><span data-stu-id="998b1-148">This makes it much easier to determine what dependencies your project has, which is useful when determining whether the necessary dependencies will work on .NET Core or not.</span></span>

<span data-ttu-id="998b1-149">Der erste Schritt zum Migrieren einer .NET Framework-App `<PackageReference>` zu .NET Core besteht darin, sie auf NuGet-Referenzen zu aktualisieren.</span><span class="sxs-lookup"><span data-stu-id="998b1-149">The first step to migrating a .NET Framework app to .NET Core is to update it to use `<PackageReference>` NuGet references.</span></span> <span data-ttu-id="998b1-150">Visual Studio macht dies einfach.</span><span class="sxs-lookup"><span data-stu-id="998b1-150">Visual Studio makes this simple.</span></span> <span data-ttu-id="998b1-151">Klicken Sie im **Projektmappen-Explorer**mit der rechten Maustaste auf die Datei *packages.config* des Projekts , und wählen Sie dann **Packages.config zu PackageReference migrieren**aus.</span><span class="sxs-lookup"><span data-stu-id="998b1-151">Just right-click the project's *packages.config* file in Visual Studio's **Solution Explorer**, and then select **Migrate packages.config to PackageReference**.</span></span>

![Aktualisieren auf PackageReference](./media/convert-project-from-net-framework/package-reference-migration.png)

<span data-ttu-id="998b1-153">Es wird ein Dialogfeld angezeigt, in dem berechnete NuGet-Abhängigkeiten der obersten Ebene angezeigt und gefragt werden, welche anderen NuGet-Pakete auf die oberste Ebene heraufgestuft werden sollen.</span><span class="sxs-lookup"><span data-stu-id="998b1-153">A dialog appears showing calculated top-level NuGet dependencies and asking which other NuGet packages should be promoted to top-level.</span></span> <span data-ttu-id="998b1-154">Keines dieser anderen Pakete muss top-level für die Bean Trader-Probe sein, so dass Sie alle diese Felder deaktivieren können.</span><span class="sxs-lookup"><span data-stu-id="998b1-154">None of these other packages need to be top-level for the Bean Trader sample, so you can uncheck all of those boxes.</span></span> <span data-ttu-id="998b1-155">Klicken Sie dann auf **OK,** und die `<PackageReference>` Datei *packages.config* wird entfernt, und elemente werden der Projektdatei hinzugefügt.</span><span class="sxs-lookup"><span data-stu-id="998b1-155">Then, click **Ok** and the *packages.config* file is removed and `<PackageReference>` elements are added to the project file.</span></span>

<span data-ttu-id="998b1-156">`<PackageReference>`-style-Referenzen speichern NuGet-Pakete nicht lokal in einem Paketordner.</span><span class="sxs-lookup"><span data-stu-id="998b1-156">`<PackageReference>`-style references don't store NuGet packages locally in a packages folder.</span></span> <span data-ttu-id="998b1-157">Stattdessen werden sie global als Optimierung gespeichert.</span><span class="sxs-lookup"><span data-stu-id="998b1-157">Instead, they're stored globally as an optimization.</span></span> <span data-ttu-id="998b1-158">Bearbeiten Sie nach Abschluss der Migration die datei `<Analyzer>` csproj, und entfernen Sie alle Elemente, die sich auf die Analysatoren beziehen, die zuvor von der *.. Verzeichnis von spackages.*</span><span class="sxs-lookup"><span data-stu-id="998b1-158">After the migration completes, edit the csproj file and remove any `<Analyzer>` elements referring to the analyzers that previously came from the *..\packages* directory.</span></span> <span data-ttu-id="998b1-159">Keine Sorge; Da Sie noch über die NuGet-Paketreferenzen verfügen, werden die Analysatoren in das Projekt einbezogen.</span><span class="sxs-lookup"><span data-stu-id="998b1-159">Don't worry; since you still have the NuGet package references, the analyzers will be included in the project.</span></span> <span data-ttu-id="998b1-160">Sie müssen nur die alten packages.config-stil-Elemente `<Analyzer>` bereinigen.</span><span class="sxs-lookup"><span data-stu-id="998b1-160">You just need to clean up the old packages.config-style `<Analyzer>` elements.</span></span>

### <a name="review-nuget-packages"></a><span data-ttu-id="998b1-161">NuGet-Pakete überprüfen</span><span class="sxs-lookup"><span data-stu-id="998b1-161">Review NuGet packages</span></span>

<span data-ttu-id="998b1-162">Nachdem Sie nun die NuGet-Pakete der obersten Ebene sehen können, von denen das Projekt abhängt, können Sie überprüfen, ob diese Pakete in .NET Core verfügbar sind.</span><span class="sxs-lookup"><span data-stu-id="998b1-162">Now that you can see the top-level NuGet packages that the project depends on, you can review whether those packages are available on .NET Core.</span></span> <span data-ttu-id="998b1-163">Sie können bestimmen, ob ein Paket .NET Core unterstützt, indem Sie sich dessen Abhängigkeiten [von nuget.org](https://www.nuget.org/). Die von der Community [erstellte fuget.org-Website](https://www.fuget.org/) zeigt diese Informationen deutlich oben auf der Paketinformationsseite an.</span><span class="sxs-lookup"><span data-stu-id="998b1-163">You can determine whether a package supports .NET Core by looking at its dependencies on [nuget.org](https://www.nuget.org/). The community-created [fuget.org](https://www.fuget.org/) site shows this information prominently at the top of the package information page.</span></span>

<span data-ttu-id="998b1-164">Wenn Sie auf .NET Core 3.0 abzielen, sollten alle Pakete, die auf .NET Core oder .NET Standard abzielen, funktionieren (da .NET Core die .NET Standard-Oberfläche implementiert).</span><span class="sxs-lookup"><span data-stu-id="998b1-164">When targeting .NET Core 3.0, any packages targeting .NET Core or .NET Standard should work (since .NET Core implements the .NET Standard surface area).</span></span> <span data-ttu-id="998b1-165">In einigen Fällen zielt die bestimmte Version eines verwendeten Pakets nicht auf .NET Core oder .NET Standard ab, aber neuere Versionen werden dies tun.</span><span class="sxs-lookup"><span data-stu-id="998b1-165">In some cases, the specific version of a package that's used won't target .NET Core or .NET Standard, but newer versions will.</span></span> <span data-ttu-id="998b1-166">In diesem Fall sollten Sie ein Upgrade auf die neueste Version des Pakets in Betracht ziehen.</span><span class="sxs-lookup"><span data-stu-id="998b1-166">In this case, you should consider upgrading to the latest version of the package.</span></span>

<span data-ttu-id="998b1-167">Sie können auch Pakete verwenden, die auf .NET Framework abzielen, aber das birgt ein gewisses Risiko.</span><span class="sxs-lookup"><span data-stu-id="998b1-167">You can use packages targeting .NET Framework, as well, but that introduces some risk.</span></span> <span data-ttu-id="998b1-168">.NET Core to .NET Framework-Abhängigkeiten sind zulässig, da .NET Core- und .NET Framework-Oberflächenbereiche so ähnlich sind, dass solche Abhängigkeiten *häufig* funktionieren.</span><span class="sxs-lookup"><span data-stu-id="998b1-168">.NET Core to .NET Framework dependencies are allowed because .NET Core and .NET Framework surface areas are similar enough that such dependencies *often* work.</span></span> <span data-ttu-id="998b1-169">Wenn das Paket jedoch versucht, eine .NET-API zu verwenden, die nicht in .NET Core vorhanden ist, wird eine Laufzeitausnahme gefunden.</span><span class="sxs-lookup"><span data-stu-id="998b1-169">However, if the package tries to use a .NET API that isn't present in .NET Core, you'll encounter a runtime exception.</span></span> <span data-ttu-id="998b1-170">Aus diesem Grund sollten Sie auf .NET Framework-Pakete nur verweisen, wenn keine anderen Optionen verfügbar sind, und verstehen, dass dies eine Testlast mit sich bringt.</span><span class="sxs-lookup"><span data-stu-id="998b1-170">Because of that, you should only reference .NET Framework packages when no other options are available and understand that doing so imposes a test burden.</span></span>

<span data-ttu-id="998b1-171">Wenn auf Pakete verwiesen wird, auf die nicht auf .NET Core oder .NET Standard ausgerichtet ist, müssen Sie über andere Alternativen nachdenken:</span><span class="sxs-lookup"><span data-stu-id="998b1-171">If there are packages referenced that don't target .NET Core or .NET Standard, you'll have to think about other alternatives:</span></span>

- <span data-ttu-id="998b1-172">Gibt es andere ähnliche Pakete, die stattdessen verwendet werden können?</span><span class="sxs-lookup"><span data-stu-id="998b1-172">Are there other similar packages that can be used instead?</span></span> <span data-ttu-id="998b1-173">Manchmal veröffentlichen NuGet-Autoren separate ' Core-Versionen ihrer Bibliotheken, die speziell auf .NET Core abzielen.</span><span class="sxs-lookup"><span data-stu-id="998b1-173">Sometimes NuGet authors publish separate '.Core' versions of their libraries specifically targeting .NET Core.</span></span> <span data-ttu-id="998b1-174">Enterprise Library-Pakete sind ein Beispiel für die Community-Veröffentlichung ". NetCore"-Alternativen.</span><span class="sxs-lookup"><span data-stu-id="998b1-174">Enterprise Library packages are an example of the community publishing ".NetCore" alternatives.</span></span> <span data-ttu-id="998b1-175">In anderen Fällen sind neuere SDKs für einen bestimmten Dienst (manchmal mit unterschiedlichen Paketnamen) für .NET Standard verfügbar.</span><span class="sxs-lookup"><span data-stu-id="998b1-175">In other cases, newer SDKs for a particular service (sometimes with different package names) are available for .NET Standard.</span></span> <span data-ttu-id="998b1-176">Wenn keine Alternativen verfügbar sind, können Sie mit den auf .NET Framework ausgerichteten Paketen fortfahren, wobei Sie berücksichtigen müssen, dass Sie sie gründlich testen müssen, sobald Sie auf .NET Core ausgeführt werden.</span><span class="sxs-lookup"><span data-stu-id="998b1-176">If no alternatives are available, you can proceed using the .NET Framework-targeted packages, bearing in mind that you'll need to test them thoroughly once running on .NET Core.</span></span>

<span data-ttu-id="998b1-177">Das Bean Trader-Beispiel hat die folgenden NuGet-Abhängigkeiten der obersten Ebene:</span><span class="sxs-lookup"><span data-stu-id="998b1-177">The Bean Trader sample has the following top-level NuGet dependencies:</span></span>

- [<span data-ttu-id="998b1-178">**Castle.Windsor, Version 4.1.1**</span><span class="sxs-lookup"><span data-stu-id="998b1-178">**Castle.Windsor, version 4.1.1**</span></span>](https://www.castleproject.org/projects/windsor/)  

  <span data-ttu-id="998b1-179">Dieses Paket zielt auf .NET Standard 1.6 ab, sodass es auf .NET Core funktioniert.</span><span class="sxs-lookup"><span data-stu-id="998b1-179">This package targets .NET Standard 1.6, so it works on .NET Core.</span></span>

- [<span data-ttu-id="998b1-180">**Microsoft.CodeAnalysis.FxCopAnalyzers, Version 2.6.3**</span><span class="sxs-lookup"><span data-stu-id="998b1-180">**Microsoft.CodeAnalysis.FxCopAnalyzers, version 2.6.3**</span></span>](https://www.nuget.org/packages/Microsoft.CodeAnalysis.FxCopAnalyzers/2.6.3)  
  <span data-ttu-id="998b1-181">Dies ist ein Metapaket, daher ist nicht sofort klar, welche Plattformen es unterstützt, aber die [Dokumentation](https://github.com/dotnet/roslyn-analyzers#microsoftcodeanalysisfxcopanalyzers) zeigt an, dass die neueste Version (2.9.2) sowohl für .NET Framework als auch für .NET Core funktioniert.</span><span class="sxs-lookup"><span data-stu-id="998b1-181">This is a meta-package, so it's not immediately obvious which platforms it supports, but [documentation](https://github.com/dotnet/roslyn-analyzers#microsoftcodeanalysisfxcopanalyzers) indicates that its newest version (2.9.2) will work for both .NET Framework and .NET Core.</span></span>

- [<span data-ttu-id="998b1-182">**Nito.AsyncEx, Version 4.0.1**</span><span class="sxs-lookup"><span data-stu-id="998b1-182">**Nito.AsyncEx, version 4.0.1**</span></span>](https://www.nuget.org/packages/Nito.AsyncEx/4.0.1)  

  <span data-ttu-id="998b1-183">Dieses Paket zielt nicht auf .NET Core ab, aber die neuere Version 5.0.</span><span class="sxs-lookup"><span data-stu-id="998b1-183">This package doesn't target .NET Core, but the newer 5.0 version does.</span></span> <span data-ttu-id="998b1-184">Dies ist bei der Migration üblich, da viele NuGet-Pakete kürzlich .NET Standard-Unterstützung hinzugefügt haben, ältere Projektversionen jedoch nur auf .NET Framework abzielen.</span><span class="sxs-lookup"><span data-stu-id="998b1-184">This is common when migrating because many NuGet packages have added .NET Standard support recently, but older project versions will only target .NET Framework.</span></span> <span data-ttu-id="998b1-185">Wenn der Versionsunterschied nur ein kleiner Versionsunterschied ist, ist es oft einfach, auf die neuere Version zu aktualisieren.</span><span class="sxs-lookup"><span data-stu-id="998b1-185">If the version difference is only a minor version difference, it's often easy to upgrade to the newer version.</span></span> <span data-ttu-id="998b1-186">Da es sich um eine wichtige Versionsänderung handelt, müssen Sie ein vorsichtiges Upgrade durchführen, da es zu Änderungen im Paket führen könnte.</span><span class="sxs-lookup"><span data-stu-id="998b1-186">Because this is a major version change, you need to be cautious upgrading since there could be breaking changes in the package.</span></span> <span data-ttu-id="998b1-187">Es gibt jedoch einen Weg nach vorn, der gut ist.</span><span class="sxs-lookup"><span data-stu-id="998b1-187">There is a path forward, though, which is good.</span></span>

- [<span data-ttu-id="998b1-188">**MahApps.Metro, Version 1.6.5**</span><span class="sxs-lookup"><span data-stu-id="998b1-188">**MahApps.Metro, version 1.6.5**</span></span>](https://www.nuget.org/packages/MahApps.Metro/1.6.5)  

  <span data-ttu-id="998b1-189">Dieses Paket zielt auch nicht auf .NET Core ab, sondern verfügt über eine neuere Vorabversion (2.0-alpha), die dies tut.</span><span class="sxs-lookup"><span data-stu-id="998b1-189">This package also doesn't target .NET Core, but has a newer pre-release (2.0-alpha) that does.</span></span> <span data-ttu-id="998b1-190">Auch hier müssen Sie auf die bahnbrechenden Änderungen achten, aber das neuere Paket ist ermutigend.</span><span class="sxs-lookup"><span data-stu-id="998b1-190">Again, you have to look out for breaking changes, but the newer package is encouraging.</span></span>

<span data-ttu-id="998b1-191">Die NuGet-Abhängigkeiten des Bean Trader-Beispiels zielen entweder auf .NET Standard/.NET Core ab oder verfügen über neuere Versionen, sodass es hier wahrscheinlich keine Blockierungsprobleme geben wird.</span><span class="sxs-lookup"><span data-stu-id="998b1-191">The Bean Trader sample's NuGet dependencies all either target .NET Standard/.NET Core or have newer versions that do, so there are unlikely to be any blocking issues here.</span></span>

### <a name="upgrade-nuget-packages"></a><span data-ttu-id="998b1-192">Aktualisieren von NuGet-Paketen</span><span class="sxs-lookup"><span data-stu-id="998b1-192">Upgrade NuGet packages</span></span>

<span data-ttu-id="998b1-193">Wenn möglich, wäre es gut, die Versionen von Paketen, die nur auf .NET Core oder .NET Standard abzielen, mit neueren Versionen zu diesem Zeitpunkt zu aktualisieren (wobei das Projekt immer noch auf .NET Framework abzielt), um änderungen dezentrale Änderungen frühzeitig zu ermitteln und zu beheben.</span><span class="sxs-lookup"><span data-stu-id="998b1-193">If possible, it would be good to upgrade the versions of any packages that only target .NET Core or .NET Standard with more recent versions at this point (with the project still targeting .NET Framework) to discover and address any breaking changes early.</span></span>

<span data-ttu-id="998b1-194">Wenn Sie keine wesentlichen Änderungen an der vorhandenen .NET Framework-Version der App vornehmen möchten, kann dies warten, bis Sie eine neue Projektdatei für .NET Core haben.</span><span class="sxs-lookup"><span data-stu-id="998b1-194">If you would rather not make any material changes to the existing .NET Framework version of the app, this can wait until you have a new project file targeting .NET Core.</span></span> <span data-ttu-id="998b1-195">Das vorherige Upgrade der NuGet-Pakete auf .NET Core-kompatible Versionen erleichtert den Migrationsprozess jedoch noch einfacher, wenn Sie die neue Projektdatei erstellen, und verringert die Anzahl der Unterschiede zwischen den .NET Framework- und .NET Core-Versionen der App.</span><span class="sxs-lookup"><span data-stu-id="998b1-195">However, upgrading the NuGet packages to .NET Core-compatible versions ahead of time makes the migration process even easier once you create the new project file and reduces the number of differences between the .NET Framework and .NET Core versions of the app.</span></span>

<span data-ttu-id="998b1-196">Mit dem Bean Trader-Beispiel können alle erforderlichen Upgrades einfach durchgeführt werden (mit Visual Studio NuGet-Paketmanager) mit einer Ausnahme: Ein Upgrade von **MahApps.Metro 1.6.5** auf **2.0** zeigt brechende Änderungen im Zusammenhang mit Design- und Akzentverwaltungs-APIs.</span><span class="sxs-lookup"><span data-stu-id="998b1-196">With the Bean Trader sample, all of the necessary upgrades can be made easily (using Visual Studio's NuGet package manager) with one exception: upgrading from **MahApps.Metro 1.6.5** to **2.0** reveals breaking changes related to theme and accent management APIs.</span></span>

<span data-ttu-id="998b1-197">Im Idealfall wird die App aktualisiert, um die neuere Version des Pakets zu verwenden (da dies wahrscheinlicher auf .NET Core funktioniert).</span><span class="sxs-lookup"><span data-stu-id="998b1-197">Ideally, the app would be updated to use the newer version of the package (since that is more likely to work on .NET Core).</span></span> <span data-ttu-id="998b1-198">In einigen Fällen ist dies jedoch möglicherweise nicht durchführbar.</span><span class="sxs-lookup"><span data-stu-id="998b1-198">In some cases, however, that may not be feasible.</span></span> <span data-ttu-id="998b1-199">Aktualisieren Sie **MahApps.Metro** in diesen Fällen nicht, da die erforderlichen Änderungen nicht trivial sind und sich dieses Tutorial auf die Migration zu .NET Core 3 und nicht auf **MahApps.Metro 2 konzentriert.**</span><span class="sxs-lookup"><span data-stu-id="998b1-199">In these cases, don't upgrade **MahApps.Metro** because the necessary changes are non-trivial and this tutorial focuses on migrating to .NET Core 3, not to **MahApps.Metro 2.**</span></span> <span data-ttu-id="998b1-200">Außerdem ist dies eine risikoarme .NET Framework-Abhängigkeit, da die Bean Trader-App nur einen kleinen Teil von **MahApps.Metro**ausübt.</span><span class="sxs-lookup"><span data-stu-id="998b1-200">Also, this is a low-risk .NET Framework dependency because the Bean Trader app only exercises a small part of **MahApps.Metro**.</span></span> <span data-ttu-id="998b1-201">Natürlich müssen Tests durchgeführt werden, um sicherzustellen, dass alles funktioniert, sobald die Migration abgeschlossen ist.</span><span class="sxs-lookup"><span data-stu-id="998b1-201">It will, of course, require testing to make sure everything's working once the migration is complete.</span></span> <span data-ttu-id="998b1-202">Wäre dies ein reales Szenario, wäre es gut, ein Problem einzureichen, um die Arbeit zu verfolgen, um zu **MahApps.Metro** Version 2.0 zu wechseln, da die Migration jetzt nicht erfolgt, hinterlässt einige technische Schulden.</span><span class="sxs-lookup"><span data-stu-id="998b1-202">If this were a real-world scenario, it would be good to file an issue to track the work to move to **MahApps.Metro** version 2.0 since not doing the migration now leaves behind some technical debt.</span></span>

<span data-ttu-id="998b1-203">Sobald die NuGet-Pakete auf aktuelle `<PackageReference>` Versionen aktualisiert wurden, sollte die Artikelgruppe in der Projektdatei des Bean Trader-Beispiels wie folgt aussehen.</span><span class="sxs-lookup"><span data-stu-id="998b1-203">Once the NuGet packages are updated to recent versions, the `<PackageReference>` item group in the Bean Trader sample's project file should look like this.</span></span>

```xml
<ItemGroup>
  <PackageReference Include="Castle.Windsor">
    <Version>4.1.1</Version>
  </PackageReference>
  <PackageReference Include="MahApps.Metro">
    <Version>1.6.5</Version>
  </PackageReference>
  <PackageReference Include="Microsoft.CodeAnalysis.FxCopAnalyzers">
    <Version>2.9.2</Version>
  </PackageReference>
  <PackageReference Include="Nito.AsyncEx">
    <Version>5.0.0</Version>
  </PackageReference>
</ItemGroup>
```

### <a name="net-framework-portability-analysis"></a><span data-ttu-id="998b1-204">.NET Framework-Portabilitätsanalyse</span><span class="sxs-lookup"><span data-stu-id="998b1-204">.NET Framework portability analysis</span></span>

<span data-ttu-id="998b1-205">Sobald Sie den Status der NuGet-Abhängigkeiten Ihres Projekts verstanden haben, sollten Sie als nächstes die .NET Framework-API-Abhängigkeiten berücksichtigen.</span><span class="sxs-lookup"><span data-stu-id="998b1-205">Once you understand the state of your project's NuGet dependencies, the next thing to consider is .NET Framework API dependencies.</span></span> <span data-ttu-id="998b1-206">Das [.NET Portability Analyzer-Tool](../../standard/analyzers/portability-analyzer.md) ist nützlich, um zu verstehen, welche der .NET-APIs, die Ihr Projekt verwendet, auf anderen .NET-Plattformen verfügbar sind.</span><span class="sxs-lookup"><span data-stu-id="998b1-206">The [.NET Portability Analyzer](../../standard/analyzers/portability-analyzer.md) tool is useful for understanding which of the .NET APIs your project uses are available on other .NET platforms.</span></span>

<span data-ttu-id="998b1-207">Das Tool kommt als [Visual Studio-Plugin](https://marketplace.visualstudio.com/items?itemName=ConnieYau.NETPortabilityAnalyzer), ein [Befehlszeilen-Tool](https://github.com/Microsoft/dotnet-apiport/releases), oder in eine [einfache GUI](https://github.com/Microsoft/dotnet-apiport-ui)eingewickelt , die seine Optionen vereinfacht.</span><span class="sxs-lookup"><span data-stu-id="998b1-207">The tool comes as a [Visual Studio plugin](https://marketplace.visualstudio.com/items?itemName=ConnieYau.NETPortabilityAnalyzer), a [command-line tool](https://github.com/Microsoft/dotnet-apiport/releases), or wrapped in a [simple GUI](https://github.com/Microsoft/dotnet-apiport-ui), which simplifies its options.</span></span> <span data-ttu-id="998b1-208">Sie können mehr über die Verwendung von .NET Portability Analyzer (API Port) mithilfe der GUI in den [Porting-Desktop-Apps zu .NET](https://devblogs.microsoft.com/dotnet/porting-desktop-apps-to-net-core/) Core-Blogbeitrag lesen.</span><span class="sxs-lookup"><span data-stu-id="998b1-208">You can read more about using the .NET Portability Analyzer (API Port) using the GUI in the [Porting desktop apps to .NET Core](https://devblogs.microsoft.com/dotnet/porting-desktop-apps-to-net-core/) blog post.</span></span> <span data-ttu-id="998b1-209">Wenn Sie die Befehlszeile verwenden möchten, sind die folgenden Schritte erforderlich:</span><span class="sxs-lookup"><span data-stu-id="998b1-209">If you prefer to use the command line, the necessary steps are:</span></span>

1. <span data-ttu-id="998b1-210">Laden Sie [den .NET Portability Analyzer](https://github.com/Microsoft/dotnet-apiport/releases) herunter, wenn Sie ihn noch nicht haben.</span><span class="sxs-lookup"><span data-stu-id="998b1-210">Download the [.NET Portability Analyzer](https://github.com/Microsoft/dotnet-apiport/releases) if you don't already have it.</span></span>
1. <span data-ttu-id="998b1-211">Stellen Sie sicher, dass die .NET Framework-App erfolgreich portiert wird (dies ist unabhängig vor der Migration eine gute Idee).</span><span class="sxs-lookup"><span data-stu-id="998b1-211">Make sure the .NET Framework app to be ported builds successfully (this is a good idea prior to migration regardless).</span></span>
1. <span data-ttu-id="998b1-212">Führen Sie API Port mit einer solchen Befehlszeile aus.</span><span class="sxs-lookup"><span data-stu-id="998b1-212">Run API Port with a command line like this.</span></span>

    ```console
    ApiPort.exe analyze -f <PathToBeanTraderBinaries> -r html -r excel -t ".NET Core"
    ```

    <span data-ttu-id="998b1-213">Das `-f` Argument gibt den Pfad an, der die zu analysierenden Binärdateien enthält.</span><span class="sxs-lookup"><span data-stu-id="998b1-213">The `-f` argument specifies the path containing the binaries to analyze.</span></span> <span data-ttu-id="998b1-214">Das `-r` Argument gibt an, welches Ausgabedateiformat Sie verwenden möchten.</span><span class="sxs-lookup"><span data-stu-id="998b1-214">The `-r` argument specifies which output file format you want.</span></span> <span data-ttu-id="998b1-215">Das `-t` Argument gibt an, gegen welche .NET-Plattform die API-Nutzung analysiert werden soll.</span><span class="sxs-lookup"><span data-stu-id="998b1-215">The `-t` argument specifies which .NET platform to analyze API usage against.</span></span> <span data-ttu-id="998b1-216">In diesem Fall soll .NET Core.</span><span class="sxs-lookup"><span data-stu-id="998b1-216">In this case, you want .NET Core.</span></span>

<span data-ttu-id="998b1-217">Wenn Sie den HTML-Bericht öffnen, werden im ersten Abschnitt alle analysierten Binärdateien und der Prozentsatz der von ihnen eingesetzten .NET-APIs auf der Zielplattform aufgeführt.</span><span class="sxs-lookup"><span data-stu-id="998b1-217">When you open the HTML report, the first section will list all of the analyzed binaries and what percentage of the .NET APIs they use are available on the targeted platform.</span></span> <span data-ttu-id="998b1-218">Der Prozentsatz ist für sich genommen nicht aussagekräftig.</span><span class="sxs-lookup"><span data-stu-id="998b1-218">The percentage is not meaningful by itself.</span></span> <span data-ttu-id="998b1-219">Was noch nützlicher ist, ist, die spezifischen APIs zu sehen, die fehlen.</span><span class="sxs-lookup"><span data-stu-id="998b1-219">What's more useful is to see the specific APIs that are missing.</span></span> <span data-ttu-id="998b1-220">Wählen Sie dazu entweder einen Assemblynamen aus, oder scrollen Sie zu den Berichten für einzelne Assemblys nach unten.</span><span class="sxs-lookup"><span data-stu-id="998b1-220">To do that, either select an assembly name or scroll down to the reports for individual assemblies.</span></span>

<span data-ttu-id="998b1-221">Konzentrieren Sie sich auf die Assemblys, für die Sie den Quellcode besitzen.</span><span class="sxs-lookup"><span data-stu-id="998b1-221">Focus on the assemblies that you own the source code for.</span></span> <span data-ttu-id="998b1-222">Im Bean Trader ApiPort-Bericht sind beispielsweise viele Binärdateien aufgeführt, aber die meisten von ihnen gehören zu NuGet-Paketen.</span><span class="sxs-lookup"><span data-stu-id="998b1-222">In the Bean Trader ApiPort report, for example, there are many binaries listed, but most of them belong to NuGet packages.</span></span> <span data-ttu-id="998b1-223">`Castle.Windsor`zeigt, dass dies von einigen System.Web-APIs abhängt, die in .NET Core fehlen.</span><span class="sxs-lookup"><span data-stu-id="998b1-223">`Castle.Windsor` shows that it depends on some System.Web APIs that are missing in .NET Core.</span></span> <span data-ttu-id="998b1-224">Dies ist kein Problem, da `Castle.Windsor` Sie zuvor überprüft haben, dass .NET Core unterstützt wird.</span><span class="sxs-lookup"><span data-stu-id="998b1-224">This isn't a concern because you previously verified that `Castle.Windsor` supports .NET Core.</span></span> <span data-ttu-id="998b1-225">Es ist üblich, dass NuGet-Pakete über unterschiedliche Binärdateien für die Verwendung mit `Castle.Windsor` verschiedenen .NET-Plattformen verfügen, sodass es irrelevant ist, ob die .NET Framework-Version von System.Web-APIs verwendet oder nicht, solange das Paket auch auf .NET Standard oder .NET Core abzielt (was dies tut).</span><span class="sxs-lookup"><span data-stu-id="998b1-225">It is common for NuGet packages to have different binaries for use with different .NET platforms, so whether the .NET Framework version of `Castle.Windsor` uses System.Web APIs or not is irrelevant as long as the package also targets .NET Standard or .NET Core (which it does).</span></span>

<span data-ttu-id="998b1-226">Mit dem Bean Trader-Beispiel ist die einzige Binärdatei, die Sie berücksichtigen müssen, **BeanTraderClient** und der Bericht zeigt, dass nur zwei .NET-APIs fehlen: `System.ServiceModel.ClientBase<T>.Close` und `System.ServiceModel.ClientBase<T>.Open`.</span><span class="sxs-lookup"><span data-stu-id="998b1-226">With the Bean Trader sample, the only binary that you need to consider is **BeanTraderClient** and the report shows that only two .NET APIs are missing: `System.ServiceModel.ClientBase<T>.Close` and `System.ServiceModel.ClientBase<T>.Open`.</span></span>

![BeanTraderClient-Portabilitätsbericht](./media/convert-project-from-net-framework/portability-report.png)

<span data-ttu-id="998b1-228">Es ist unwahrscheinlich, dass sie Probleme blockieren, da WCF-Client-APIs (meist) auf .NET Core unterstützt werden, sodass für diese zentralen APIs Alternativen verfügbar sein müssen.</span><span class="sxs-lookup"><span data-stu-id="998b1-228">These are unlikely to be blocking issues because WCF Client APIs are (mostly) supported on .NET Core, so there must be alternatives available for these central APIs.</span></span> <span data-ttu-id="998b1-229">Wenn Sie sich `System.ServiceModel`die .NET Core-Oberfläche <https://apisof.net>von sehen (verwenden ), sehen Sie stattdessen, dass es stattdessen Async-Alternativen in .NET Core gibt.</span><span class="sxs-lookup"><span data-stu-id="998b1-229">In fact, looking at `System.ServiceModel`'s .NET Core surface area (using <https://apisof.net>), you see that there are async alternatives in .NET Core instead.</span></span>

<span data-ttu-id="998b1-230">Basierend auf diesem Bericht und der vorherigen NuGet-Abhängigkeitsanalyse scheint es keine größeren Probleme bei der Migration des Bean Trader-Beispiels zu .NET Core zu geben.</span><span class="sxs-lookup"><span data-stu-id="998b1-230">Based on this report and the previous NuGet dependency analysis, it looks like there should be no major issues migrating the Bean Trader sample to .NET Core.</span></span> <span data-ttu-id="998b1-231">Sie sind bereit für den nächsten Schritt, in dem Sie die Migration tatsächlich starten.</span><span class="sxs-lookup"><span data-stu-id="998b1-231">You're ready for the next step in which you'll actually start the migration.</span></span>

## <a name="migrating-the-project-file"></a><span data-ttu-id="998b1-232">Migrieren der Projektdatei</span><span class="sxs-lookup"><span data-stu-id="998b1-232">Migrating the project file</span></span>

<span data-ttu-id="998b1-233">Wenn Ihre App nicht das neue [Projektdateiformat im SDK-Stil](../../core/tools/csproj.md)verwendet, benötigen Sie eine neue Projektdatei, um .NET Core als Ziel zu verwenden.</span><span class="sxs-lookup"><span data-stu-id="998b1-233">If your app isn't using the new [SDK-style project file format](../../core/tools/csproj.md), you'll need a new project file to target .NET Core.</span></span> <span data-ttu-id="998b1-234">Sie können die vorhandene csproj-Datei ersetzen oder, wenn Sie es vorziehen, das vorhandene Projekt im aktuellen Zustand unberührt zu lassen, eine neue csproj-Datei hinzufügen, die auf .NET Core abzielt.</span><span class="sxs-lookup"><span data-stu-id="998b1-234">You can replace the existing csproj file or, if you prefer to keep the existing project untouched in its current state, you can add a new csproj file targeting .NET Core.</span></span> <span data-ttu-id="998b1-235">Sie können Versionen der App für .NET Framework und .NET Core mit einer einzigen Projektdatei `<TargetFrameworks>` im SDK-Stil mit [Multi-Targeting](../../standard/library-guidance/cross-platform-targeting.md) (unter Angabe mehrerer Ziele) erstellen.</span><span class="sxs-lookup"><span data-stu-id="998b1-235">You can build versions of the app for .NET Framework and .NET Core with a single SDK-style project file with [multi-targeting](../../standard/library-guidance/cross-platform-targeting.md) (specifying multiple `<TargetFrameworks>` targets).</span></span>

<span data-ttu-id="998b1-236">Um die neue Projektdatei zu erstellen, können Sie ein neues `dotnet new wpf` WPF-Projekt in Visual Studio erstellen oder den Befehl in einem temporären Verzeichnis verwenden, um die Projektdatei zu generieren und sie dann an den richtigen Speicherort zu kopieren/umbenennen.</span><span class="sxs-lookup"><span data-stu-id="998b1-236">To create the new project file, you can create a new WPF project in Visual Studio or use the `dotnet new wpf` command in a temporary directory to generate the project file and then copy/rename it to the correct location.</span></span> <span data-ttu-id="998b1-237">Es gibt auch ein von der Community erstelltes Tool, [CsprojToVs2017](https://github.com/hvanbakel/CsprojToVs2017), das einen Teil der Projektdateimigration automatisieren kann.</span><span class="sxs-lookup"><span data-stu-id="998b1-237">There is also a community-created tool, [CsprojToVs2017](https://github.com/hvanbakel/CsprojToVs2017), that can automate some of the project file migration.</span></span> <span data-ttu-id="998b1-238">Das Tool ist hilfreich, benötigt aber immer noch einen Benutzer, um die Ergebnisse zu überprüfen, um sicherzustellen, dass alle Details der Migration korrekt sind.</span><span class="sxs-lookup"><span data-stu-id="998b1-238">The tool is helpful but still needs a human to review the results to make sure all the details of the migration are correct.</span></span> <span data-ttu-id="998b1-239">Ein besonderer Bereich, den das Tool nicht optimal behandelt, ist das Migrieren von NuGet-Paketen aus *packages.config-Dateien.*</span><span class="sxs-lookup"><span data-stu-id="998b1-239">One particular area that the tool doesn't handle optimally is migrating NuGet packages from *packages.config* files.</span></span> <span data-ttu-id="998b1-240">Wenn das Tool auf einer Projektdatei ausgeführt wird, die immer noch eine *packages.config-Datei* verwendet, um auf NuGet-Pakete zu verweisen, wird es automatisch zu `<PackageReference>` Elementen migriert, fügt `<PackageReference>` jedoch Elemente für *alle* Pakete hinzu, anstatt nur die Pakete der obersten Ebene.</span><span class="sxs-lookup"><span data-stu-id="998b1-240">If the tool runs on a project file that still uses a *packages.config* file to reference NuGet packages, it will migrate to `<PackageReference>` elements automatically, but will add `<PackageReference>` elements for *all* of the packages instead of just top-level ones.</span></span> <span data-ttu-id="998b1-241">Wenn Sie bereits`<PackageReference>` zu Elementen mit Visual Studio migriert haben (wie in diesem Beispiel), kann das Tool bei der restlichen Konvertierung helfen.</span><span class="sxs-lookup"><span data-stu-id="998b1-241">If you have already migrated to`<PackageReference>` elements with Visual Studio (as you've done in this sample), then the tool can help with the rest of the conversion.</span></span> <span data-ttu-id="998b1-242">Wie Scott Hanselman empfiehlt in [seinem Blog-Beitrag über die Migration von csproj-Dateien](https://www.hanselman.com/blog/UpgradingAnExistingNETProjectFilesToTheLeanNewCSPROJFormatFromNETCore.aspx), Portieren von Hand ist pädagogisch und wird bessere Ergebnisse geben, wenn Sie nur ein paar Projekte zu portieren haben.</span><span class="sxs-lookup"><span data-stu-id="998b1-242">Like Scott Hanselman recommends in [his blog post on migrating csproj files](https://www.hanselman.com/blog/UpgradingAnExistingNETProjectFilesToTheLeanNewCSPROJFormatFromNETCore.aspx), porting by hand is educational and will give better results if you only have a few projects to port.</span></span> <span data-ttu-id="998b1-243">Aber wenn Sie Dutzende oder Hunderte von Projektdateien portieren, dann kann ein Tool wie [CsprojToVs2017] eine Hilfe sein.</span><span class="sxs-lookup"><span data-stu-id="998b1-243">But if you are porting dozens or hundreds of project files, then a tool like [CsprojToVs2017] can be a help.</span></span>

<span data-ttu-id="998b1-244">Um eine neue Projektdatei für das Bean Trader-Beispiel zu erstellen, `dotnet new wpf` führen Sie in einem temporären Verzeichnis aus und verschieben Sie die generierte *.csproj-Datei* in den *BeanTraderClient-Ordner* und benennen Sie sie in **BeanTraderClient.Core.csproj**um.</span><span class="sxs-lookup"><span data-stu-id="998b1-244">To create a new project file for the Bean Trader sample, run `dotnet new wpf` in a temporary directory and move the generated *.csproj* file into the *BeanTraderClient* folder and rename it **BeanTraderClient.Core.csproj**.</span></span>

<span data-ttu-id="998b1-245">Da das neue Projektdateiformat automatisch C-Dateien, *Resx-Dateien* und XAML-Dateien enthält, die es in oder unter seinem Verzeichnis findet, ist die Projektdatei bereits fast vollständig!</span><span class="sxs-lookup"><span data-stu-id="998b1-245">Because the new project file format automatically includes C# files, *resx* files, and XAML files that it finds in or under its directory, the project file is already almost complete!</span></span> <span data-ttu-id="998b1-246">Um die Migration abzuschließen, öffnen Sie die alten und neuen Projektdateien nebeneinander, und schauen Sie sich die alte an, um zu sehen, ob die darin enthaltenen Informationen migriert werden müssen.</span><span class="sxs-lookup"><span data-stu-id="998b1-246">To finish the migration, open the old and new project files side-by-side and look through the old one to see if any information it contains needs to be migrated.</span></span> <span data-ttu-id="998b1-247">Im Beispielfall Bean Trader sollten die folgenden Elemente in das neue Projekt kopiert werden:</span><span class="sxs-lookup"><span data-stu-id="998b1-247">In the Bean Trader sample case, the following items should be copied to the new project:</span></span>

- <span data-ttu-id="998b1-248">Die `<RootNamespace>` `<AssemblyName>`, `<ApplicationIcon>` und Eigenschaften sollten alle kopiert werden.</span><span class="sxs-lookup"><span data-stu-id="998b1-248">The `<RootNamespace>`, `<AssemblyName>`, and `<ApplicationIcon>` properties should all be copied.</span></span>

- <span data-ttu-id="998b1-249">Sie müssen der `<GenerateAssemblyInfo>false</GenerateAssemblyInfo>` neuen Projektdatei auch eine Eigenschaft hinzufügen, da das Bean `[AssemblyTitle]`Trader-Beispiel Attribute auf Assemblyebene (wie ) in einer AssemblyInfo.cs-Datei enthält.</span><span class="sxs-lookup"><span data-stu-id="998b1-249">You also need to add a `<GenerateAssemblyInfo>false</GenerateAssemblyInfo>` property to the new project file since the Bean Trader sample includes assembly-level attributes (like `[AssemblyTitle]`) in an AssemblyInfo.cs file.</span></span> <span data-ttu-id="998b1-250">Standardmäßig werden neue Projekte im SDK-Stil diese Attribute automatisch basierend auf Eigenschaften in der csproj-Datei automatisch generieren.</span><span class="sxs-lookup"><span data-stu-id="998b1-250">By default, new SDK-style projects will autogenerate these attributes based on properties in the csproj file.</span></span> <span data-ttu-id="998b1-251">Da dies in diesem Fall nicht geschehen soll (die automatisch generierten Attribute würden mit denen `<GenerateAssemblyInfo>`von AssemblyInfo.cs in Konflikt stehen), deaktivieren Sie die automatisch generierten Attribute mit .</span><span class="sxs-lookup"><span data-stu-id="998b1-251">Because you don't want that to happen in this case (the autogenerated attributes would conflict with those from AssemblyInfo.cs), you disable the autogenerated attributes with `<GenerateAssemblyInfo>`.</span></span>

- <span data-ttu-id="998b1-252">Obwohl *resx-Dateien* automatisch als eingebettete Ressourcen eingeschlossen werden, sind andere `<Resource>` Elemente wie Bilder nicht enthalten.</span><span class="sxs-lookup"><span data-stu-id="998b1-252">Although *resx* files are automatically included as embedded resources, other `<Resource>` items like images are not.</span></span> <span data-ttu-id="998b1-253">Kopieren Sie `<Resource>` also die Elemente zum Einbetten von Bild- und Symboldateien.</span><span class="sxs-lookup"><span data-stu-id="998b1-253">So, copy the `<Resource>` elements for embedding image and icon files.</span></span> <span data-ttu-id="998b1-254">Sie können die Png-Referenzen auf eine einzelne Zeile vereinfachen, indem Sie `<Resource Include="**\*.png" />`die Unterstützung des neuen Projektdateiformats für Globbing-Muster verwenden: .</span><span class="sxs-lookup"><span data-stu-id="998b1-254">You can simplify the png references to a single line by using the new project file format's support for globbing patterns: `<Resource Include="**\*.png" />`.</span></span>

- <span data-ttu-id="998b1-255">Ebenso `<None>` werden Elemente automatisch eingeschlossen, aber standardmäßig nicht in das Ausgabeverzeichnis kopiert.</span><span class="sxs-lookup"><span data-stu-id="998b1-255">Similarly, `<None>` items are included automatically, but they aren't copied to the output directory, by default.</span></span> <span data-ttu-id="998b1-256">Da das Bean Trader-Projekt `<None>` ein Element enthält, das in das Ausgabeverzeichnis kopiert *wird* (mit `PreserveNewest` Verhalten), müssen Sie das automatisch ausgefüllte `<None>` Element für diese Datei wie diese aktualisieren.</span><span class="sxs-lookup"><span data-stu-id="998b1-256">Because the Bean Trader project includes a `<None>` item that *is* copied to the output directory (using `PreserveNewest` behaviors), you need to update the automatically populated `<None>` item for that file, like this.</span></span>

  ```xml
  <None Update="BeanTrader.pfx">
    <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
  </None>
  ```

- <span data-ttu-id="998b1-257">Das Bean Trader-Beispiel enthält eine XAML-Datei (Default.Accent.xaml) als `Content` (anstelle als `Page`) da in dieser Datei definierte Designs und Akzente zur Laufzeit aus dem XAML der Datei geladen werden, anstatt in die App selbst eingebettet zu werden.</span><span class="sxs-lookup"><span data-stu-id="998b1-257">The Bean Trader sample includes a XAML file (Default.Accent.xaml) as `Content` (rather than as a `Page`) because themes and accents defined in this file are loaded from the file's XAML at runtime, rather than being embedded in the app itself.</span></span> <span data-ttu-id="998b1-258">Das neue Projektsystem enthält diese `<Page>`Datei jedoch automatisch als , da es sich um eine XAML-Datei handelt.</span><span class="sxs-lookup"><span data-stu-id="998b1-258">The new project system automatically includes this file as a `<Page>`, however, since it's a XAML file.</span></span> <span data-ttu-id="998b1-259">Sie müssen also sowohl die XAML-Datei`<Page Remove="**\Default.Accent.xaml" />`als Seite ( ) entfernen als auch als Inhalt hinzufügen.</span><span class="sxs-lookup"><span data-stu-id="998b1-259">So, you need to both remove the XAML file as a page (`<Page Remove="**\Default.Accent.xaml" />`) and add it as content.</span></span>

  ```xml
  <Content Include="Resources\Themes\Default.Accent.xaml">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
  </Content>
  ```

- <span data-ttu-id="998b1-260">Fügen Sie schließlich NuGet-Referenzen hinzu, indem Sie die `<ItemGroup>` mit allen `<PackageReference>` Elementen kopieren.</span><span class="sxs-lookup"><span data-stu-id="998b1-260">Finally, add NuGet references by copying the `<ItemGroup>` with all the `<PackageReference>` elements.</span></span> <span data-ttu-id="998b1-261">Wenn Sie die NuGet-Pakete zuvor nicht auf .NET Core-kompatible Versionen aktualisiert haben, können Sie dies jetzt tun, da sich die Paketverweise in einem .NET Core-spezifischen Projekt befinden.</span><span class="sxs-lookup"><span data-stu-id="998b1-261">If you hadn't previously upgraded the NuGet packages to .NET Core-compatible versions, you could do that now that the package references are in a .NET Core-specific project.</span></span>

<span data-ttu-id="998b1-262">An dieser Stelle sollte es möglich sein, das neue Projekt zur BeanTrader-Lösung hinzuzufügen und es in Visual Studio zu öffnen.</span><span class="sxs-lookup"><span data-stu-id="998b1-262">At this point, it should be possible to add the new project to the BeanTrader solution and open it in Visual Studio.</span></span> <span data-ttu-id="998b1-263">Das Projekt sollte im **Projektmappen-Explorer**korrekt aussehen und `dotnet restore BeanTraderClient.Core.csproj` Pakete erfolgreich wiederherstellen (mit zwei erwarteten Warnungen im Zusammenhang mit der MahApps.Metro-Version, die Sie für .NET Framework verwenden).</span><span class="sxs-lookup"><span data-stu-id="998b1-263">The project should look correct in **Solution Explorer**, and `dotnet restore BeanTraderClient.Core.csproj` should successfully restore packages (with two expected warnings related to the MahApps.Metro version you're using targeting .NET Framework).</span></span>

<span data-ttu-id="998b1-264">Obwohl es möglich ist, beide Projektdateien nebeneinander zu halten (und sogar wünschenswert sein kann, wenn Sie das alte Projekt so weiterbauen möchten, wie es war), erschwert es den Migrationsprozess (die beiden Projekte versuchen, dieselben Ordner bin und obj zu verwenden) und ist normalerweise nicht erforderlich.</span><span class="sxs-lookup"><span data-stu-id="998b1-264">Although it's possible to keep both project files side-by-side (and may even be desirable if you want to keep building the old project exactly as it was), it complicates the migration process (the two projects will try to use the same bin and obj folders) and usually isn't necessary.</span></span> <span data-ttu-id="998b1-265">Wenn Sie sowohl für .NET Core- als auch für `<TargetFramework>netcoreapp3.0</TargetFramework>` .NET Framework-Ziele `<TargetFrameworks>netcoreapp3.0;net472</TargetFrameworks>` erstellen möchten, können Sie die Eigenschaft in der neuen Projektdatei stattdessen ersetzen.</span><span class="sxs-lookup"><span data-stu-id="998b1-265">If you want to build for both .NET Core and .NET Framework targets, you can replace the `<TargetFramework>netcoreapp3.0</TargetFramework>` property in the new project file with `<TargetFrameworks>netcoreapp3.0;net472</TargetFrameworks>` instead.</span></span> <span data-ttu-id="998b1-266">Löschen Sie für das Bean Trader-Beispiel die alte Projektdatei (BeanTraderClient.csproj), da sie nicht mehr benötigt wird.</span><span class="sxs-lookup"><span data-stu-id="998b1-266">For the Bean Trader sample, delete the old project file (BeanTraderClient.csproj) since it's no longer needed.</span></span> <span data-ttu-id="998b1-267">Wenn Sie es vorziehen, beide Projektdateien beizubehalten, stellen Sie sicher, dass sie auf unterschiedliche Ausgabe- und Zwischenausgabepfade erstellt werden.</span><span class="sxs-lookup"><span data-stu-id="998b1-267">If you prefer to keep both project files, be sure to have them build to different output and intermediate output paths.</span></span>

## <a name="fix-build-issues"></a><span data-ttu-id="998b1-268">Beheben von Buildproblemen</span><span class="sxs-lookup"><span data-stu-id="998b1-268">Fix build issues</span></span>

<span data-ttu-id="998b1-269">Der dritte Schritt des Portierungsprozesses besteht darin, das Projekt zu erstellen.</span><span class="sxs-lookup"><span data-stu-id="998b1-269">The third step of the porting process is getting the project to build.</span></span> <span data-ttu-id="998b1-270">Einige Apps werden bereits erfolgreich erstellt, sobald die Projektdatei in ein Projekt im SDK-Stil konvertiert wurde.</span><span class="sxs-lookup"><span data-stu-id="998b1-270">Some apps will already build successfully once the project file is converted to an SDK-style project.</span></span> <span data-ttu-id="998b1-271">Wenn das der Fall für Ihre App ist, herzlichen Glückwunsch!</span><span class="sxs-lookup"><span data-stu-id="998b1-271">If that's the case for your app, congratulations!</span></span> <span data-ttu-id="998b1-272">Sie können zu Schritt 4 gehen.</span><span class="sxs-lookup"><span data-stu-id="998b1-272">You can go on to Step 4.</span></span> <span data-ttu-id="998b1-273">Andere Apps benötigen einige Updates, um sie für .NET Core erstellen zu lassen.</span><span class="sxs-lookup"><span data-stu-id="998b1-273">Other apps will need some updates to get them building for .NET Core.</span></span> <span data-ttu-id="998b1-274">Wenn Sie versuchen, jetzt beispielsweise auf dem Bean Trader-Beispielprojekt auszuführen `dotnet build` (oder es in Visual Studio zu erstellen), gibt es viele Fehler, aber Sie werden sie schnell beheben.</span><span class="sxs-lookup"><span data-stu-id="998b1-274">If you try to run `dotnet build` on the Bean Trader sample project now, for example, (or build it in Visual Studio), there will be many errors, but you'll get them fixed quickly.</span></span>

### <a name="systemservicemodel-references-and-microsoftwindowscompatibility"></a><span data-ttu-id="998b1-275">System.ServiceModel-Referenzen und Microsoft.Windows.Compatibility</span><span class="sxs-lookup"><span data-stu-id="998b1-275">System.ServiceModel references and Microsoft.Windows.Compatibility</span></span>

<span data-ttu-id="998b1-276">Eine häufige Fehlerquelle sind fehlende Verweise für APIs, die für .NET Core verfügbar sind, aber nicht automatisch im .NET Core-App-Metapaket enthalten sind.</span><span class="sxs-lookup"><span data-stu-id="998b1-276">A common source of errors is missing references for APIs that are available for .NET Core but not automatically included in the .NET Core app metapackage.</span></span> <span data-ttu-id="998b1-277">Um dies zu beheben, sollten Sie auf das `Microsoft.Windows.Compatibility` Paket verweisen.</span><span class="sxs-lookup"><span data-stu-id="998b1-277">To address this, you should reference the `Microsoft.Windows.Compatibility` package.</span></span> <span data-ttu-id="998b1-278">Das Kompatibilitätspaket enthält eine breite Palette von APIs, die in Windows-Desktop-Apps üblich sind, z. B. WCF-Client, Verzeichnisdienste, Registrierung, Konfiguration, ACLs-APIs und mehr.</span><span class="sxs-lookup"><span data-stu-id="998b1-278">The compatibility package includes a broad set of APIs that are common in Windows desktop apps, such as WCF client, directory services, registry, configuration, ACLs APIs, and more.</span></span>

<span data-ttu-id="998b1-279">Mit dem Bean Trader-Beispiel sind die meisten <xref:System.ServiceModel> Buildfehler auf fehlende Typen zurückzuführen.</span><span class="sxs-lookup"><span data-stu-id="998b1-279">With the Bean Trader sample, the majority of the build errors are due to missing <xref:System.ServiceModel> types.</span></span> <span data-ttu-id="998b1-280">Diese können durch Verweisen auf die erforderlichen WCF NuGet-Pakete behoben werden.</span><span class="sxs-lookup"><span data-stu-id="998b1-280">These could be addressed by referencing the necessary WCF NuGet packages.</span></span> <span data-ttu-id="998b1-281">WCF-Client-APIs gehören jedoch `Microsoft.Windows.Compatibility` zu den im Paket vorhandenen ApIs, daher ist das Verweisen auf das Kompatibilitätspaket eine noch bessere Lösung (da es auch alle Probleme im Zusammenhang mit APIs sowie Lösungen für die WCF-Probleme behandelt, die das Kompatibilitätspaket zur Verfügung stellt).</span><span class="sxs-lookup"><span data-stu-id="998b1-281">WCF client APIs are among those present in the `Microsoft.Windows.Compatibility` package, though, so referencing the compatibility package is an even better solution (since it also addresses any issues related to APIs as well as solutions to the WCF issues that the compatibility package makes available).</span></span> <span data-ttu-id="998b1-282">Das `Microsoft.Windows.Compatibility` Paket hilft in den meisten .NET Core 3.0 WPF- und WinForms-Portierungsszenarien.</span><span class="sxs-lookup"><span data-stu-id="998b1-282">The `Microsoft.Windows.Compatibility` package helps in most .NET Core 3.0 WPF and WinForms porting scenarios.</span></span> <span data-ttu-id="998b1-283">Nach dem Hinzufügen des `Microsoft.Windows.Compatibility`NuGet-Verweises zu bleibt nur ein Buildfehler übrig!</span><span class="sxs-lookup"><span data-stu-id="998b1-283">After adding the NuGet reference to `Microsoft.Windows.Compatibility`, only one build error remains!</span></span>

### <a name="cleaning-up-unused-files"></a><span data-ttu-id="998b1-284">Bereinigen nicht verwendeter Dateien</span><span class="sxs-lookup"><span data-stu-id="998b1-284">Cleaning up unused files</span></span>

<span data-ttu-id="998b1-285">Eine Art von Migrationsproblem, das häufig auftaucht, bezieht sich auf C-Dateien und XAML-Dateien, die zuvor nicht im Build enthalten waren und von den neuen SDK-Projekten übernommen werden, die *alle* Quellen automatisch enthalten.</span><span class="sxs-lookup"><span data-stu-id="998b1-285">One type of migration issue that comes up often relates to C# and XAML files that weren't previously included in the build getting picked up by the new SDK-style projects that include *all* source automatically.</span></span>

<span data-ttu-id="998b1-286">Der nächste Buildfehler, den Sie im Bean Trader-Beispiel sehen, bezieht sich auf eine fehlerhafte Schnittstellenimplementierung in *OldUnusedViewModel.cs*.</span><span class="sxs-lookup"><span data-stu-id="998b1-286">The next build error you see in the Bean Trader sample refers to a bad interface implementation in *OldUnusedViewModel.cs*.</span></span> <span data-ttu-id="998b1-287">Der Dateiname ist ein Hinweis, aber bei der Überprüfung werden Sie feststellen, dass diese Quelldatei falsch ist.</span><span class="sxs-lookup"><span data-stu-id="998b1-287">The file name is a hint, but on inspection, you'll find that this source file is incorrect.</span></span> <span data-ttu-id="998b1-288">Zuvor wurden keine Probleme verursacht, da es nicht im ursprünglichen .NET Framework-Projekt enthalten war.</span><span class="sxs-lookup"><span data-stu-id="998b1-288">It didn't cause issues previously because it wasn't included in the original .NET Framework project.</span></span> <span data-ttu-id="998b1-289">Quelldateien, die auf dem Datenträger vorhanden waren, aber nicht im alten *csproj* enthalten waren, werden jetzt automatisch eingeschlossen.</span><span class="sxs-lookup"><span data-stu-id="998b1-289">Source files that were present on disk but not included in the old *csproj* are included automatically now.</span></span>

<span data-ttu-id="998b1-290">Bei einmaligen Problemen wie diesem ist es einfach, mit dem vorherigen *csproj* zu vergleichen, um `<Compile Remove="" />` zu bestätigen, dass die Datei nicht benötigt wird, und dann entweder oder, wenn die Quelldatei nirgendwo mehr benötigt wird, sie zu löschen.</span><span class="sxs-lookup"><span data-stu-id="998b1-290">For one-off issues like this, it's easy to compare to the previous *csproj* to confirm that the file isn't needed, and then either `<Compile Remove="" />` it or, if the source file isn't needed anywhere anymore, delete it.</span></span> <span data-ttu-id="998b1-291">In diesem Fall ist es sicher, *einfach OldUnusedViewModel.cs*zu löschen.</span><span class="sxs-lookup"><span data-stu-id="998b1-291">In this case, it's safe to just delete *OldUnusedViewModel.cs*.</span></span>

<span data-ttu-id="998b1-292">Wenn Sie viele Quelldateien haben, die auf diese Weise ausgeschlossen werden müssen, können `<EnableDefaultCompileItems>` Sie die automatische Einbindung von C-Dateien deaktivieren, indem Sie die Eigenschaft in der Projektdatei auf false festlegen.</span><span class="sxs-lookup"><span data-stu-id="998b1-292">If you have many source files that would need to be excluded this way, you can disable auto-inclusion of C# files by setting the `<EnableDefaultCompileItems>` property to false in the project file.</span></span> <span data-ttu-id="998b1-293">Anschließend können Sie `<Compile Include>` Elemente aus der alten Projektdatei in die neue kopieren, um nur Quellen zu erstellen, die Sie einschließen möchten.</span><span class="sxs-lookup"><span data-stu-id="998b1-293">Then, you can copy `<Compile Include>` items from the old project file to the new one in order to only build sources you intended to include.</span></span> <span data-ttu-id="998b1-294">Auf `<EnableDefaultPageItems>` ähnliche Weise kann verwendet werden, um die `<EnableDefaultItems>` automatische Integration von XAML-Seiten zu deaktivieren und beide mit einer einzigen Eigenschaft zu steuern.</span><span class="sxs-lookup"><span data-stu-id="998b1-294">Similarly, `<EnableDefaultPageItems>` can be used to turn off auto-inclusion of XAML pages and `<EnableDefaultItems>` can control both with a single property.</span></span>

### <a name="a-brief-aside-on-multi-pass-compilers"></a><span data-ttu-id="998b1-295">Eine kurze Nebenbemerkung zu Multipass-Compilern</span><span class="sxs-lookup"><span data-stu-id="998b1-295">A brief aside on multi-pass compilers</span></span>

<span data-ttu-id="998b1-296">Nachdem Sie die beleidigende Datei aus dem Bean Trader-Beispiel entfernt haben, können Sie neu erstellen und erhalten vier Fehler.</span><span class="sxs-lookup"><span data-stu-id="998b1-296">After removing the offending file from the Bean Trader sample, you can re-build and will get four errors.</span></span> <span data-ttu-id="998b1-297">Hatten Sie nicht vorher eine?</span><span class="sxs-lookup"><span data-stu-id="998b1-297">Didn't you have one before?</span></span> <span data-ttu-id="998b1-298">Warum ist die Anzahl der Fehler gestiegen?</span><span class="sxs-lookup"><span data-stu-id="998b1-298">Why did the number of errors go up?</span></span> <span data-ttu-id="998b1-299">Der C-Compiler ist ein [Multi-Pass-Compiler](https://docs.microsoft.com/archive/blogs/ericlippert/how-many-passes).</span><span class="sxs-lookup"><span data-stu-id="998b1-299">The C# compiler is a [multi-pass compiler](https://docs.microsoft.com/archive/blogs/ericlippert/how-many-passes).</span></span> <span data-ttu-id="998b1-300">Dies bedeutet, dass es durch jede Quelldatei zweimal geht.</span><span class="sxs-lookup"><span data-stu-id="998b1-300">This means that it goes through each source file twice.</span></span> <span data-ttu-id="998b1-301">Zunächst betrachtet der Compiler nur Metadaten und Deklarationen in jeder Quelldatei und identifiziert Probleme auf Deklarationsebene.</span><span class="sxs-lookup"><span data-stu-id="998b1-301">First, the compiler just looks at metadata and declarations in each source file and identifies any declaration-level problems.</span></span> <span data-ttu-id="998b1-302">Das sind die Fehler, die Sie behoben haben.</span><span class="sxs-lookup"><span data-stu-id="998b1-302">Those are the errors you've fixed.</span></span> <span data-ttu-id="998b1-303">Dann wird der Code erneut durchgehen, um die C-Quelle in IL zu erstellen. Dies sind die zweiten Fehler, die Sie jetzt sehen.</span><span class="sxs-lookup"><span data-stu-id="998b1-303">Then it goes through the code again to build the C# source into IL; those are the second set of errors that you're seeing now.</span></span>

> [!NOTE]
> <span data-ttu-id="998b1-304">Der C-Compiler führt [mehr als nur zwei Durchläufe](https://docs.microsoft.com/archive/blogs/ericlippert/how-many-passes)durch, aber das Endergebnis ist, dass Compilerfehler bei großen Codeänderungen wie diesem in zwei Wellen auftreten.</span><span class="sxs-lookup"><span data-stu-id="998b1-304">The C# compiler does [more than just two passes](https://docs.microsoft.com/archive/blogs/ericlippert/how-many-passes), but the end result is that compiler errors for large code changes like this tend to come in two waves.</span></span>

### <a name="third-party-dependency-fixes-castlewindsor"></a><span data-ttu-id="998b1-305">Abhängigkeitsfixes von Drittanbietern (Castle.Windsor)</span><span class="sxs-lookup"><span data-stu-id="998b1-305">Third-party dependency fixes (Castle.Windsor)</span></span>

<span data-ttu-id="998b1-306">Eine weitere Problemklasse, die in einigen Migrationsszenarien auftritt, sind API-Unterschiede zwischen .NET Framework- und .NET Core-Versionen von Abhängigkeiten.</span><span class="sxs-lookup"><span data-stu-id="998b1-306">Another class of issue that comes up in some migration scenarios is API differences between .NET Framework and .NET Core versions of dependencies.</span></span> <span data-ttu-id="998b1-307">Selbst wenn ein NuGet-Paket sowohl auf .NET Framework als auch auf .NET Standard oder .NET Core abzielt, können unterschiedliche Bibliotheken für die Verwendung mit unterschiedlichen .NET-Zielen vorhanden sein.</span><span class="sxs-lookup"><span data-stu-id="998b1-307">Even if a NuGet package targets both .NET Framework and .NET Standard or .NET Core, there may be different libraries for use with different .NET targets.</span></span> <span data-ttu-id="998b1-308">Dadurch können die Pakete viele verschiedene .NET-Plattformen unterstützen, die möglicherweise unterschiedliche Implementierungen erfordern.</span><span class="sxs-lookup"><span data-stu-id="998b1-308">This allows the packages to support many different .NET platforms, which may require different implementations.</span></span> <span data-ttu-id="998b1-309">Dies bedeutet auch, dass es kleine API-Unterschiede in den Bibliotheken geben kann, wenn verschiedene .NET-Plattformen ausgerichtet sind.</span><span class="sxs-lookup"><span data-stu-id="998b1-309">It also means that there may be small API differences in the libraries when targeting different .NET platforms.</span></span>

<span data-ttu-id="998b1-310">Der nächste Satz von Fehlern, die Sie im Bean Trader-Beispiel sehen, bezieht sich auf `Castle.Windsor` APIs.</span><span class="sxs-lookup"><span data-stu-id="998b1-310">The next set of errors you'll see in the Bean Trader sample are related to `Castle.Windsor` APIs.</span></span> <span data-ttu-id="998b1-311">Das .NET Core Bean Trader-Projekt `Castle.Windsor` verwendet dieselbe Version des auf .NET Framework ausgerichteten Projekts (4.1.1), aber die Implementierungen für diese beiden Plattformen unterscheiden sich geringfügig.</span><span class="sxs-lookup"><span data-stu-id="998b1-311">The .NET Core Bean Trader project uses the same version of `Castle.Windsor` as the .NET Framework-targeted project (4.1.1), but the implementations for those two platforms are slightly different.</span></span>

<span data-ttu-id="998b1-312">In diesem Fall werden die folgenden Probleme angezeigt, die behoben werden müssen:</span><span class="sxs-lookup"><span data-stu-id="998b1-312">In this case, you see the following issues that need to be fixed:</span></span>

1. <span data-ttu-id="998b1-313">`Castle.MicroKernel.Registration.Classes.FromThisAssembly`ist auf .NET Core nicht verfügbar.</span><span class="sxs-lookup"><span data-stu-id="998b1-313">`Castle.MicroKernel.Registration.Classes.FromThisAssembly` isn't available on .NET Core.</span></span> <span data-ttu-id="998b1-314">Es ist jedoch die `Classes.FromAssemblyContaining` ähnliche API verfügbar, so `Classes.FromThisAssembly()` dass wir `Classes.FromAssemblyContaining(t)`beide `t` Verwendungen von durch Aufrufe von ersetzen können, wobei der Typ der Aufruf ist.</span><span class="sxs-lookup"><span data-stu-id="998b1-314">There is, however, the similar API `Classes.FromAssemblyContaining` available, so we can replace both uses of `Classes.FromThisAssembly()` with calls to `Classes.FromAssemblyContaining(t)`, where `t` is the type making the call.</span></span>
1. <span data-ttu-id="998b1-315">Ebenso in *Bootstrapper.cs*Bootstrapper.cs `Castle.Windsor.Installer.FromAssembly`, . Dies ist auf .NET Core nicht verfügbar.</span><span class="sxs-lookup"><span data-stu-id="998b1-315">Similarly, in *Bootstrapper.cs*, `Castle.Windsor.Installer.FromAssembly`.This is unavailable on .NET Core.</span></span> <span data-ttu-id="998b1-316">Stattdessen kann dieser Aufruf `FromAssembly.Containing(typeof(Bootstrapper))`durch ersetzt werden.</span><span class="sxs-lookup"><span data-stu-id="998b1-316">Instead, that call can be replaced with `FromAssembly.Containing(typeof(Bootstrapper))`.</span></span>

### <a name="updating-wcf-client-usage"></a><span data-ttu-id="998b1-317">Aktualisieren der WCF-Clientnutzung</span><span class="sxs-lookup"><span data-stu-id="998b1-317">Updating WCF client usage</span></span>

<span data-ttu-id="998b1-318">Nachdem die `Castle.Windsor` Unterschiede behoben wurden, ist der letzte verbleibende Buildfehler `BeanTraderServiceClient` im .NET `DuplexClientBase`Core Bean Trader-Projekt, dass (das von ableitet) keine `Open` Methode hat.</span><span class="sxs-lookup"><span data-stu-id="998b1-318">Having fixed the `Castle.Windsor` differences, the last remaining build error in the .NET Core Bean Trader project is that `BeanTraderServiceClient` (which derives from `DuplexClientBase`) doesn't have an `Open` method.</span></span> <span data-ttu-id="998b1-319">Dies ist nicht verwunderlich, da es sich um eine API handelt, die vom .NET Portability Analyzer zu Beginn dieses Migrationsprozesses hervorgehoben wurde.</span><span class="sxs-lookup"><span data-stu-id="998b1-319">This isn't surprising since this is an API that was highlighted by the .NET Portability Analyzer at the beginning of this migration process.</span></span> <span data-ttu-id="998b1-320">Ein `BeanTraderServiceClient` Blick auf die Aufmerksamkeit lenkt unsere Aufmerksamkeit jedoch auf ein größeres Problem.</span><span class="sxs-lookup"><span data-stu-id="998b1-320">Looking at `BeanTraderServiceClient` draws our attention to a larger issue, though.</span></span> <span data-ttu-id="998b1-321">Dieser WCF-Client wurde vom Tool [Svcutil.exe](../../framework/wcf/servicemodel-metadata-utility-tool-svcutil-exe.md) automatisch generiert.</span><span class="sxs-lookup"><span data-stu-id="998b1-321">This WCF client was autogenerated by the [Svcutil.exe](../../framework/wcf/servicemodel-metadata-utility-tool-svcutil-exe.md) tool.</span></span>

<span data-ttu-id="998b1-322">**Von Svcutil generierte WCF-Clients sind für die Verwendung in .NET Framework vorgesehen.**</span><span class="sxs-lookup"><span data-stu-id="998b1-322">**WCF clients generated by Svcutil are meant for use on .NET Framework.**</span></span>

<span data-ttu-id="998b1-323">Lösungen, die svcutil-generierte WCF-Clients verwenden, müssen .NET Standardkompatible Clients für die Verwendung mit .NET Core regenerieren.</span><span class="sxs-lookup"><span data-stu-id="998b1-323">Solutions that use svcutil-generated WCF clients will need to regenerate .NET Standard-compatible clients for use with .NET Core.</span></span> <span data-ttu-id="998b1-324">Einer der Hauptgründe, warum die alten Clients nicht funktionieren, ist, dass sie für die Definition von WCF-Bindungen und Endpunkten von der App-Konfiguration abhängig sind.</span><span class="sxs-lookup"><span data-stu-id="998b1-324">One of the main reasons the old clients won't work is that they depend on app configuration for defining WCF bindings and endpoints.</span></span> <span data-ttu-id="998b1-325">Da .NET Standard-WCF-APIs plattformübergreifend arbeiten können (wobei System.Configuration-APIs nicht verfügbar sind), müssen WCF-Clients für .NET Core- und .NET-Standardszenarien Bindungen und Endpunkte programmgesteuert und nicht in der Konfiguration definieren.</span><span class="sxs-lookup"><span data-stu-id="998b1-325">Because .NET Standard WCF APIs can work cross-platform (where System.Configuration APIs aren't available), WCF clients for .NET Core and .NET Standard scenarios must define bindings and endpoints programmatically instead of in configuration.</span></span>

<span data-ttu-id="998b1-326">Tatsächlich muss jede WCF-Clientnutzung, `<system.serviceModel>` die vom Abschnitt app.config abhängt (ob mit Svcutil oder manuell erstellt), geändert werden, um auf .NET Core zu funktionieren.</span><span class="sxs-lookup"><span data-stu-id="998b1-326">In fact, any WCF client usage that depends on the `<system.serviceModel>` app.config section (whether created with Svcutil or manually) will need to be changed to work on .NET Core.</span></span>

<span data-ttu-id="998b1-327">Es gibt zwei Möglichkeiten, .NET Standardkompatible WCF-Clients automatisch zu generieren:</span><span class="sxs-lookup"><span data-stu-id="998b1-327">There are two ways to automatically generate .NET Standard-compatible WCF clients:</span></span>

- <span data-ttu-id="998b1-328">Das `dotnet-svcutil` Tool ist ein .NET-Tool, das WCF-Clients auf eine Ähnliche generiert, die der früheren Arbeitsweise von Svcutil ähnelt.</span><span class="sxs-lookup"><span data-stu-id="998b1-328">The `dotnet-svcutil` tool is a .NET tool that generates WCF clients in a way that is similar to how Svcutil worked previously.</span></span>
- <span data-ttu-id="998b1-329">Visual Studio kann WCF-Clients mithilfe der [WCF-Webdienstreferenzoption](../../core/additional-tools/wcf-web-service-reference-guide.md) der Funktion "Verbundene Dienste" generieren.</span><span class="sxs-lookup"><span data-stu-id="998b1-329">Visual Studio can generate WCF clients using the [WCF Web Service Reference](../../core/additional-tools/wcf-web-service-reference-guide.md) option of its Connected Services feature.</span></span>

<span data-ttu-id="998b1-330">Beide Ansätze funktionieren gut.</span><span class="sxs-lookup"><span data-stu-id="998b1-330">Either approach works well.</span></span> <span data-ttu-id="998b1-331">Alternativ können Sie den WCF-Clientcode selbst schreiben.</span><span class="sxs-lookup"><span data-stu-id="998b1-331">Alternatively, of course, you could write the WCF client code yourself.</span></span> <span data-ttu-id="998b1-332">Für dieses Beispiel habe ich die Visual Studio Connected Service-Funktion verwendet.</span><span class="sxs-lookup"><span data-stu-id="998b1-332">For this sample, I chose to use the Visual Studio Connected Service feature.</span></span> <span data-ttu-id="998b1-333">Klicken Sie dazu mit der rechten Maustaste auf das *BeanTraderClient.Core-Projekt* im Lösungs-Explorer von Visual Studio, und wählen Sie**Connected Service** **hinzufügen** > aus.</span><span class="sxs-lookup"><span data-stu-id="998b1-333">To do that, right-click on the *BeanTraderClient.Core* project in Visual Studio's solution explorer and select **Add** > **Connected Service**.</span></span> <span data-ttu-id="998b1-334">Wählen Sie als Nächstes den WCF-Webdienstreferenzanbieter aus.</span><span class="sxs-lookup"><span data-stu-id="998b1-334">Next, choose the WCF Web Service Reference Provider.</span></span> <span data-ttu-id="998b1-335">Dadurch wird ein Dialogfeld ausgelöst, in dem Sie die Adresse`localhost:8080` des Backend Bean Trader-Webdienstes (wenn Sie den Server lokal ausführen) und den Namespace angeben können, den generierte Typen verwenden sollen ( beanTrader.Service , z. B.**BeanTrader.Service).**</span><span class="sxs-lookup"><span data-stu-id="998b1-335">This will bring up a dialog where you can specify the address of the backend Bean Trader web service (`localhost:8080` if you are running the server locally) and the namespace that generated types should use (**BeanTrader.Service**, for example).</span></span>

![WCF Web Service Reference Connected Service Dialog](./media/convert-project-from-net-framework/connected-service-dialog.png)

<span data-ttu-id="998b1-337">Nachdem Sie die Schaltfläche **Fertig stellen** ausgewählt haben, wird dem Projekt ein neuer Connected Services-Knoten hinzugefügt, und unter diesem Knoten wird eine Reference.cs-Datei hinzugefügt, die den neuen .NET Standard WCF-Client für den Zugriff auf den Bean Trader-Dienst enthält.</span><span class="sxs-lookup"><span data-stu-id="998b1-337">After you select the **Finish** button, a new Connected Services node is added to the project and a Reference.cs file is added under that node containing the new .NET Standard WCF client for accessing the Bean Trader service.</span></span> <span data-ttu-id="998b1-338">Wenn Sie sich `GetEndpointAddress` `GetBindingForEndpoint` die oder Methoden in dieser Datei ansehen, werden Sie sehen, dass Bindungen und Endpunkte jetzt programmgesteuert generiert werden (anstelle von App config).</span><span class="sxs-lookup"><span data-stu-id="998b1-338">If you look at the `GetEndpointAddress` or `GetBindingForEndpoint` methods in that file, you'll see that bindings and endpoints are now generated programmatically (instead of via app config).</span></span> <span data-ttu-id="998b1-339">Die Funktion "Verbundene Dienste hinzufügen" kann auch Verweise auf einige System.ServiceModel-Pakete in der Projektdatei hinzufügen, die nicht benötigt werden, da alle erforderlichen WCF-Pakete über Microsoft.Windows.Compatibility enthalten sind.</span><span class="sxs-lookup"><span data-stu-id="998b1-339">The 'Add Connected Services' feature may also add references to some System.ServiceModel packages in the project file, which aren't needed since all necessary WCF packages are included via Microsoft.Windows.Compatibility.</span></span> <span data-ttu-id="998b1-340">Überprüfen Sie die csproj, um festzustellen, ob zusätzliche System.ServiceModel-Elemente `<PackageReference>` hinzugefügt wurden, und wenn ja, entfernen Sie sie.</span><span class="sxs-lookup"><span data-stu-id="998b1-340">Check the csproj to see if any extra System.ServiceModel `<PackageReference>` items have been added, and if so, remove them.</span></span>

<span data-ttu-id="998b1-341">Unser Projekt hat jetzt neue WCF-Clientklassen (in *Reference.cs*), aber es hat auch noch die alten (in BeanTrader.cs).</span><span class="sxs-lookup"><span data-stu-id="998b1-341">Our project has new WCF client classes now (in *Reference.cs*), but it also still has the old ones (in BeanTrader.cs).</span></span> <span data-ttu-id="998b1-342">An dieser Stelle gibt es zwei Möglichkeiten:</span><span class="sxs-lookup"><span data-stu-id="998b1-342">There are two options at this point:</span></span>

- <span data-ttu-id="998b1-343">Wenn Sie das ursprüngliche .NET Framework-Projekt (neben dem neuen .NET Core-Zielprojekt) erstellen möchten, können Sie ein `<Compile Remove="BeanTrader.cs" />` Element in der csproj-Datei des .NET Core-Projekts verwenden, sodass die .NET Framework- und .NET Core-Versionen der App unterschiedliche WCF-Clients verwenden.</span><span class="sxs-lookup"><span data-stu-id="998b1-343">If you want to be able to build the original .NET Framework project (alongside the new .NET Core-targeted one), you can use a `<Compile Remove="BeanTrader.cs" />` item in the .NET Core project's csproj file so that the .NET Framework and .NET Core versions of the app use different WCF clients.</span></span> <span data-ttu-id="998b1-344">Dies hat den Vorteil, dass das vorhandene .NET Framework-Projekt unverändert bleibt, hat jedoch den Nachteil, dass Code, der die generierten WCF-Clients verwendet, `#if` im .NET Core-Fall möglicherweise etwas anders sein muss als im .NET Framework-Projekt.</span><span class="sxs-lookup"><span data-stu-id="998b1-344">This has the advantage of leaving the existing .NET Framework project unchanged, but has the disadvantage that code using the generated WCF clients may need to be slightly different in the .NET Core case than it was in the .NET Framework project, so you'll likely need to use `#if` directives to conditionally compile some WCF client usage (creating clients, for example) to work one way when built for .NET Core and another way when built for .NET Framework.</span></span>

- <span data-ttu-id="998b1-345">Wenn hingegen eine Codeabwanderung im vorhandenen .NET Framework-Projekt akzeptabel ist, können Sie *BeanTrader.cs* alle zusammen entfernen.</span><span class="sxs-lookup"><span data-stu-id="998b1-345">If, on the other hand, some code churn in the existing .NET Framework project is acceptable, you can remove *BeanTrader.cs* all together.</span></span> <span data-ttu-id="998b1-346">Da der neue WCF-Client für .NET Standard erstellt wurde, funktioniert er sowohl in .NET Core- als auch in .NET Framework-Szenarien.</span><span class="sxs-lookup"><span data-stu-id="998b1-346">Because the new WCF client is built for .NET Standard, it will work in both .NET Core and .NET Framework scenarios.</span></span> <span data-ttu-id="998b1-347">Wenn Sie zusätzlich zu .NET Core für .NET Framework erstellen (entweder durch Multi-Targeting oder durch zwei csproj-Dateien), können Sie diese neue *Reference.cs-Datei* für beide Ziele verwenden.</span><span class="sxs-lookup"><span data-stu-id="998b1-347">If you are building for .NET Framework in addition to .NET Core (either by multi-targeting or by having two csproj files), you can use this new *Reference.cs* file for both targets.</span></span> <span data-ttu-id="998b1-348">Dieser Ansatz hat den Vorteil, dass der Code nicht bifurcate muss, um zwei verschiedene WCF-Clients zu unterstützen. der gleiche Code wird überall verwendet.</span><span class="sxs-lookup"><span data-stu-id="998b1-348">This approach has the advantage that the code won't need to bifurcate to support two different WCF clients; the same code will be used everywhere.</span></span> <span data-ttu-id="998b1-349">Der Nachteil besteht darin, dass es sich um die Änderung des (vermutlich stabilen) .NET Framework-Projekts handelt.</span><span class="sxs-lookup"><span data-stu-id="998b1-349">The drawback is that it involves changing the (presumably stable) .NET Framework project.</span></span>

<span data-ttu-id="998b1-350">Im Fall des Bean Trader-Beispiels können Sie kleine Änderungen am ursprünglichen Projekt vornehmen, wenn diemigration dadurch vereinfacht wird. Führen Sie daher die folgenden Schritte aus, um die WCF-Clientnutzung abzugleichen:</span><span class="sxs-lookup"><span data-stu-id="998b1-350">In the case of the Bean Trader sample, you can make small changes to the original project if it makes migration easier, so follow these steps to reconcile WCF client usage:</span></span>

01. <span data-ttu-id="998b1-351">Fügen Sie die neue Reference.cs Datei zum .NET Framework *BeanTraderClient.csproj-Projekt* hinzu, indem Sie das Kontextmenü "Vorhandenes Element hinzufügen" aus dem Projektmappen-Explorer verwenden.</span><span class="sxs-lookup"><span data-stu-id="998b1-351">Add the new Reference.cs file to the .NET Framework *BeanTraderClient.csproj* project using the 'Add existing item' context menu from the solution explorer.</span></span> <span data-ttu-id="998b1-352">Achten Sie darauf, 'als Link' hinzuzufügen, damit dieselbe Datei von beiden Projekten verwendet wird (im Gegensatz zum Kopieren der C-Datei).</span><span class="sxs-lookup"><span data-stu-id="998b1-352">Be sure to add 'as link' so that the same file is used by both projects (as opposed to copying the C# file).</span></span> <span data-ttu-id="998b1-353">Wenn Sie für .NET Core und .NET Framework mit einem einzelnen csproj (mit Multi-Targeting) erstellen, ist dieser Schritt nicht erforderlich.</span><span class="sxs-lookup"><span data-stu-id="998b1-353">If you are building for both .NET Core and .NET Framework with a single csproj (using multi-targeting) then this step isn't necessary.</span></span>

01. <span data-ttu-id="998b1-354">Löschen Sie *BeanTrader.cs*.</span><span class="sxs-lookup"><span data-stu-id="998b1-354">Delete *BeanTrader.cs*.</span></span>

01. <span data-ttu-id="998b1-355">Der neue WCF-Client ähnelt dem alten, aber eine Reihe von Namespaces im generierten Code unterscheiden sich.</span><span class="sxs-lookup"><span data-stu-id="998b1-355">The new WCF client is similar to the old one, but a number of namespaces in the generated code are different.</span></span> <span data-ttu-id="998b1-356">Aus diesem Grund ist es notwendig, das Projekt so zu aktualisieren, dass WCF-Clienttypen von BeanTrader.Service (oder dem beliebigen Namespacenamen) anstelle von BeanTrader.Model oder ohne Namespace verwendet werden.</span><span class="sxs-lookup"><span data-stu-id="998b1-356">Because of this, it is necessary to update the project so that WCF client types are used from BeanTrader.Service (or whatever namespace name you chose) instead of BeanTrader.Model or without a namespace.</span></span> <span data-ttu-id="998b1-357">Das Erstellen von *BeanTraderClient.Core.csproj* hilft ihnen, zu ermitteln, wo diese Änderungen vorgenommen werden müssen.</span><span class="sxs-lookup"><span data-stu-id="998b1-357">Building *BeanTraderClient.Core.csproj* will help to identify where these changes need to be made.</span></span> <span data-ttu-id="998b1-358">Korrekturen werden sowohl in den XAML-Quelldateien als auch in XAML-Quelldateien benötigt.</span><span class="sxs-lookup"><span data-stu-id="998b1-358">Fixes will be needed both in C# and in XAML source files.</span></span>

01. <span data-ttu-id="998b1-359">Schließlich stellen Sie fest, dass ein Fehler in *BeanTraderServiceClientFactory.cs* vorliegt, da sich die verfügbaren Konstruktoren für den `BeanTraderServiceClient` Typ geändert haben.</span><span class="sxs-lookup"><span data-stu-id="998b1-359">Finally, you'll discover that there is an error in *BeanTraderServiceClientFactory.cs* because the available constructors for the `BeanTraderServiceClient` type have changed.</span></span> <span data-ttu-id="998b1-360">Früher war es möglich, `InstanceContext` ein Argument zu `CallbackHandler` liefern `Castle.Windsor` (das mit einem aus dem IoC-Container erstellt wurde).</span><span class="sxs-lookup"><span data-stu-id="998b1-360">It used to be possible to supply an `InstanceContext` argument (which was created using a `CallbackHandler` from the `Castle.Windsor` IoC container).</span></span> <span data-ttu-id="998b1-361">Die neuen Konstruktoren `CallbackHandler`erstellen neue s.</span><span class="sxs-lookup"><span data-stu-id="998b1-361">The new constructors create new `CallbackHandler`s.</span></span> <span data-ttu-id="998b1-362">Es gibt jedoch Konstruktoren `BeanTraderServiceClient`im Basistyp von, die ihren Wünschen entsprechen.</span><span class="sxs-lookup"><span data-stu-id="998b1-362">There are, however, constructors in `BeanTraderServiceClient`'s base type that match what you want.</span></span> <span data-ttu-id="998b1-363">Da der automatisch generierte WCF-Clientcode alle in Partiellenklassen vorhanden ist, können Sie ihn problemlos erweitern.</span><span class="sxs-lookup"><span data-stu-id="998b1-363">Since the autogenerated WCF client code all exists in partial classes, you can easily extend it.</span></span> <span data-ttu-id="998b1-364">Erstellen Sie dazu eine neue Datei mit dem Namen *BeanTraderServiceClient.cs* und dann eine Partipartenklasse mit demselben Namen (mit dem Namespace BeanTrader.Service).</span><span class="sxs-lookup"><span data-stu-id="998b1-364">To do this, create a new file called *BeanTraderServiceClient.cs* and then create a partial class with that same name (using the BeanTrader.Service namespace).</span></span> <span data-ttu-id="998b1-365">Fügen Sie dann dem teilartiger Typ einen Konstruktor hinzu, wie hier gezeigt.</span><span class="sxs-lookup"><span data-stu-id="998b1-365">Then, add one constructor to the partial type as shown here.</span></span>

    ```csharp
    public BeanTraderServiceClient(System.ServiceModel.InstanceContext callbackInstance) :
        base(callbackInstance, EndpointConfiguration.NetTcpBinding_BeanTraderService)
            { }
    ```

<span data-ttu-id="998b1-366">Mit diesen Änderungen wird das Bean Trader-Beispiel nun einen neuen .NET Standard-kompatiblen WCF-Client verwenden, und Sie können die letzte Korrektur des Änderns des `Open` Aufrufs in *TradingService.cs* stattdessen vornehmen. `await OpenAsync`</span><span class="sxs-lookup"><span data-stu-id="998b1-366">With those changes made, the Bean Trader sample will now be using a new .NET Standard-compatible WCF client and you can make the final fix of changing the `Open` call in *TradingService.cs* to use `await OpenAsync` instead.</span></span>

<span data-ttu-id="998b1-367">Nachdem die WCF-Probleme behoben wurden, wird die .NET Core-Version des Bean Trader-Beispiels jetzt sauber erstellt!</span><span class="sxs-lookup"><span data-stu-id="998b1-367">With the WCF issues addressed, the .NET Core version of the Bean Trader sample now builds cleanly!</span></span>

## <a name="runtime-testing"></a><span data-ttu-id="998b1-368">Laufzeittests</span><span class="sxs-lookup"><span data-stu-id="998b1-368">Runtime testing</span></span>

<span data-ttu-id="998b1-369">Es ist leicht zu vergessen, dass die Migrationsarbeit nicht ausgeführt wird, sobald das Projekt sauber für .NET Core erstellt wird.</span><span class="sxs-lookup"><span data-stu-id="998b1-369">It's easy to forget that migration work isn't done as soon as the project builds cleanly against .NET Core.</span></span> <span data-ttu-id="998b1-370">Es ist wichtig, auch Zeit für das Testen der portierten App zu lassen.</span><span class="sxs-lookup"><span data-stu-id="998b1-370">It's important to leave time for testing the ported app, too.</span></span> <span data-ttu-id="998b1-371">Stellen Sie nach erfolgreicher Erstellung der Dinge sicher, dass die App wie erwartet ausgeführt wird und funktioniert, insbesondere wenn Sie Pakete verwenden, die auf .NET Framework abzielen.</span><span class="sxs-lookup"><span data-stu-id="998b1-371">Once things build successfully, make sure the app runs and works as expected, especially if you are using any packages targeting .NET Framework.</span></span>

<span data-ttu-id="998b1-372">Lassen Sie uns versuchen, die portierte Bean Trader App zu starten und zu sehen, was passiert.</span><span class="sxs-lookup"><span data-stu-id="998b1-372">Let's try launching the ported Bean Trader app and see what happens.</span></span> <span data-ttu-id="998b1-373">Die App kommt nicht weit, bevor sie mit der folgenden Ausnahme fehlschlägt.</span><span class="sxs-lookup"><span data-stu-id="998b1-373">The app doesn't get far before failing with the following exception.</span></span>

```output
System.Configuration.ConfigurationErrorsException: 'Configuration system failed to initialize'

Inner Exception
ConfigurationErrorsException: Unrecognized configuration section system.serviceModel.
```

<span data-ttu-id="998b1-374">Das macht natürlich Sinn.</span><span class="sxs-lookup"><span data-stu-id="998b1-374">This makes sense, of course.</span></span> <span data-ttu-id="998b1-375">Denken Sie daran, dass WCF keine App-Konfiguration mehr verwendet, daher muss der alte Abschnitt system.serviceModel der Datei app.config entfernt werden.</span><span class="sxs-lookup"><span data-stu-id="998b1-375">Remember that WCF no longer uses app configuration, so the old system.serviceModel section of the app.config file needs to be removed.</span></span> <span data-ttu-id="998b1-376">Der aktualisierte WCF-Client enthält alle gleichen Informationen in seinem Code, sodass der Konfigurationsabschnitt nicht mehr benötigt wird.</span><span class="sxs-lookup"><span data-stu-id="998b1-376">The updated WCF client includes all of the same information in its code, so the config section isn't needed anymore.</span></span> <span data-ttu-id="998b1-377">Wenn der WCF-Endpunkt in app.config konfigurierbar sein soll, können Sie ihn als App-Einstellung hinzufügen und den WCF-Clientcode aktualisieren, um den WCF-Dienstendpunkt aus der Konfiguration abzurufen.</span><span class="sxs-lookup"><span data-stu-id="998b1-377">If you wanted the WCF endpoint to be configurable in app.config, you could add it as an app setting and update the WCF client code to retrieve the WCF service endpoint from configuration.</span></span>

<span data-ttu-id="998b1-378">Nach dem Entfernen des Abschnitts system.serviceModel von *app.config*wird die App gestartet, schlägt jedoch mit einer anderen Ausnahme fehl, wenn sich ein Benutzer anmeldet.</span><span class="sxs-lookup"><span data-stu-id="998b1-378">After removing the system.serviceModel section of *app.config*, the app launches but fails with another exception when a user signs in.</span></span>

```output
System.PlatformNotSupportedException: 'Operation is not supported on this platform.'
```

<span data-ttu-id="998b1-379">Die nicht unterstützte `Func<T>.BeginInvoke`API ist .</span><span class="sxs-lookup"><span data-stu-id="998b1-379">The unsupported API is `Func<T>.BeginInvoke`.</span></span> <span data-ttu-id="998b1-380">Wie in [dotnet/corefx-5940](https://github.com/dotnet/corefx/issues/5940)erläutert, unterstützt .NET `BeginInvoke` `EndInvoke` Core die und Methoden für Delegattypen aufgrund zugrunde liegender Remoting-Abhängigkeiten nicht.</span><span class="sxs-lookup"><span data-stu-id="998b1-380">As explained in [dotnet/corefx#5940](https://github.com/dotnet/corefx/issues/5940), .NET Core doesn't support the `BeginInvoke` and `EndInvoke` methods on delegate types due to underlying remoting dependencies.</span></span> <span data-ttu-id="998b1-381">Dieses Problem und seine Lösung werden im [Blogbeitrag "Migrating Delegate.BeginInvoke Calls for .NET Core"](https://devblogs.microsoft.com/dotnet/migrating-delegate-begininvoke-calls-for-net-core/) ausführlicher erläutert, aber der Kern ist, dass `BeginInvoke` und `EndInvoke` Aufrufe durch `Task.Run` (oder asynchrone Alternativen, wenn möglich) ersetzt werden sollten.</span><span class="sxs-lookup"><span data-stu-id="998b1-381">This issue and its fix are explained in more detail in the [Migrating Delegate.BeginInvoke Calls for .NET Core](https://devblogs.microsoft.com/dotnet/migrating-delegate-begininvoke-calls-for-net-core/) blog post, but the gist is that `BeginInvoke` and `EndInvoke` calls should be replaced with `Task.Run` (or async alternatives, if possible).</span></span> <span data-ttu-id="998b1-382">Wenn Sie hier die `BeginInvoke` allgemeine Lösung anwenden, kann der Aufruf durch einen `Invoke` Aufruf ersetzt werden, der von `Task.Run`gestartet wird.</span><span class="sxs-lookup"><span data-stu-id="998b1-382">Applying the general solution here, the `BeginInvoke` call can be replaced with an `Invoke` call launched by `Task.Run`.</span></span>

```csharp
Task.Run(() =>
{
    return userInfoRetriever.Invoke();
}).ContinueWith(result =>
{
    // BeginInvoke's callback is replaced with ContinueWith
    var task = result.ConfigureAwait(false);
    CurrentTrader = task.GetAwaiter().GetResult();
}, TaskScheduler.Default);
```

<span data-ttu-id="998b1-383">Nach dem `BeginInvoke` Entfernen der Nutzung wird die Bean Trader App erfolgreich auf .NET Core ausgeführt!</span><span class="sxs-lookup"><span data-stu-id="998b1-383">After removing the `BeginInvoke` usage, the Bean Trader app runs successfully on .NET Core!</span></span>

![Bean Trader läuft auf .NET Core](./media/convert-project-from-net-framework/running-on-core.png)

<span data-ttu-id="998b1-385">Alle Apps sind unterschiedlich, sodass die spezifischen Schritte zum Migrieren Ihrer eigenen Apps zu .NET Core variieren.</span><span class="sxs-lookup"><span data-stu-id="998b1-385">All apps are different, so the specific steps needed to migrate your own apps to .NET Core will vary.</span></span> <span data-ttu-id="998b1-386">Aber hoffentlich zeigt das Bean Trader-Beispiel den allgemeinen Workflow und die Arten von Problemen, die zu erwarten sind.</span><span class="sxs-lookup"><span data-stu-id="998b1-386">But hopefully the Bean Trader sample demonstrates the general workflow and the types of issues that can be expected.</span></span> <span data-ttu-id="998b1-387">Und trotz der Länge dieses Artikels waren die tatsächlichen Änderungen, die im Bean Trader-Beispiel erforderlich waren, um es auf .NET Core funktionieren zu lassen, ziemlich begrenzt.</span><span class="sxs-lookup"><span data-stu-id="998b1-387">And, despite this article's length, the actual changes needed in the Bean Trader sample to make it work on .NET Core were fairly limited.</span></span> <span data-ttu-id="998b1-388">Viele Apps werden auf die gleiche Weise zu .NET Core migriert. mit begrenzten oder gar keinen Codeänderungen erforderlich.</span><span class="sxs-lookup"><span data-stu-id="998b1-388">Many apps migrate to .NET Core in this same way; with limited or even no code changes needed.</span></span>
