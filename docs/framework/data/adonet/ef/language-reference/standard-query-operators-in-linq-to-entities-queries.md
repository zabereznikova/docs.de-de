---
title: Standardabfrageoperatoren in LINQ to Entities-Abfragen
ms.custom: 
ms.date: 03/30/2017
ms.prod: .net-framework
ms.reviewer: 
ms.suite: 
ms.technology: dotnet-ado
ms.tgt_pltfrm: 
ms.topic: article
ms.assetid: 7fa55a9b-6219-473d-b1e5-2884a32dcdff
caps.latest.revision: "3"
author: JennieHubbard
ms.author: jhubbard
manager: jhubbard
ms.openlocfilehash: 97782834480e8acb5f66d8da2099089b1c47e093
ms.sourcegitcommit: 4f3fef493080a43e70e951223894768d36ce430a
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 11/21/2017
---
# <a name="standard-query-operators-in-linq-to-entities-queries"></a><span data-ttu-id="ecdef-102">Standardabfrageoperatoren in LINQ to Entities-Abfragen</span><span class="sxs-lookup"><span data-stu-id="ecdef-102">Standard Query Operators in LINQ to Entities Queries</span></span>
<span data-ttu-id="ecdef-103">In einer Abfrage geben Sie die Informationen an, die aus der Datenquelle abgerufen werden sollen.</span><span class="sxs-lookup"><span data-stu-id="ecdef-103">In a query, you specify the information that you want to retrieve from the data source.</span></span> <span data-ttu-id="ecdef-104">In der Abfrage kann auch angegeben werden, wie die Abfrageergebnisse sortiert, gruppiert und formatiert werden sollen, bevor sie zurückgegeben werden.</span><span class="sxs-lookup"><span data-stu-id="ecdef-104">A query can also specify how that information should be sorted, grouped, and shaped before it is returned.</span></span> <span data-ttu-id="ecdef-105">LINQ stellt eine Reihe von Standardabfragemethoden für die Verwendung in einer Abfrage bereit.</span><span class="sxs-lookup"><span data-stu-id="ecdef-105">LINQ provides a set of standard query methods that you can use in a query.</span></span> <span data-ttu-id="ecdef-106">Die meisten dieser Methoden bearbeiten Sequenzen; In diesem Kontext ist eine Sequenz ein Objekt, dessen Typ implementiert, den <xref:System.Collections.Generic.IEnumerable%601> Schnittstelle oder die <xref:System.Linq.IQueryable%601> Schnittstelle.</span><span class="sxs-lookup"><span data-stu-id="ecdef-106">Most of these methods operate on sequences; in this context, a sequence is an object whose type implements the <xref:System.Collections.Generic.IEnumerable%601> interface or the <xref:System.Linq.IQueryable%601> interface.</span></span> <span data-ttu-id="ecdef-107">Die Standardabfrageoperatoren stellen Abfragefunktionen wie Filterung, Projektion, Aggregation, Sortierung, Gruppierung, Paging und mehr bereit.</span><span class="sxs-lookup"><span data-stu-id="ecdef-107">The standard query operators query functionality includes filtering, projection, aggregation, sorting, grouping, paging, and more.</span></span> <span data-ttu-id="ecdef-108">Einige der häufiger verwendeten Standardabfrageoperatoren verfügen über eine dedizierte Schlüsselwortsyntax, sodass sie mithilfe von Abfrageausdruckssyntax aufgerufen werden können.</span><span class="sxs-lookup"><span data-stu-id="ecdef-108">Some of the more frequently used standard query operators have dedicated keyword syntax so that they can be called by using query expression syntax.</span></span> <span data-ttu-id="ecdef-109">Mit einem Abfrageausdruck kann eine Abfrage besser lesbar ausgedrückt werden als mit dessen methodenbasierter Entsprechung.</span><span class="sxs-lookup"><span data-stu-id="ecdef-109">A query expression is a different, more readable way to express a query than the method-based equivalent.</span></span> <span data-ttu-id="ecdef-110">Die Abfrageausdrucksklauseln werden bei der Kompilierung in Aufrufe der Abfragemethoden übersetzt.</span><span class="sxs-lookup"><span data-stu-id="ecdef-110">Query expression clauses are translated into calls to the query methods at compile time.</span></span> <span data-ttu-id="ecdef-111">Eine Liste der Standardabfrageoperatoren, die über entsprechende Abfrageausdrucksklauseln verfügen, finden Sie unter [Übersicht über Standard Standardabfrageoperatoren](http://msdn.microsoft.com/library/24cda21e-8af8-4632-b519-c404a839b9b2).</span><span class="sxs-lookup"><span data-stu-id="ecdef-111">For a list of standard query operators that have equivalent query expression clauses, see [Standard Query Operators Overview](http://msdn.microsoft.com/library/24cda21e-8af8-4632-b519-c404a839b9b2).</span></span>  
  
 <span data-ttu-id="ecdef-112">Nicht alle Standardabfrageoperatoren werden in [!INCLUDE[linq_entities](../../../../../../includes/linq-entities-md.md)]-Abfragen unterstützt.</span><span class="sxs-lookup"><span data-stu-id="ecdef-112">Not all of the standard query operators are supported in [!INCLUDE[linq_entities](../../../../../../includes/linq-entities-md.md)] queries.</span></span> <span data-ttu-id="ecdef-113">Weitere Informationen finden Sie unter [unterstützte und nicht unterstützte LINQ-Methoden (LINQ to Entities)](../../../../../../docs/framework/data/adonet/ef/language-reference/supported-and-unsupported-linq-methods-linq-to-entities.md).</span><span class="sxs-lookup"><span data-stu-id="ecdef-113">For more information, see [Supported and Unsupported LINQ Methods (LINQ to Entities)](../../../../../../docs/framework/data/adonet/ef/language-reference/supported-and-unsupported-linq-methods-linq-to-entities.md).</span></span> <span data-ttu-id="ecdef-114">Dieses Thema enthält Informationen über die für [!INCLUDE[linq_entities](../../../../../../includes/linq-entities-md.md)] spezifischen Standardabfrageoperatoren.</span><span class="sxs-lookup"><span data-stu-id="ecdef-114">This topic provides information about the standard query operators that is specific to [!INCLUDE[linq_entities](../../../../../../includes/linq-entities-md.md)].</span></span> <span data-ttu-id="ecdef-115">Weitere Informationen zu bekannten Problemen in [!INCLUDE[linq_entities](../../../../../../includes/linq-entities-md.md)] Abfragen finden Sie im [bekannte Probleme und Überlegungen zu LINQ to Entities](../../../../../../docs/framework/data/adonet/ef/language-reference/known-issues-and-considerations-in-linq-to-entities.md).</span><span class="sxs-lookup"><span data-stu-id="ecdef-115">For more information about known issues in [!INCLUDE[linq_entities](../../../../../../includes/linq-entities-md.md)] queries, see [Known Issues and Considerations in LINQ to Entities](../../../../../../docs/framework/data/adonet/ef/language-reference/known-issues-and-considerations-in-linq-to-entities.md).</span></span>  
  
## <a name="projection-and-filtering-methods"></a><span data-ttu-id="ecdef-116">Projektions- und Filtermethoden</span><span class="sxs-lookup"><span data-stu-id="ecdef-116">Projection and Filtering Methods</span></span>  
 <span data-ttu-id="ecdef-117">*Projektion* bezieht sich auf die Elemente eines Resultsets in eine gewünschte Form transformieren.</span><span class="sxs-lookup"><span data-stu-id="ecdef-117">*Projection* refers to transforming the elements of a result set into a desired form.</span></span> <span data-ttu-id="ecdef-118">Beispielsweise kann eine Teilmenge der benötigten Eigenschaften der einzelnen Objekte im Resultset projiziert werden, es kann eine Eigenschaft projiziert werden, um eine mathematische Berechnung damit auszuführen, oder es kann das ganze Objekt aus dem Resultset projiziert werden.</span><span class="sxs-lookup"><span data-stu-id="ecdef-118">For example, you can project a subset of the properties you need from each object in the result set, you can project a property and perform a mathematical calculation on it, or you can project the entire object from the result set.</span></span> <span data-ttu-id="ecdef-119">Zur Projektion werden die `Select`-Methode und die `SelectMany`-Methode verwendet.</span><span class="sxs-lookup"><span data-stu-id="ecdef-119">The projection methods are `Select` and `SelectMany`.</span></span>  
  
 <span data-ttu-id="ecdef-120">*Filtern von* bezieht sich auf den Betrieb der Einschränkung des Resultsets, die nur die Elemente enthält, die eine bestimmte Bedingung erfüllen.</span><span class="sxs-lookup"><span data-stu-id="ecdef-120">*Filtering* refers to the operation of restricting the result set to contain only those elements that match a specified condition.</span></span> <span data-ttu-id="ecdef-121">Zum Filtern wird die `Where`-Methode verwendet.</span><span class="sxs-lookup"><span data-stu-id="ecdef-121">The filtering method is `Where`.</span></span>  
  
 <span data-ttu-id="ecdef-122">Die meisten Überladungen von Methoden zur Projektion und Filterung werden in [!INCLUDE[linq_entities](../../../../../../includes/linq-entities-md.md)] unterstützt. Davon ausgenommen sind die Methoden, denen positionelle Argumente übergeben werden.</span><span class="sxs-lookup"><span data-stu-id="ecdef-122">Most overloads of the projection and filtering methods are supported in [!INCLUDE[linq_entities](../../../../../../includes/linq-entities-md.md)], with the exception of those that accept a positional argument.</span></span>  
  
## <a name="join-methods"></a><span data-ttu-id="ecdef-123">Methoden zur Verknüpfung</span><span class="sxs-lookup"><span data-stu-id="ecdef-123">Join Methods</span></span>  
 <span data-ttu-id="ecdef-124">Das Verknüpfen ist eine wichtige Operation bei Abfragen von Daten aus Datenquellen, die nicht über navigierbare Beziehungen verfügen.</span><span class="sxs-lookup"><span data-stu-id="ecdef-124">Joining is an important operation in queries that target data sources that have no navigable relationships to each other.</span></span> <span data-ttu-id="ecdef-125">Eine Verknüpfung zweier Datenquellen ist die Zuordnung von Objekten der einen Datenquelle zu Objekten in der anderen Datenquelle, die über ein gemeinsames Attribut oder eine gemeinsame Eigenschaft verfügen.</span><span class="sxs-lookup"><span data-stu-id="ecdef-125">A join of two data sources is the association of objects in one data source with objects in the other data source that share a common attribute or property.</span></span> <span data-ttu-id="ecdef-126">Zur Verknüpfung werden die `Join`-Methode und die `GroupJoin`-Methode verwendet.</span><span class="sxs-lookup"><span data-stu-id="ecdef-126">The join methods are `Join` and `GroupJoin`.</span></span>  
  
 <span data-ttu-id="ecdef-127">Die meisten Überladungen der Methoden zur Verknüpfung werden unterstützt. Davon ausgenommen sind Methoden, die einen <xref:System.Collections.Generic.IEqualityComparer%601> verwenden.</span><span class="sxs-lookup"><span data-stu-id="ecdef-127">Most overloads of the join methods are supported, with the exception of those that use a <xref:System.Collections.Generic.IEqualityComparer%601>.</span></span> <span data-ttu-id="ecdef-128">Der Grund dafür ist, dass der Vergleich nicht für die Datenquelle übersetzt werden kann.</span><span class="sxs-lookup"><span data-stu-id="ecdef-128">This is because the comparer cannot be translated to the data source.</span></span>  
  
## <a name="set-methods"></a><span data-ttu-id="ecdef-129">Methoden für Mengen</span><span class="sxs-lookup"><span data-stu-id="ecdef-129">Set Methods</span></span>  
 <span data-ttu-id="ecdef-130">Mengenoperationen in LINQ sind Abfrageoperationen, deren Resultsets auf der Gegenwart oder Abwesenheit äquivalenter Elemente in derselben oder in einer anderen Auflistung (oder Menge) basieren.</span><span class="sxs-lookup"><span data-stu-id="ecdef-130">Set operations in LINQ are query operations that base their result sets on the presence or absence of equivalent elements within the same or in another collection (or set).</span></span> <span data-ttu-id="ecdef-131">Für Mengen werden die Methoden `All`, `Any`, `Concat`, `Contains`, `DefaultIfEmpty`, `Distinct`, `EqualAll`, `Except`, `Intersect` und `Union` verwendet.</span><span class="sxs-lookup"><span data-stu-id="ecdef-131">The set methods are `All`, `Any`, `Concat`, `Contains`, `DefaultIfEmpty`, `Distinct`, `EqualAll`, `Except`, `Intersect`, and `Union`.</span></span>  
  
 <span data-ttu-id="ecdef-132">Die meisten Überladungen von Methoden für Mengen werden in [!INCLUDE[linq_entities](../../../../../../includes/linq-entities-md.md)] unterstützt. Im Vergleich mit LINQ to Objects gibt es jedoch einige Unterschiede im Verhalten.</span><span class="sxs-lookup"><span data-stu-id="ecdef-132">Most overloads of the set methods are supported in [!INCLUDE[linq_entities](../../../../../../includes/linq-entities-md.md)], though there are some differences in behavior compared to LINQ to Objects.</span></span> <span data-ttu-id="ecdef-133">Allerdings set-Methoden, mit denen ein <xref:System.Collections.Generic.IEqualityComparer%601> werden nicht unterstützt, da der Vergleich mit der Datenquelle übersetzt werden kann.</span><span class="sxs-lookup"><span data-stu-id="ecdef-133">However, set methods that use an <xref:System.Collections.Generic.IEqualityComparer%601> are not supported because the comparer cannot be translated to the data source.</span></span>  
  
## <a name="ordering-methods"></a><span data-ttu-id="ecdef-134">Sortiermethoden</span><span class="sxs-lookup"><span data-stu-id="ecdef-134">Ordering Methods</span></span>  
 <span data-ttu-id="ecdef-135">Mit Sortierung wird das auf einem oder mehreren Attributen basierende Sortieren von Elementen eines Resultsets bezeichnet.</span><span class="sxs-lookup"><span data-stu-id="ecdef-135">Ordering, or sorting, refers to the ordering the elements of a result set based on one or more attributes.</span></span> <span data-ttu-id="ecdef-136">Durch die Angabe eines oder mehrerer Sortierkriterien können Verbindungen innerhalb einer Gruppe aufgelöst werden.</span><span class="sxs-lookup"><span data-stu-id="ecdef-136">By specifying more than one sort criterion, you can break ties within a group.</span></span>  
  
 <span data-ttu-id="ecdef-137">Die meisten Überladungen der Sortiermethoden werden unterstützt. Davon ausgenommen sind Methoden, die einen <xref:System.Collections.Generic.IComparer%601> verwenden.</span><span class="sxs-lookup"><span data-stu-id="ecdef-137">Most overloads of the ordering methods are supported, with the exception of those that use an <xref:System.Collections.Generic.IComparer%601>.</span></span> <span data-ttu-id="ecdef-138">Der Grund dafür ist, dass der Vergleich nicht für die Datenquelle übersetzt werden kann.</span><span class="sxs-lookup"><span data-stu-id="ecdef-138">This is because the comparer cannot be translated to the data source.</span></span> <span data-ttu-id="ecdef-139">Zum Sortieren werden die Methoden `OrderBy`, `OrderByDescending`, `ThenBy`, `ThenByDescending` und `Reverse` verwendet.</span><span class="sxs-lookup"><span data-stu-id="ecdef-139">The ordering methods are `OrderBy`, `OrderByDescending`, `ThenBy`, `ThenByDescending`, and `Reverse`.</span></span>  
  
 <span data-ttu-id="ecdef-140">Da die Abfrage für die Datenquelle ausgeführt wird, kann sich das Sortierverhalten von den Abfragen unterscheiden, die in der CLR ausgeführt werden.</span><span class="sxs-lookup"><span data-stu-id="ecdef-140">Because the query is executed on the data source, the ordering behavior may differ from queries executed in the CLR.</span></span> <span data-ttu-id="ecdef-141">Der Grund dafür ist, dass in der Datenquelle Sortieroptionen wie Fallsortierung, Kanjisortierung und Nullsortierung festgelegt werden können.</span><span class="sxs-lookup"><span data-stu-id="ecdef-141">This is because ordering options, such as case ordering, kanji ordering, and null ordering, can be set in the data source.</span></span> <span data-ttu-id="ecdef-142">Je nach Datenquelle können diese Sortieroptionen zu anderen Ergebnissen als in der CLR führen.</span><span class="sxs-lookup"><span data-stu-id="ecdef-142">Depending on the data source, these ordering options might produce different results than in the CLR.</span></span>  
  
 <span data-ttu-id="ecdef-143">Wenn dieselbe Schlüsselauswahlfunktion in mehr als einem Sortiervorgang angegeben wird, wird eine doppelte Sortierung ausgeführt.</span><span class="sxs-lookup"><span data-stu-id="ecdef-143">If you specify the same key selector in more than one ordering operation, a duplicate ordering will be produced.</span></span> <span data-ttu-id="ecdef-144">Da dies nicht gültig ist, wird eine Ausnahme ausgelöst.</span><span class="sxs-lookup"><span data-stu-id="ecdef-144">This is not valid and an exception will be thrown.</span></span>  
  
## <a name="grouping-methods"></a><span data-ttu-id="ecdef-145">Gruppierungsmethoden</span><span class="sxs-lookup"><span data-stu-id="ecdef-145">Grouping Methods</span></span>  
 <span data-ttu-id="ecdef-146">Mit Gruppierung wird das Anordnen von Daten in Gruppen bezeichnet, sodass die Elemente in jeder Gruppe über ein gemeinsames Attribut verfügen.</span><span class="sxs-lookup"><span data-stu-id="ecdef-146">Grouping refers to placing data into groups so that the elements in each group share a common attribute.</span></span> <span data-ttu-id="ecdef-147">Zum Gruppieren wird die `GroupBy`-Methode verwendet.</span><span class="sxs-lookup"><span data-stu-id="ecdef-147">The grouping method is `GroupBy`.</span></span>  
  
 <span data-ttu-id="ecdef-148">Die meisten Überladungen der Gruppierungsmethoden werden unterstützt. Davon ausgenommen sind Methoden, die einen <xref:System.Collections.Generic.IEqualityComparer%601> verwenden.</span><span class="sxs-lookup"><span data-stu-id="ecdef-148">Most overloads of the grouping methods are supported, with the exception of those that use an <xref:System.Collections.Generic.IEqualityComparer%601>.</span></span> <span data-ttu-id="ecdef-149">Der Grund dafür ist, dass der Vergleich nicht für die Datenquelle übersetzt werden kann.</span><span class="sxs-lookup"><span data-stu-id="ecdef-149">This is because the comparer cannot be translated to the data source.</span></span>  
  
 <span data-ttu-id="ecdef-150">Die Gruppierungsmethoden werden der Datenquelle mithilfe einer eigenen Unterabfrage für die Schlüsselauswahlfunktion zugeordnet.</span><span class="sxs-lookup"><span data-stu-id="ecdef-150">The grouping methods are mapped to the data source using a distinct sub-query for the key selector.</span></span> <span data-ttu-id="ecdef-151">Die Unterabfrage für den Vergleich mit der Schlüsselauswahlfunktion wird unter Verwendung der Semantik der Datenquelle ausgeführt. Dies ist beispielsweise beim Vergleich von `null`-Werten bedeutsam.</span><span class="sxs-lookup"><span data-stu-id="ecdef-151">The key selector comparison sub-query is executed by using the semantics of the data source, including issues related to comparing `null` values.</span></span>  
  
## <a name="aggregate-methods"></a><span data-ttu-id="ecdef-152">Aggregatmethoden</span><span class="sxs-lookup"><span data-stu-id="ecdef-152">Aggregate Methods</span></span>  
 <span data-ttu-id="ecdef-153">Während eines Aggregationsvorgangs wird aus einer Auflistung von Werten ein einzelner Wert berechnet.</span><span class="sxs-lookup"><span data-stu-id="ecdef-153">An aggregation operation computes a single value from a collection of values.</span></span> <span data-ttu-id="ecdef-154">Ein Beispiel für einen Aggregationsvorgang ist die Berechnung der durchschnittlichen Tagestemperatur aus den Tagestemperaturen eines Monats.</span><span class="sxs-lookup"><span data-stu-id="ecdef-154">For example, calculating the average daily temperature from a month's worth of daily temperature values is an aggregation operation.</span></span> <span data-ttu-id="ecdef-155">Für die Aggregation werden die Methoden `Aggregate`, `Average`, `Count`, `LongCount`, `Max`, `Min` und `Sum` verwendet.</span><span class="sxs-lookup"><span data-stu-id="ecdef-155">The aggregate methods are `Aggregate`, `Average`, `Count`, `LongCount`, `Max`, `Min`, and `Sum`.</span></span>  
  
 <span data-ttu-id="ecdef-156">Die meisten Überladungen der Aggregatmethoden werden unterstützt.</span><span class="sxs-lookup"><span data-stu-id="ecdef-156">Most overloads of the aggregate methods are supported.</span></span> <span data-ttu-id="ecdef-157">Das Verhalten der Aggregatmethoden bzgl. NULL-Werten wird durch die Semantik der Datenquelle gesteuert.</span><span class="sxs-lookup"><span data-stu-id="ecdef-157">For behavior related to null values, the aggregate methods use the data source semantics.</span></span> <span data-ttu-id="ecdef-158">Das Verhalten der Aggregatmethoden im Zusammenhang mit NULL-Werten ist je nach verwendeter Backenddatenquelle unterschiedlich.</span><span class="sxs-lookup"><span data-stu-id="ecdef-158">The behavior of the aggregation methods when null values are involved might be different, depending on which back-end data source is being used.</span></span> <span data-ttu-id="ecdef-159">Das Verhalten der Aggregatmethoden, das die Semantik der Datenquelle verwendet, kann sich auch von dem von CLR-Methoden erwarteten Verhalten unterscheiden.</span><span class="sxs-lookup"><span data-stu-id="ecdef-159">Aggregate method behavior using the semantics of the data source might also be different from what is expected from CLR methods.</span></span> <span data-ttu-id="ecdef-160">Beim Standardverhalten der `Sum`-Methode in SQL Server werden beispielsweise NULL-Werte ignoriert und keine Ausnahmen ausgelöst.</span><span class="sxs-lookup"><span data-stu-id="ecdef-160">For example, the default behavior for the `Sum` method on SQL Server is to ignore any null values instead of throwing an exception.</span></span>  
  
 <span data-ttu-id="ecdef-161">Sämtliche bei einer Aggregation ausgelösten Ausnahmen, wie ein Überlauf der `Sum`-Funktion, werden als Datenquellenausnahmen oder Entity Framework-Ausnahmen während der Materialisierung der Abfrageergebnisse ausgelöst.</span><span class="sxs-lookup"><span data-stu-id="ecdef-161">Any exceptions that result from aggregation, such as an overflow from the `Sum` function, are thrown as data source exceptions or Entity Framework exceptions during the materialization of the query results.</span></span>  
  
 <span data-ttu-id="ecdef-162">Für Methoden, die eine Berechnung aus einer Sequenz beinhalten, wie `Sum` oder `Average`, wird die eigentliche Berechnung auf dem Server ausgeführt.</span><span class="sxs-lookup"><span data-stu-id="ecdef-162">For those methods that involve a calculation over a sequence, such as `Sum` or `Average`, the actual calculation is performed on the server.</span></span> <span data-ttu-id="ecdef-163">Daher können Typkonvertierungen und Präzisionsverlust auf dem Server auftreten, und die Ergebnisse können sich von denen, die bei der Verwendung der CLR-Semantik zu erwarten sind, unterscheiden.</span><span class="sxs-lookup"><span data-stu-id="ecdef-163">As a result, type conversions and loss of precision might occur on the server, and the results might differ from what is expected using CLR semantics.</span></span>  
  
 <span data-ttu-id="ecdef-164">Das Standardverhalten der Aggregatmethoden für NULL-Werte und Werte, die von NULL verschieden sind, wird in der folgenden Tabelle dargestellt:</span><span class="sxs-lookup"><span data-stu-id="ecdef-164">The default behavior of the aggregate methods for null/non-null values is shown in the following table:</span></span>  
  
|<span data-ttu-id="ecdef-165">Methode</span><span class="sxs-lookup"><span data-stu-id="ecdef-165">Method</span></span>|<span data-ttu-id="ecdef-166">Keine Daten</span><span class="sxs-lookup"><span data-stu-id="ecdef-166">No data</span></span>|<span data-ttu-id="ecdef-167">Alle NULL-Werte</span><span class="sxs-lookup"><span data-stu-id="ecdef-167">All null values</span></span>|<span data-ttu-id="ecdef-168">Einige NULL-Werte</span><span class="sxs-lookup"><span data-stu-id="ecdef-168">Some null values</span></span>|<span data-ttu-id="ecdef-169">Keine NULL-Werte</span><span class="sxs-lookup"><span data-stu-id="ecdef-169">No null values</span></span>|  
|------------|-------------|---------------------|----------------------|--------------------|  
|`Average`|<span data-ttu-id="ecdef-170">Gibt NULL zurück.</span><span class="sxs-lookup"><span data-stu-id="ecdef-170">Returns null.</span></span>|<span data-ttu-id="ecdef-171">Gibt NULL zurück.</span><span class="sxs-lookup"><span data-stu-id="ecdef-171">Returns null.</span></span>|<span data-ttu-id="ecdef-172">Gibt den Durchschnitt der von NULL verschiedenen Werte in einer Sequenz zurück.</span><span class="sxs-lookup"><span data-stu-id="ecdef-172">Returns the average of the non-null values in a sequence.</span></span>|<span data-ttu-id="ecdef-173">Berechnet den Durchschnitt einer Sequenz von numerischen Werten.</span><span class="sxs-lookup"><span data-stu-id="ecdef-173">Computes the average of a sequence of numeric values.</span></span>|  
|`Count`|<span data-ttu-id="ecdef-174">Gibt 0 (null) zurück</span><span class="sxs-lookup"><span data-stu-id="ecdef-174">Returns 0</span></span>|<span data-ttu-id="ecdef-175">Gibt die Anzahl der NULL-Werte in der Sequenz zurück.</span><span class="sxs-lookup"><span data-stu-id="ecdef-175">Returns the number of null values in the sequence.</span></span>|<span data-ttu-id="ecdef-176">Gibt die Anzahl der NULL-Werte und der von NULL verschiedenen Werte in einer Sequenz zurück.</span><span class="sxs-lookup"><span data-stu-id="ecdef-176">Returns the number of null and non-null values in the sequence.</span></span>|<span data-ttu-id="ecdef-177">Gibt die Anzahl der Elemente in der Sequenz zurück.</span><span class="sxs-lookup"><span data-stu-id="ecdef-177">Returns the number of elements in the sequence.</span></span>|  
|`Max`|<span data-ttu-id="ecdef-178">Gibt NULL zurück.</span><span class="sxs-lookup"><span data-stu-id="ecdef-178">Returns null.</span></span>|<span data-ttu-id="ecdef-179">Gibt NULL zurück.</span><span class="sxs-lookup"><span data-stu-id="ecdef-179">Returns null.</span></span>|<span data-ttu-id="ecdef-180">Gibt den maximalen von NULL verschiedenen Wert in einer Sequenz zurück.</span><span class="sxs-lookup"><span data-stu-id="ecdef-180">Returns the maximum non-null value in a sequence.</span></span>|<span data-ttu-id="ecdef-181">Gibt den Maximalwert in einer Sequenz zurück.</span><span class="sxs-lookup"><span data-stu-id="ecdef-181">Returns the maximum value in a sequence.</span></span>|  
|`Min`|<span data-ttu-id="ecdef-182">Gibt NULL zurück.</span><span class="sxs-lookup"><span data-stu-id="ecdef-182">Returns null.</span></span>|<span data-ttu-id="ecdef-183">Gibt NULL zurück.</span><span class="sxs-lookup"><span data-stu-id="ecdef-183">Returns null.</span></span>|<span data-ttu-id="ecdef-184">Gibt den minimalen von NULL verschiedenen Wert in einer Sequenz zurück.</span><span class="sxs-lookup"><span data-stu-id="ecdef-184">Returns the minimum non-null value in a sequence.</span></span>|<span data-ttu-id="ecdef-185">Gibt den Minimalwert in einer Sequenz zurück.</span><span class="sxs-lookup"><span data-stu-id="ecdef-185">Returns the minimum value in a sequence.</span></span>|  
|`Sum`|<span data-ttu-id="ecdef-186">Gibt NULL zurück.</span><span class="sxs-lookup"><span data-stu-id="ecdef-186">Returns null.</span></span>|<span data-ttu-id="ecdef-187">Gibt NULL zurück.</span><span class="sxs-lookup"><span data-stu-id="ecdef-187">Returns null.</span></span>|<span data-ttu-id="ecdef-188">Gibt die Summe der von NULL verschiedenen Werte in einer Sequenz zurück.</span><span class="sxs-lookup"><span data-stu-id="ecdef-188">Returns the sum of the non-null value in a sequence.</span></span>|<span data-ttu-id="ecdef-189">Berechnet die Summe einer Sequenz von numerischen Werten.</span><span class="sxs-lookup"><span data-stu-id="ecdef-189">Computes the sum of a sequence of numeric values.</span></span>|  
  
## <a name="type-methods"></a><span data-ttu-id="ecdef-190">Typmethoden</span><span class="sxs-lookup"><span data-stu-id="ecdef-190">Type Methods</span></span>  
 <span data-ttu-id="ecdef-191">Die beiden LINQ-Methoden, die für Typkonvertierung und -test verwendet werden, werden im Kontext des [!INCLUDE[adonet_ef](../../../../../../includes/adonet-ef-md.md)] unterstützt.</span><span class="sxs-lookup"><span data-stu-id="ecdef-191">The two LINQ methods that deal with type conversion and testing are both supported in the context of the [!INCLUDE[adonet_ef](../../../../../../includes/adonet-ef-md.md)].</span></span> <span data-ttu-id="ecdef-192">Dies bedeutet, dass nur Typen unterstützt werden, die einem entsprechenden [!INCLUDE[adonet_ef](../../../../../../includes/adonet-ef-md.md)]-Typ zugeordnet werden können.</span><span class="sxs-lookup"><span data-stu-id="ecdef-192">This means that the only supported types are types that map to the appropriate [!INCLUDE[adonet_ef](../../../../../../includes/adonet-ef-md.md)] type.</span></span> <span data-ttu-id="ecdef-193">Eine Liste dieser Typen finden Sie [konzeptionellen Modell Typen (CSDL)](http://msdn.microsoft.com/en-us/987b995f-e429-4569-9559-b4146744def4).</span><span class="sxs-lookup"><span data-stu-id="ecdef-193">For a list of these types, see [Conceptual Model Types (CSDL)](http://msdn.microsoft.com/en-us/987b995f-e429-4569-9559-b4146744def4).</span></span> <span data-ttu-id="ecdef-194">Als Typmethoden werden `Convert` und `OfType` verwendet.</span><span class="sxs-lookup"><span data-stu-id="ecdef-194">The type methods are `Convert` and `OfType`.</span></span>  
  
 <span data-ttu-id="ecdef-195">`OfType` wird für Entitätstypen unterstützt.</span><span class="sxs-lookup"><span data-stu-id="ecdef-195">`OfType` is supported for entity types.</span></span> <span data-ttu-id="ecdef-196">`Convert` wird für primitive Typen in einem konzeptionellen Modell unterstützt.</span><span class="sxs-lookup"><span data-stu-id="ecdef-196">`Convert` is supported for conceptual model primitive types.</span></span>  <span data-ttu-id="ecdef-197">Die C#-Methoden `is` und `as` werden ebenfalls unterstützt.</span><span class="sxs-lookup"><span data-stu-id="ecdef-197">The C# `is` and `as` methods are also supported.</span></span>  
  
## <a name="paging-methods"></a><span data-ttu-id="ecdef-198">Pagingmethoden</span><span class="sxs-lookup"><span data-stu-id="ecdef-198">Paging Methods</span></span>  
 <span data-ttu-id="ecdef-199">Bei Pagingvorgängen werden einzelne, spezifische Elemente aus einer Sequenz zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="ecdef-199">Paging operations return a single, specific element from a sequence.</span></span> <span data-ttu-id="ecdef-200">Als Elementmethoden werden `ElementAt`, `First`, `FirstOrDefault`, `Last`, `LastOrDefault`, `Single`, `Skip`, `Take` und `TakeWhile` verwendet.</span><span class="sxs-lookup"><span data-stu-id="ecdef-200">The element methods are `ElementAt`, `First`, `FirstOrDefault`, `Last`, `LastOrDefault`, `Single`, `Skip`, `Take`, `TakeWhile`.</span></span>  
  
 <span data-ttu-id="ecdef-201">Einige Pagingmethoden werden nicht unterstützt, da entweder Funktionen nicht der Datenquelle zugeordnet werden können oder keine implizite Sortierung der Mengen in der Datenquelle vorgenommen wird.</span><span class="sxs-lookup"><span data-stu-id="ecdef-201">A number of the paging methods are not supported, due either to the inability to map functions to the data source or to the lack of implicit ordering of sets on the data source.</span></span> <span data-ttu-id="ecdef-202">Methoden, die einen Standardwert zurückgeben, sind auf primitive Typen in einem konzeptionellen Modell und Referenztypen mit dem Standardwert NULL beschränkt.</span><span class="sxs-lookup"><span data-stu-id="ecdef-202">Methods that return a default value are restricted to conceptual model primitive types and reference types with null defaults.</span></span> <span data-ttu-id="ecdef-203">Pagingmethoden, die für eine leere Sequenz ausgeführt werden, geben NULL zurück.</span><span class="sxs-lookup"><span data-stu-id="ecdef-203">Paging methods that are executed on an empty sequence will return null.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="ecdef-204">Siehe auch</span><span class="sxs-lookup"><span data-stu-id="ecdef-204">See Also</span></span>  
 [<span data-ttu-id="ecdef-205">Unterstützte und nicht unterstützte LINQ-Methoden (LINQ to Entities)</span><span class="sxs-lookup"><span data-stu-id="ecdef-205">Supported and Unsupported LINQ Methods (LINQ to Entities)</span></span>](../../../../../../docs/framework/data/adonet/ef/language-reference/supported-and-unsupported-linq-methods-linq-to-entities.md)  
 [<span data-ttu-id="ecdef-206">Übersicht über Standardabfrageoperatoren</span><span class="sxs-lookup"><span data-stu-id="ecdef-206">Standard Query Operators Overview</span></span>](http://msdn.microsoft.com/library/24cda21e-8af8-4632-b519-c404a839b9b2)
