---
title: Standardabfrageoperatoren in LINQ to Entities-Abfragen
ms.date: 08/21/2018
ms.assetid: 7fa55a9b-6219-473d-b1e5-2884a32dcdff
ms.openlocfilehash: a61296d924ab1ff32b5bee523d6f0a06dda95db3
ms.sourcegitcommit: 27a15a55019f6b5f2733961738babe94aec0def3
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 09/15/2020
ms.locfileid: "90557774"
---
# <a name="standard-query-operators-in-linq-to-entities-queries"></a><span data-ttu-id="ee875-102">Standardabfrageoperatoren in LINQ to Entities-Abfragen</span><span class="sxs-lookup"><span data-stu-id="ee875-102">Standard Query Operators in LINQ to Entities Queries</span></span>
<span data-ttu-id="ee875-103">In einer Abfrage geben Sie die Informationen an, die aus der Datenquelle abgerufen werden sollen.</span><span class="sxs-lookup"><span data-stu-id="ee875-103">In a query, you specify the information that you want to retrieve from the data source.</span></span> <span data-ttu-id="ee875-104">In der Abfrage kann auch angegeben werden, wie die Abfrageergebnisse sortiert, gruppiert und formatiert werden sollen, bevor sie zurückgegeben werden.</span><span class="sxs-lookup"><span data-stu-id="ee875-104">A query can also specify how that information should be sorted, grouped, and shaped before it is returned.</span></span> <span data-ttu-id="ee875-105">LINQ stellt eine Reihe von Standardabfragemethoden für die Verwendung in einer Abfrage bereit.</span><span class="sxs-lookup"><span data-stu-id="ee875-105">LINQ provides a set of standard query methods that you can use in a query.</span></span> <span data-ttu-id="ee875-106">Die meisten dieser Methoden arbeiten mit Sequenzen. in diesem Kontext ist eine Sequenz ein Objekt, dessen Typ die- <xref:System.Collections.Generic.IEnumerable%601> Schnittstelle oder die- <xref:System.Linq.IQueryable%601> Schnittstelle implementiert.</span><span class="sxs-lookup"><span data-stu-id="ee875-106">Most of these methods operate on sequences; in this context, a sequence is an object whose type implements the <xref:System.Collections.Generic.IEnumerable%601> interface or the <xref:System.Linq.IQueryable%601> interface.</span></span> <span data-ttu-id="ee875-107">Die Standardabfrageoperatoren stellen Abfragefunktionen wie Filterung, Projektion, Aggregation, Sortierung, Gruppierung, Paging und mehr bereit.</span><span class="sxs-lookup"><span data-stu-id="ee875-107">The standard query operators query functionality includes filtering, projection, aggregation, sorting, grouping, paging, and more.</span></span> <span data-ttu-id="ee875-108">Einige der häufiger verwendeten Standardabfrageoperatoren verfügen über eine dedizierte Schlüsselwortsyntax, sodass sie mithilfe von Abfrageausdruckssyntax aufgerufen werden können.</span><span class="sxs-lookup"><span data-stu-id="ee875-108">Some of the more frequently used standard query operators have dedicated keyword syntax so that they can be called by using query expression syntax.</span></span> <span data-ttu-id="ee875-109">Mit einem Abfrageausdruck kann eine Abfrage besser lesbar ausgedrückt werden als mit dessen methodenbasierter Entsprechung.</span><span class="sxs-lookup"><span data-stu-id="ee875-109">A query expression is a different, more readable way to express a query than the method-based equivalent.</span></span> <span data-ttu-id="ee875-110">Die Abfrageausdrucksklauseln werden bei der Kompilierung in Aufrufe der Abfragemethoden übersetzt.</span><span class="sxs-lookup"><span data-stu-id="ee875-110">Query expression clauses are translated into calls to the query methods at compile time.</span></span> <span data-ttu-id="ee875-111">Eine Liste der Standard Abfrage Operatoren, die über äquivalente Abfrage Ausdrucks Klauseln verfügen, finden Sie unter [Übersicht über Standard Abfrage Operatoren](/previous-versions/visualstudio/visual-studio-2013/bb397896(v=vs.120)).</span><span class="sxs-lookup"><span data-stu-id="ee875-111">For a list of standard query operators that have equivalent query expression clauses, see [Standard Query Operators Overview](/previous-versions/visualstudio/visual-studio-2013/bb397896(v=vs.120)).</span></span>  
  
 <span data-ttu-id="ee875-112">Nicht alle Standard Abfrage Operatoren werden in LINQ to Entities Abfragen unterstützt.</span><span class="sxs-lookup"><span data-stu-id="ee875-112">Not all of the standard query operators are supported in LINQ to Entities queries.</span></span> <span data-ttu-id="ee875-113">Weitere Informationen finden Sie [unter Unterstützte und nicht unterstützte LINQ-Methoden (LINQ to Entities)](supported-and-unsupported-linq-methods-linq-to-entities.md).</span><span class="sxs-lookup"><span data-stu-id="ee875-113">For more information, see [Supported and Unsupported LINQ Methods (LINQ to Entities)](supported-and-unsupported-linq-methods-linq-to-entities.md).</span></span> <span data-ttu-id="ee875-114">Dieses Thema enthält Informationen zu den-Standard Abfrage Operatoren, die für LINQ to Entities spezifisch sind.</span><span class="sxs-lookup"><span data-stu-id="ee875-114">This topic provides information about the standard query operators that is specific to LINQ to Entities.</span></span> <span data-ttu-id="ee875-115">Weitere Informationen zu bekannten Problemen in LINQ to Entities Abfragen finden Sie unter [bekannte Probleme und Überlegungen in LINQ to Entities](known-issues-and-considerations-in-linq-to-entities.md).</span><span class="sxs-lookup"><span data-stu-id="ee875-115">For more information about known issues in LINQ to Entities queries, see [Known Issues and Considerations in LINQ to Entities](known-issues-and-considerations-in-linq-to-entities.md).</span></span>  
  
## <a name="projection-and-filtering-methods"></a><span data-ttu-id="ee875-116">Projektions- und Filtermethoden</span><span class="sxs-lookup"><span data-stu-id="ee875-116">Projection and Filtering Methods</span></span>  
 <span data-ttu-id="ee875-117">*Projektion* bezieht sich auf das Transformieren der Elemente eines Resultsets in eine gewünschte Form.</span><span class="sxs-lookup"><span data-stu-id="ee875-117">*Projection* refers to transforming the elements of a result set into a desired form.</span></span> <span data-ttu-id="ee875-118">Beispielsweise kann eine Teilmenge der benötigten Eigenschaften der einzelnen Objekte im Resultset projiziert werden, es kann eine Eigenschaft projiziert werden, um eine mathematische Berechnung damit auszuführen, oder es kann das ganze Objekt aus dem Resultset projiziert werden.</span><span class="sxs-lookup"><span data-stu-id="ee875-118">For example, you can project a subset of the properties you need from each object in the result set, you can project a property and perform a mathematical calculation on it, or you can project the entire object from the result set.</span></span> <span data-ttu-id="ee875-119">Zur Projektion werden die `Select`-Methode und die `SelectMany`-Methode verwendet.</span><span class="sxs-lookup"><span data-stu-id="ee875-119">The projection methods are `Select` and `SelectMany`.</span></span>  
  
 <span data-ttu-id="ee875-120">*Filterung* bezieht sich auf den Vorgang, bei dem das Resultset auf die Elemente beschränkt wird, die einer bestimmten Bedingung entsprechen.</span><span class="sxs-lookup"><span data-stu-id="ee875-120">*Filtering* refers to the operation of restricting the result set to contain only those elements that match a specified condition.</span></span> <span data-ttu-id="ee875-121">Zum Filtern wird die `Where`-Methode verwendet.</span><span class="sxs-lookup"><span data-stu-id="ee875-121">The filtering method is `Where`.</span></span>  
  
 <span data-ttu-id="ee875-122">Die meisten über Ladungen der Projektions-und Filter Methoden werden in LINQ to Entities unterstützt, mit Ausnahme derjenigen, die ein Positions Argument akzeptieren.</span><span class="sxs-lookup"><span data-stu-id="ee875-122">Most overloads of the projection and filtering methods are supported in LINQ to Entities, with the exception of those that accept a positional argument.</span></span>  
  
## <a name="join-methods"></a><span data-ttu-id="ee875-123">Methoden zur Verknüpfung</span><span class="sxs-lookup"><span data-stu-id="ee875-123">Join Methods</span></span>  
 <span data-ttu-id="ee875-124">Das Verknüpfen ist eine wichtige Operation bei Abfragen von Daten aus Datenquellen, die nicht über navigierbare Beziehungen verfügen.</span><span class="sxs-lookup"><span data-stu-id="ee875-124">Joining is an important operation in queries that target data sources that have no navigable relationships to each other.</span></span> <span data-ttu-id="ee875-125">Eine Verknüpfung zweier Datenquellen ist die Zuordnung von Objekten der einen Datenquelle zu Objekten in der anderen Datenquelle, die über ein gemeinsames Attribut oder eine gemeinsame Eigenschaft verfügen.</span><span class="sxs-lookup"><span data-stu-id="ee875-125">A join of two data sources is the association of objects in one data source with objects in the other data source that share a common attribute or property.</span></span> <span data-ttu-id="ee875-126">Zur Verknüpfung werden die `Join`-Methode und die `GroupJoin`-Methode verwendet.</span><span class="sxs-lookup"><span data-stu-id="ee875-126">The join methods are `Join` and `GroupJoin`.</span></span>  
  
 <span data-ttu-id="ee875-127">Die meisten Überladungen der Methoden zur Verknüpfung werden unterstützt. Davon ausgenommen sind Methoden, die einen <xref:System.Collections.Generic.IEqualityComparer%601> verwenden.</span><span class="sxs-lookup"><span data-stu-id="ee875-127">Most overloads of the join methods are supported, with the exception of those that use a <xref:System.Collections.Generic.IEqualityComparer%601>.</span></span> <span data-ttu-id="ee875-128">Der Grund dafür ist, dass der Vergleich nicht für die Datenquelle übersetzt werden kann.</span><span class="sxs-lookup"><span data-stu-id="ee875-128">This is because the comparer cannot be translated to the data source.</span></span>  
  
## <a name="set-methods"></a><span data-ttu-id="ee875-129">Methoden für Mengen</span><span class="sxs-lookup"><span data-stu-id="ee875-129">Set Methods</span></span>  
 <span data-ttu-id="ee875-130">Mengenoperationen in LINQ sind Abfrageoperationen, deren Resultsets auf der Gegenwart oder Abwesenheit äquivalenter Elemente in derselben oder in einer anderen Auflistung (oder Menge) basieren.</span><span class="sxs-lookup"><span data-stu-id="ee875-130">Set operations in LINQ are query operations that base their result sets on the presence or absence of equivalent elements within the same or in another collection (or set).</span></span> <span data-ttu-id="ee875-131">Für Mengen werden die Methoden `All`, `Any`, `Concat`, `Contains`, `DefaultIfEmpty`, `Distinct`, `EqualAll`, `Except`, `Intersect` und `Union` verwendet.</span><span class="sxs-lookup"><span data-stu-id="ee875-131">The set methods are `All`, `Any`, `Concat`, `Contains`, `DefaultIfEmpty`, `Distinct`, `EqualAll`, `Except`, `Intersect`, and `Union`.</span></span>  
  
 <span data-ttu-id="ee875-132">Die meisten über Ladungen der Set-Methoden werden in LINQ to Entities unterstützt, es gibt jedoch einige Unterschiede im Verhalten im Vergleich zu LINQ to Objects.</span><span class="sxs-lookup"><span data-stu-id="ee875-132">Most overloads of the set methods are supported in LINQ to Entities, though there are some differences in behavior compared to LINQ to Objects.</span></span> <span data-ttu-id="ee875-133">Set-Methoden, die einen verwenden, <xref:System.Collections.Generic.IEqualityComparer%601> werden jedoch nicht unterstützt, da der Vergleich nicht in die Datenquelle übersetzt werden kann.</span><span class="sxs-lookup"><span data-stu-id="ee875-133">However, set methods that use an <xref:System.Collections.Generic.IEqualityComparer%601> are not supported because the comparer cannot be translated to the data source.</span></span>  
  
## <a name="ordering-methods"></a><span data-ttu-id="ee875-134">Sortiermethoden</span><span class="sxs-lookup"><span data-stu-id="ee875-134">Ordering Methods</span></span>  
 <span data-ttu-id="ee875-135">Mit Sortierung wird das auf einem oder mehreren Attributen basierende Sortieren von Elementen eines Resultsets bezeichnet.</span><span class="sxs-lookup"><span data-stu-id="ee875-135">Ordering, or sorting, refers to the ordering the elements of a result set based on one or more attributes.</span></span> <span data-ttu-id="ee875-136">Durch die Angabe eines oder mehrerer Sortierkriterien können Verbindungen innerhalb einer Gruppe aufgelöst werden.</span><span class="sxs-lookup"><span data-stu-id="ee875-136">By specifying more than one sort criterion, you can break ties within a group.</span></span>  
  
 <span data-ttu-id="ee875-137">Die meisten Überladungen der Sortiermethoden werden unterstützt. Davon ausgenommen sind Methoden, die einen <xref:System.Collections.Generic.IComparer%601> verwenden.</span><span class="sxs-lookup"><span data-stu-id="ee875-137">Most overloads of the ordering methods are supported, with the exception of those that use an <xref:System.Collections.Generic.IComparer%601>.</span></span> <span data-ttu-id="ee875-138">Der Grund dafür ist, dass der Vergleich nicht für die Datenquelle übersetzt werden kann.</span><span class="sxs-lookup"><span data-stu-id="ee875-138">This is because the comparer cannot be translated to the data source.</span></span> <span data-ttu-id="ee875-139">Zum Sortieren werden die Methoden `OrderBy`, `OrderByDescending`, `ThenBy`, `ThenByDescending` und `Reverse` verwendet.</span><span class="sxs-lookup"><span data-stu-id="ee875-139">The ordering methods are `OrderBy`, `OrderByDescending`, `ThenBy`, `ThenByDescending`, and `Reverse`.</span></span>  
  
 <span data-ttu-id="ee875-140">Da die Abfrage für die Datenquelle ausgeführt wird, kann sich das Sortierverhalten von den Abfragen unterscheiden, die in der CLR ausgeführt werden.</span><span class="sxs-lookup"><span data-stu-id="ee875-140">Because the query is executed on the data source, the ordering behavior may differ from queries executed in the CLR.</span></span> <span data-ttu-id="ee875-141">Der Grund dafür ist, dass in der Datenquelle Sortieroptionen wie Fallsortierung, Kanjisortierung und Nullsortierung festgelegt werden können.</span><span class="sxs-lookup"><span data-stu-id="ee875-141">This is because ordering options, such as case ordering, kanji ordering, and null ordering, can be set in the data source.</span></span> <span data-ttu-id="ee875-142">Je nach Datenquelle können diese Sortieroptionen zu anderen Ergebnissen als in der CLR führen.</span><span class="sxs-lookup"><span data-stu-id="ee875-142">Depending on the data source, these ordering options might produce different results than in the CLR.</span></span>  
  
 <span data-ttu-id="ee875-143">Wenn dieselbe Schlüsselauswahlfunktion in mehr als einem Sortiervorgang angegeben wird, wird eine doppelte Sortierung ausgeführt.</span><span class="sxs-lookup"><span data-stu-id="ee875-143">If you specify the same key selector in more than one ordering operation, a duplicate ordering will be produced.</span></span> <span data-ttu-id="ee875-144">Da dies nicht gültig ist, wird eine Ausnahme ausgelöst.</span><span class="sxs-lookup"><span data-stu-id="ee875-144">This is not valid and an exception will be thrown.</span></span>  
  
## <a name="grouping-methods"></a><span data-ttu-id="ee875-145">Gruppierungsmethoden</span><span class="sxs-lookup"><span data-stu-id="ee875-145">Grouping Methods</span></span>  
 <span data-ttu-id="ee875-146">Mit Gruppierung wird das Anordnen von Daten in Gruppen bezeichnet, sodass die Elemente in jeder Gruppe über ein gemeinsames Attribut verfügen.</span><span class="sxs-lookup"><span data-stu-id="ee875-146">Grouping refers to placing data into groups so that the elements in each group share a common attribute.</span></span> <span data-ttu-id="ee875-147">Zum Gruppieren wird die `GroupBy`-Methode verwendet.</span><span class="sxs-lookup"><span data-stu-id="ee875-147">The grouping method is `GroupBy`.</span></span>  
  
 <span data-ttu-id="ee875-148">Die meisten Überladungen der Gruppierungsmethoden werden unterstützt. Davon ausgenommen sind Methoden, die einen <xref:System.Collections.Generic.IEqualityComparer%601> verwenden.</span><span class="sxs-lookup"><span data-stu-id="ee875-148">Most overloads of the grouping methods are supported, with the exception of those that use an <xref:System.Collections.Generic.IEqualityComparer%601>.</span></span> <span data-ttu-id="ee875-149">Der Grund dafür ist, dass der Vergleich nicht für die Datenquelle übersetzt werden kann.</span><span class="sxs-lookup"><span data-stu-id="ee875-149">This is because the comparer cannot be translated to the data source.</span></span>  
  
 <span data-ttu-id="ee875-150">Die Gruppierungsmethoden werden der Datenquelle mithilfe einer eigenen Unterabfrage für die Schlüsselauswahlfunktion zugeordnet.</span><span class="sxs-lookup"><span data-stu-id="ee875-150">The grouping methods are mapped to the data source using a distinct sub-query for the key selector.</span></span> <span data-ttu-id="ee875-151">Die Unterabfrage für den Vergleich mit der Schlüsselauswahlfunktion wird unter Verwendung der Semantik der Datenquelle ausgeführt. Dies ist beispielsweise beim Vergleich von `null`-Werten bedeutsam.</span><span class="sxs-lookup"><span data-stu-id="ee875-151">The key selector comparison sub-query is executed by using the semantics of the data source, including issues related to comparing `null` values.</span></span>  
  
## <a name="aggregate-methods"></a><span data-ttu-id="ee875-152">Aggregatmethoden</span><span class="sxs-lookup"><span data-stu-id="ee875-152">Aggregate Methods</span></span>  
 <span data-ttu-id="ee875-153">Während eines Aggregationsvorgangs wird aus einer Auflistung von Werten ein einzelner Wert berechnet.</span><span class="sxs-lookup"><span data-stu-id="ee875-153">An aggregation operation computes a single value from a collection of values.</span></span> <span data-ttu-id="ee875-154">Ein Beispiel für einen Aggregationsvorgang ist die Berechnung der durchschnittlichen Tagestemperatur aus den Tagestemperaturen eines Monats.</span><span class="sxs-lookup"><span data-stu-id="ee875-154">For example, calculating the average daily temperature from a month's worth of daily temperature values is an aggregation operation.</span></span> <span data-ttu-id="ee875-155">Für die Aggregation werden die Methoden `Aggregate`, `Average`, `Count`, `LongCount`, `Max`, `Min` und `Sum` verwendet.</span><span class="sxs-lookup"><span data-stu-id="ee875-155">The aggregate methods are `Aggregate`, `Average`, `Count`, `LongCount`, `Max`, `Min`, and `Sum`.</span></span>  
  
 <span data-ttu-id="ee875-156">Die meisten Überladungen der Aggregatmethoden werden unterstützt.</span><span class="sxs-lookup"><span data-stu-id="ee875-156">Most overloads of the aggregate methods are supported.</span></span> <span data-ttu-id="ee875-157">Das Verhalten der Aggregatmethoden bzgl. NULL-Werten wird durch die Semantik der Datenquelle gesteuert.</span><span class="sxs-lookup"><span data-stu-id="ee875-157">For behavior related to null values, the aggregate methods use the data source semantics.</span></span> <span data-ttu-id="ee875-158">Das Verhalten der Aggregatmethoden im Zusammenhang mit NULL-Werten ist je nach verwendeter Backenddatenquelle unterschiedlich.</span><span class="sxs-lookup"><span data-stu-id="ee875-158">The behavior of the aggregation methods when null values are involved might be different, depending on which back-end data source is being used.</span></span> <span data-ttu-id="ee875-159">Das Verhalten der Aggregatmethoden, das die Semantik der Datenquelle verwendet, kann sich auch von dem von CLR-Methoden erwarteten Verhalten unterscheiden.</span><span class="sxs-lookup"><span data-stu-id="ee875-159">Aggregate method behavior using the semantics of the data source might also be different from what is expected from CLR methods.</span></span> <span data-ttu-id="ee875-160">Beim Standardverhalten der `Sum`-Methode in SQL Server werden beispielsweise NULL-Werte ignoriert und keine Ausnahmen ausgelöst.</span><span class="sxs-lookup"><span data-stu-id="ee875-160">For example, the default behavior for the `Sum` method on SQL Server is to ignore any null values instead of throwing an exception.</span></span>  
  
 <span data-ttu-id="ee875-161">Sämtliche bei einer Aggregation ausgelösten Ausnahmen, wie ein Überlauf der `Sum`-Funktion, werden als Datenquellenausnahmen oder Entity Framework-Ausnahmen während der Materialisierung der Abfrageergebnisse ausgelöst.</span><span class="sxs-lookup"><span data-stu-id="ee875-161">Any exceptions that result from aggregation, such as an overflow from the `Sum` function, are thrown as data source exceptions or Entity Framework exceptions during the materialization of the query results.</span></span>  
  
 <span data-ttu-id="ee875-162">Für Methoden, die eine Berechnung aus einer Sequenz beinhalten, wie `Sum` oder `Average`, wird die eigentliche Berechnung auf dem Server ausgeführt.</span><span class="sxs-lookup"><span data-stu-id="ee875-162">For those methods that involve a calculation over a sequence, such as `Sum` or `Average`, the actual calculation is performed on the server.</span></span> <span data-ttu-id="ee875-163">Daher können Typkonvertierungen und Präzisionsverlust auf dem Server auftreten, und die Ergebnisse können sich von denen, die bei der Verwendung der CLR-Semantik zu erwarten sind, unterscheiden.</span><span class="sxs-lookup"><span data-stu-id="ee875-163">As a result, type conversions and loss of precision might occur on the server, and the results might differ from what is expected using CLR semantics.</span></span>  
  
 <span data-ttu-id="ee875-164">Das Standardverhalten der Aggregatmethoden für NULL-Werte und Werte, die von NULL verschieden sind, wird in der folgenden Tabelle dargestellt:</span><span class="sxs-lookup"><span data-stu-id="ee875-164">The default behavior of the aggregate methods for null/non-null values is shown in the following table:</span></span>  
  
|<span data-ttu-id="ee875-165">Methode</span><span class="sxs-lookup"><span data-stu-id="ee875-165">Method</span></span>|<span data-ttu-id="ee875-166">Keine Daten</span><span class="sxs-lookup"><span data-stu-id="ee875-166">No data</span></span>|<span data-ttu-id="ee875-167">Alle NULL-Werte</span><span class="sxs-lookup"><span data-stu-id="ee875-167">All null values</span></span>|<span data-ttu-id="ee875-168">Einige NULL-Werte</span><span class="sxs-lookup"><span data-stu-id="ee875-168">Some null values</span></span>|<span data-ttu-id="ee875-169">Keine NULL-Werte</span><span class="sxs-lookup"><span data-stu-id="ee875-169">No null values</span></span>|  
|------------|-------------|---------------------|----------------------|--------------------|  
|`Average`|<span data-ttu-id="ee875-170">Gibt NULL zurück.</span><span class="sxs-lookup"><span data-stu-id="ee875-170">Returns null.</span></span>|<span data-ttu-id="ee875-171">Gibt NULL zurück.</span><span class="sxs-lookup"><span data-stu-id="ee875-171">Returns null.</span></span>|<span data-ttu-id="ee875-172">Gibt den Durchschnitt der von NULL verschiedenen Werte in einer Sequenz zurück.</span><span class="sxs-lookup"><span data-stu-id="ee875-172">Returns the average of the non-null values in a sequence.</span></span>|<span data-ttu-id="ee875-173">Berechnet den Durchschnitt einer Sequenz von numerischen Werten.</span><span class="sxs-lookup"><span data-stu-id="ee875-173">Computes the average of a sequence of numeric values.</span></span>|  
|`Count`|<span data-ttu-id="ee875-174">Gibt 0 (null) zurück</span><span class="sxs-lookup"><span data-stu-id="ee875-174">Returns 0</span></span>|<span data-ttu-id="ee875-175">Gibt die Anzahl der NULL-Werte in der Sequenz zurück.</span><span class="sxs-lookup"><span data-stu-id="ee875-175">Returns the number of null values in the sequence.</span></span>|<span data-ttu-id="ee875-176">Gibt die Anzahl der NULL-Werte und der von NULL verschiedenen Werte in einer Sequenz zurück.</span><span class="sxs-lookup"><span data-stu-id="ee875-176">Returns the number of null and non-null values in the sequence.</span></span>|<span data-ttu-id="ee875-177">Gibt die Anzahl der Elemente in der Sequenz zurück.</span><span class="sxs-lookup"><span data-stu-id="ee875-177">Returns the number of elements in the sequence.</span></span>|  
|`Max`|<span data-ttu-id="ee875-178">Gibt NULL zurück.</span><span class="sxs-lookup"><span data-stu-id="ee875-178">Returns null.</span></span>|<span data-ttu-id="ee875-179">Gibt NULL zurück.</span><span class="sxs-lookup"><span data-stu-id="ee875-179">Returns null.</span></span>|<span data-ttu-id="ee875-180">Gibt den maximalen von NULL verschiedenen Wert in einer Sequenz zurück.</span><span class="sxs-lookup"><span data-stu-id="ee875-180">Returns the maximum non-null value in a sequence.</span></span>|<span data-ttu-id="ee875-181">Gibt den Maximalwert in einer Sequenz zurück.</span><span class="sxs-lookup"><span data-stu-id="ee875-181">Returns the maximum value in a sequence.</span></span>|  
|`Min`|<span data-ttu-id="ee875-182">Gibt NULL zurück.</span><span class="sxs-lookup"><span data-stu-id="ee875-182">Returns null.</span></span>|<span data-ttu-id="ee875-183">Gibt NULL zurück.</span><span class="sxs-lookup"><span data-stu-id="ee875-183">Returns null.</span></span>|<span data-ttu-id="ee875-184">Gibt den minimalen von NULL verschiedenen Wert in einer Sequenz zurück.</span><span class="sxs-lookup"><span data-stu-id="ee875-184">Returns the minimum non-null value in a sequence.</span></span>|<span data-ttu-id="ee875-185">Gibt den Minimalwert in einer Sequenz zurück.</span><span class="sxs-lookup"><span data-stu-id="ee875-185">Returns the minimum value in a sequence.</span></span>|  
|`Sum`|<span data-ttu-id="ee875-186">Gibt NULL zurück.</span><span class="sxs-lookup"><span data-stu-id="ee875-186">Returns null.</span></span>|<span data-ttu-id="ee875-187">Gibt NULL zurück.</span><span class="sxs-lookup"><span data-stu-id="ee875-187">Returns null.</span></span>|<span data-ttu-id="ee875-188">Gibt die Summe der von NULL verschiedenen Werte in einer Sequenz zurück.</span><span class="sxs-lookup"><span data-stu-id="ee875-188">Returns the sum of the non-null value in a sequence.</span></span>|<span data-ttu-id="ee875-189">Berechnet die Summe einer Sequenz von numerischen Werten.</span><span class="sxs-lookup"><span data-stu-id="ee875-189">Computes the sum of a sequence of numeric values.</span></span>|  
  
## <a name="type-methods"></a><span data-ttu-id="ee875-190">Typmethoden</span><span class="sxs-lookup"><span data-stu-id="ee875-190">Type Methods</span></span>  
 <span data-ttu-id="ee875-191">Beide LINQ-Methoden, die mit Typkonvertierung und-Tests zu tun haben, werden im Kontext der Entity Framework unterstützt.</span><span class="sxs-lookup"><span data-stu-id="ee875-191">The two LINQ methods that deal with type conversion and testing are both supported in the context of the Entity Framework.</span></span> <span data-ttu-id="ee875-192">Dies bedeutet, dass es sich bei den einzigen unterstützten Typen um Typen handelt, die dem entsprechenden Entity Framework Typ zugeordnet werden.</span><span class="sxs-lookup"><span data-stu-id="ee875-192">This means that the only supported types are types that map to the appropriate Entity Framework type.</span></span> <span data-ttu-id="ee875-193">Eine Liste dieser Typen finden Sie unter [konzeptionelle Modelltypen (CSDL)](/ef/ef6/modeling/designer/advanced/edmx/csdl-spec#conceptual-model-types-csdl).</span><span class="sxs-lookup"><span data-stu-id="ee875-193">For a list of these types, see [Conceptual Model Types (CSDL)](/ef/ef6/modeling/designer/advanced/edmx/csdl-spec#conceptual-model-types-csdl).</span></span> <span data-ttu-id="ee875-194">Als Typmethoden werden `Convert` und `OfType` verwendet.</span><span class="sxs-lookup"><span data-stu-id="ee875-194">The type methods are `Convert` and `OfType`.</span></span>  
  
 <span data-ttu-id="ee875-195">`OfType` wird für Entitätstypen unterstützt.</span><span class="sxs-lookup"><span data-stu-id="ee875-195">`OfType` is supported for entity types.</span></span> <span data-ttu-id="ee875-196">`Convert` wird für primitive Typen in einem konzeptionellen Modell unterstützt.</span><span class="sxs-lookup"><span data-stu-id="ee875-196">`Convert` is supported for conceptual model primitive types.</span></span>  <span data-ttu-id="ee875-197">Die C#-Methoden `is` und `as` werden ebenfalls unterstützt.</span><span class="sxs-lookup"><span data-stu-id="ee875-197">The C# `is` and `as` methods are also supported.</span></span>  
  
## <a name="paging-methods"></a><span data-ttu-id="ee875-198">Pagingmethoden</span><span class="sxs-lookup"><span data-stu-id="ee875-198">Paging Methods</span></span>  
 <span data-ttu-id="ee875-199">Paging-Vorgänge geben ein einzelnes Element oder mehrere Elemente aus einer Sequenz zurück.</span><span class="sxs-lookup"><span data-stu-id="ee875-199">Paging operations return a single element or multiple elements from a sequence.</span></span> <span data-ttu-id="ee875-200">Die unterstützten Paging-Methoden sind `First` , `FirstOrDefault` , `Single` , `SingleOrDefault` , `Skip` und `Take` .</span><span class="sxs-lookup"><span data-stu-id="ee875-200">The supported paging methods are `First`, `FirstOrDefault`, `Single`, `SingleOrDefault`, `Skip`, and `Take`.</span></span>  
  
 <span data-ttu-id="ee875-201">Eine Reihe von Pagingmethoden werden nicht unterstützt, da Funktionen nicht der Datenquelle zugeordnet werden können oder nicht die implizite Reihenfolge von Sätzen in der Datenquelle.</span><span class="sxs-lookup"><span data-stu-id="ee875-201">A number of paging methods are not supported, due either to the inability to map functions to the data source or to the lack of implicit ordering of sets on the data source.</span></span> <span data-ttu-id="ee875-202">Methoden, die einen Standardwert zurückgeben, sind auf primitive Typen in einem konzeptionellen Modell und Referenztypen mit dem Standardwert NULL beschränkt.</span><span class="sxs-lookup"><span data-stu-id="ee875-202">Methods that return a default value are restricted to conceptual model primitive types and reference types with null defaults.</span></span> <span data-ttu-id="ee875-203">Pagingmethoden, die für eine leere Sequenz ausgeführt werden, geben NULL zurück.</span><span class="sxs-lookup"><span data-stu-id="ee875-203">Paging methods that are executed on an empty sequence will return null.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="ee875-204">Siehe auch</span><span class="sxs-lookup"><span data-stu-id="ee875-204">See also</span></span>

- [<span data-ttu-id="ee875-205">Unterstützte und nicht unterstützte LINQ-Methoden (LINQ to Entities)</span><span class="sxs-lookup"><span data-stu-id="ee875-205">Supported and Unsupported LINQ Methods (LINQ to Entities)</span></span>](supported-and-unsupported-linq-methods-linq-to-entities.md)
- <span data-ttu-id="ee875-206">[Übersicht über Standardabfrageoperatoren](/previous-versions/visualstudio/visual-studio-2013/bb397896(v=vs.120))</span><span class="sxs-lookup"><span data-stu-id="ee875-206">[Standard Query Operators Overview](/previous-versions/visualstudio/visual-studio-2013/bb397896(v=vs.120))</span></span>
